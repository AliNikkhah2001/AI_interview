State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Screens prompts and responses for prompt injection, sensitive data, malicious URLs, and content-safety risks around model applications. Think of Google Cloud Model Armor as a managed ai security service between prompt or response and sanitized content + findings; its defining mechanism is managed security screening.
\item[2. Structure and execution] Follow the data path Prompt or response → Managed security screening → Sanitized content + findings. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when google Cloud applications need centralized managed inspection across supported model and agent paths. Reject it when a local open-source control or business authorization is the actual requirement. Compare the same representative workload with Bedrock Guardrails, Azure AI Content Safety, LLM Guard.
\item[5. Production proof] Start with the failure hypothesis: Detection coverage, false positives, region, and latency must be tested, and screening is not authorization. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Google Cloud managed service\\
Choose when & Google Cloud applications need centralized managed inspection across supported model and agent paths.\\
Reject when & A local open-source control or business authorization is the actual requirement.\\
Primary failure & Detection coverage, false positives, region, and latency must be tested, and screening is not authorization.\\
Compare with & Bedrock Guardrails; Azure AI Content Safety; LLM Guard\\
\end{tabularx}
\paragraph{Minimal use in REST / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
gcloud model-armor templates create TEMPLATE
gcloud model-armor templates sanitize-user-prompt TEMPLATE
\end{Verbatim}
\textbf{Primary sources.} \href{https://cloud.google.com/security-command-center/docs/model-armor-overview}{Google Cloud Model Armor official documentation}.
\clearpage\subsection{07. MCP and tool connectivity}
\textbf{Selection question.} Where is authority enforced?
\begin{center}\begin{tikzpicture}[tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]
\node[tech](c0){Model intent + capability};
\node[tech,right=of c0](c1){Discover, authorize, invoke};
\node[tech,right=of c1](c2){Typed tool result};
\draw[arr](c0)--(c1);\draw[arr](c1)--(c2);
\end{tikzpicture}\end{center}
\paragraph{Comparison matrix.}
\begin{longtable}{@{}p{20mm}p{22mm}p{26mm}p{46mm}p{46mm}@{}}
\toprule\textbf{Tool} & \textbf{Kind} & \textbf{Language / boundary} & \textbf{Choose when} & \textbf{Reject when}\\\midrule\endhead
Model Context Protocol & Open protocol & JSON-RPC, TypeScript, Python, C\#, Java and more; Local stdio or remote HTTP transports & Independent applications and capability providers need a standard integration boundary with explicit host control. & A private in-process function call is simpler, or the integration cannot implement authentication and authorization safely.\\
Official MCP SDKs & Protocol SDKs & TypeScript, Python, C\#, Java, Kotlin, Swift, Rust, Go, Ruby, PHP; Embedded in MCP clients and servers & You are implementing an MCP client or server and want protocol-correct primitives instead of hand-written JSON-RPC. & You only consume a stable product SDK and do not need MCP interoperability.\\
FastMCP & MCP framework & Python, TypeScript; Library; local or remote server & Developers need a concise typed API for production Python or TypeScript MCP services. & You require only low-level protocol control or an existing framework already owns routing and auth.\\
MCP Inspector & Developer inspection tool & TypeScript; Local web UI and proxy & You are developing or debugging a server and need to see exact schemas, messages, and responses. & You need production monitoring, authorization, or a security boundary.\\
Docker MCP Toolkit and Gateway & Tool catalog and gateway & Go, Docker configuration; Docker Desktop and containerized gateway & Desktop or enterprise teams want curated containerized servers and centralized client configuration. & Container trust, image provenance, or Docker Desktop availability cannot be accepted.\\
Smithery & MCP registry and deployment platform & TypeScript, Python, CLI; Hosted registry and remote server infrastructure & Teams need to find and connect interoperable servers quickly with hosted deployment options. & Strict private catalogs, on-premises execution, or bespoke authorization dominate.\\
Composio & Managed tool integration platform & Python, TypeScript, REST; Managed service and SDKs & Many SaaS integrations and per-user OAuth connections must be delivered quickly. & On-premises secrets, custom APIs, or avoiding an external auth and tool data plane is mandatory.\\
Arcade & Agent tool and authorization platform & Python, TypeScript, REST; Managed or gateway-oriented platform & Agents need end-user authorization and a curated tool layer without rebuilding OAuth for every integration. & The organization requires fully self-hosted bespoke connectors or direct provider APIs only.\\
Official MCP Registry & MCP metadata registry & REST, JSON; Hosted registry API with ecosystem clients & Clients or catalogs need a common source of server identity and distribution metadata. & You need runtime authorization, trust decisions, or private internal discovery by itself.\\
GitHub MCP Server & Repository tool server & Go, Docker; Local stdio, containers, or remote GitHub service & Agents need typed GitHub operations with token scopes and explicit toolsets. & Direct REST or GraphQL clients are simpler, or broad repository mutation is too risky.\\
AWS MCP Servers & Cloud service MCP server collection & Python, TypeScript; Local or remote servers for AWS services & Agents need typed access to selected AWS services under existing IAM and audit controls. & Direct SDK calls are clearer or a centralized gateway must own every tool schema and policy.\\
MCP Toolbox for Databases & Database tool server & Go, YAML, client SDKs; Local binary, container, or cloud deployment & Agents need governed, reusable database operations rather than arbitrary SQL generation. & A direct application data layer or unrestricted analyst SQL is the actual boundary.\\
Azure MCP Server & Cloud service MCP server & TypeScript, Docker; Local stdio or container with Azure identity & Agents need governed access to Azure resources through familiar identity and service-specific tools. & Direct Azure SDKs are clearer or remote shared tool execution violates the trust boundary.\\
Zapier MCP & Managed application tool gateway & MCP, REST; Managed remote MCP server & A user wants quick authenticated access to many SaaS actions without building each OAuth connector. & High-impact actions, self-hosting, or deeply customized schemas require an owned integration service.\\
mcp-use & MCP client and agent library & Python, TypeScript; Library and optional cloud services & You need a focused programmatic MCP client layer across models and server transports. & A framework already supplies adequate MCP integration or an enterprise gateway must mediate every call.\\
\bottomrule\end{longtable}
\subsubsection{Model Context Protocol}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries MCP}}}\quad \badge{Open protocol} \quad \textbf{Languages:} JSON-RPC / TypeScript / Python / C\# / Java and more
\par\smallskip Protocol for hosts, clients, and servers to negotiate and expose tools, resources, prompts, roots, sampling, elicitation, and related capabilities.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Model Context Protocol as a open protocol between host intent and typed result or resource; its defining mechanism is mcp discovery + invocation.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Host intent};
\node[tech,right=of t0](t1){MCP discovery + invocation};
\node[tech,right=of t1](t2){Typed result or resource};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Host intent -> MCP discovery + invocation -> Typed result or resource\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Protocol for hosts, clients, and servers to negotiate and expose tools, resources, prompts, roots, sampling, elicitation, and related capabilities. Think of Model Context Protocol as a open protocol between host intent and typed result or resource; its defining mechanism is mcp discovery + invocation.
\item[2. Structure and execution] Follow the data path Host intent → MCP discovery + invocation → Typed result or resource. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when independent applications and capability providers need a standard integration boundary with explicit host control. Reject it when a private in-process function call is simpler, or the integration cannot implement authentication and authorization safely. Compare the same representative workload with direct SDKs, OpenAPI, gRPC.
\item[5. Production proof] Start with the failure hypothesis: Discovery is not trust: a host that auto-approves servers or tool calls can grant attacker-controlled authority. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local stdio or remote HTTP transports\\
Choose when & Independent applications and capability providers need a standard integration boundary with explicit host control.\\
Reject when & A private in-process function call is simpler, or the integration cannot implement authentication and authorization safely.\\
Primary failure & Discovery is not trust: a host that auto-approves servers or tool calls can grant attacker-controlled authority.\\
Compare with & direct SDKs; OpenAPI; gRPC\\
\end{tabularx}
\paragraph{Minimal use in JSON-RPC / TypeScript / Python / C\# / Java and more.}
\begin{Verbatim}[fontsize=\scriptsize]
Connect an MCP client to a trusted server; inspect capabilities; authorize each call.
\end{Verbatim}
\textbf{Primary sources.} \href{https://modelcontextprotocol.io/specification/2026-07-28}{Model Context Protocol official documentation} \quad \href{https://modelcontextprotocol.io/docs/learn/architecture}{MCP architecture} \quad \href{https://modelcontextprotocol.io/specification/latest/basic/authorization}{MCP authorization}.
\subsubsection{Official MCP SDKs}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries OMS}}}\quad \badge{Protocol SDKs} \quad \textbf{Languages:} TypeScript / Python / C\# / Java / Kotlin / Swift / Rust / Go / Ruby / PHP
\par\smallskip Official and community-tier SDK implementations for protocol types, transports, server features, client sessions, and conformance.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Official MCP SDKs as a protocol sdks between application capability and mcp messages; its defining mechanism is sdk session + transport.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Application capability};
\node[tech,right=of t0](t1){SDK session + transport};
\node[tech,right=of t1](t2){MCP messages};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Application capability -> SDK session + transport -> MCP messages\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Official and community-tier SDK implementations for protocol types, transports, server features, client sessions, and conformance. Think of Official MCP SDKs as a protocol sdks between application capability and mcp messages; its defining mechanism is sdk session + transport.
\item[2. Structure and execution] Follow the data path Application capability → SDK session + transport → MCP messages. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when you are implementing an MCP client or server and want protocol-correct primitives instead of hand-written JSON-RPC. Reject it when you only consume a stable product SDK and do not need MCP interoperability. Compare the same representative workload with FastMCP, raw JSON-RPC, vendor tool APIs.
\item[5. Production proof] Start with the failure hypothesis: Pinning an SDK without pinning the negotiated protocol version can create capability and transport mismatches. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Embedded in MCP clients and servers\\
Choose when & You are implementing an MCP client or server and want protocol-correct primitives instead of hand-written JSON-RPC.\\
Reject when & You only consume a stable product SDK and do not need MCP interoperability.\\
Primary failure & Pinning an SDK without pinning the negotiated protocol version can create capability and transport mismatches.\\
Compare with & FastMCP; raw JSON-RPC; vendor tool APIs\\
\end{tabularx}
\paragraph{Minimal use in TypeScript / Python / C\# / Java / Kotlin / Swift / Rust / Go / Ruby / PHP.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install mcp
from mcp.server.fastmcp import FastMCP
\end{Verbatim}
\textbf{Primary sources.} \href{https://modelcontextprotocol.io/docs/sdk}{Official MCP SDKs official documentation}.
\subsubsection{FastMCP}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries F}}}\quad \badge{MCP framework} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip High-level framework for creating, composing, proxying, deploying, and authenticating MCP servers and clients.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of FastMCP as a mcp framework between typed function and discoverable mcp tool; its defining mechanism is fastmcp schema + transport.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Typed function};
\node[tech,right=of t0](t1){FastMCP schema + transport};
\node[tech,right=of t1](t2){Discoverable MCP tool};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Typed function -> FastMCP schema + transport -> Discoverable MCP tool\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] High-level framework for creating, composing, proxying, deploying, and authenticating MCP servers and clients. Think of FastMCP as a mcp framework between typed function and discoverable mcp tool; its defining mechanism is fastmcp schema + transport.
\item[2. Structure and execution] Follow the data path Typed function → FastMCP schema + transport → Discoverable MCP tool. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when developers need a concise typed API for production Python or TypeScript MCP services. Reject it when you require only low-level protocol control or an existing framework already owns routing and auth. Compare the same representative workload with official MCP SDKs, custom server, platform gateways.
\item[5. Production proof] Start with the failure hypothesis: A one-decorator tool can still expose broad credentials or unsafe parameters unless policy is designed around it. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; local or remote server\\
Choose when & Developers need a concise typed API for production Python or TypeScript MCP services.\\
Reject when & You require only low-level protocol control or an existing framework already owns routing and auth.\\
Primary failure & A one-decorator tool can still expose broad credentials or unsafe parameters unless policy is designed around it.\\
Compare with & official MCP SDKs; custom server; platform gateways\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install fastmcp
from fastmcp import FastMCP; mcp = FastMCP('server')
\end{Verbatim}
\textbf{Primary sources.} \href{https://gofastmcp.com/getting-started/welcome}{FastMCP official documentation}.
\subsubsection{MCP Inspector}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries MI}}}\quad \badge{Developer inspection tool} \quad \textbf{Languages:} TypeScript
\par\smallskip Interactive protocol workbench for connecting to MCP servers, viewing negotiated capabilities, and exercising tools, resources, and prompts.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of MCP Inspector as a developer inspection tool between server endpoint and protocol transcript; its defining mechanism is inspect and invoke.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Server endpoint};
\node[tech,right=of t0](t1){Inspect and invoke};
\node[tech,right=of t1](t2){Protocol transcript};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Server endpoint -> Inspect and invoke -> Protocol transcript\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Interactive protocol workbench for connecting to MCP servers, viewing negotiated capabilities, and exercising tools, resources, and prompts. Think of MCP Inspector as a developer inspection tool between server endpoint and protocol transcript; its defining mechanism is inspect and invoke.
\item[2. Structure and execution] Follow the data path Server endpoint → Inspect and invoke → Protocol transcript. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when you are developing or debugging a server and need to see exact schemas, messages, and responses. Reject it when you need production monitoring, authorization, or a security boundary. Compare the same representative workload with protocol logs, integration tests, Postman-like tools.
\item[5. Production proof] Start with the failure hypothesis: Successfully invoking a tool in Inspector does not test hostile inputs, tenant isolation, or production authentication. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local web UI and proxy\\
Choose when & You are developing or debugging a server and need to see exact schemas, messages, and responses.\\
Reject when & You need production monitoring, authorization, or a security boundary.\\
Primary failure & Successfully invoking a tool in Inspector does not test hostile inputs, tenant isolation, or production authentication.\\
Compare with & protocol logs; integration tests; Postman-like tools\\
\end{tabularx}
\paragraph{Minimal use in TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
npx @modelcontextprotocol/inspector COMMAND ARGS
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/modelcontextprotocol/inspector}{MCP Inspector official documentation}.
\subsubsection{Docker MCP Toolkit and Gateway}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries DMT}}}\quad \badge{Tool catalog and gateway} \quad \textbf{Languages:} Go / Docker configuration
\par\smallskip Catalog, configuration, credential management, container isolation, and a gateway that exposes selected MCP servers through one endpoint.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Docker MCP Toolkit and Gateway as a tool catalog and gateway between selected servers and one governed client endpoint; its defining mechanism is containerized mcp gateway.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Selected servers};
\node[tech,right=of t0](t1){Containerized MCP gateway};
\node[tech,right=of t1](t2){One governed client endpoint};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Selected servers -> Containerized MCP gateway -> One governed client endpoint\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Catalog, configuration, credential management, container isolation, and a gateway that exposes selected MCP servers through one endpoint. Think of Docker MCP Toolkit and Gateway as a tool catalog and gateway between selected servers and one governed client endpoint; its defining mechanism is containerized mcp gateway.
\item[2. Structure and execution] Follow the data path Selected servers → Containerized MCP gateway → One governed client endpoint. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when desktop or enterprise teams want curated containerized servers and centralized client configuration. Reject it when container trust, image provenance, or Docker Desktop availability cannot be accepted. Compare the same representative workload with Smithery, self-hosted gateway, direct servers.
\item[5. Production proof] Start with the failure hypothesis: Installing a catalog entry without verifying image, scopes, egress, and secret handling imports supply-chain risk. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Docker Desktop and containerized gateway\\
Choose when & Desktop or enterprise teams want curated containerized servers and centralized client configuration.\\
Reject when & Container trust, image provenance, or Docker Desktop availability cannot be accepted.\\
Primary failure & Installing a catalog entry without verifying image, scopes, egress, and secret handling imports supply-chain risk.\\
Compare with & Smithery; self-hosted gateway; direct servers\\
\end{tabularx}
\paragraph{Minimal use in Go / Docker configuration.}
\begin{Verbatim}[fontsize=\scriptsize]
docker mcp gateway run
docker mcp server enable SERVER
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.docker.com/ai/mcp-catalog-and-toolkit/}{Docker MCP Toolkit and Gateway official documentation}.
\subsubsection{Smithery}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries S}}}\quad \badge{MCP registry and deployment platform} \quad \textbf{Languages:} TypeScript / Python / CLI
\par\smallskip Discovery, installation, hosting, and connection tooling around MCP servers and clients.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Smithery as a mcp registry and deployment platform between capability search and configured mcp server; its defining mechanism is registry + connection profile.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Capability search};
\node[tech,right=of t0](t1){Registry + connection profile};
\node[tech,right=of t1](t2){Configured MCP server};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Capability search -> Registry + connection profile -> Configured MCP server\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Discovery, installation, hosting, and connection tooling around MCP servers and clients. Think of Smithery as a mcp registry and deployment platform between capability search and configured mcp server; its defining mechanism is registry + connection profile.
\item[2. Structure and execution] Follow the data path Capability search → Registry + connection profile → Configured MCP server. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when teams need to find and connect interoperable servers quickly with hosted deployment options. Reject it when strict private catalogs, on-premises execution, or bespoke authorization dominate. Compare the same representative workload with Docker MCP Toolkit, self-hosted registry, direct MCP servers.
\item[5. Production proof] Start with the failure hypothesis: Registry presence is not a security review; server code, data paths, scopes, and updates still need verification. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Hosted registry and remote server infrastructure\\
Choose when & Teams need to find and connect interoperable servers quickly with hosted deployment options.\\
Reject when & Strict private catalogs, on-premises execution, or bespoke authorization dominate.\\
Primary failure & Registry presence is not a security review; server code, data paths, scopes, and updates still need verification.\\
Compare with & Docker MCP Toolkit; self-hosted registry; direct MCP servers\\
\end{tabularx}
\paragraph{Minimal use in TypeScript / Python / CLI.}
\begin{Verbatim}[fontsize=\scriptsize]
npx @smithery/cli run SERVER
\end{Verbatim}
\textbf{Primary sources.} \href{https://smithery.ai/docs}{Smithery official documentation}.
\subsubsection{Composio}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries C}}}\quad \badge{Managed tool integration platform} \quad \textbf{Languages:} Python / TypeScript / REST
\par\smallskip Large catalog of application actions with managed authentication, tool schemas, triggers, and agent-framework or MCP integrations.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Composio as a managed tool integration platform between user identity + intent and external saas effect; its defining mechanism is managed auth + action.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){User identity + intent};
\node[tech,right=of t0](t1){Managed auth + action};
\node[tech,right=of t1](t2){External SaaS effect};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & User identity + intent -> Managed auth + action -> External SaaS effect\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Large catalog of application actions with managed authentication, tool schemas, triggers, and agent-framework or MCP integrations. Think of Composio as a managed tool integration platform between user identity + intent and external saas effect; its defining mechanism is managed auth + action.
\item[2. Structure and execution] Follow the data path User identity + intent → Managed auth + action → External SaaS effect. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when many SaaS integrations and per-user OAuth connections must be delivered quickly. Reject it when on-premises secrets, custom APIs, or avoiding an external auth and tool data plane is mandatory. Compare the same representative workload with Nango plus custom tools, Arcade, direct OAuth integrations.
\item[5. Production proof] Start with the failure hypothesis: Broad connected accounts can create excessive scopes and cross-user confusion unless identity is bound at every invocation. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed service and SDKs\\
Choose when & Many SaaS integrations and per-user OAuth connections must be delivered quickly.\\
Reject when & On-premises secrets, custom APIs, or avoiding an external auth and tool data plane is mandatory.\\
Primary failure & Broad connected accounts can create excessive scopes and cross-user confusion unless identity is bound at every invocation.\\
Compare with & Nango plus custom tools; Arcade; direct OAuth integrations\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install composio
composio.tools.get(user_id=USER, tools=['GITHUB'])
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.composio.dev/}{Composio official documentation}.
\subsubsection{Arcade}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries A}}}\quad \badge{Agent tool and authorization platform} \quad \textbf{Languages:} Python / TypeScript / REST
\par\smallskip Authenticated tools, user authorization, tool calling, MCP connectivity, and policy-oriented execution for agents.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Arcade as a agent tool and authorization platform between user + proposed action and audited external result; its defining mechanism is authorize + execute tool.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){User + proposed action};
\node[tech,right=of t0](t1){Authorize + execute tool};
\node[tech,right=of t1](t2){Audited external result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & User + proposed action -> Authorize + execute tool -> Audited external result\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Authenticated tools, user authorization, tool calling, MCP connectivity, and policy-oriented execution for agents. Think of Arcade as a agent tool and authorization platform between user + proposed action and audited external result; its defining mechanism is authorize + execute tool.
\item[2. Structure and execution] Follow the data path User + proposed action → Authorize + execute tool → Audited external result. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when agents need end-user authorization and a curated tool layer without rebuilding OAuth for every integration. Reject it when the organization requires fully self-hosted bespoke connectors or direct provider APIs only. Compare the same representative workload with Composio, custom OAuth broker, MCP gateway.
\item[5. Production proof] Start with the failure hypothesis: Delegating OAuth does not remove the need to validate user, tenant, resource, and high-impact arguments per action. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed or gateway-oriented platform\\
Choose when & Agents need end-user authorization and a curated tool layer without rebuilding OAuth for every integration.\\
Reject when & The organization requires fully self-hosted bespoke connectors or direct provider APIs only.\\
Primary failure & Delegating OAuth does not remove the need to validate user, tenant, resource, and high-impact arguments per action.\\
Compare with & Composio; custom OAuth broker; MCP gateway\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install arcadepy
from arcadepy import Arcade
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.arcade.dev/}{Arcade official documentation}.
\subsubsection{Official MCP Registry}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries OMR}}}\quad \badge{MCP metadata registry} \quad \textbf{Languages:} REST / JSON
\par\smallskip Official registry for publishing and discovering MCP server metadata, packages, versions, and transport information.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Official MCP Registry as a mcp metadata registry between server metadata + version and install or connection coordinates; its defining mechanism is publish and discover registry entry.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Server metadata + version};
\node[tech,right=of t0](t1){Publish and discover registry entry};
\node[tech,right=of t1](t2){Install or connection coordinates};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Server metadata + version -> Publish and discover registry entry -> Install or connection coordinates\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Official registry for publishing and discovering MCP server metadata, packages, versions, and transport information. Think of Official MCP Registry as a mcp metadata registry between server metadata + version and install or connection coordinates; its defining mechanism is publish and discover registry entry.
\item[2. Structure and execution] Follow the data path Server metadata + version → Publish and discover registry entry → Install or connection coordinates. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when clients or catalogs need a common source of server identity and distribution metadata. Reject it when you need runtime authorization, trust decisions, or private internal discovery by itself. Compare the same representative workload with Smithery, private catalog, GitHub search.
\item[5. Production proof] Start with the failure hypothesis: A registry record is metadata, not a security review, runtime health check, or grant of authority. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Hosted registry API with ecosystem clients\\
Choose when & Clients or catalogs need a common source of server identity and distribution metadata.\\
Reject when & You need runtime authorization, trust decisions, or private internal discovery by itself.\\
Primary failure & A registry record is metadata, not a security review, runtime health check, or grant of authority.\\
Compare with & Smithery; private catalog; GitHub search\\
\end{tabularx}
\paragraph{Minimal use in REST / JSON.}
\begin{Verbatim}[fontsize=\scriptsize]
curl https://registry.modelcontextprotocol.io/v0.1/servers
\end{Verbatim}
\textbf{Primary sources.} \href{https://modelcontextprotocol.io/registry/about}{Official MCP Registry official documentation} \quad \href{https://registry.modelcontextprotocol.io/docs}{Registry API}.
\subsubsection{GitHub MCP Server}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries GMS}}}\quad \badge{Repository tool server} \quad \textbf{Languages:} Go / Docker
\par\smallskip Official GitHub MCP server exposing repositories, issues, pull requests, code, actions, and other GitHub capabilities as tools.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of GitHub MCP Server as a repository tool server between agent request + github identity and repository result or mutation; its defining mechanism is mcp tools + github apis.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Agent request + GitHub identity};
\node[tech,right=of t0](t1){MCP tools + GitHub APIs};
\node[tech,right=of t1](t2){Repository result or mutation};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Agent request + GitHub identity -> MCP tools + GitHub APIs -> Repository result or mutation\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Official GitHub MCP server exposing repositories, issues, pull requests, code, actions, and other GitHub capabilities as tools. Think of GitHub MCP Server as a repository tool server between agent request + github identity and repository result or mutation; its defining mechanism is mcp tools + github apis.
\item[2. Structure and execution] Follow the data path Agent request + GitHub identity → MCP tools + GitHub APIs → Repository result or mutation. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when agents need typed GitHub operations with token scopes and explicit toolsets. Reject it when direct REST or GraphQL clients are simpler, or broad repository mutation is too risky. Compare the same representative workload with GitHub API SDKs, Composio, custom MCP server.
\item[5. Production proof] Start with the failure hypothesis: An over-scoped token or broad toolset can turn prompt injection into repository or workflow modification. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local stdio, containers, or remote GitHub service\\
Choose when & Agents need typed GitHub operations with token scopes and explicit toolsets.\\
Reject when & Direct REST or GraphQL clients are simpler, or broad repository mutation is too risky.\\
Primary failure & An over-scoped token or broad toolset can turn prompt injection into repository or workflow modification.\\
Compare with & GitHub API SDKs; Composio; custom MCP server\\
\end{tabularx}
\paragraph{Minimal use in Go / Docker.}
\begin{Verbatim}[fontsize=\scriptsize]
docker run ghcr.io/github/github-mcp-server stdio
export GITHUB_PERSONAL_ACCESS_TOKEN=...
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/github/github-mcp-server}{GitHub MCP Server official documentation}.
\subsubsection{AWS MCP Servers}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries AMS}}}\quad \badge{Cloud service MCP server collection} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip AWS Labs collection of specialized MCP servers and guidance for cloud resources, documentation, operations, and developer workflows.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of AWS MCP Servers as a cloud service mcp server collection between agent intent + aws credentials and aws api result; its defining mechanism is service-specific mcp server.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Agent intent + AWS credentials};
\node[tech,right=of t0](t1){Service-specific MCP server};
\node[tech,right=of t1](t2){AWS API result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Agent intent + AWS credentials -> Service-specific MCP server -> AWS API result\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] AWS Labs collection of specialized MCP servers and guidance for cloud resources, documentation, operations, and developer workflows. Think of AWS MCP Servers as a cloud service mcp server collection between agent intent + aws credentials and aws api result; its defining mechanism is service-specific mcp server.
\item[2. Structure and execution] Follow the data path Agent intent + AWS credentials → Service-specific MCP server → AWS API result. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when agents need typed access to selected AWS services under existing IAM and audit controls. Reject it when direct SDK calls are clearer or a centralized gateway must own every tool schema and policy. Compare the same representative workload with Azure MCP Server, Google Cloud MCP tools, custom servers.
\item[5. Production proof] Start with the failure hypothesis: MCP does not narrow IAM automatically; destructive or expensive operations require explicit tool selection and policy. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local or remote servers for AWS services\\
Choose when & Agents need typed access to selected AWS services under existing IAM and audit controls.\\
Reject when & Direct SDK calls are clearer or a centralized gateway must own every tool schema and policy.\\
Primary failure & MCP does not narrow IAM automatically; destructive or expensive operations require explicit tool selection and policy.\\
Compare with & Azure MCP Server; Google Cloud MCP tools; custom servers\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
uvx awslabs.aws-documentation-mcp-server@latest
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/awslabs/mcp}{AWS MCP Servers official documentation}.
\subsubsection{MCP Toolbox for Databases}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries MTF}}}\quad \badge{Database tool server} \quad \textbf{Languages:} Go / YAML / client SDKs
\par\smallskip Google open-source server for defining parameterized database tools and exposing them to agents through MCP and other SDK integrations.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of MCP Toolbox for Databases as a database tool server between tool definition + identity and bounded rows + metadata; its defining mechanism is validated database operation.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Tool definition + identity};
\node[tech,right=of t0](t1){Validated database operation};
\node[tech,right=of t1](t2){Bounded rows + metadata};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Tool definition + identity -> Validated database operation -> Bounded rows + metadata\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Google open-source server for defining parameterized database tools and exposing them to agents through MCP and other SDK integrations. Think of MCP Toolbox for Databases as a database tool server between tool definition + identity and bounded rows + metadata; its defining mechanism is validated database operation.
\item[2. Structure and execution] Follow the data path Tool definition + identity → Validated database operation → Bounded rows + metadata. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when agents need governed, reusable database operations rather than arbitrary SQL generation. Reject it when a direct application data layer or unrestricted analyst SQL is the actual boundary. Compare the same representative workload with custom MCP server, LangChain SQL tools, database API.
\item[5. Production proof] Start with the failure hypothesis: A safe tool depends on parameter validation, least-privilege credentials, row policy, result limits, and audit context. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local binary, container, or cloud deployment\\
Choose when & Agents need governed, reusable database operations rather than arbitrary SQL generation.\\
Reject when & A direct application data layer or unrestricted analyst SQL is the actual boundary.\\
Primary failure & A safe tool depends on parameter validation, least-privilege credentials, row policy, result limits, and audit context.\\
Compare with & custom MCP server; LangChain SQL tools; database API\\
\end{tabularx}
\paragraph{Minimal use in Go / YAML / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
toolbox --tools-file tools.yaml
curl http://localhost:5000/api/toolset
\end{Verbatim}
\textbf{Primary sources.} \href{https://googleapis.github.io/genai-toolbox/}{MCP Toolbox for Databases official documentation}.
\subsubsection{Azure MCP Server}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries AMS}}}\quad \badge{Cloud service MCP server} \quad \textbf{Languages:} TypeScript / Docker
\par\smallskip Microsoft server exposing selected Azure services and developer operations through MCP tools and Azure authentication.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Azure MCP Server as a cloud service mcp server between agent intent + azure identity and cloud result or mutation; its defining mechanism is service tool + azure api.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Agent intent + Azure identity};
\node[tech,right=of t0](t1){Service tool + Azure API};
\node[tech,right=of t1](t2){Cloud result or mutation};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Agent intent + Azure identity -> Service tool + Azure API -> Cloud result or mutation\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Microsoft server exposing selected Azure services and developer operations through MCP tools and Azure authentication. Think of Azure MCP Server as a cloud service mcp server between agent intent + azure identity and cloud result or mutation; its defining mechanism is service tool + azure api.
\item[2. Structure and execution] Follow the data path Agent intent + Azure identity → Service tool + Azure API → Cloud result or mutation. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when agents need governed access to Azure resources through familiar identity and service-specific tools. Reject it when direct Azure SDKs are clearer or remote shared tool execution violates the trust boundary. Compare the same representative workload with AWS MCP Servers, custom MCP server, Azure SDKs.
\item[5. Production proof] Start with the failure hypothesis: Default credentials and broad subscriptions can expose resources beyond the user's intended tenant or project. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local stdio or container with Azure identity\\
Choose when & Agents need governed access to Azure resources through familiar identity and service-specific tools.\\
Reject when & Direct Azure SDKs are clearer or remote shared tool execution violates the trust boundary.\\
Primary failure & Default credentials and broad subscriptions can expose resources beyond the user's intended tenant or project.\\
Compare with & AWS MCP Servers; custom MCP server; Azure SDKs\\
\end{tabularx}
\paragraph{Minimal use in TypeScript / Docker.}
\begin{Verbatim}[fontsize=\scriptsize]
npx -y @azure/mcp@latest server start
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/microsoft/mcp}{Azure MCP Server official documentation}.
\subsubsection{Zapier MCP}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries ZM}}}\quad \badge{Managed application tool gateway} \quad \textbf{Languages:} MCP / REST
\par\smallskip Connects agents to selected actions across Zapier's application ecosystem through a managed MCP endpoint.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Zapier MCP as a managed application tool gateway between agent tool call + user connection and external application result; its defining mechanism is zapier mcp action.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Agent tool call + user connection};
\node[tech,right=of t0](t1){Zapier MCP action};
\node[tech,right=of t1](t2){External application result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Agent tool call + user connection -> Zapier MCP action -> External application result\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Connects agents to selected actions across Zapier's application ecosystem through a managed MCP endpoint. Think of Zapier MCP as a managed application tool gateway between agent tool call + user connection and external application result; its defining mechanism is zapier mcp action.
\item[2. Structure and execution] Follow the data path Agent tool call + user connection → Zapier MCP action → External application result. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when a user wants quick authenticated access to many SaaS actions without building each OAuth connector. Reject it when high-impact actions, self-hosting, or deeply customized schemas require an owned integration service. Compare the same representative workload with Composio, Pipedream Connect, Arcade.
\item[5. Production proof] Start with the failure hypothesis: A convenient connector can expose broad user authority unless enabled actions, arguments, confirmation, and audit are tightly scoped. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed remote MCP server\\
Choose when & A user wants quick authenticated access to many SaaS actions without building each OAuth connector.\\
Reject when & High-impact actions, self-hosting, or deeply customized schemas require an owned integration service.\\
Primary failure & A convenient connector can expose broad user authority unless enabled actions, arguments, confirmation, and audit are tightly scoped.\\
Compare with & Composio; Pipedream Connect; Arcade\\
\end{tabularx}
\paragraph{Minimal use in MCP / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
Configure the Zapier MCP endpoint in the client
Enable only required actions
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.zapier.com/mcp/home}{Zapier MCP official documentation}.
\subsubsection{mcp-use}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries M}}}\quad \badge{MCP client and agent library} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Developer library for connecting agents and applications to one or more MCP servers, discovering tools, and running tool loops.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of mcp-use as a mcp client and agent library between server connections + model and typed tool results; its defining mechanism is mcp client session + tool loop.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Server connections + model};
\node[tech,right=of t0](t1){MCP client session + tool loop};
\node[tech,right=of t1](t2){Typed tool results};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Server connections + model -> MCP client session + tool loop -> Typed tool results\\
Control plane & The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.\\
State and trust boundary & Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Developer library for connecting agents and applications to one or more MCP servers, discovering tools, and running tool loops. Think of mcp-use as a mcp client and agent library between server connections + model and typed tool results; its defining mechanism is mcp client session + tool loop.
\item[2. Structure and execution] Follow the data path Server connections + model → MCP client session + tool loop → Typed tool results. The host selects servers, exposes a bounded tool surface, obtains authorization, validates arguments, invokes the server, and mediates results.
\item[3. State, security, and operations] Server identity, protocol version, capability negotiation, schemas, user consent, tokens, resource scope, and audit receipts are separate concerns.
\item[4. Selection decision] Choose it when you need a focused programmatic MCP client layer across models and server transports. Reject it when a framework already supplies adequate MCP integration or an enterprise gateway must mediate every call. Compare the same representative workload with Official MCP SDKs, LangChain adapters, custom client.
\item[5. Production proof] Start with the failure hypothesis: Dynamic tool discovery can expand context, cost, and authority without allowlists and schema pinning. Exercise schema changes, confused-deputy attacks, prompt injection, revoked credentials, timeouts, duplicate calls, malicious results, and least privilege.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library and optional cloud services\\
Choose when & You need a focused programmatic MCP client layer across models and server transports.\\
Reject when & A framework already supplies adequate MCP integration or an enterprise gateway must mediate every call.\\
Primary failure & Dynamic tool discovery can expand context, cost, and authority without allowlists and schema pinning.\\
Compare with & Official MCP SDKs; LangChain adapters; custom client\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install mcp-use
from mcp_use import MCPAgent, MCPClient
\end{Verbatim}
\textbf{Primary sources.} \href{https://mcp-use.com/docs/}{mcp-use official documentation}.
\clearpage\subsection{08. Sandbox and code execution}
\textbf{Selection question.} What survives a guest escape attempt?
\begin{center}\begin{tikzpicture}[tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]
\node[tech](c0){Untrusted program};
\node[tech,right=of c0](c1){Isolate + constrain};
\node[tech,right=of c1](c2){Result + audit trail};
\draw[arr](c0)--(c1);\draw[arr](c1)--(c2);
\end{tikzpicture}\end{center}
\paragraph{Comparison matrix.}
\begin{longtable}{@{}p{20mm}p{22mm}p{26mm}p{46mm}p{46mm}@{}}
\toprule\textbf{Tool} & \textbf{Kind} & \textbf{Language / boundary} & \textbf{Choose when} & \textbf{Reject when}\\\midrule\endhead
E2B & Cloud sandbox platform & Python, TypeScript; Managed isolated sandboxes & Agents need disposable code execution with fast startup and an application-level SDK. & Air-gapped or self-hosted-only requirements, custom kernel controls, or long-running fixed compute dominate.\\
Daytona & Development environment and sandbox platform & TypeScript, Python, REST; Self-hosted or managed & Coding agents need reproducible developer environments, repository workflows, and self-hosting options. & A tiny stateless code snippet or the strongest microVM isolation with no workspace features is required.\\
Modal Sandboxes & Elastic sandbox API & Python; Managed CPU or GPU sandboxes & The same managed platform should run agents, sandboxes, GPU workloads, and parallel evaluations. & On-premises execution, custom hypervisor controls, or stable always-on hosts are required.\\
Docker & Container runtime and packaging & Go, Dockerfile, OCI; Local or clustered containers & Reproducible application packaging and ordinary workload isolation are needed across development and deployment. & Hostile multi-tenant code requires a stronger kernel boundary without additional hardening.\\
gVisor & Application kernel sandbox & Go; runsc OCI runtime on Linux or Kubernetes & Untrusted Linux containers need stronger isolation than runc with lower overhead than a full VM for supported workloads. & System-call compatibility or near-native I/O performance is critical and the code is trusted.\\
Firecracker & MicroVM monitor & Rust; Linux KVM microVM building block & A platform team is building strong multi-tenant serverless or agent isolation and can own kernel, image, network, and lifecycle machinery. & You need a ready-to-use developer sandbox API rather than a low-level virtualization component.\\
Kata Containers & VM-isolated container runtime & Go, Rust, OCI; Kubernetes-compatible runtime classes & Kubernetes workloads need a VM boundary while preserving pod and OCI operations. & Startup density and maximum compatibility with ordinary runc are more important than the stronger boundary.\\
Kubernetes Jobs & Batch workload controller & Go, YAML, container languages; Kubernetes cluster & The organization already runs Kubernetes and needs auditable finite agent or evaluation tasks with cluster controls. & Millisecond startup, hostile code isolation by itself, or a lightweight local API is required.\\
Cloudflare Sandbox SDK & Managed agent sandbox SDK & TypeScript; Cloudflare Workers with Containers and optional storage & Agents on Cloudflare need elastic code execution close to Workers and platform storage. & A local sandbox, non-Cloudflare deployment, or stronger custom microVM ownership is required.\\
Runloop & Cloud development environment platform & Python, TypeScript, REST; Managed devboxes and blueprints & Coding agents need reproducible long-lived devboxes, repository workflows, and environment snapshots. & A short stateless code interpreter or fully self-hosted sandbox is sufficient.\\
Sprites & Persistent cloud execution environment & HTTP, command-line tools; Managed persistent Linux environments & An agent needs resumable Linux state and long-lived processes without managing VMs. & Strict ephemeral isolation or self-hosted infrastructure is required.\\
Browserbase & Managed browser automation platform & JavaScript, Python, CDP; Managed remote browser sessions & Agents need scalable observable browser sessions without operating Chromium fleets. & No browser interaction is needed or sensitive workflows require a fully owned environment.\\
Docker Sandboxes & Local agent sandbox runtime & Go, containers, microVMs; Docker Desktop or supported Docker environments & Developers want local reproducible agent workspaces with familiar Docker lifecycle and stronger separation options. & A managed elastic service or bare minimal container process is preferable.\\
Wasmtime & WebAssembly runtime & Rust, C, Python, .NET; Embedded process, server, or component runtime & Untrusted plugins or functions can be compiled to WebAssembly and need a small capability-oriented runtime. & Full Linux compatibility, GPUs, browsers, or arbitrary native binaries are required.\\
nsjail & Linux process isolation tool & C++, Linux namespaces; Self-hosted Linux process wrapper & A Linux service needs a low-level configurable process sandbox and can own kernel hardening. & Cross-platform support, microVM isolation, or a managed execution API is required.\\
\bottomrule\end{longtable}
\subsubsection{E2B}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries E}}}\quad \badge{Cloud sandbox platform} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip On-demand Linux sandboxes with files, processes, networking, templates, persistence controls, and agent/code-interpreter SDKs.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of E2B as a cloud sandbox platform between untrusted code + scoped files and outputs + artifacts; its defining mechanism is remote isolated sandbox.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Untrusted code + scoped files};
\node[tech,right=of t0](t1){Remote isolated sandbox};
\node[tech,right=of t1](t2){Outputs + artifacts};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Untrusted code + scoped files -> Remote isolated sandbox -> Outputs + artifacts\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] On-demand Linux sandboxes with files, processes, networking, templates, persistence controls, and agent/code-interpreter SDKs. Think of E2B as a cloud sandbox platform between untrusted code + scoped files and outputs + artifacts; its defining mechanism is remote isolated sandbox.
\item[2. Structure and execution] Follow the data path Untrusted code + scoped files → Remote isolated sandbox → Outputs + artifacts. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when agents need disposable code execution with fast startup and an application-level SDK. Reject it when air-gapped or self-hosted-only requirements, custom kernel controls, or long-running fixed compute dominate. Compare the same representative workload with Daytona, Modal Sandboxes, Firecracker platform.
\item[5. Production proof] Start with the failure hypothesis: Passing ambient tokens or unrestricted egress into an otherwise isolated sandbox defeats the security boundary. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed isolated sandboxes\\
Choose when & Agents need disposable code execution with fast startup and an application-level SDK.\\
Reject when & Air-gapped or self-hosted-only requirements, custom kernel controls, or long-running fixed compute dominate.\\
Primary failure & Passing ambient tokens or unrestricted egress into an otherwise isolated sandbox defeats the security boundary.\\
Compare with & Daytona; Modal Sandboxes; Firecracker platform\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install e2b-code-interpreter
Sandbox.create().run_code('print(2+2)')
\end{Verbatim}
\textbf{Primary sources.} \href{https://e2b.dev/docs}{E2B official documentation}.
\subsubsection{Daytona}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries D}}}\quad \badge{Development environment and sandbox platform} \quad \textbf{Languages:} TypeScript / Python / REST
\par\smallskip Programmable workspaces for AI-generated code with repositories, processes, files, snapshots, networking, and language SDKs.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Daytona as a development environment and sandbox platform between repo + task and patch + test evidence; its defining mechanism is programmable workspace.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Repo + task};
\node[tech,right=of t0](t1){Programmable workspace};
\node[tech,right=of t1](t2){Patch + test evidence};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Repo + task -> Programmable workspace -> Patch + test evidence\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Programmable workspaces for AI-generated code with repositories, processes, files, snapshots, networking, and language SDKs. Think of Daytona as a development environment and sandbox platform between repo + task and patch + test evidence; its defining mechanism is programmable workspace.
\item[2. Structure and execution] Follow the data path Repo + task → Programmable workspace → Patch + test evidence. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when coding agents need reproducible developer environments, repository workflows, and self-hosting options. Reject it when a tiny stateless code snippet or the strongest microVM isolation with no workspace features is required. Compare the same representative workload with E2B, Codespaces, custom Kubernetes workspaces.
\item[5. Production proof] Start with the failure hypothesis: Workspace persistence can retain secrets or attacker changes unless snapshots, credentials, and teardown are explicit. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or managed\\
Choose when & Coding agents need reproducible developer environments, repository workflows, and self-hosting options.\\
Reject when & A tiny stateless code snippet or the strongest microVM isolation with no workspace features is required.\\
Primary failure & Workspace persistence can retain secrets or attacker changes unless snapshots, credentials, and teardown are explicit.\\
Compare with & E2B; Codespaces; custom Kubernetes workspaces\\
\end{tabularx}
\paragraph{Minimal use in TypeScript / Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install daytona-sdk
from daytona import Daytona
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.daytona.io/docs/}{Daytona official documentation}.
\subsubsection{Modal Sandboxes}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries MS}}}\quad \badge{Elastic sandbox API} \quad \textbf{Languages:} Python
\par\smallskip Dynamically start isolated containers, execute commands, manage files and networks, snapshot memory, and scale large agent or evaluation fleets.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Modal Sandboxes as a elastic sandbox api between image + command and process result or endpoint; its defining mechanism is elastic managed sandbox.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Image + command};
\node[tech,right=of t0](t1){Elastic managed sandbox};
\node[tech,right=of t1](t2){Process result or endpoint};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Image + command -> Elastic managed sandbox -> Process result or endpoint\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Dynamically start isolated containers, execute commands, manage files and networks, snapshot memory, and scale large agent or evaluation fleets. Think of Modal Sandboxes as a elastic sandbox api between image + command and process result or endpoint; its defining mechanism is elastic managed sandbox.
\item[2. Structure and execution] Follow the data path Image + command → Elastic managed sandbox → Process result or endpoint. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when the same managed platform should run agents, sandboxes, GPU workloads, and parallel evaluations. Reject it when on-premises execution, custom hypervisor controls, or stable always-on hosts are required. Compare the same representative workload with E2B, Daytona, Kubernetes Jobs.
\item[5. Production proof] Start with the failure hypothesis: Warm pools and snapshots improve startup but demand secure state reset and careful secret lifecycle. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed CPU or GPU sandboxes\\
Choose when & The same managed platform should run agents, sandboxes, GPU workloads, and parallel evaluations.\\
Reject when & On-premises execution, custom hypervisor controls, or stable always-on hosts are required.\\
Primary failure & Warm pools and snapshots improve startup but demand secure state reset and careful secret lifecycle.\\
Compare with & E2B; Daytona; Kubernetes Jobs\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
with modal.Sandbox.create(app=app) as sb:
    sb.exec('python', 'task.py')
\end{Verbatim}
\textbf{Primary sources.} \href{https://modal.com/docs/guide/sandbox}{Modal Sandboxes official documentation}.
\subsubsection{Docker}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries D}}}\quad \badge{Container runtime and packaging} \quad \textbf{Languages:} Go / Dockerfile / OCI
\par\smallskip OCI images, namespaces, cgroups, filesystems, networks, registries, and a ubiquitous packaging/runtime workflow.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Docker as a container runtime and packaging between image + limits and isolated process tree; its defining mechanism is namespaces + cgroups.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Image + limits};
\node[tech,right=of t0](t1){Namespaces + cgroups};
\node[tech,right=of t1](t2){Isolated process tree};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Image + limits -> Namespaces + cgroups -> Isolated process tree\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] OCI images, namespaces, cgroups, filesystems, networks, registries, and a ubiquitous packaging/runtime workflow. Think of Docker as a container runtime and packaging between image + limits and isolated process tree; its defining mechanism is namespaces + cgroups.
\item[2. Structure and execution] Follow the data path Image + limits → Namespaces + cgroups → Isolated process tree. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when reproducible application packaging and ordinary workload isolation are needed across development and deployment. Reject it when hostile multi-tenant code requires a stronger kernel boundary without additional hardening. Compare the same representative workload with Podman, containerd, Kata Containers.
\item[5. Production proof] Start with the failure hypothesis: A default container shares the host kernel and may expose sockets, capabilities, metadata endpoints, or secrets. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local or clustered containers\\
Choose when & Reproducible application packaging and ordinary workload isolation are needed across development and deployment.\\
Reject when & Hostile multi-tenant code requires a stronger kernel boundary without additional hardening.\\
Primary failure & A default container shares the host kernel and may expose sockets, capabilities, metadata endpoints, or secrets.\\
Compare with & Podman; containerd; Kata Containers\\
\end{tabularx}
\paragraph{Minimal use in Go / Dockerfile / OCI.}
\begin{Verbatim}[fontsize=\scriptsize]
docker run --read-only --network none --cap-drop ALL IMAGE
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.docker.com/engine/security/}{Docker official documentation}.
\subsubsection{gVisor}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries G}}}\quad \badge{Application kernel sandbox} \quad \textbf{Languages:} Go
\par\smallskip User-space application kernel that intercepts system calls to reduce direct exposure of the host kernel while retaining container workflows.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of gVisor as a application kernel sandbox between oci container and restricted host interaction; its defining mechanism is runsc application kernel.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){OCI container};
\node[tech,right=of t0](t1){runsc application kernel};
\node[tech,right=of t1](t2){Restricted host interaction};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & OCI container -> runsc application kernel -> Restricted host interaction\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] User-space application kernel that intercepts system calls to reduce direct exposure of the host kernel while retaining container workflows. Think of gVisor as a application kernel sandbox between oci container and restricted host interaction; its defining mechanism is runsc application kernel.
\item[2. Structure and execution] Follow the data path OCI container → runsc application kernel → Restricted host interaction. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when untrusted Linux containers need stronger isolation than runc with lower overhead than a full VM for supported workloads. Reject it when system-call compatibility or near-native I/O performance is critical and the code is trusted. Compare the same representative workload with Kata Containers, Firecracker, hardened containers.
\item[5. Production proof] Start with the failure hypothesis: Assuming complete Linux compatibility or ignoring runtime configuration can break applications or weaken the intended boundary. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & runsc OCI runtime on Linux or Kubernetes\\
Choose when & Untrusted Linux containers need stronger isolation than runc with lower overhead than a full VM for supported workloads.\\
Reject when & System-call compatibility or near-native I/O performance is critical and the code is trusted.\\
Primary failure & Assuming complete Linux compatibility or ignoring runtime configuration can break applications or weaken the intended boundary.\\
Compare with & Kata Containers; Firecracker; hardened containers\\
\end{tabularx}
\paragraph{Minimal use in Go.}
\begin{Verbatim}[fontsize=\scriptsize]
docker run --runtime=runsc IMAGE
\end{Verbatim}
\textbf{Primary sources.} \href{https://gvisor.dev/docs/}{gVisor official documentation}.
\subsubsection{Firecracker}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries F}}}\quad \badge{MicroVM monitor} \quad \textbf{Languages:} Rust
\par\smallskip Minimal virtual machine monitor using KVM, designed for fast-starting lightweight microVMs with a reduced device model.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Firecracker as a microvm monitor between kernel + rootfs and strong guest boundary; its defining mechanism is minimal kvm microvm.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Kernel + rootfs};
\node[tech,right=of t0](t1){Minimal KVM microVM};
\node[tech,right=of t1](t2){Strong guest boundary};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Kernel + rootfs -> Minimal KVM microVM -> Strong guest boundary\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Minimal virtual machine monitor using KVM, designed for fast-starting lightweight microVMs with a reduced device model. Think of Firecracker as a microvm monitor between kernel + rootfs and strong guest boundary; its defining mechanism is minimal kvm microvm.
\item[2. Structure and execution] Follow the data path Kernel + rootfs → Minimal KVM microVM → Strong guest boundary. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when a platform team is building strong multi-tenant serverless or agent isolation and can own kernel, image, network, and lifecycle machinery. Reject it when you need a ready-to-use developer sandbox API rather than a low-level virtualization component. Compare the same representative workload with Kata Containers, Cloud Hypervisor, gVisor.
\item[5. Production proof] Start with the failure hypothesis: Firecracker alone does not supply tenancy, image provenance, networking policy, secret brokering, or secure reset. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Linux KVM microVM building block\\
Choose when & A platform team is building strong multi-tenant serverless or agent isolation and can own kernel, image, network, and lifecycle machinery.\\
Reject when & You need a ready-to-use developer sandbox API rather than a low-level virtualization component.\\
Primary failure & Firecracker alone does not supply tenancy, image provenance, networking policy, secret brokering, or secure reset.\\
Compare with & Kata Containers; Cloud Hypervisor; gVisor\\
\end{tabularx}
\paragraph{Minimal use in Rust.}
\begin{Verbatim}[fontsize=\scriptsize]
firecracker --api-sock /tmp/firecracker.socket
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/firecracker-microvm/firecracker}{Firecracker official documentation}.
\subsubsection{Kata Containers}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries KC}}}\quad \badge{VM-isolated container runtime} \quad \textbf{Languages:} Go / Rust / OCI
\par\smallskip Container-compatible workloads executed inside lightweight virtual machines for stronger tenant isolation.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Kata Containers as a vm-isolated container runtime between pod spec and vm-isolated pod; its defining mechanism is kata runtime + lightweight vm.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Pod spec};
\node[tech,right=of t0](t1){Kata runtime + lightweight VM};
\node[tech,right=of t1](t2){VM-isolated pod};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Pod spec -> Kata runtime + lightweight VM -> VM-isolated pod\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Container-compatible workloads executed inside lightweight virtual machines for stronger tenant isolation. Think of Kata Containers as a vm-isolated container runtime between pod spec and vm-isolated pod; its defining mechanism is kata runtime + lightweight vm.
\item[2. Structure and execution] Follow the data path Pod spec → Kata runtime + lightweight VM → VM-isolated pod. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when kubernetes workloads need a VM boundary while preserving pod and OCI operations. Reject it when startup density and maximum compatibility with ordinary runc are more important than the stronger boundary. Compare the same representative workload with gVisor, Firecracker platform, confidential containers.
\item[5. Production proof] Start with the failure hypothesis: Deploying the runtime without selecting RuntimeClass and validating storage, networking, and observability leaves workloads on the default runtime. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Kubernetes-compatible runtime classes\\
Choose when & Kubernetes workloads need a VM boundary while preserving pod and OCI operations.\\
Reject when & Startup density and maximum compatibility with ordinary runc are more important than the stronger boundary.\\
Primary failure & Deploying the runtime without selecting RuntimeClass and validating storage, networking, and observability leaves workloads on the default runtime.\\
Compare with & gVisor; Firecracker platform; confidential containers\\
\end{tabularx}
\paragraph{Minimal use in Go / Rust / OCI.}
\begin{Verbatim}[fontsize=\scriptsize]
kubectl apply -f runtimeclass.yaml
kubectl run agent --overrides KATA_CONFIG
\end{Verbatim}
\textbf{Primary sources.} \href{https://katacontainers.io/docs/}{Kata Containers official documentation}.
\subsubsection{Kubernetes Jobs}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries KJ}}}\quad \badge{Batch workload controller} \quad \textbf{Languages:} Go / YAML / container languages
\par\smallskip Controller for finite pod executions with retries, parallelism, deadlines, completion tracking, quotas, and policy integration.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Kubernetes Jobs as a batch workload controller between image + job spec and completion status + artifacts; its defining mechanism is scheduled finite pods.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Image + Job spec};
\node[tech,right=of t0](t1){Scheduled finite pods};
\node[tech,right=of t1](t2){Completion status + artifacts};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Image + Job spec -> Scheduled finite pods -> Completion status + artifacts\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Controller for finite pod executions with retries, parallelism, deadlines, completion tracking, quotas, and policy integration. Think of Kubernetes Jobs as a batch workload controller between image + job spec and completion status + artifacts; its defining mechanism is scheduled finite pods.
\item[2. Structure and execution] Follow the data path Image + Job spec → Scheduled finite pods → Completion status + artifacts. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when the organization already runs Kubernetes and needs auditable finite agent or evaluation tasks with cluster controls. Reject it when millisecond startup, hostile code isolation by itself, or a lightweight local API is required. Compare the same representative workload with Modal Sandboxes, Nomad batch, cloud batch services.
\item[5. Production proof] Start with the failure hypothesis: A Job is scheduling, not a security boundary; default pods may have network, service-account, and node exposure. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Kubernetes cluster\\
Choose when & The organization already runs Kubernetes and needs auditable finite agent or evaluation tasks with cluster controls.\\
Reject when & Millisecond startup, hostile code isolation by itself, or a lightweight local API is required.\\
Primary failure & A Job is scheduling, not a security boundary; default pods may have network, service-account, and node exposure.\\
Compare with & Modal Sandboxes; Nomad batch; cloud batch services\\
\end{tabularx}
\paragraph{Minimal use in Go / YAML / container languages.}
\begin{Verbatim}[fontsize=\scriptsize]
kubectl create job eval --image=IMAGE -- COMMAND
\end{Verbatim}
\textbf{Primary sources.} \href{https://kubernetes.io/docs/concepts/workloads/controllers/job/}{Kubernetes Jobs official documentation}.
\subsubsection{Cloudflare Sandbox SDK}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries CSS}}}\quad \badge{Managed agent sandbox SDK} \quad \textbf{Languages:} TypeScript
\par\smallskip Creates isolated code-execution environments with processes, files, networking policy, previews, lifecycle, and persistent bucket mounts.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Cloudflare Sandbox SDK as a managed agent sandbox sdk between code + sandbox policy and files, logs, and result; its defining mechanism is container or microvm session.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Code + sandbox policy};
\node[tech,right=of t0](t1){Container or microVM session};
\node[tech,right=of t1](t2){Files, logs, and result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Code + sandbox policy -> Container or microVM session -> Files, logs, and result\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Creates isolated code-execution environments with processes, files, networking policy, previews, lifecycle, and persistent bucket mounts. Think of Cloudflare Sandbox SDK as a managed agent sandbox sdk between code + sandbox policy and files, logs, and result; its defining mechanism is container or microvm session.
\item[2. Structure and execution] Follow the data path Code + sandbox policy → Container or microVM session → Files, logs, and result. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when agents on Cloudflare need elastic code execution close to Workers and platform storage. Reject it when a local sandbox, non-Cloudflare deployment, or stronger custom microVM ownership is required. Compare the same representative workload with E2B, Runloop, Modal Sandboxes.
\item[5. Production proof] Start with the failure hypothesis: Egress, mounted credentials, image contents, persistence, and session reuse define the real boundary, not the SDK call. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Cloudflare Workers with Containers and optional storage\\
Choose when & Agents on Cloudflare need elastic code execution close to Workers and platform storage.\\
Reject when & A local sandbox, non-Cloudflare deployment, or stronger custom microVM ownership is required.\\
Primary failure & Egress, mounted credentials, image contents, persistence, and session reuse define the real boundary, not the SDK call.\\
Compare with & E2B; Runloop; Modal Sandboxes\\
\end{tabularx}
\paragraph{Minimal use in TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
npm install @cloudflare/sandbox
import { getSandbox } from '@cloudflare/sandbox'
\end{Verbatim}
\textbf{Primary sources.} \href{https://developers.cloudflare.com/sandbox/}{Cloudflare Sandbox SDK official documentation} \quad \href{https://developers.cloudflare.com/sandbox/api/storage/}{Sandbox storage and persistence}.
\subsubsection{Runloop}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries R}}}\quad \badge{Cloud development environment platform} \quad \textbf{Languages:} Python / TypeScript / REST
\par\smallskip Remote development environments for coding agents with snapshots, files, commands, repositories, networking, and lifecycle APIs.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Runloop as a cloud development environment platform between repository + blueprint and patch, logs, and artifacts; its defining mechanism is remote devbox + commands.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Repository + blueprint};
\node[tech,right=of t0](t1){Remote devbox + commands};
\node[tech,right=of t1](t2){Patch, logs, and artifacts};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Repository + blueprint -> Remote devbox + commands -> Patch, logs, and artifacts\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Remote development environments for coding agents with snapshots, files, commands, repositories, networking, and lifecycle APIs. Think of Runloop as a cloud development environment platform between repository + blueprint and patch, logs, and artifacts; its defining mechanism is remote devbox + commands.
\item[2. Structure and execution] Follow the data path Repository + blueprint → Remote devbox + commands → Patch, logs, and artifacts. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when coding agents need reproducible long-lived devboxes, repository workflows, and environment snapshots. Reject it when a short stateless code interpreter or fully self-hosted sandbox is sufficient. Compare the same representative workload with E2B, Daytona, Sprites.
\item[5. Production proof] Start with the failure hypothesis: Warm snapshots and reused environments can preserve secrets or state across tasks unless tenancy and teardown are explicit. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed devboxes and blueprints\\
Choose when & Coding agents need reproducible long-lived devboxes, repository workflows, and environment snapshots.\\
Reject when & A short stateless code interpreter or fully self-hosted sandbox is sufficient.\\
Primary failure & Warm snapshots and reused environments can preserve secrets or state across tasks unless tenancy and teardown are explicit.\\
Compare with & E2B; Daytona; Sprites\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install runloop-api-client
from runloop_api_client import RunloopSDK
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.runloop.ai/}{Runloop official documentation}.
\subsubsection{Sprites}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries S}}}\quad \badge{Persistent cloud execution environment} \quad \textbf{Languages:} HTTP / command-line tools
\par\smallskip Fast-starting persistent cloud environments for running agent code, processes, files, and networked development workloads.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Sprites as a persistent cloud execution environment between task + sprite identity and resumable process + files; its defining mechanism is persistent isolated linux environment.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Task + sprite identity};
\node[tech,right=of t0](t1){Persistent isolated Linux environment};
\node[tech,right=of t1](t2){Resumable process + files};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Task + sprite identity -> Persistent isolated Linux environment -> Resumable process + files\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Fast-starting persistent cloud environments for running agent code, processes, files, and networked development workloads. Think of Sprites as a persistent cloud execution environment between task + sprite identity and resumable process + files; its defining mechanism is persistent isolated linux environment.
\item[2. Structure and execution] Follow the data path Task + sprite identity → Persistent isolated Linux environment → Resumable process + files. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when an agent needs resumable Linux state and long-lived processes without managing VMs. Reject it when strict ephemeral isolation or self-hosted infrastructure is required. Compare the same representative workload with Runloop, Daytona, Fly Machines.
\item[5. Production proof] Start with the failure hypothesis: Persistence is useful but can retain compromised state, stale credentials, or cross-task artifacts. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed persistent Linux environments\\
Choose when & An agent needs resumable Linux state and long-lived processes without managing VMs.\\
Reject when & Strict ephemeral isolation or self-hosted infrastructure is required.\\
Primary failure & Persistence is useful but can retain compromised state, stale credentials, or cross-task artifacts.\\
Compare with & Runloop; Daytona; Fly Machines\\
\end{tabularx}
\paragraph{Minimal use in HTTP / command-line tools.}
\begin{Verbatim}[fontsize=\scriptsize]
sprite create interview-agent
sprite exec interview-agent -- python task.py
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.sprites.dev/}{Sprites official documentation}.
\subsubsection{Browserbase}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries B}}}\quad \badge{Managed browser automation platform} \quad \textbf{Languages:} JavaScript / Python / CDP
\par\smallskip Cloud browser infrastructure with sessions, proxies, recordings, debugging, and automation integrations for agents.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Browserbase as a managed browser automation platform between url + session policy and dom result, recording, or artifact; its defining mechanism is remote browser + automation.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){URL + session policy};
\node[tech,right=of t0](t1){Remote browser + automation};
\node[tech,right=of t1](t2){DOM result, recording, or artifact};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & URL + session policy -> Remote browser + automation -> DOM result, recording, or artifact\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Cloud browser infrastructure with sessions, proxies, recordings, debugging, and automation integrations for agents. Think of Browserbase as a managed browser automation platform between url + session policy and dom result, recording, or artifact; its defining mechanism is remote browser + automation.
\item[2. Structure and execution] Follow the data path URL + session policy → Remote browser + automation → DOM result, recording, or artifact. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when agents need scalable observable browser sessions without operating Chromium fleets. Reject it when no browser interaction is needed or sensitive workflows require a fully owned environment. Compare the same representative workload with Playwright self-hosted, Browser Use, cloud browser grids.
\item[5. Production proof] Start with the failure hypothesis: A remote browser handles hostile pages and authenticated sessions, so credentials, downloads, prompts, and egress remain high risk. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed remote browser sessions\\
Choose when & Agents need scalable observable browser sessions without operating Chromium fleets.\\
Reject when & No browser interaction is needed or sensitive workflows require a fully owned environment.\\
Primary failure & A remote browser handles hostile pages and authenticated sessions, so credentials, downloads, prompts, and egress remain high risk.\\
Compare with & Playwright self-hosted; Browser Use; cloud browser grids\\
\end{tabularx}
\paragraph{Minimal use in JavaScript / Python / CDP.}
\begin{Verbatim}[fontsize=\scriptsize]
npm install @browserbasehq/sdk
const session = await bb.sessions.create()
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.browserbase.com/}{Browserbase official documentation}.
\subsubsection{Docker Sandboxes}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries DS}}}\quad \badge{Local agent sandbox runtime} \quad \textbf{Languages:} Go / containers / microVMs
\par\smallskip Agent-oriented isolated workspaces integrated with Docker tooling, images, repositories, files, and controlled execution backends.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Docker Sandboxes as a local agent sandbox runtime between repository + image + limits and changed files + logs; its defining mechanism is docker-managed sandbox.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Repository + image + limits};
\node[tech,right=of t0](t1){Docker-managed sandbox};
\node[tech,right=of t1](t2){Changed files + logs};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Repository + image + limits -> Docker-managed sandbox -> Changed files + logs\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Agent-oriented isolated workspaces integrated with Docker tooling, images, repositories, files, and controlled execution backends. Think of Docker Sandboxes as a local agent sandbox runtime between repository + image + limits and changed files + logs; its defining mechanism is docker-managed sandbox.
\item[2. Structure and execution] Follow the data path Repository + image + limits → Docker-managed sandbox → Changed files + logs. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when developers want local reproducible agent workspaces with familiar Docker lifecycle and stronger separation options. Reject it when a managed elastic service or bare minimal container process is preferable. Compare the same representative workload with Daytona, E2B, Cloudflare Sandbox.
\item[5. Production proof] Start with the failure hypothesis: Mounts, Docker socket access, cached credentials, and backend configuration determine whether the sandbox is actually bounded. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Docker Desktop or supported Docker environments\\
Choose when & Developers want local reproducible agent workspaces with familiar Docker lifecycle and stronger separation options.\\
Reject when & A managed elastic service or bare minimal container process is preferable.\\
Primary failure & Mounts, Docker socket access, cached credentials, and backend configuration determine whether the sandbox is actually bounded.\\
Compare with & Daytona; E2B; Cloudflare Sandbox\\
\end{tabularx}
\paragraph{Minimal use in Go / containers / microVMs.}
\begin{Verbatim}[fontsize=\scriptsize]
docker sandbox run AGENT
docker sandbox ls
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.docker.com/ai/sandboxes/}{Docker Sandboxes official documentation}.
\subsubsection{Wasmtime}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries W}}}\quad \badge{WebAssembly runtime} \quad \textbf{Languages:} Rust / C / Python / .NET
\par\smallskip Bytecode runtime for WebAssembly and WASI with capabilities, resource controls, components, and embeddable host interfaces.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Wasmtime as a webassembly runtime between wasm component + capabilities and bounded result; its defining mechanism is wasmtime runtime + host functions.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Wasm component + capabilities};
\node[tech,right=of t0](t1){Wasmtime runtime + host functions};
\node[tech,right=of t1](t2){Bounded result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Wasm component + capabilities -> Wasmtime runtime + host functions -> Bounded result\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Bytecode runtime for WebAssembly and WASI with capabilities, resource controls, components, and embeddable host interfaces. Think of Wasmtime as a webassembly runtime between wasm component + capabilities and bounded result; its defining mechanism is wasmtime runtime + host functions.
\item[2. Structure and execution] Follow the data path Wasm component + capabilities → Wasmtime runtime + host functions → Bounded result. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when untrusted plugins or functions can be compiled to WebAssembly and need a small capability-oriented runtime. Reject it when full Linux compatibility, GPUs, browsers, or arbitrary native binaries are required. Compare the same representative workload with gVisor, Firecracker, Wasmer.
\item[5. Production proof] Start with the failure hypothesis: Host functions are the authority boundary; exposing filesystem, network, clocks, or secrets defeats capability isolation. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Embedded process, server, or component runtime\\
Choose when & Untrusted plugins or functions can be compiled to WebAssembly and need a small capability-oriented runtime.\\
Reject when & Full Linux compatibility, GPUs, browsers, or arbitrary native binaries are required.\\
Primary failure & Host functions are the authority boundary; exposing filesystem, network, clocks, or secrets defeats capability isolation.\\
Compare with & gVisor; Firecracker; Wasmer\\
\end{tabularx}
\paragraph{Minimal use in Rust / C / Python / .NET.}
\begin{Verbatim}[fontsize=\scriptsize]
wasmtime run --dir=. module.wasm
cargo add wasmtime
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.wasmtime.dev/}{Wasmtime official documentation}.
\subsubsection{nsjail}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries N}}}\quad \badge{Linux process isolation tool} \quad \textbf{Languages:} C++ / Linux namespaces
\par\smallskip Uses namespaces, cgroups, resource limits, seccomp, chroot, and policy configuration to isolate individual processes.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of nsjail as a linux process isolation tool between command + policy and constrained process result; its defining mechanism is namespaces, seccomp, cgroups.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Command + policy};
\node[tech,right=of t0](t1){Namespaces, seccomp, cgroups};
\node[tech,right=of t1](t2){Constrained process result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Command + policy -> Namespaces, seccomp, cgroups -> Constrained process result\\
Control plane & A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.\\
State and trust boundary & Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Uses namespaces, cgroups, resource limits, seccomp, chroot, and policy configuration to isolate individual processes. Think of nsjail as a linux process isolation tool between command + policy and constrained process result; its defining mechanism is namespaces, seccomp, cgroups.
\item[2. Structure and execution] Follow the data path Command + policy → Namespaces, seccomp, cgroups → Constrained process result. A broker creates an execution boundary, attaches an image and capabilities, enforces resource and egress policy, observes work, and destroys or snapshots it.
\item[3. State, security, and operations] Filesystem layers, processes, network, credentials, mounts, caches, artifacts, tenant identity, and teardown semantics define the real isolation boundary.
\item[4. Selection decision] Choose it when a Linux service needs a low-level configurable process sandbox and can own kernel hardening. Reject it when cross-platform support, microVM isolation, or a managed execution API is required. Compare the same representative workload with bubblewrap, gVisor, Firecracker.
\item[5. Production proof] Start with the failure hypothesis: Kernel sharing, configuration mistakes, privileged launchers, and exposed files or sockets remain escape paths. Attempt escapes, fork bombs, disk exhaustion, metadata access, secret discovery, persistence abuse, cross-tenant leakage, and cleanup failure.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted Linux process wrapper\\
Choose when & A Linux service needs a low-level configurable process sandbox and can own kernel hardening.\\
Reject when & Cross-platform support, microVM isolation, or a managed execution API is required.\\
Primary failure & Kernel sharing, configuration mistakes, privileged launchers, and exposed files or sockets remain escape paths.\\
Compare with & bubblewrap; gVisor; Firecracker\\
\end{tabularx}
\paragraph{Minimal use in C++ / Linux namespaces.}
\begin{Verbatim}[fontsize=\scriptsize]
nsjail --chroot /sandbox --disable_proc -- /bin/program
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/google/nsjail}{nsjail official documentation}.
\clearpage\subsection{09. Data, workflow, and ML lifecycle}
\textbf{Selection question.} Can every effect be replayed or reconciled?
\begin{center}\begin{tikzpicture}[tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]
\node[tech](c0){Versioned inputs + event};
\node[tech,right=of c0](c1){Schedule, execute, lineage};
\node[tech,right=of c1](c2){Reproducible artifact};
\draw[arr](c0)--(c1);\draw[arr](c1)--(c2);
\end{tikzpicture}\end{center}
\paragraph{Comparison matrix.}
\begin{longtable}{@{}p{20mm}p{22mm}p{26mm}p{46mm}p{46mm}@{}}
\toprule\textbf{Tool} & \textbf{Kind} & \textbf{Language / boundary} & \textbf{Choose when} & \textbf{Reject when}\\\midrule\endhead
Temporal & Durable execution platform & Go server; SDKs for Go, Java, Python, TypeScript, .NET, PHP, Ruby; Self-hosted or Temporal Cloud & Business processes or agent effects span minutes to months and must recover deterministically across failures. & A short stateless request or a data DAG is sufficient and workflow-history constraints are unwanted.\\
Apache Airflow & Batch workflow orchestrator & Python; Self-hosted or managed distributions & Time- or dataset-scheduled batch pipelines and backfills need mature operations and plugins. & Low-latency event handling, dynamic long-running business state, or millisecond task startup is needed.\\
Prefect & Python workflow orchestrator & Python; Self-hosted server or Prefect Cloud & Dynamic Python workflows and developer ergonomics matter, with flexible worker infrastructure. & Strict asset lineage semantics or a durable business-process event history is the main abstraction.\\
Dagster & Data orchestrator & Python; Self-hosted or Dagster+ & Teams organize pipelines around durable data assets, partition health, and lineage rather than only task order. & The workload is a long-running interactive business process or simple cron script.\\
DVC & Data and pipeline version control & Python, CLI, Git; Local and remote object storage & A code repository needs reproducible data/model versions without storing large blobs directly in Git. & A central metadata and model registry or streaming feature platform is the primary need.\\
lakeFS & Data lake version-control service & Go, Python, REST; Self-hosted or managed over object storage & Large lake datasets need atomic snapshots, test branches, and reproducible promotion without copying every object. & Small local artifacts or row-level transactional analytics are the main problem.\\
Feast & Feature store & Python, Go; Registry plus offline and optional online stores & Training-serving consistency and reusable structured features matter for production ML. & The system mainly uses unstructured RAG embeddings or lacks shared feature semantics.\\
Kubeflow Pipelines & Kubernetes ML pipeline platform & Python, Kubernetes YAML; Kubernetes & Containerized training and ML pipelines already run on Kubernetes and require portable component graphs. & The team lacks cluster operations or primarily needs business workflow durability.\\
ZenML & MLOps pipeline framework & Python; Local or orchestrator-backed stacks & Teams want one Python ML workflow across local development and interchangeable production stacks. & A single established orchestrator already covers the lifecycle with less abstraction.\\
Argo Workflows & Kubernetes workflow engine & Go, YAML; Kubernetes CRDs and controllers & Workloads are containerized and Kubernetes is already the execution and policy plane. & Python-native local development or a non-Kubernetes control plane is required.\\
Flyte & Data and ML workflow platform & Python, Go; Kubernetes control plane and SDK & Data and ML teams need strongly typed reproducible workflows on Kubernetes across local and production environments. & A general scheduler or simple Python task runner provides lower operational cost.\\
Metaflow & Human-friendly data science workflow framework & Python; Local development with cloud batch and orchestration backends & Data scientists need a gentle local-to-cloud path with inspectable run artifacts and step semantics. & A Kubernetes-native platform or asset-centric data orchestration is the organization standard.\\
ClearML & Experiment and pipeline platform & Python; Self-hosted or managed server with agents & A team wants one self-hostable control plane for experiments, data, models, and remote workers. & Existing specialized tools already cover the lifecycle with simpler ownership.\\
Label Studio & Data labeling platform & Python, JavaScript; Self-hosted or managed application & Teams need human labeling and review workflows with flexible interfaces and model preannotations. & Programmatic weak supervision or a highly specialized vertical labeling product is more appropriate.\\
Great Expectations & Data quality validation framework & Python; Library, CI, orchestrators, or managed cloud & Pipelines need explicit data contracts and human-readable validation evidence before model or index builds. & Streaming runtime monitoring or a tiny schema check is the only requirement.\\
Airbyte & Data integration platform & Java, Python, connector SDKs; Self-hosted or managed cloud & Many SaaS and database sources must enter a governed lake or warehouse through maintained connectors. & Low-latency event streaming or bespoke high-control ingestion is required.\\
Unstructured & Document ingestion and partitioning toolkit & Python, REST; Library, open-source API, or managed service & A heterogeneous enterprise corpus needs one extensible document-ingestion layer. & A narrow file type, custom parser, or preservation-critical layout workflow deserves a specialized tool.\\
Docling & Document conversion toolkit & Python; Local library, CLI, or service & High-quality local parsing of PDFs and office documents matters and structured provenance must be retained. & A managed connector platform or simple text extraction is enough.\\
\bottomrule\end{longtable}
\subsubsection{Temporal}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries T}}}\quad \badge{Durable execution platform} \quad \textbf{Languages:} Go server; SDKs for Go / Java / Python / TypeScript / .NET / PHP / Ruby
\par\smallskip Event-history-based workflows that survive process failure, with activities, retries, timers, signals, queries, and versioning.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Temporal as a durable execution platform between business command and recoverable effects; its defining mechanism is durable workflow history.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Business command};
\node[tech,right=of t0](t1){Durable workflow history};
\node[tech,right=of t1](t2){Recoverable effects};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Business command -> Durable workflow history -> Recoverable effects\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Event-history-based workflows that survive process failure, with activities, retries, timers, signals, queries, and versioning. Think of Temporal as a durable execution platform between business command and recoverable effects; its defining mechanism is durable workflow history.
\item[2. Structure and execution] Follow the data path Business command → Durable workflow history → Recoverable effects. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when business processes or agent effects span minutes to months and must recover deterministically across failures. Reject it when a short stateless request or a data DAG is sufficient and workflow-history constraints are unwanted. Compare the same representative workload with LangGraph, Prefect, AWS Step Functions.
\item[5. Production proof] Start with the failure hypothesis: Nondeterministic workflow code or unsafe version changes can make history replay fail. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Temporal Cloud\\
Choose when & Business processes or agent effects span minutes to months and must recover deterministically across failures.\\
Reject when & A short stateless request or a data DAG is sufficient and workflow-history constraints are unwanted.\\
Primary failure & Nondeterministic workflow code or unsafe version changes can make history replay fail.\\
Compare with & LangGraph; Prefect; AWS Step Functions\\
\end{tabularx}
\paragraph{Minimal use in Go server; SDKs for Go / Java / Python / TypeScript / .NET / PHP / Ruby.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install temporalio
await client.start_workflow(MyWorkflow.run, id='order-1')
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.temporal.io/}{Temporal official documentation}.
\subsubsection{Apache Airflow}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries AA}}}\quad \badge{Batch workflow orchestrator} \quad \textbf{Languages:} Python
\par\smallskip Scheduled DAGs, operators, sensors, backfills, metadata, and a broad integration ecosystem for batch data workflows.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Apache Airflow as a batch workflow orchestrator between schedule + dag and batch data product; its defining mechanism is task instances + executor.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Schedule + DAG};
\node[tech,right=of t0](t1){Task instances + executor};
\node[tech,right=of t1](t2){Batch data product};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Schedule + DAG -> Task instances + executor -> Batch data product\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Scheduled DAGs, operators, sensors, backfills, metadata, and a broad integration ecosystem for batch data workflows. Think of Apache Airflow as a batch workflow orchestrator between schedule + dag and batch data product; its defining mechanism is task instances + executor.
\item[2. Structure and execution] Follow the data path Schedule + DAG → Task instances + executor → Batch data product. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when time- or dataset-scheduled batch pipelines and backfills need mature operations and plugins. Reject it when low-latency event handling, dynamic long-running business state, or millisecond task startup is needed. Compare the same representative workload with Dagster, Prefect, Temporal.
\item[5. Production proof] Start with the failure hypothesis: Treating the scheduler as a data transport or running huge dynamic maps can overload metadata and scheduling. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or managed distributions\\
Choose when & Time- or dataset-scheduled batch pipelines and backfills need mature operations and plugins.\\
Reject when & Low-latency event handling, dynamic long-running business state, or millisecond task startup is needed.\\
Primary failure & Treating the scheduler as a data transport or running huge dynamic maps can overload metadata and scheduling.\\
Compare with & Dagster; Prefect; Temporal\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install apache-airflow
airflow dags test my_dag 2026-09-05
\end{Verbatim}
\textbf{Primary sources.} \href{https://airflow.apache.org/docs/}{Apache Airflow official documentation}.
\subsubsection{Prefect}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries P}}}\quad \badge{Python workflow orchestrator} \quad \textbf{Languages:} Python
\par\smallskip Python-native flows and tasks with deployments, work pools, events, caching, retries, concurrency, and observability.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Prefect as a python workflow orchestrator between python call + event and observed workflow result; its defining mechanism is flow and task runtime.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Python call + event};
\node[tech,right=of t0](t1){Flow and task runtime};
\node[tech,right=of t1](t2){Observed workflow result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Python call + event -> Flow and task runtime -> Observed workflow result\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Python-native flows and tasks with deployments, work pools, events, caching, retries, concurrency, and observability. Think of Prefect as a python workflow orchestrator between python call + event and observed workflow result; its defining mechanism is flow and task runtime.
\item[2. Structure and execution] Follow the data path Python call + event → Flow and task runtime → Observed workflow result. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when dynamic Python workflows and developer ergonomics matter, with flexible worker infrastructure. Reject it when strict asset lineage semantics or a durable business-process event history is the main abstraction. Compare the same representative workload with Dagster, Airflow, Temporal.
\item[5. Production proof] Start with the failure hypothesis: Convenient retries can duplicate writes unless tasks define idempotency and cache identity deliberately. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted server or Prefect Cloud\\
Choose when & Dynamic Python workflows and developer ergonomics matter, with flexible worker infrastructure.\\
Reject when & Strict asset lineage semantics or a durable business-process event history is the main abstraction.\\
Primary failure & Convenient retries can duplicate writes unless tasks define idempotency and cache identity deliberately.\\
Compare with & Dagster; Airflow; Temporal\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install prefect
from prefect import flow, task
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.prefect.io/}{Prefect official documentation}.
\subsubsection{Dagster}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries D}}}\quad \badge{Data orchestrator} \quad \textbf{Languages:} Python
\par\smallskip Software-defined assets, partitions, lineage, resources, sensors, schedules, tests, and an integrated control plane.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Dagster as a data orchestrator between source assets and materialized data + lineage; its defining mechanism is partitioned asset graph.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Source assets};
\node[tech,right=of t0](t1){Partitioned asset graph};
\node[tech,right=of t1](t2){Materialized data + lineage};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Source assets -> Partitioned asset graph -> Materialized data + lineage\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Software-defined assets, partitions, lineage, resources, sensors, schedules, tests, and an integrated control plane. Think of Dagster as a data orchestrator between source assets and materialized data + lineage; its defining mechanism is partitioned asset graph.
\item[2. Structure and execution] Follow the data path Source assets → Partitioned asset graph → Materialized data + lineage. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when teams organize pipelines around durable data assets, partition health, and lineage rather than only task order. Reject it when the workload is a long-running interactive business process or simple cron script. Compare the same representative workload with Airflow, Prefect, dbt Cloud.
\item[5. Production proof] Start with the failure hypothesis: Asset materialization success can hide semantic data defects unless checks and upstream versions are first-class. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Dagster+\\
Choose when & Teams organize pipelines around durable data assets, partition health, and lineage rather than only task order.\\
Reject when & The workload is a long-running interactive business process or simple cron script.\\
Primary failure & Asset materialization success can hide semantic data defects unless checks and upstream versions are first-class.\\
Compare with & Airflow; Prefect; dbt Cloud\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install dagster
from dagster import asset
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.dagster.io/}{Dagster official documentation}.
\subsubsection{DVC}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries D}}}\quad \badge{Data and pipeline version control} \quad \textbf{Languages:} Python / CLI / Git
\par\smallskip Git-adjacent metadata for large data, model artifacts, reproducible stages, metrics, experiments, and remote caches.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of DVC as a data and pipeline version control between git revision + data hash and reproducible artifact; its defining mechanism is dvc cache + pipeline.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Git revision + data hash};
\node[tech,right=of t0](t1){DVC cache + pipeline};
\node[tech,right=of t1](t2){Reproducible artifact};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Git revision + data hash -> DVC cache + pipeline -> Reproducible artifact\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Git-adjacent metadata for large data, model artifacts, reproducible stages, metrics, experiments, and remote caches. Think of DVC as a data and pipeline version control between git revision + data hash and reproducible artifact; its defining mechanism is dvc cache + pipeline.
\item[2. Structure and execution] Follow the data path Git revision + data hash → DVC cache + pipeline → Reproducible artifact. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when a code repository needs reproducible data/model versions without storing large blobs directly in Git. Reject it when a central metadata and model registry or streaming feature platform is the primary need. Compare the same representative workload with lakeFS, Git LFS, MLflow.
\item[5. Production proof] Start with the failure hypothesis: Committing a .dvc pointer without remote availability, hashes, environment, and access controls does not reproduce a run. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local and remote object storage\\
Choose when & A code repository needs reproducible data/model versions without storing large blobs directly in Git.\\
Reject when & A central metadata and model registry or streaming feature platform is the primary need.\\
Primary failure & Committing a .dvc pointer without remote availability, hashes, environment, and access controls does not reproduce a run.\\
Compare with & lakeFS; Git LFS; MLflow\\
\end{tabularx}
\paragraph{Minimal use in Python / CLI / Git.}
\begin{Verbatim}[fontsize=\scriptsize]
dvc init
dvc add data/train.parquet
\end{Verbatim}
\textbf{Primary sources.} \href{https://dvc.org/doc}{DVC official documentation}.
\subsubsection{lakeFS}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Data lake version-control service} \quad \textbf{Languages:} Go / Python / REST
\par\smallskip Git-like branches, commits, merges, hooks, and zero-copy isolation for data lakes on object storage.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of lakeFS as a data lake version-control service between object-store namespace and atomic data snapshot; its defining mechanism is branch, commit, merge.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Object-store namespace};
\node[tech,right=of t0](t1){Branch, commit, merge};
\node[tech,right=of t1](t2){Atomic data snapshot};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Object-store namespace -> Branch, commit, merge -> Atomic data snapshot\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Git-like branches, commits, merges, hooks, and zero-copy isolation for data lakes on object storage. Think of lakeFS as a data lake version-control service between object-store namespace and atomic data snapshot; its defining mechanism is branch, commit, merge.
\item[2. Structure and execution] Follow the data path Object-store namespace → Branch, commit, merge → Atomic data snapshot. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when large lake datasets need atomic snapshots, test branches, and reproducible promotion without copying every object. Reject it when small local artifacts or row-level transactional analytics are the main problem. Compare the same representative workload with DVC, Delta Lake, Apache Iceberg catalogs.
\item[5. Production proof] Start with the failure hypothesis: A branch protects object versions, not automatically schema validity, privacy, or downstream index consistency. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or managed over object storage\\
Choose when & Large lake datasets need atomic snapshots, test branches, and reproducible promotion without copying every object.\\
Reject when & Small local artifacts or row-level transactional analytics are the main problem.\\
Primary failure & A branch protects object versions, not automatically schema validity, privacy, or downstream index consistency.\\
Compare with & DVC; Delta Lake; Apache Iceberg catalogs\\
\end{tabularx}
\paragraph{Minimal use in Go / Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
lakectl branch create lakefs://repo/exp --source lakefs://repo/main
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.lakefs.io/}{lakeFS official documentation}.
\subsubsection{Feast}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries F}}}\quad \badge{Feature store} \quad \textbf{Languages:} Python / Go
\par\smallskip Feature definitions, point-in-time correct historical retrieval, materialization, and low-latency online serving across existing stores.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Feast as a feature store between event data + feature views and training set or online features; its defining mechanism is point-in-time join or materialize.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Event data + feature views};
\node[tech,right=of t0](t1){Point-in-time join or materialize};
\node[tech,right=of t1](t2){Training set or online features};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Event data + feature views -> Point-in-time join or materialize -> Training set or online features\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Feature definitions, point-in-time correct historical retrieval, materialization, and low-latency online serving across existing stores. Think of Feast as a feature store between event data + feature views and training set or online features; its defining mechanism is point-in-time join or materialize.
\item[2. Structure and execution] Follow the data path Event data + feature views → Point-in-time join or materialize → Training set or online features. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when training-serving consistency and reusable structured features matter for production ML. Reject it when the system mainly uses unstructured RAG embeddings or lacks shared feature semantics. Compare the same representative workload with Tecton, Hopsworks, custom feature pipelines.
\item[5. Production proof] Start with the failure hypothesis: Incorrect event timestamps or joins leak future information and produce offline-online skew. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Registry plus offline and optional online stores\\
Choose when & Training-serving consistency and reusable structured features matter for production ML.\\
Reject when & The system mainly uses unstructured RAG embeddings or lacks shared feature semantics.\\
Primary failure & Incorrect event timestamps or joins leak future information and produce offline-online skew.\\
Compare with & Tecton; Hopsworks; custom feature pipelines\\
\end{tabularx}
\paragraph{Minimal use in Python / Go.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install feast
feast apply && feast materialize-incremental now
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.feast.dev/}{Feast official documentation}.
\subsubsection{Kubeflow Pipelines}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries KP}}}\quad \badge{Kubernetes ML pipeline platform} \quad \textbf{Languages:} Python / Kubernetes YAML
\par\smallskip Containerized ML workflow components, artifacts, metadata, caching, experiment runs, and Kubernetes execution.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Kubeflow Pipelines as a kubernetes ml pipeline platform between components + artifacts and tracked ml artifacts; its defining mechanism is kubernetes pipeline run.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Components + artifacts};
\node[tech,right=of t0](t1){Kubernetes pipeline run};
\node[tech,right=of t1](t2){Tracked ML artifacts};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Components + artifacts -> Kubernetes pipeline run -> Tracked ML artifacts\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Containerized ML workflow components, artifacts, metadata, caching, experiment runs, and Kubernetes execution. Think of Kubeflow Pipelines as a kubernetes ml pipeline platform between components + artifacts and tracked ml artifacts; its defining mechanism is kubernetes pipeline run.
\item[2. Structure and execution] Follow the data path Components + artifacts → Kubernetes pipeline run → Tracked ML artifacts. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when containerized training and ML pipelines already run on Kubernetes and require portable component graphs. Reject it when the team lacks cluster operations or primarily needs business workflow durability. Compare the same representative workload with Argo Workflows, Vertex AI Pipelines, Flyte.
\item[5. Production proof] Start with the failure hypothesis: Mutable images and undeclared external data make a visually reproducible pipeline graph non-reproducible. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Kubernetes\\
Choose when & Containerized training and ML pipelines already run on Kubernetes and require portable component graphs.\\
Reject when & The team lacks cluster operations or primarily needs business workflow durability.\\
Primary failure & Mutable images and undeclared external data make a visually reproducible pipeline graph non-reproducible.\\
Compare with & Argo Workflows; Vertex AI Pipelines; Flyte\\
\end{tabularx}
\paragraph{Minimal use in Python / Kubernetes YAML.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install kfp
@dsl.pipeline def training_pipeline(): ...
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.kubeflow.org/docs/components/pipelines/}{Kubeflow Pipelines official documentation}.
\subsubsection{ZenML}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries Z}}}\quad \badge{MLOps pipeline framework} \quad \textbf{Languages:} Python
\par\smallskip Portable steps and pipelines connected to artifact stores, orchestrators, experiment trackers, deployers, and infrastructure stacks.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of ZenML as a mlops pipeline framework between python steps + stack and versioned artifacts and run; its defining mechanism is portable ml pipeline.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Python steps + stack};
\node[tech,right=of t0](t1){Portable ML pipeline};
\node[tech,right=of t1](t2){Versioned artifacts and run};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Python steps + stack -> Portable ML pipeline -> Versioned artifacts and run\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Portable steps and pipelines connected to artifact stores, orchestrators, experiment trackers, deployers, and infrastructure stacks. Think of ZenML as a mlops pipeline framework between python steps + stack and versioned artifacts and run; its defining mechanism is portable ml pipeline.
\item[2. Structure and execution] Follow the data path Python steps + stack → Portable ML pipeline → Versioned artifacts and run. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when teams want one Python ML workflow across local development and interchangeable production stacks. Reject it when a single established orchestrator already covers the lifecycle with less abstraction. Compare the same representative workload with Metaflow, Kubeflow Pipelines, Flyte.
\item[5. Production proof] Start with the failure hypothesis: An abstraction layer cannot make incompatible backend semantics identical; cache and artifact behavior must be verified per stack. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local or orchestrator-backed stacks\\
Choose when & Teams want one Python ML workflow across local development and interchangeable production stacks.\\
Reject when & A single established orchestrator already covers the lifecycle with less abstraction.\\
Primary failure & An abstraction layer cannot make incompatible backend semantics identical; cache and artifact behavior must be verified per stack.\\
Compare with & Metaflow; Kubeflow Pipelines; Flyte\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install zenml
zenml init
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.zenml.io/}{ZenML official documentation}.
\subsubsection{Argo Workflows}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries AW}}}\quad \badge{Kubernetes workflow engine} \quad \textbf{Languages:} Go / YAML
\par\smallskip Container-native DAG and step workflows with artifacts, retries, parameters, templates, events, and Kubernetes scheduling.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Argo Workflows as a kubernetes workflow engine between event + workflow template and artifacts + workflow status; its defining mechanism is kubernetes pods + dag controller.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Event + workflow template};
\node[tech,right=of t0](t1){Kubernetes pods + DAG controller};
\node[tech,right=of t1](t2){Artifacts + workflow status};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Event + workflow template -> Kubernetes pods + DAG controller -> Artifacts + workflow status\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Container-native DAG and step workflows with artifacts, retries, parameters, templates, events, and Kubernetes scheduling. Think of Argo Workflows as a kubernetes workflow engine between event + workflow template and artifacts + workflow status; its defining mechanism is kubernetes pods + dag controller.
\item[2. Structure and execution] Follow the data path Event + workflow template → Kubernetes pods + DAG controller → Artifacts + workflow status. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when workloads are containerized and Kubernetes is already the execution and policy plane. Reject it when python-native local development or a non-Kubernetes control plane is required. Compare the same representative workload with Kubeflow Pipelines, Airflow, Flyte.
\item[5. Production proof] Start with the failure hypothesis: Workflow retries can duplicate external effects and YAML templates can conceal image or artifact version drift. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Kubernetes CRDs and controllers\\
Choose when & Workloads are containerized and Kubernetes is already the execution and policy plane.\\
Reject when & Python-native local development or a non-Kubernetes control plane is required.\\
Primary failure & Workflow retries can duplicate external effects and YAML templates can conceal image or artifact version drift.\\
Compare with & Kubeflow Pipelines; Airflow; Flyte\\
\end{tabularx}
\paragraph{Minimal use in Go / YAML.}
\begin{Verbatim}[fontsize=\scriptsize]
kubectl create -f workflow.yaml
argo watch WORKFLOW
\end{Verbatim}
\textbf{Primary sources.} \href{https://argo-workflows.readthedocs.io/}{Argo Workflows official documentation}.
\subsubsection{Flyte}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries F}}}\quad \badge{Data and ML workflow platform} \quad \textbf{Languages:} Python / Go
\par\smallskip Typed tasks and workflows with versioning, caching, artifacts, dynamic execution, scheduling, and scalable ML workloads.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Flyte as a data and ml workflow platform between typed inputs + image and typed outputs + lineage; its defining mechanism is versioned task graph.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Typed inputs + image};
\node[tech,right=of t0](t1){Versioned task graph};
\node[tech,right=of t1](t2){Typed outputs + lineage};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Typed inputs + image -> Versioned task graph -> Typed outputs + lineage\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Typed tasks and workflows with versioning, caching, artifacts, dynamic execution, scheduling, and scalable ML workloads. Think of Flyte as a data and ml workflow platform between typed inputs + image and typed outputs + lineage; its defining mechanism is versioned task graph.
\item[2. Structure and execution] Follow the data path Typed inputs + image → Versioned task graph → Typed outputs + lineage. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when data and ML teams need strongly typed reproducible workflows on Kubernetes across local and production environments. Reject it when a general scheduler or simple Python task runner provides lower operational cost. Compare the same representative workload with Kubeflow Pipelines, Dagster, Metaflow.
\item[5. Production proof] Start with the failure hypothesis: Cache keys, image versions, dynamic tasks, and large artifacts can break reproducibility or control-plane scale. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Kubernetes control plane and SDK\\
Choose when & Data and ML teams need strongly typed reproducible workflows on Kubernetes across local and production environments.\\
Reject when & A general scheduler or simple Python task runner provides lower operational cost.\\
Primary failure & Cache keys, image versions, dynamic tasks, and large artifacts can break reproducibility or control-plane scale.\\
Compare with & Kubeflow Pipelines; Dagster; Metaflow\\
\end{tabularx}
\paragraph{Minimal use in Python / Go.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install flytekit
pyflyte run workflow.py my_workflow
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.flyte.org/}{Flyte official documentation}.
\subsubsection{Metaflow}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries M}}}\quad \badge{Human-friendly data science workflow framework} \quad \textbf{Languages:} Python
\par\smallskip Python framework for steps, artifacts, versions, retries, cards, parallelism, and moving data-science workflows to cloud compute.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Metaflow as a human-friendly data science workflow framework between python flow + data and run artifacts + cards; its defining mechanism is versioned steps on local or cloud compute.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Python flow + data};
\node[tech,right=of t0](t1){Versioned steps on local or cloud compute};
\node[tech,right=of t1](t2){Run artifacts + cards};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Python flow + data -> Versioned steps on local or cloud compute -> Run artifacts + cards\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Python framework for steps, artifacts, versions, retries, cards, parallelism, and moving data-science workflows to cloud compute. Think of Metaflow as a human-friendly data science workflow framework between python flow + data and run artifacts + cards; its defining mechanism is versioned steps on local or cloud compute.
\item[2. Structure and execution] Follow the data path Python flow + data → Versioned steps on local or cloud compute → Run artifacts + cards. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when data scientists need a gentle local-to-cloud path with inspectable run artifacts and step semantics. Reject it when a Kubernetes-native platform or asset-centric data orchestration is the organization standard. Compare the same representative workload with Flyte, Prefect, Dagster.
\item[5. Production proof] Start with the failure hypothesis: Implicit artifact capture and environment differences can create large state, slow resumes, or non-reproducible cloud runs. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local development with cloud batch and orchestration backends\\
Choose when & Data scientists need a gentle local-to-cloud path with inspectable run artifacts and step semantics.\\
Reject when & A Kubernetes-native platform or asset-centric data orchestration is the organization standard.\\
Primary failure & Implicit artifact capture and environment differences can create large state, slow resumes, or non-reproducible cloud runs.\\
Compare with & Flyte; Prefect; Dagster\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install metaflow
python flow.py run
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.metaflow.org/}{Metaflow official documentation}.
\subsubsection{ClearML}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries C}}}\quad \badge{Experiment and pipeline platform} \quad \textbf{Languages:} Python
\par\smallskip Experiment tracking, remote execution, dataset and model management, pipelines, queues, and agents for ML workloads.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of ClearML as a experiment and pipeline platform between code + config + data and artifacts, model, and metrics; its defining mechanism is tracked task on worker queue.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Code + config + data};
\node[tech,right=of t0](t1){Tracked task on worker queue};
\node[tech,right=of t1](t2){Artifacts, model, and metrics};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Code + config + data -> Tracked task on worker queue -> Artifacts, model, and metrics\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Experiment tracking, remote execution, dataset and model management, pipelines, queues, and agents for ML workloads. Think of ClearML as a experiment and pipeline platform between code + config + data and artifacts, model, and metrics; its defining mechanism is tracked task on worker queue.
\item[2. Structure and execution] Follow the data path Code + config + data → Tracked task on worker queue → Artifacts, model, and metrics. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when a team wants one self-hostable control plane for experiments, data, models, and remote workers. Reject it when existing specialized tools already cover the lifecycle with simpler ownership. Compare the same representative workload with MLflow, Weights \& Biases, Kubeflow.
\item[5. Production proof] Start with the failure hypothesis: Automatic code and environment capture can leak secrets or create an illusion of reproducibility when external data is mutable. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or managed server with agents\\
Choose when & A team wants one self-hostable control plane for experiments, data, models, and remote workers.\\
Reject when & Existing specialized tools already cover the lifecycle with simpler ownership.\\
Primary failure & Automatic code and environment capture can leak secrets or create an illusion of reproducibility when external data is mutable.\\
Compare with & MLflow; Weights \& Biases; Kubeflow\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install clearml
clearml-init
\end{Verbatim}
\textbf{Primary sources.} \href{https://clear.ml/docs/latest/docs/}{ClearML official documentation}.
\subsubsection{Label Studio}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries LS}}}\quad \badge{Data labeling platform} \quad \textbf{Languages:} Python / JavaScript
\par\smallskip Configurable annotation UI, projects, import and export, model-assisted labeling, webhooks, and review workflows across data types.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Label Studio as a data labeling platform between raw examples + label config and versioned labeled dataset; its defining mechanism is human and model-assisted annotation.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Raw examples + label config};
\node[tech,right=of t0](t1){Human and model-assisted annotation};
\node[tech,right=of t1](t2){Versioned labeled dataset};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Raw examples + label config -> Human and model-assisted annotation -> Versioned labeled dataset\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Configurable annotation UI, projects, import and export, model-assisted labeling, webhooks, and review workflows across data types. Think of Label Studio as a data labeling platform between raw examples + label config and versioned labeled dataset; its defining mechanism is human and model-assisted annotation.
\item[2. Structure and execution] Follow the data path Raw examples + label config → Human and model-assisted annotation → Versioned labeled dataset. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when teams need human labeling and review workflows with flexible interfaces and model preannotations. Reject it when programmatic weak supervision or a highly specialized vertical labeling product is more appropriate. Compare the same representative workload with Argilla, Prodigy, custom labeling UI.
\item[5. Production proof] Start with the failure hypothesis: Label guidelines, reviewer agreement, sampling, and exported schema can drift even when the UI works well. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or managed application\\
Choose when & Teams need human labeling and review workflows with flexible interfaces and model preannotations.\\
Reject when & Programmatic weak supervision or a highly specialized vertical labeling product is more appropriate.\\
Primary failure & Label guidelines, reviewer agreement, sampling, and exported schema can drift even when the UI works well.\\
Compare with & Argilla; Prodigy; custom labeling UI\\
\end{tabularx}
\paragraph{Minimal use in Python / JavaScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install label-studio
label-studio start
\end{Verbatim}
\textbf{Primary sources.} \href{https://labelstud.io/guide/}{Label Studio official documentation}.
\subsubsection{Great Expectations}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries GE}}}\quad \badge{Data quality validation framework} \quad \textbf{Languages:} Python
\par\smallskip Declarative expectations, validation definitions, checkpoints, data docs, and integrations for testing data assets.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Great Expectations as a data quality validation framework between data batch + expectation suite and results + documentation; its defining mechanism is validation checkpoint.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Data batch + expectation suite};
\node[tech,right=of t0](t1){Validation checkpoint};
\node[tech,right=of t1](t2){Results + documentation};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Data batch + expectation suite -> Validation checkpoint -> Results + documentation\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Declarative expectations, validation definitions, checkpoints, data docs, and integrations for testing data assets. Think of Great Expectations as a data quality validation framework between data batch + expectation suite and results + documentation; its defining mechanism is validation checkpoint.
\item[2. Structure and execution] Follow the data path Data batch + expectation suite → Validation checkpoint → Results + documentation. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when pipelines need explicit data contracts and human-readable validation evidence before model or index builds. Reject it when streaming runtime monitoring or a tiny schema check is the only requirement. Compare the same representative workload with Soda, Pandera, dbt tests.
\item[5. Production proof] Start with the failure hypothesis: Passing expectations can still miss semantic drift, label leakage, or model-specific data defects. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library, CI, orchestrators, or managed cloud\\
Choose when & Pipelines need explicit data contracts and human-readable validation evidence before model or index builds.\\
Reject when & Streaming runtime monitoring or a tiny schema check is the only requirement.\\
Primary failure & Passing expectations can still miss semantic drift, label leakage, or model-specific data defects.\\
Compare with & Soda; Pandera; dbt tests\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install great_expectations
gx init
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.greatexpectations.io/docs/}{Great Expectations official documentation}.
\subsubsection{Airbyte}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries A}}}\quad \badge{Data integration platform} \quad \textbf{Languages:} Java / Python / connector SDKs
\par\smallskip Connector platform for extracting and loading operational data with incremental syncs, schemas, normalization, and orchestration integrations.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Airbyte as a data integration platform between source systems + connection and raw versioned destination tables; its defining mechanism is incremental extract and load.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Source systems + connection};
\node[tech,right=of t0](t1){Incremental extract and load};
\node[tech,right=of t1](t2){Raw versioned destination tables};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Source systems + connection -> Incremental extract and load -> Raw versioned destination tables\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Connector platform for extracting and loading operational data with incremental syncs, schemas, normalization, and orchestration integrations. Think of Airbyte as a data integration platform between source systems + connection and raw versioned destination tables; its defining mechanism is incremental extract and load.
\item[2. Structure and execution] Follow the data path Source systems + connection → Incremental extract and load → Raw versioned destination tables. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when many SaaS and database sources must enter a governed lake or warehouse through maintained connectors. Reject it when low-latency event streaming or bespoke high-control ingestion is required. Compare the same representative workload with Fivetran, Debezium, custom connectors.
\item[5. Production proof] Start with the failure hypothesis: Schema changes, cursor mistakes, deletions, and source API limits can silently create incomplete downstream corpora. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or managed cloud\\
Choose when & Many SaaS and database sources must enter a governed lake or warehouse through maintained connectors.\\
Reject when & Low-latency event streaming or bespoke high-control ingestion is required.\\
Primary failure & Schema changes, cursor mistakes, deletions, and source API limits can silently create incomplete downstream corpora.\\
Compare with & Fivetran; Debezium; custom connectors\\
\end{tabularx}
\paragraph{Minimal use in Java / Python / connector SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
abctl local install
Configure source, destination, and sync
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.airbyte.com/}{Airbyte official documentation}.
\subsubsection{Unstructured}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries U}}}\quad \badge{Document ingestion and partitioning toolkit} \quad \textbf{Languages:} Python / REST
\par\smallskip Partitions documents into typed elements with metadata, connectors, chunking, enrichment, and ingestion workflows for RAG.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Unstructured as a document ingestion and partitioning toolkit between files + parser config and typed elements + provenance; its defining mechanism is partition, enrich, chunk.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Files + parser config};
\node[tech,right=of t0](t1){Partition, enrich, chunk};
\node[tech,right=of t1](t2){Typed elements + provenance};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Files + parser config -> Partition, enrich, chunk -> Typed elements + provenance\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Partitions documents into typed elements with metadata, connectors, chunking, enrichment, and ingestion workflows for RAG. Think of Unstructured as a document ingestion and partitioning toolkit between files + parser config and typed elements + provenance; its defining mechanism is partition, enrich, chunk.
\item[2. Structure and execution] Follow the data path Files + parser config → Partition, enrich, chunk → Typed elements + provenance. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when a heterogeneous enterprise corpus needs one extensible document-ingestion layer. Reject it when a narrow file type, custom parser, or preservation-critical layout workflow deserves a specialized tool. Compare the same representative workload with Docling, Apache Tika, custom parsers.
\item[5. Production proof] Start with the failure hypothesis: OCR, tables, reading order, and metadata extraction vary by document and can silently damage retrieval quality. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library, open-source API, or managed service\\
Choose when & A heterogeneous enterprise corpus needs one extensible document-ingestion layer.\\
Reject when & A narrow file type, custom parser, or preservation-critical layout workflow deserves a specialized tool.\\
Primary failure & OCR, tables, reading order, and metadata extraction vary by document and can silently damage retrieval quality.\\
Compare with & Docling; Apache Tika; custom parsers\\
\end{tabularx}
\paragraph{Minimal use in Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install unstructured
from unstructured.partition.auto import partition
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.unstructured.io/}{Unstructured official documentation}.
\subsubsection{Docling}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries D}}}\quad \badge{Document conversion toolkit} \quad \textbf{Languages:} Python
\par\smallskip Open-source document conversion with layout, tables, reading order, OCR, multiple formats, and structured output for downstream AI.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Docling as a document conversion toolkit between documents + conversion options and structured document + assets; its defining mechanism is layout and table pipeline.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Documents + conversion options};
\node[tech,right=of t0](t1){Layout and table pipeline};
\node[tech,right=of t1](t2){Structured document + assets};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Documents + conversion options -> Layout and table pipeline -> Structured document + assets\\
Control plane & Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.\\
State and trust boundary & Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Open-source document conversion with layout, tables, reading order, OCR, multiple formats, and structured output for downstream AI. Think of Docling as a document conversion toolkit between documents + conversion options and structured document + assets; its defining mechanism is layout and table pipeline.
\item[2. Structure and execution] Follow the data path Documents + conversion options → Layout and table pipeline → Structured document + assets. Events or schedules materialize a versioned task or asset graph with retries, backfills, lineage, validation, and promotion gates.
\item[3. State, security, and operations] Source snapshots, schemas, code, environments, artifacts, partitions, run ids, lineage, quality results, and approvals must be replayable.
\item[4. Selection decision] Choose it when high-quality local parsing of PDFs and office documents matters and structured provenance must be retained. Reject it when a managed connector platform or simple text extraction is enough. Compare the same representative workload with Unstructured, Marker, Apache Tika.
\item[5. Production proof] Start with the failure hypothesis: Complex scans, equations, nested tables, and multilingual layouts still require page-level evaluation. Backfill at scale, kill workers mid-effect, change schemas, restore artifacts, reproduce a release, and reconcile every ambiguous outcome.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local library, CLI, or service\\
Choose when & High-quality local parsing of PDFs and office documents matters and structured provenance must be retained.\\
Reject when & A managed connector platform or simple text extraction is enough.\\
Primary failure & Complex scans, equations, nested tables, and multilingual layouts still require page-level evaluation.\\
Compare with & Unstructured; Marker; Apache Tika\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install docling
docling document.pdf
\end{Verbatim}
\textbf{Primary sources.} \href{https://docling-project.github.io/docling/}{Docling official documentation}.
\clearpage\section{Technology interview question bank}
Every listed technology has one medium selection question, a hard architecture question, a hard comparison and measurement question, and one very-hard failure, migration, and recovery question. Product APIs evolve; defend answers with workload evidence and the linked official documentation.
\subsection{Agent and application frameworks}
\subsubsection*{LangChain}
\paragraph{MEDIUM - TECH-MCQ-001} Which workload is the strongest fit for LangChain?
\begin{enumerate}[label=\Alph*.]
\item The product team is TypeScript-first and wants agents and workflows close to application code.
\item A role/task metaphor helps a team prototype coordinated research, operations, or content workflows.
\item You need provider-neutral components, rapid RAG construction, or standard tool calling.
\item You are building a long-horizon research or coding agent and want a batteries-included harness.
\end{enumerate}
\textbf{Answer.} You need provider-neutral components, rapid RAG construction, or standard tool calling.
\textit{Composable model, prompt, retriever, tool, and agent interfaces with a broad integration ecosystem. Compare it with LlamaIndex, Haystack, PydanticAI; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-001} Compare LangChain with LlamaIndex, Haystack. What measurements would decide the choice?
\textbf{Answer.} Choose LangChain when you need provider-neutral components, rapid RAG construction, or standard tool calling. Avoid it when a small direct SDK call is clearer, or the workflow needs durable explicit state without LangGraph. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-001} Explain the internal structure and design of LangChain. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of LangChain as a application library between user request and model or tool result; its defining mechanism is runnable composition. Trace User request -> Runnable composition -> Model or tool result. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-001} You selected LangChain for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Default abstractions can hide prompts, state growth, retries, and provider-specific semantics. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path User request -> Runnable composition -> Model or tool result; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (LlamaIndex), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{LangGraph}
\paragraph{MEDIUM - TECH-MCQ-002} Which workload is the strongest fit for LangGraph?
\begin{enumerate}[label=\Alph*.]
\item You need provider-neutral components, rapid RAG construction, or standard tool calling.
\item Deterministic business conversations, channel integration, and controlled dialogue policy matter alongside LLM flexibility.
\item The application branches, loops, pauses for approval, or must resume from persisted state.
\item A JavaScript team wants fast visual assembly of LangChain-style RAG and agents.
\end{enumerate}
\textbf{Answer.} The application branches, loops, pauses for approval, or must resume from persisted state.
\textit{Graph-based agent runtime with typed state, checkpoints, interrupts, streaming, and durable execution patterns. Compare it with Temporal, AutoGen, custom state machine; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-002} Compare LangGraph with Temporal, AutoGen. What measurements would decide the choice?
\textbf{Answer.} Choose LangGraph when the application branches, loops, pauses for approval, or must resume from persisted state. Avoid it when a linear one-shot chain is enough, or business effects require a general workflow engine outside the agent graph. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-002} Explain the internal structure and design of LangGraph. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of LangGraph as a stateful orchestration library between durable state and checkpointed outcome; its defining mechanism is graph node + transition. Trace Durable state -> Graph node + transition -> Checkpointed outcome. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-002} You selected LangGraph for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: A retried node can duplicate an external effect unless the effect is idempotent and separately recorded. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Durable state -> Graph node + transition -> Checkpointed outcome; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Temporal), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Deep Agents}
\paragraph{MEDIUM - TECH-MCQ-003} Which workload is the strongest fit for Deep Agents?
\begin{enumerate}[label=\Alph*.]
\item You are building a long-horizon research or coding agent and want a batteries-included harness.
\item Personalization across sessions matters and memory extraction can be evaluated against explicit product policy.
\item You want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem.
\item Deterministic business conversations, channel integration, and controlled dialogue policy matter alongside LLM flexibility.
\end{enumerate}
\textbf{Answer.} You are building a long-horizon research or coding agent and want a batteries-included harness.
\textit{LangGraph-based harness with planning, filesystem context, subagents, memory, skills, and pluggable execution backends. Compare it with LangGraph, Claude Agent SDK, OpenAI Agents SDK; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-003} Compare Deep Agents with LangGraph, Claude Agent SDK. What measurements would decide the choice?
\textbf{Answer.} Choose Deep Agents when you are building a long-horizon research or coding agent and want a batteries-included harness. Avoid it when you need a narrowly bounded single agent, or cannot accept filesystem and delegation complexity. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-003} Explain the internal structure and design of Deep Agents. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Deep Agents as a agent harness between task + files and verified artifact; its defining mechanism is plan, delegate, use skills. Trace Task + files -> Plan, delegate, use skills -> Verified artifact. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-003} You selected Deep Agents for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Giving the harness a powerful backend without matching permission rules turns convenience into broad ambient authority. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Task + files -> Plan, delegate, use skills -> Verified artifact; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (LangGraph), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{LlamaIndex}
\paragraph{MEDIUM - TECH-MCQ-004} Which workload is the strongest fit for LlamaIndex?
\begin{enumerate}[label=\Alph*.]
\item You need multi-agent messaging, distributed runtimes, or research-friendly team patterns.
\item The hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG.
\item A product team wants an integrated low-code environment for RAG and agent applications.
\item You use Gemini or Google Cloud but want a framework that can also express model-agnostic agent teams.
\end{enumerate}
\textbf{Answer.} The hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG.
\textit{Connectors, parsing, indexes, retrievers, query engines, workflows, and agents centered on private-data applications. Compare it with Haystack, LangChain, custom retrieval services; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-004} Compare LlamaIndex with Haystack, LangChain. What measurements would decide the choice?
\textbf{Answer.} Choose LlamaIndex when the hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG. Avoid it when you need a tiny retrieval loop, or want framework-independent ingestion contracts. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-004} Explain the internal structure and design of LlamaIndex. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of LlamaIndex as a data and rag framework between documents and evidence-bound response; its defining mechanism is parse, index, retrieve. Trace Documents -> Parse, index, retrieve -> Evidence-bound response. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-004} You selected LlamaIndex for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Convenient ingestion defaults can erase source structure, provenance, and version identity. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Documents -> Parse, index, retrieve -> Evidence-bound response; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Haystack), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Haystack}
\paragraph{MEDIUM - TECH-MCQ-005} Which workload is the strongest fit for Haystack?
\begin{enumerate}[label=\Alph*.]
\item You want explicit modular pipelines, strong search heritage, and inspectable component connections.
\item The hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG.
\item Persistent agent memory and inspectable state are central product requirements.
\item You need globally distributed stateful agents and already accept the Cloudflare runtime model.
\end{enumerate}
\textbf{Answer.} You want explicit modular pipelines, strong search heritage, and inspectable component connections.
\textit{Typed components and pipelines for search, RAG, agents, evaluation, and production integration. Compare it with LlamaIndex, LangChain, DSPy; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-005} Compare Haystack with LlamaIndex, LangChain. What measurements would decide the choice?
\textbf{Answer.} Choose Haystack when you want explicit modular pipelines, strong search heritage, and inspectable component connections. Avoid it when your team depends on TypeScript-first application code or only needs a direct model SDK. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-005} Explain the internal structure and design of Haystack. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Haystack as a pipeline framework between documents + query and answer + metadata; its defining mechanism is pipeline components. Trace Documents + query -> Pipeline components -> Answer + metadata. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-005} You selected Haystack for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: A visually clean pipeline can still conceal incompatible document schemas or unmeasured retriever quality. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Documents + query -> Pipeline components -> Answer + metadata; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (LlamaIndex), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Semantic Kernel}
\paragraph{MEDIUM - TECH-MCQ-006} Which workload is the strongest fit for Semantic Kernel?
\begin{enumerate}[label=\Alph*.]
\item Your estate is .NET, Java, Microsoft 365, or Azure and plugins must fit established dependency-injection patterns.
\item You use Gemini or Google Cloud but want a framework that can also express model-agnostic agent teams.
\item A TypeScript web product needs streaming model and tool interactions close to its UI framework.
\item You need a coding or research agent with mature filesystem, terminal, and MCP behavior.
\end{enumerate}
\textbf{Answer.} Your estate is .NET, Java, Microsoft 365, or Azure and plugins must fit established dependency-injection patterns.
\textit{Microsoft SDK for agents, plugins, process orchestration, memory integrations, and enterprise application embedding. Compare it with LangChain, AutoGen, OpenAI Agents SDK; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-006} Compare Semantic Kernel with LangChain, AutoGen. What measurements would decide the choice?
\textbf{Answer.} Choose Semantic Kernel when your estate is .NET, Java, Microsoft 365, or Azure and plugins must fit established dependency-injection patterns. Avoid it when you need the broadest open-source Python integration catalog or a very small provider-specific client. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-006} Explain the internal structure and design of Semantic Kernel. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Semantic Kernel as a enterprise agent sdk between enterprise context and governed ai feature; its defining mechanism is kernel + plugins. Trace Enterprise context -> Kernel + plugins -> Governed AI feature. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-006} You selected Semantic Kernel for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Treating natural-language planning as authorization can bypass the surrounding enterprise control plane. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Enterprise context -> Kernel + plugins -> Governed AI feature; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (LangChain), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{AutoGen}
\paragraph{MEDIUM - TECH-MCQ-007} Which workload is the strongest fit for AutoGen?
\begin{enumerate}[label=\Alph*.]
\item The hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG.
\item You want open multi-language agent components with middleware and production integration points.
\item You need multi-agent messaging, distributed runtimes, or research-friendly team patterns.
\item You are researching multi-agent coordination, role specialization, or agent-society behavior.
\end{enumerate}
\textbf{Answer.} You need multi-agent messaging, distributed runtimes, or research-friendly team patterns.
\textit{Microsoft framework for conversational, event-driven, single-agent and multi-agent systems with extensible runtimes. Compare it with LangGraph, CrewAI, Semantic Kernel; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-007} Compare AutoGen with LangGraph, CrewAI. What measurements would decide the choice?
\textbf{Answer.} Choose AutoGen when you need multi-agent messaging, distributed runtimes, or research-friendly team patterns. Avoid it when a deterministic graph or ordinary function composition expresses the workflow more safely. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-007} Explain the internal structure and design of AutoGen. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of AutoGen as a event-driven agent framework between events + roles and team outcome; its defining mechanism is agent messages + tools. Trace Events + roles -> Agent messages + tools -> Team outcome. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-007} You selected AutoGen for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Free-form agent conversations can loop, amplify cost, and obscure which participant owns a decision. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Events + roles -> Agent messages + tools -> Team outcome; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (LangGraph), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{CrewAI}
\paragraph{MEDIUM - TECH-MCQ-008} Which workload is the strongest fit for CrewAI?
\begin{enumerate}[label=\Alph*.]
\item Deterministic business conversations, channel integration, and controlled dialogue policy matter alongside LLM flexibility.
\item The hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG.
\item A role/task metaphor helps a team prototype coordinated research, operations, or content workflows.
\item You need provider-neutral components, rapid RAG construction, or standard tool calling.
\end{enumerate}
\textbf{Answer.} A role/task metaphor helps a team prototype coordinated research, operations, or content workflows.
\textit{Agents, crews, tasks, and flows for role-oriented collaboration and event-driven automation. Compare it with AutoGen, LangGraph, PydanticAI; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-008} Compare CrewAI with AutoGen, LangGraph. What measurements would decide the choice?
\textbf{Answer.} Choose CrewAI when a role/task metaphor helps a team prototype coordinated research, operations, or content workflows. Avoid it when you need low-level state-machine semantics, strict event schemas, or non-Python first-class support. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-008} Explain the internal structure and design of CrewAI. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of CrewAI as a role-based agent framework between goal + roles and delegated deliverable; its defining mechanism is crew or flow. Trace Goal + roles -> Crew or flow -> Delegated deliverable. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-008} You selected CrewAI for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Role labels can suggest organizational rigor while retries, authority, and shared memory remain underspecified. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Goal + roles -> Crew or flow -> Delegated deliverable; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (AutoGen), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{PydanticAI}
\paragraph{MEDIUM - TECH-MCQ-009} Which workload is the strongest fit for PydanticAI?
\begin{enumerate}[label=\Alph*.]
\item You are researching multi-agent coordination, role specialization, or agent-society behavior.
\item A Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET.
\item You want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem.
\item Python typing and Pydantic validation should define the boundary between models and application code.
\end{enumerate}
\textbf{Answer.} Python typing and Pydantic validation should define the boundary between models and application code.
\textit{Model-agnostic agents with typed dependencies, tools, structured results, validation, testing, and graph support. Compare it with OpenAI Agents SDK, LangChain, Semantic Kernel; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-009} Compare PydanticAI with OpenAI Agents SDK, LangChain. What measurements would decide the choice?
\textbf{Answer.} Choose PydanticAI when python typing and Pydantic validation should define the boundary between models and application code. Avoid it when you need a visual multi-agent workbench or a TypeScript-native runtime. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-009} Explain the internal structure and design of PydanticAI. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of PydanticAI as a typed agent framework between typed dependencies and typed result; its defining mechanism is agent + validators. Trace Typed dependencies -> Agent + validators -> Typed result. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-009} You selected PydanticAI for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Schema-valid output can still be false, unsafe, or unauthorized; semantic checks remain necessary. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Typed dependencies -> Agent + validators -> Typed result; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (OpenAI Agents SDK), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{DSPy}
\paragraph{MEDIUM - TECH-MCQ-010} Which workload is the strongest fit for DSPy?
\begin{enumerate}[label=\Alph*.]
\item The product team is TypeScript-first and wants agents and workflows close to application code.
\item You need provider-neutral components, rapid RAG construction, or standard tool calling.
\item You have representative training/evaluation examples and want systematic optimization rather than manual prompt editing.
\item Cross-functional teams need to prototype and inspect component graphs visually before hardening them.
\end{enumerate}
\textbf{Answer.} You have representative training/evaluation examples and want systematic optimization rather than manual prompt editing.
\textit{Declarative LM modules and optimizers that compile prompts or weights against examples and metrics. Compare it with manual prompting, LangChain, prompt optimizers; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-010} Compare DSPy with manual prompting, LangChain. What measurements would decide the choice?
\textbf{Answer.} Choose DSPy when you have representative training/evaluation examples and want systematic optimization rather than manual prompt editing. Avoid it when the task lacks a measurable objective, stable examples, or budget for repeated model calls. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-010} Explain the internal structure and design of DSPy. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of DSPy as a program optimization framework between examples + metric and optimized predictor; its defining mechanism is compile lm program. Trace Examples + metric -> Compile LM program -> Optimized predictor. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-010} You selected DSPy for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Optimizing against a narrow metric or leaky development set produces impressive but brittle programs. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Examples + metric -> Compile LM program -> Optimized predictor; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (manual prompting), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{OpenAI Agents SDK}
\paragraph{MEDIUM - TECH-MCQ-011} Which workload is the strongest fit for OpenAI Agents SDK?
\begin{enumerate}[label=\Alph*.]
\item You want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem.
\item The product team is TypeScript-first and wants agents and workflows close to application code.
\item A Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET.
\item You want an intentionally small agent API with first-class OpenAI platform integration and inspectable handoffs.
\end{enumerate}
\textbf{Answer.} You want an intentionally small agent API with first-class OpenAI platform integration and inspectable handoffs.
\textit{Small primitives for agents, handoffs, guardrails, sessions, tracing, and hosted or local tools. Compare it with PydanticAI, LangGraph, Google ADK; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-011} Compare OpenAI Agents SDK with PydanticAI, LangGraph. What measurements would decide the choice?
\textbf{Answer.} Choose OpenAI Agents SDK when you want an intentionally small agent API with first-class OpenAI platform integration and inspectable handoffs. Avoid it when you require a provider-neutral high-level RAG ecosystem or durable graph checkpoints as the main abstraction. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-011} Explain the internal structure and design of OpenAI Agents SDK. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of OpenAI Agents SDK as a agent sdk between input + tools and traced result; its defining mechanism is agent loop + handoffs. Trace Input + tools -> Agent loop + handoffs -> Traced result. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-011} You selected OpenAI Agents SDK for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: A handoff can silently expand capability scope unless tools and session state are narrowed per receiving agent. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Input + tools -> Agent loop + handoffs -> Traced result; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (PydanticAI), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Google Agent Development Kit}
\paragraph{MEDIUM - TECH-MCQ-012} Which workload is the strongest fit for Google Agent Development Kit?
\begin{enumerate}[label=\Alph*.]
\item You use Gemini or Google Cloud but want a framework that can also express model-agnostic agent teams.
\item A TypeScript web product needs streaming model and tool interactions close to its UI framework.
\item You need provider-neutral components, rapid RAG construction, or standard tool calling.
\item Your estate is .NET, Java, Microsoft 365, or Azure and plugins must fit established dependency-injection patterns.
\end{enumerate}
\textbf{Answer.} You use Gemini or Google Cloud but want a framework that can also express model-agnostic agent teams.
\textit{Google framework for model-flexible agents, tools, sessions, evaluation, multi-agent patterns, and deployment. Compare it with OpenAI Agents SDK, Semantic Kernel, LangGraph; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-012} Compare Google Agent Development Kit with OpenAI Agents SDK, Semantic Kernel. What measurements would decide the choice?
\textbf{Answer.} Choose Google Agent Development Kit when you use Gemini or Google Cloud but want a framework that can also express model-agnostic agent teams. Avoid it when your application is tightly aligned to another provider or needs LangGraph-style explicit checkpoint semantics. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-012} Explain the internal structure and design of Google Agent Development Kit. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Google Agent Development Kit as a agent development kit between user/session and deployable service; its defining mechanism is agent + tools + events. Trace User/session -> Agent + tools + events -> Deployable service. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-012} You selected Google Agent Development Kit for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Local success can mask identity, quota, state-store, and region behavior at managed deployment time. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path User/session -> Agent + tools + events -> Deployable service; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (OpenAI Agents SDK), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Mastra}
\paragraph{MEDIUM - TECH-MCQ-013} Which workload is the strongest fit for Mastra?
\begin{enumerate}[label=\Alph*.]
\item You want an intentionally small agent API with first-class OpenAI platform integration and inspectable handoffs.
\item The product team is TypeScript-first and wants agents and workflows close to application code.
\item A JavaScript team wants fast visual assembly of LangChain-style RAG and agents.
\item A Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET.
\end{enumerate}
\textbf{Answer.} The product team is TypeScript-first and wants agents and workflows close to application code.
\textit{TypeScript-native agents, workflows, RAG, memory, evaluations, and observability for modern web stacks. Compare it with LangChain.js, Vercel AI SDK, Temporal; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-013} Compare Mastra with LangChain.js, Vercel AI SDK. What measurements would decide the choice?
\textbf{Answer.} Choose Mastra when the product team is TypeScript-first and wants agents and workflows close to application code. Avoid it when python-only model research integrations dominate or a general durable workflow engine is already mandated. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-013} Explain the internal structure and design of Mastra. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Mastra as a ai application framework between app event and typed application result; its defining mechanism is agent or workflow. Trace App event -> Agent or workflow -> Typed application result. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-013} You selected Mastra for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: In-process workflow state without deliberate persistence and idempotency can be lost or replay side effects. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path App event -> Agent or workflow -> Typed application result; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (LangChain.js), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Zep}
\paragraph{MEDIUM - TECH-MCQ-083} Which workload is the strongest fit for Zep?
\begin{enumerate}[label=\Alph*.]
\item You want explicit modular pipelines, strong search heritage, and inspectable component connections.
\item Cross-functional teams need to prototype and inspect component graphs visually before hardening them.
\item The application branches, loops, pauses for approval, or must resume from persisted state.
\item Agents need durable, evolving user or entity context with retrieval and provenance beyond a short chat buffer.
\end{enumerate}
\textbf{Answer.} Agents need durable, evolving user or entity context with retrieval and provenance beyond a short chat buffer.
\textit{Temporal knowledge and conversation memory designed to assemble relevant user and agent context over time. Compare it with Mem0, Neo4j, PostgreSQL; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-083} Compare Zep with Mem0, Neo4j. What measurements would decide the choice?
\textbf{Answer.} Choose Zep when agents need durable, evolving user or entity context with retrieval and provenance beyond a short chat buffer. Avoid it when simple session history, explicit relational facts, or application-owned event sourcing is sufficient. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-083} Explain the internal structure and design of Zep. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Zep as a agent memory and context platform between conversation events and context for the next turn; its defining mechanism is temporal memory graph. Trace Conversation events -> Temporal memory graph -> Context for the next turn. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-083} You selected Zep for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Automatically extracted memories can become stale, cross tenant boundaries, or be treated as authority without provenance and correction. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Conversation events -> Temporal memory graph -> Context for the next turn; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Mem0), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Mem0}
\paragraph{MEDIUM - TECH-MCQ-084} Which workload is the strongest fit for Mem0?
\begin{enumerate}[label=\Alph*.]
\item The hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG.
\item You need an in-process model-agnostic agent loop with production lifecycle controls and strong extension points.
\item A TypeScript web product needs streaming model and tool interactions close to its UI framework.
\item Personalization across sessions matters and memory extraction can be evaluated against explicit product policy.
\end{enumerate}
\textbf{Answer.} Personalization across sessions matters and memory extraction can be evaluated against explicit product policy.
\textit{Extracts, stores, updates, and retrieves personalized memories for agents across sessions and data backends. Compare it with Zep, LangGraph stores, custom memory service; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-084} Compare Mem0 with Zep, LangGraph stores. What measurements would decide the choice?
\textbf{Answer.} Choose Mem0 when personalization across sessions matters and memory extraction can be evaluated against explicit product policy. Avoid it when a bounded conversation window or application-owned profile table is clearer and easier to govern. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-084} Explain the internal structure and design of Mem0. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Mem0 as a agent memory layer between interaction + identity and selected context; its defining mechanism is extract and reconcile memory. Trace Interaction + identity -> Extract and reconcile memory -> Selected context. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-084} You selected Mem0 for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: A plausible extracted preference can be wrong, overshared, or impossible for a user to inspect and delete. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Interaction + identity -> Extract and reconcile memory -> Selected context; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Zep), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Agno}
\paragraph{MEDIUM - TECH-MCQ-112} Which workload is the strongest fit for Agno?
\begin{enumerate}[label=\Alph*.]
\item You want a Python-first framework that joins agent construction with sessions, memory, knowledge, and serving.
\item Python typing and Pydantic validation should define the boundary between models and application code.
\item You want explicit modular pipelines, strong search heritage, and inspectable component connections.
\item You want Python-first multi-agent collaboration with relatively transparent message and task control.
\end{enumerate}
\textbf{Answer.} You want a Python-first framework that joins agent construction with sessions, memory, knowledge, and serving.
\textit{Framework for model-agnostic agents, teams, workflows, knowledge, memory, tools, and an operational runtime. Compare it with LangGraph, CrewAI, PydanticAI; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-112} Compare Agno with LangGraph, CrewAI. What measurements would decide the choice?
\textbf{Answer.} Choose Agno when you want a Python-first framework that joins agent construction with sessions, memory, knowledge, and serving. Avoid it when you need an explicit durable state graph or a tiny direct model loop. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-112} Explain the internal structure and design of Agno. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Agno as a multi-agent framework and runtime between request + session and tool result or response; its defining mechanism is agent or team runtime. Trace Request + session -> Agent or team runtime -> Tool result or response. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-112} You selected Agno for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Convenient shared memory and team abstractions can blur tenant boundaries and the owner of a side effect. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Request + session -> Agent or team runtime -> Tool result or response; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (LangGraph), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Hugging Face smolagents}
\paragraph{MEDIUM - TECH-MCQ-113} Which workload is the strongest fit for Hugging Face smolagents?
\begin{enumerate}[label=\Alph*.]
\item You need multi-agent messaging, distributed runtimes, or research-friendly team patterns.
\item You want Python-first multi-agent collaboration with relatively transparent message and task control.
\item You need a coding or research agent with mature filesystem, terminal, and MCP behavior.
\item You want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem.
\end{enumerate}
\textbf{Answer.} You want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem.
\textit{Compact framework centered on CodeAgent, ToolCallingAgent, typed tools, model adapters, and optional sandbox execution. Compare it with OpenAI Agents SDK, PydanticAI, LangGraph; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-113} Compare Hugging Face smolagents with OpenAI Agents SDK, PydanticAI. What measurements would decide the choice?
\textbf{Answer.} Choose Hugging Face smolagents when you want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem. Avoid it when you require durable workflow state, a broad managed control plane, or complex multi-tenant orchestration. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-113} Explain the internal structure and design of Hugging Face smolagents. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Hugging Face smolagents as a minimal agent framework between task + tools and observed answer; its defining mechanism is code or tool-calling loop. Trace Task + tools -> Code or tool-calling loop -> Observed answer. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-113} You selected Hugging Face smolagents for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Generated code can exceed intended authority unless imports, network, filesystem, time, and credentials are constrained. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Task + tools -> Code or tool-calling loop -> Observed answer; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (OpenAI Agents SDK), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Letta}
\paragraph{MEDIUM - TECH-MCQ-114} Which workload is the strongest fit for Letta?
\begin{enumerate}[label=\Alph*.]
\item Persistent agent memory and inspectable state are central product requirements.
\item You want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem.
\item You are building a long-horizon research or coding agent and want a batteries-included harness.
\item The hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG.
\end{enumerate}
\textbf{Answer.} Persistent agent memory and inspectable state are central product requirements.
\textit{Stateful agent runtime built around explicit memory blocks, archival memory, tools, messages, and long-running agent identity. Compare it with Mem0, Zep, LangGraph; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-114} Compare Letta with Mem0, Zep. What measurements would decide the choice?
\textbf{Answer.} Choose Letta when persistent agent memory and inspectable state are central product requirements. Avoid it when a stateless request pipeline or application-owned profile store is simpler and sufficient. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-114} Explain the internal structure and design of Letta. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Letta as a stateful agent platform between messages + memory blocks and updated memory + response; its defining mechanism is stateful agent loop. Trace Messages + memory blocks -> Stateful agent loop -> Updated memory + response. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-114} You selected Letta for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Derived memories can become stale or over-authoritative without provenance, correction, expiration, and deletion controls. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Messages + memory blocks -> Stateful agent loop -> Updated memory + response; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Mem0), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Langroid}
\paragraph{MEDIUM - TECH-MCQ-115} Which workload is the strongest fit for Langroid?
\begin{enumerate}[label=\Alph*.]
\item A Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET.
\item You want Python-first multi-agent collaboration with relatively transparent message and task control.
\item Agents need durable, evolving user or entity context with retrieval and provenance beyond a short chat buffer.
\item The application branches, loops, pauses for approval, or must resume from persisted state.
\end{enumerate}
\textbf{Answer.} You want Python-first multi-agent collaboration with relatively transparent message and task control.
\textit{Message-oriented framework for agents, tasks, tools, retrieval, and multi-agent delegation with explicit Python components. Compare it with AutoGen, CrewAI, LangGraph; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-115} Compare Langroid with AutoGen, CrewAI. What measurements would decide the choice?
\textbf{Answer.} Choose Langroid when you want Python-first multi-agent collaboration with relatively transparent message and task control. Avoid it when a managed platform, TypeScript runtime, or durable graph checkpointing is required. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-115} Explain the internal structure and design of Langroid. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Langroid as a multi-agent programming framework between task + agent messages and result or delegated message; its defining mechanism is task orchestration + tools. Trace Task + agent messages -> Task orchestration + tools -> Result or delegated message. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-115} You selected Langroid for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Conversational delegation can loop or lose ownership unless budgets, termination, and tool authority are explicit. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Task + agent messages -> Task orchestration + tools -> Result or delegated message; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (AutoGen), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{BeeAI Framework}
\paragraph{MEDIUM - TECH-MCQ-116} Which workload is the strongest fit for BeeAI Framework?
\begin{enumerate}[label=\Alph*.]
\item A Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET.
\item The product team is TypeScript-first and wants agents and workflows close to application code.
\item You want open multi-language agent components with middleware and production integration points.
\item You are researching multi-agent coordination, role specialization, or agent-society behavior.
\end{enumerate}
\textbf{Answer.} You want open multi-language agent components with middleware and production integration points.
\textit{Framework for composable agents, tools, workflows, memory, middleware, and observability across model providers. Compare it with LangChain, Mastra, Semantic Kernel; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-116} Compare BeeAI Framework with LangChain, Mastra. What measurements would decide the choice?
\textbf{Answer.} Choose BeeAI Framework when you want open multi-language agent components with middleware and production integration points. Avoid it when a vendor-native SDK or explicit graph runtime already matches the product boundary. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-116} Explain the internal structure and design of BeeAI Framework. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of BeeAI Framework as a open-source agent framework between request + context and tool calls + response; its defining mechanism is agent runtime + middleware. Trace Request + context -> Agent runtime + middleware -> Tool calls + response. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-116} You selected BeeAI Framework for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Middleware ordering and implicit context propagation can create surprising policy or tracing gaps. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Request + context -> Agent runtime + middleware -> Tool calls + response; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (LangChain), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Strands Agents SDK}
\paragraph{MEDIUM - TECH-MCQ-117} Which workload is the strongest fit for Strands Agents SDK?
\begin{enumerate}[label=\Alph*.]
\item You want a Python-first framework that joins agent construction with sessions, memory, knowledge, and serving.
\item You need multi-agent messaging, distributed runtimes, or research-friendly team patterns.
\item Python typing and Pydantic validation should define the boundary between models and application code.
\item You need an in-process model-agnostic agent loop with production lifecycle controls and strong extension points.
\end{enumerate}
\textbf{Answer.} You need an in-process model-agnostic agent loop with production lifecycle controls and strong extension points.
\textit{SDK for building an agent harness with lifecycle controls, tools, hooks, sessions, multi-agent patterns, MCP, guardrails, tracing, and evaluation. Compare it with OpenAI Agents SDK, Google ADK, LangGraph; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-117} Compare Strands Agents SDK with OpenAI Agents SDK, Google ADK. What measurements would decide the choice?
\textbf{Answer.} Choose Strands Agents SDK when you need an in-process model-agnostic agent loop with production lifecycle controls and strong extension points. Avoid it when a managed control plane or explicit durable workflow graph is the dominant requirement. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-117} Explain the internal structure and design of Strands Agents SDK. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Strands Agents SDK as a agent harness sdk between prompt + tools and response + trace; its defining mechanism is controlled agent loop + hooks. Trace Prompt + tools -> Controlled agent loop + hooks -> Response + trace. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-117} You selected Strands Agents SDK for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: A model-driven loop may repeatedly select costly or high-impact tools unless hooks enforce budgets and authorization. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Prompt + tools -> Controlled agent loop + hooks -> Response + trace; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (OpenAI Agents SDK), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Claude Agent SDK}
\paragraph{MEDIUM - TECH-MCQ-118} Which workload is the strongest fit for Claude Agent SDK?
\begin{enumerate}[label=\Alph*.]
\item You are researching multi-agent coordination, role specialization, or agent-society behavior.
\item You need a coding or research agent with mature filesystem, terminal, and MCP behavior.
\item You are building a long-horizon research or coding agent and want a batteries-included harness.
\item You want a Python-first framework that joins agent construction with sessions, memory, knowledge, and serving.
\end{enumerate}
\textbf{Answer.} You need a coding or research agent with mature filesystem, terminal, and MCP behavior.
\textit{SDK for building agents with Claude Code's tool loop, filesystem and shell tools, MCP, hooks, permissions, and sessions. Compare it with Deep Agents, OpenAI Agents SDK, smolagents; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-118} Compare Claude Agent SDK with Deep Agents, OpenAI Agents SDK. What measurements would decide the choice?
\textbf{Answer.} Choose Claude Agent SDK when you need a coding or research agent with mature filesystem, terminal, and MCP behavior. Avoid it when a provider-neutral application framework or narrowly scoped tool caller is required. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-118} Explain the internal structure and design of Claude Agent SDK. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Claude Agent SDK as a coding and computer-use agent sdk between task + workspace and changed files + result; its defining mechanism is claude agent loop + tools. Trace Task + workspace -> Claude agent loop + tools -> Changed files + result. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-118} You selected Claude Agent SDK for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Broad filesystem and shell access can turn prompt injection into code execution or secret exposure. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Task + workspace -> Claude agent loop + tools -> Changed files + result; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Deep Agents), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Vercel AI SDK}
\paragraph{MEDIUM - TECH-MCQ-119} Which workload is the strongest fit for Vercel AI SDK?
\begin{enumerate}[label=\Alph*.]
\item You have representative training/evaluation examples and want systematic optimization rather than manual prompt editing.
\item A TypeScript web product needs streaming model and tool interactions close to its UI framework.
\item Python typing and Pydantic validation should define the boundary between models and application code.
\item The application branches, loops, pauses for approval, or must resume from persisted state.
\end{enumerate}
\textbf{Answer.} A TypeScript web product needs streaming model and tool interactions close to its UI framework.
\textit{Provider-neutral toolkit for text and object generation, streaming UI, tools, agents, embeddings, and frontend chat state. Compare it with Mastra, LangChain.js, provider SDKs; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-119} Compare Vercel AI SDK with Mastra, LangChain.js. What measurements would decide the choice?
\textbf{Answer.} Choose Vercel AI SDK when a TypeScript web product needs streaming model and tool interactions close to its UI framework. Avoid it when durable multi-step orchestration or Python data and ML libraries dominate. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-119} Explain the internal structure and design of Vercel AI SDK. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Vercel AI SDK as a typescript ai application toolkit between ui message + model config and typed stream + ui state; its defining mechanism is streaming generate or tool loop. Trace UI message + model config -> Streaming generate or tool loop -> Typed stream + UI state. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-119} You selected Vercel AI SDK for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: UI and server streams can diverge after retries or reconnects unless message ids and tool effects are durable. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path UI message + model config -> Streaming generate or tool loop -> Typed stream + UI state; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Mastra), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Microsoft Agent Framework}
\paragraph{MEDIUM - TECH-MCQ-120} Which workload is the strongest fit for Microsoft Agent Framework?
\begin{enumerate}[label=\Alph*.]
\item Agents need durable, evolving user or entity context with retrieval and provenance beyond a short chat buffer.
\item A Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET.
\item You need an in-process model-agnostic agent loop with production lifecycle controls and strong extension points.
\item You want open multi-language agent components with middleware and production integration points.
\end{enumerate}
\textbf{Answer.} A Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET.
\textit{Microsoft framework unifying agent abstractions, workflows, middleware, tools, MCP, state, and enterprise hosting patterns. Compare it with Semantic Kernel, AutoGen, LangGraph; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-120} Compare Microsoft Agent Framework with Semantic Kernel, AutoGen. What measurements would decide the choice?
\textbf{Answer.} Choose Microsoft Agent Framework when a Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET. Avoid it when another established framework already owns orchestration or the application only needs direct model calls. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-120} Explain the internal structure and design of Microsoft Agent Framework. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Microsoft Agent Framework as a enterprise agent and workflow sdk between enterprise event + state and governed result; its defining mechanism is agent or workflow runtime. Trace Enterprise event + state -> Agent or workflow runtime -> Governed result. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-120} You selected Microsoft Agent Framework for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Migration from predecessor abstractions can hide changed state, serialization, or tool-call semantics. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Enterprise event + state -> Agent or workflow runtime -> Governed result; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Semantic Kernel), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{CAMEL-AI}
\paragraph{MEDIUM - TECH-MCQ-121} Which workload is the strongest fit for CAMEL-AI?
\begin{enumerate}[label=\Alph*.]
\item You are researching multi-agent coordination, role specialization, or agent-society behavior.
\item You need a coding or research agent with mature filesystem, terminal, and MCP behavior.
\item You have representative training/evaluation examples and want systematic optimization rather than manual prompt editing.
\item You want an intentionally small agent API with first-class OpenAI platform integration and inspectable handoffs.
\end{enumerate}
\textbf{Answer.} You are researching multi-agent coordination, role specialization, or agent-society behavior.
\textit{Framework for communicative agents, role playing, societies, workforce orchestration, tools, memory, and simulation. Compare it with AutoGen, CrewAI, Langroid; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-121} Compare CAMEL-AI with AutoGen, CrewAI. What measurements would decide the choice?
\textbf{Answer.} Choose CAMEL-AI when you are researching multi-agent coordination, role specialization, or agent-society behavior. Avoid it when a small production workflow needs strict control, simple operations, and narrow authority. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-121} Explain the internal structure and design of CAMEL-AI. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of CAMEL-AI as a multi-agent research framework between task + roles and coordinated output; its defining mechanism is agent society + messages. Trace Task + roles -> Agent society + messages -> Coordinated output. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-121} You selected CAMEL-AI for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: More agents can amplify correlated errors, token cost, and unclear accountability rather than improve quality. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Task + roles -> Agent society + messages -> Coordinated output; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (AutoGen), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Rasa}
\paragraph{MEDIUM - TECH-MCQ-122} Which workload is the strongest fit for Rasa?
\begin{enumerate}[label=\Alph*.]
\item You use Gemini or Google Cloud but want a framework that can also express model-agnostic agent teams.
\item Deterministic business conversations, channel integration, and controlled dialogue policy matter alongside LLM flexibility.
\item The application branches, loops, pauses for approval, or must resume from persisted state.
\item You need globally distributed stateful agents and already accept the Cloudflare runtime model.
\end{enumerate}
\textbf{Answer.} Deterministic business conversations, channel integration, and controlled dialogue policy matter alongside LLM flexibility.
\textit{Conversation stack for NLU, dialogue policy, flows, business logic, channels, and enterprise conversational assistants. Compare it with Semantic Kernel, LangGraph, custom dialogue state machine; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-122} Compare Rasa with Semantic Kernel, LangGraph. What measurements would decide the choice?
\textbf{Answer.} Choose Rasa when deterministic business conversations, channel integration, and controlled dialogue policy matter alongside LLM flexibility. Avoid it when a free-form research agent or simple single-turn assistant is the product. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-122} Explain the internal structure and design of Rasa. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Rasa as a conversational ai and dialogue framework between message + conversation state and action + response; its defining mechanism is nlu and dialogue policy. Trace Message + conversation state -> NLU and dialogue policy -> Action + response. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-122} You selected Rasa for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Training stories and flows can drift from production language, while generative fallbacks bypass deterministic policy. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Message + conversation state -> NLU and dialogue policy -> Action + response; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Semantic Kernel), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Langflow}
\paragraph{MEDIUM - TECH-MCQ-123} Which workload is the strongest fit for Langflow?
\begin{enumerate}[label=\Alph*.]
\item You are researching multi-agent coordination, role specialization, or agent-society behavior.
\item Cross-functional teams need to prototype and inspect component graphs visually before hardening them.
\item You are building a long-horizon research or coding agent and want a batteries-included harness.
\item You want an intentionally small agent API with first-class OpenAI platform integration and inspectable handoffs.
\end{enumerate}
\textbf{Answer.} Cross-functional teams need to prototype and inspect component graphs visually before hardening them.
\textit{Visual builder for LangChain-oriented components, agents, RAG flows, API exposure, and reusable components. Compare it with Flowise, Dify, LangGraph; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-123} Compare Langflow with Flowise, Dify. What measurements would decide the choice?
\textbf{Answer.} Choose Langflow when cross-functional teams need to prototype and inspect component graphs visually before hardening them. Avoid it when code review, typed contracts, or high-scale runtime control must be the primary authoring model. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-123} Explain the internal structure and design of Langflow. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Langflow as a visual ai workflow builder between inputs + visual graph and api or playground result; its defining mechanism is components + agent nodes. Trace Inputs + visual graph -> Components + agent nodes -> API or playground result. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-123} You selected Langflow for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: A readable canvas can conceal mutable prompts, credentials, package versions, and weak test coverage. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Inputs + visual graph -> Components + agent nodes -> API or playground result; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Flowise), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Dify}
\paragraph{MEDIUM - TECH-MCQ-124} Which workload is the strongest fit for Dify?
\begin{enumerate}[label=\Alph*.]
\item Deterministic business conversations, channel integration, and controlled dialogue policy matter alongside LLM flexibility.
\item Your estate is .NET, Java, Microsoft 365, or Azure and plugins must fit established dependency-injection patterns.
\item A product team wants an integrated low-code environment for RAG and agent applications.
\item You want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem.
\end{enumerate}
\textbf{Answer.} A product team wants an integrated low-code environment for RAG and agent applications.
\textit{Platform for visual workflows, agents, RAG knowledge bases, prompt operations, model providers, APIs, and application publishing. Compare it with Langflow, Flowise, custom LangGraph app; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-124} Compare Dify with Langflow, Flowise. What measurements would decide the choice?
\textbf{Answer.} Choose Dify when a product team wants an integrated low-code environment for RAG and agent applications. Avoid it when fine-grained runtime ownership, minimal infrastructure, or code-native composition is mandatory. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-124} Explain the internal structure and design of Dify. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Dify as a llm application platform between dataset + workflow and published ai application; its defining mechanism is visual orchestration + model tools. Trace Dataset + workflow -> Visual orchestration + model tools -> Published AI application. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-124} You selected Dify for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Platform convenience can concentrate secrets, tenant data, vector stores, and model routing in one large trust boundary. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Dataset + workflow -> Visual orchestration + model tools -> Published AI application; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Langflow), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Flowise}
\paragraph{MEDIUM - TECH-MCQ-125} Which workload is the strongest fit for Flowise?
\begin{enumerate}[label=\Alph*.]
\item A JavaScript team wants fast visual assembly of LangChain-style RAG and agents.
\item You want explicit modular pipelines, strong search heritage, and inspectable component connections.
\item Agents need durable, evolving user or entity context with retrieval and provenance beyond a short chat buffer.
\item A role/task metaphor helps a team prototype coordinated research, operations, or content workflows.
\end{enumerate}
\textbf{Answer.} A JavaScript team wants fast visual assembly of LangChain-style RAG and agents.
\textit{Drag-and-drop builder for chatflows, agentflows, tools, vector stores, evaluations, and embeddable APIs. Compare it with Langflow, Dify, n8n; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-125} Compare Flowise with Langflow, Dify. What measurements would decide the choice?
\textbf{Answer.} Choose Flowise when a JavaScript team wants fast visual assembly of LangChain-style RAG and agents. Avoid it when strict typed application code or durable general workflow semantics outweigh visual speed. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-125} Explain the internal structure and design of Flowise. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Flowise as a visual llm application builder between request + visual flow and chat or api response; its defining mechanism is nodes, agents, and tools. Trace Request + visual flow -> Nodes, agents, and tools -> Chat or API response. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-125} You selected Flowise for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Exported flows can depend on hidden credentials, component versions, and server-local state. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Request + visual flow -> Nodes, agents, and tools -> Chat or API response; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Langflow), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsubsection*{Cloudflare Agents SDK}
\paragraph{MEDIUM - TECH-MCQ-126} Which workload is the strongest fit for Cloudflare Agents SDK?
\begin{enumerate}[label=\Alph*.]
\item A role/task metaphor helps a team prototype coordinated research, operations, or content workflows.
\item You are building a long-horizon research or coding agent and want a batteries-included harness.
\item You want an intentionally small agent API with first-class OpenAI platform integration and inspectable handoffs.
\item You need globally distributed stateful agents and already accept the Cloudflare runtime model.
\end{enumerate}
\textbf{Answer.} You need globally distributed stateful agents and already accept the Cloudflare runtime model.
\textit{SDK for stateful agents on Workers with durable identity, scheduling, WebSockets, SQL-backed state, MCP, and platform services. Compare it with Mastra, Vercel AI SDK, Durable Objects directly; the deciding evidence is the actual workload and operational boundary.}
\paragraph{HARD - TECH-HARD-126} Compare Cloudflare Agents SDK with Mastra, Vercel AI SDK. What measurements would decide the choice?
\textbf{Answer.} Choose Cloudflare Agents SDK when you need globally distributed stateful agents and already accept the Cloudflare runtime model. Avoid it when portable self-hosting, GPU-local inference, or Python-first ML integration is required. Build the same representative slice on the alternatives and compare quality, p95 and p99 latency, throughput, failure recovery, operability, security boundary, migration cost, and total cost. Preserve an exit path rather than selecting from feature lists.
\textit{A hard answer converts product differences into measurable workload and operating constraints.}
\paragraph{HARD - TECH-ARCH-126} Explain the internal structure and design of Cloudflare Agents SDK. Trace one request, identify the control plane and data plane, and place state, security, retries, and telemetry.
\textbf{Answer.} Mental model: Think of Cloudflare Agents SDK as a durable edge agent sdk between client event + agent identity and state update + streamed response; its defining mechanism is durable object agent runtime. Trace Client event + agent identity -> Durable Object agent runtime -> State update + streamed response. Agent loop, graph, workflow, or handoff policy decides the next bounded action. Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state. Put validation at the entry and result contracts, authorize immediately before side effects, make retries idempotent, and emit correlated evidence at every boundary.
\textit{A framework answer is complete only when it explains mechanisms and ownership boundaries rather than repeating the product feature list.}
\paragraph{VERY-HARD - TECH-VH-126} You selected Cloudflare Agents SDK for a production system. Design the failure experiment, telemetry, fallback, and migration plan that would disprove or defend that choice.
\textbf{Answer.} Start from this primary risk: Per-agent durable state does not automatically solve cross-agent transactions, regional data rules, or tool authorization. Reproduce it with representative scale, concurrency, data shape, filters or tool permissions, and dependency failures. Trace the path Client event + agent identity -> Durable Object agent runtime -> State update + streamed response; define quality, saturation, tail-latency, cost, and recovery signals at each boundary. Predefine a degraded mode and rollback trigger, keep artifacts and schemas versioned, dual-run or shadow the strongest alternative (Mastra), and prove that data and callers can migrate without an outage or authority expansion.
\textit{A very-hard answer must make the selection falsifiable and include recovery, not merely defend a preferred product.}
\subsection{Model, embedding, and optimization libraries}
