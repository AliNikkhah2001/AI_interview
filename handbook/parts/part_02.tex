\subsubsection{NVIDIA Dynamo}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries ND}}}\quad \badge{Distributed inference serving framework} \quad \textbf{Languages:} Python / Rust / CUDA
\par\smallskip NVIDIA framework for disaggregated serving, smart routing, KV-cache management, worker coordination, and high-performance inference backends.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of NVIDIA Dynamo as a distributed inference serving framework between requests + model workers and distributed token stream; its defining mechanism is router, cache, prefill and decode.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Requests + model workers};
\node[tech,right=of t0](t1){Router, cache, prefill and decode};
\node[tech,right=of t1](t2){Distributed token stream};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Requests + model workers -> Router, cache, prefill and decode -> Distributed token stream\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] NVIDIA framework for disaggregated serving, smart routing, KV-cache management, worker coordination, and high-performance inference backends. Think of NVIDIA Dynamo as a distributed inference serving framework between requests + model workers and distributed token stream; its defining mechanism is router, cache, prefill and decode.
\item[2. Structure and execution] Follow the data path Requests + model workers → Router, cache, prefill and decode → Distributed token stream. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when large NVIDIA fleets need a programmable distributed data plane around TensorRT-LLM, vLLM, or SGLang. Reject it when portability across accelerators or a small stateless deployment is the priority. Compare the same representative workload with llm-d, KServe, Ray Serve.
\item[5. Production proof] Start with the failure hypothesis: Disaggregation adds network and control-plane failure modes that can dominate at short prompts or low concurrency. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Containers or Kubernetes on accelerated clusters\\
Choose when & Large NVIDIA fleets need a programmable distributed data plane around TensorRT-LLM, vLLM, or SGLang.\\
Reject when & Portability across accelerators or a small stateless deployment is the priority.\\
Primary failure & Disaggregation adds network and control-plane failure modes that can dominate at short prompts or low concurrency.\\
Compare with & llm-d; KServe; Ray Serve\\
\end{tabularx}
\paragraph{Minimal use in Python / Rust / CUDA.}
\begin{Verbatim}[fontsize=\scriptsize]
git clone https://github.com/ai-dynamo/dynamo
docker compose up
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.nvidia.com/dynamo/latest/}{NVIDIA Dynamo official documentation}.
\subsubsection{NVIDIA NIM}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries NN}}}\quad \badge{Packaged inference microservices} \quad \textbf{Languages:} Python / CUDA / containers
\par\smallskip Prebuilt model microservices with optimized engines, standard APIs, profiles, and enterprise support for NVIDIA infrastructure.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of NVIDIA NIM as a packaged inference microservices between model profile + request and supported inference api; its defining mechanism is nim container + optimized backend.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model profile + request};
\node[tech,right=of t0](t1){NIM container + optimized backend};
\node[tech,right=of t1](t2){Supported inference API};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model profile + request -> NIM container + optimized backend -> Supported inference API\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Prebuilt model microservices with optimized engines, standard APIs, profiles, and enterprise support for NVIDIA infrastructure. Think of NVIDIA NIM as a packaged inference microservices between model profile + request and supported inference api; its defining mechanism is nim container + optimized backend.
\item[2. Structure and execution] Follow the data path Model profile + request → NIM container + optimized backend → Supported inference API. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when you need supported accelerated deployment with packaged model-specific optimizations and NVIDIA enterprise operations. Reject it when open-source engine control, non-NVIDIA hardware, or rapid unsupported model experimentation dominates. Compare the same representative workload with vLLM, TensorRT-LLM, managed APIs.
\item[5. Production proof] Start with the failure hypothesis: The available profile, license, GPU, driver, model version, and resource assumptions must match exactly. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & NVIDIA AI Enterprise containers and APIs\\
Choose when & You need supported accelerated deployment with packaged model-specific optimizations and NVIDIA enterprise operations.\\
Reject when & Open-source engine control, non-NVIDIA hardware, or rapid unsupported model experimentation dominates.\\
Primary failure & The available profile, license, GPU, driver, model version, and resource assumptions must match exactly.\\
Compare with & vLLM; TensorRT-LLM; managed APIs\\
\end{tabularx}
\paragraph{Minimal use in Python / CUDA / containers.}
\begin{Verbatim}[fontsize=\scriptsize]
docker run nvcr.io/nim/MODEL:TAG
curl http://localhost:8000/v1/chat/completions
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.nvidia.com/nim/large-language-models/latest/introduction.html}{NVIDIA NIM official documentation} \quad \href{https://docs.nvidia.com/nim/large-language-models/latest/reference/architecture.html}{NIM LLM architecture}.
\subsubsection{TorchServe}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries T}}}\quad \badge{PyTorch model server} \quad \textbf{Languages:} Python / Java
\par\smallskip PyTorch serving stack with model archives, handlers, batching, metrics, management APIs, and large-model integrations.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of TorchServe as a pytorch model server between model archive + request and prediction response; its defining mechanism is frontend + worker handler.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model archive + request};
\node[tech,right=of t0](t1){Frontend + worker handler};
\node[tech,right=of t1](t2){Prediction response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model archive + request -> Frontend + worker handler -> Prediction response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] PyTorch serving stack with model archives, handlers, batching, metrics, management APIs, and large-model integrations. Think of TorchServe as a pytorch model server between model archive + request and prediction response; its defining mechanism is frontend + worker handler.
\item[2. Structure and execution] Follow the data path Model archive + request → Frontend + worker handler → Prediction response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when legacy or established PyTorch serving estates need standardized handlers and model management. Reject it when a new LLM system needs an actively optimized token scheduler and modern model support. Compare the same representative workload with Triton, BentoML, vLLM.
\item[5. Production proof] Start with the failure hypothesis: Limited maintenance status and custom handlers can create security, compatibility, and upgrade risk. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Container or server process\\
Choose when & Legacy or established PyTorch serving estates need standardized handlers and model management.\\
Reject when & A new LLM system needs an actively optimized token scheduler and modern model support.\\
Primary failure & Limited maintenance status and custom handlers can create security, compatibility, and upgrade risk.\\
Compare with & Triton; BentoML; vLLM\\
\end{tabularx}
\paragraph{Minimal use in Python / Java.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install torchserve torch-model-archiver
torchserve --start --model-store model_store
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.pytorch.org/serve/}{TorchServe official documentation}.
\subsubsection{DeepSpeed-MII}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries D}}}\quad \badge{DeepSpeed inference serving library} \quad \textbf{Languages:} Python / CUDA
\par\smallskip Model inference and serving wrapper using DeepSpeed optimizations, tensor parallelism, quantization, and replica management.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of DeepSpeed-MII as a deepspeed inference serving library between model + deployment config and generated responses; its defining mechanism is deepspeed inference replicas.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model + deployment config};
\node[tech,right=of t0](t1){DeepSpeed inference replicas};
\node[tech,right=of t1](t2){Generated responses};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model + deployment config -> DeepSpeed inference replicas -> Generated responses\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Model inference and serving wrapper using DeepSpeed optimizations, tensor parallelism, quantization, and replica management. Think of DeepSpeed-MII as a deepspeed inference serving library between model + deployment config and generated responses; its defining mechanism is deepspeed inference replicas.
\item[2. Structure and execution] Follow the data path Model + deployment config → DeepSpeed inference replicas → Generated responses. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when a DeepSpeed-centered GPU stack needs a direct path from supported models to optimized inference. Reject it when broad OpenAI-compatible ecosystem support or Kubernetes-native fleet control is more important. Compare the same representative workload with vLLM, TensorRT-LLM, TGI.
\item[5. Production proof] Start with the failure hypothesis: Supported-model and kernel combinations can lag model releases or fail across driver and library versions. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library and server on GPU clusters\\
Choose when & A DeepSpeed-centered GPU stack needs a direct path from supported models to optimized inference.\\
Reject when & Broad OpenAI-compatible ecosystem support or Kubernetes-native fleet control is more important.\\
Primary failure & Supported-model and kernel combinations can lag model releases or fail across driver and library versions.\\
Compare with & vLLM; TensorRT-LLM; TGI\\
\end{tabularx}
\paragraph{Minimal use in Python / CUDA.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install deepspeed-mii
from mii import pipeline
\end{Verbatim}
\textbf{Primary sources.} \href{https://deepspeed-mii.readthedocs.io/en/latest/}{DeepSpeed-MII official documentation}.
\subsubsection{OpenLLM}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries O}}}\quad \badge{Open model service toolkit} \quad \textbf{Languages:} Python
\par\smallskip BentoML project for running open models behind OpenAI-compatible APIs with model and runtime packaging.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of OpenLLM as a open model service toolkit between model identifier + config and openai-compatible response; its defining mechanism is openllm service runtime.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model identifier + config};
\node[tech,right=of t0](t1){OpenLLM service runtime};
\node[tech,right=of t1](t2){OpenAI-compatible response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model identifier + config -> OpenLLM service runtime -> OpenAI-compatible response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] BentoML project for running open models behind OpenAI-compatible APIs with model and runtime packaging. Think of OpenLLM as a open model service toolkit between model identifier + config and openai-compatible response; its defining mechanism is openllm service runtime.
\item[2. Structure and execution] Follow the data path Model identifier + config → OpenLLM service runtime → OpenAI-compatible response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when a Python team wants a convenient bridge from open models to a BentoML-style service. Reject it when you need the strongest specialized scheduler or a fully managed inference API. Compare the same representative workload with BentoML, vLLM, LocalAI.
\item[5. Production proof] Start with the failure hypothesis: Project maturity and backend support must be verified before committing a production control plane. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local service or container\\
Choose when & A Python team wants a convenient bridge from open models to a BentoML-style service.\\
Reject when & You need the strongest specialized scheduler or a fully managed inference API.\\
Primary failure & Project maturity and backend support must be verified before committing a production control plane.\\
Compare with & BentoML; vLLM; LocalAI\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install openllm
openllm serve MODEL
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/bentoml/OpenLLM}{OpenLLM official documentation}.
\subsubsection{LMDeploy}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{LLM inference and deployment toolkit} \quad \textbf{Languages:} Python / C++ / CUDA
\par\smallskip Toolkit with TurboMind and PyTorch engines, quantization, serving APIs, batching, and supported multimodal models.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of LMDeploy as a llm inference and deployment toolkit between model + quantization and streaming api; its defining mechanism is turbomind or pytorch engine.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model + quantization};
\node[tech,right=of t0](t1){TurboMind or PyTorch engine};
\node[tech,right=of t1](t2){Streaming API};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model + quantization -> TurboMind or PyTorch engine -> Streaming API\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Toolkit with TurboMind and PyTorch engines, quantization, serving APIs, batching, and supported multimodal models. Think of LMDeploy as a llm inference and deployment toolkit between model + quantization and streaming api; its defining mechanism is turbomind or pytorch engine.
\item[2. Structure and execution] Follow the data path Model + quantization → TurboMind or PyTorch engine → Streaming API. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when supported models need high-throughput deployment and quantization in its CUDA-oriented ecosystem. Reject it when your hardware, model, or operations standard aligns better with vLLM, SGLang, or TensorRT-LLM. Compare the same representative workload with vLLM, SGLang, TensorRT-LLM.
\item[5. Production proof] Start with the failure hypothesis: Model conversion, quantization, engine, and CUDA compatibility can invalidate benchmark assumptions. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local server, containers, or cluster integration\\
Choose when & Supported models need high-throughput deployment and quantization in its CUDA-oriented ecosystem.\\
Reject when & Your hardware, model, or operations standard aligns better with vLLM, SGLang, or TensorRT-LLM.\\
Primary failure & Model conversion, quantization, engine, and CUDA compatibility can invalidate benchmark assumptions.\\
Compare with & vLLM; SGLang; TensorRT-LLM\\
\end{tabularx}
\paragraph{Minimal use in Python / C++ / CUDA.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install lmdeploy
lmdeploy serve api_server MODEL
\end{Verbatim}
\textbf{Primary sources.} \href{https://lmdeploy.readthedocs.io/en/latest/}{LMDeploy official documentation}.
\subsubsection{MLC LLM}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries ML}}}\quad \badge{Machine-learning compilation deployment stack} \quad \textbf{Languages:} Python / C++ / TVM
\par\smallskip Compilation-based deployment of language models across GPUs, CPUs, browsers, and mobile devices using TVM-derived runtimes.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of MLC LLM as a machine-learning compilation deployment stack between model + target profile and device-specific inference; its defining mechanism is compile and package runtime.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model + target profile};
\node[tech,right=of t0](t1){Compile and package runtime};
\node[tech,right=of t1](t2){Device-specific inference};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model + target profile -> Compile and package runtime -> Device-specific inference\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Compilation-based deployment of language models across GPUs, CPUs, browsers, and mobile devices using TVM-derived runtimes. Think of MLC LLM as a machine-learning compilation deployment stack between model + target profile and device-specific inference; its defining mechanism is compile and package runtime.
\item[2. Structure and execution] Follow the data path Model + target profile → Compile and package runtime → Device-specific inference. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when one model must run efficiently across edge, browser, mobile, and heterogeneous hardware. Reject it when a centralized CUDA fleet needs mature multi-tenant scheduling rather than portable compilation. Compare the same representative workload with llama.cpp, ONNX Runtime, TensorRT-LLM.
\item[5. Production proof] Start with the failure hypothesis: Compiled artifacts bind model, quantization, runtime, and target assumptions that complicate updates. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Web, mobile, desktop, edge, and server targets\\
Choose when & One model must run efficiently across edge, browser, mobile, and heterogeneous hardware.\\
Reject when & A centralized CUDA fleet needs mature multi-tenant scheduling rather than portable compilation.\\
Primary failure & Compiled artifacts bind model, quantization, runtime, and target assumptions that complicate updates.\\
Compare with & llama.cpp; ONNX Runtime; TensorRT-LLM\\
\end{tabularx}
\paragraph{Minimal use in Python / C++ / TVM.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install mlc-llm
mlc_llm chat MODEL
\end{Verbatim}
\textbf{Primary sources.} \href{https://llm.mlc.ai/docs/}{MLC LLM official documentation}.
\subsubsection{Replicate}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries R}}}\quad \badge{Managed model execution platform} \quad \textbf{Languages:} Python / JavaScript / REST
\par\smallskip Runs versioned public or custom model containers through APIs, webhooks, predictions, and autoscaled hardware.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Replicate as a managed model execution platform between versioned model + input and output or webhook; its defining mechanism is managed prediction worker.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Versioned model + input};
\node[tech,right=of t0](t1){Managed prediction worker};
\node[tech,right=of t1](t2){Output or webhook};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Versioned model + input -> Managed prediction worker -> Output or webhook\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Runs versioned public or custom model containers through APIs, webhooks, predictions, and autoscaled hardware. Think of Replicate as a managed model execution platform between versioned model + input and output or webhook; its defining mechanism is managed prediction worker.
\item[2. Structure and execution] Follow the data path Versioned model + input → Managed prediction worker → Output or webhook. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when a team wants rapid access to diverse models or simple custom container deployment without operating accelerators. Reject it when strict private networking, predictable dedicated capacity, or self-hosted control is required. Compare the same representative workload with Modal, Baseten, Runpod Serverless.
\item[5. Production proof] Start with the failure hypothesis: Cold starts, model-version drift, webhook retries, and per-run cost can surprise interactive products. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed cloud API\\
Choose when & A team wants rapid access to diverse models or simple custom container deployment without operating accelerators.\\
Reject when & Strict private networking, predictable dedicated capacity, or self-hosted control is required.\\
Primary failure & Cold starts, model-version drift, webhook retries, and per-run cost can surprise interactive products.\\
Compare with & Modal; Baseten; Runpod Serverless\\
\end{tabularx}
\paragraph{Minimal use in Python / JavaScript / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install replicate
replicate.run('owner/model:version', input={})
\end{Verbatim}
\textbf{Primary sources.} \href{https://replicate.com/docs}{Replicate official documentation}.
\subsubsection{Runpod Serverless}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries RS}}}\quad \badge{GPU serverless platform} \quad \textbf{Languages:} Python / containers / REST
\par\smallskip Queue-based and load-balanced serverless GPU workers with templates, autoscaling, network volumes, and custom containers.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Runpod Serverless as a gpu serverless platform between request + container template and result or streamed output; its defining mechanism is autoscaled gpu worker.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Request + container template};
\node[tech,right=of t0](t1){Autoscaled GPU worker};
\node[tech,right=of t1](t2){Result or streamed output};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Request + container template -> Autoscaled GPU worker -> Result or streamed output\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Queue-based and load-balanced serverless GPU workers with templates, autoscaling, network volumes, and custom containers. Think of Runpod Serverless as a gpu serverless platform between request + container template and result or streamed output; its defining mechanism is autoscaled gpu worker.
\item[2. Structure and execution] Follow the data path Request + container template → Autoscaled GPU worker → Result or streamed output. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when variable GPU workloads need quick deployment and per-second-style capacity without owning a cluster. Reject it when tight tail-latency guarantees or a fully integrated managed ML control plane is mandatory. Compare the same representative workload with Modal, Replicate, Baseten.
\item[5. Production proof] Start with the failure hypothesis: Cold starts, queue time, image pulls, and scarce GPU types can dominate model execution time. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed GPU workers and endpoints\\
Choose when & Variable GPU workloads need quick deployment and per-second-style capacity without owning a cluster.\\
Reject when & Tight tail-latency guarantees or a fully integrated managed ML control plane is mandatory.\\
Primary failure & Cold starts, queue time, image pulls, and scarce GPU types can dominate model execution time.\\
Compare with & Modal; Replicate; Baseten\\
\end{tabularx}
\paragraph{Minimal use in Python / containers / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install runpod
runpodctl project deploy
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.runpod.io/serverless/overview}{Runpod Serverless official documentation}.
\subsubsection{Together AI Inference}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries TAI}}}\quad \badge{Managed inference API} \quad \textbf{Languages:} Python / TypeScript / REST
\par\smallskip Hosted open-model inference with chat, embeddings, reranking, fine-tuning, dedicated endpoints, and OpenAI-compatible interfaces.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Together AI Inference as a managed inference api between openai-style request and tokens, embeddings, or ranks; its defining mechanism is managed model fleet.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){OpenAI-style request};
\node[tech,right=of t0](t1){Managed model fleet};
\node[tech,right=of t1](t2){Tokens, embeddings, or ranks};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & OpenAI-style request -> Managed model fleet -> Tokens, embeddings, or ranks\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Hosted open-model inference with chat, embeddings, reranking, fine-tuning, dedicated endpoints, and OpenAI-compatible interfaces. Think of Together AI Inference as a managed inference api between openai-style request and tokens, embeddings, or ranks; its defining mechanism is managed model fleet.
\item[2. Structure and execution] Follow the data path OpenAI-style request → Managed model fleet → Tokens, embeddings, or ranks. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when you need broad open-model access, fast experimentation, or dedicated hosted capacity through a familiar API. Reject it when self-hosting, a different cloud boundary, or a model unavailable in the catalog is required. Compare the same representative workload with Fireworks AI, GroqCloud, self-hosted vLLM.
\item[5. Production proof] Start with the failure hypothesis: OpenAI compatibility does not make rate limits, tools, tokenization, or model revisions identical. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed serverless or dedicated endpoints\\
Choose when & You need broad open-model access, fast experimentation, or dedicated hosted capacity through a familiar API.\\
Reject when & Self-hosting, a different cloud boundary, or a model unavailable in the catalog is required.\\
Primary failure & OpenAI compatibility does not make rate limits, tools, tokenization, or model revisions identical.\\
Compare with & Fireworks AI; GroqCloud; self-hosted vLLM\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install together
from together import Together
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.together.ai/docs/inference-overview}{Together AI Inference official documentation}.
\subsubsection{Fireworks AI}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries FA}}}\quad \badge{Managed generative AI inference platform} \quad \textbf{Languages:} Python / REST
\par\smallskip Hosted open and custom models with optimized inference, fine-tuning, deployments, structured output, and OpenAI-compatible APIs.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Fireworks AI as a managed generative ai inference platform between request + deployment and structured or streamed response; its defining mechanism is optimized managed inference.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Request + deployment};
\node[tech,right=of t0](t1){Optimized managed inference};
\node[tech,right=of t1](t2){Structured or streamed response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Request + deployment -> Optimized managed inference -> Structured or streamed response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Hosted open and custom models with optimized inference, fine-tuning, deployments, structured output, and OpenAI-compatible APIs. Think of Fireworks AI as a managed generative ai inference platform between request + deployment and structured or streamed response; its defining mechanism is optimized managed inference.
\item[2. Structure and execution] Follow the data path Request + deployment → Optimized managed inference → Structured or streamed response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when high-performance managed open-model inference and deployment flexibility matter more than infrastructure ownership. Reject it when a self-hosted control plane or a provider with a required model and region is mandatory. Compare the same representative workload with Together AI, Baseten, vLLM.
\item[5. Production proof] Start with the failure hypothesis: Performance, quantization, and model-version behavior must be measured on the exact deployment tier. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Serverless, on-demand, or dedicated cloud deployment\\
Choose when & High-performance managed open-model inference and deployment flexibility matter more than infrastructure ownership.\\
Reject when & A self-hosted control plane or a provider with a required model and region is mandatory.\\
Primary failure & Performance, quantization, and model-version behavior must be measured on the exact deployment tier.\\
Compare with & Together AI; Baseten; vLLM\\
\end{tabularx}
\paragraph{Minimal use in Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install fireworks-ai
from fireworks.client import Fireworks
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.fireworks.ai/}{Fireworks AI official documentation}.
\subsubsection{GroqCloud}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries G}}}\quad \badge{Managed low-latency inference API} \quad \textbf{Languages:} Python / TypeScript / REST
\par\smallskip Hosted inference on Groq hardware with OpenAI-compatible APIs, streaming, tool use, and a curated model catalog.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of GroqCloud as a managed low-latency inference api between prompt + supported model and low-latency token stream; its defining mechanism is groq accelerator service.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Prompt + supported model};
\node[tech,right=of t0](t1){Groq accelerator service};
\node[tech,right=of t1](t2){Low-latency token stream};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Prompt + supported model -> Groq accelerator service -> Low-latency token stream\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Hosted inference on Groq hardware with OpenAI-compatible APIs, streaming, tool use, and a curated model catalog. Think of GroqCloud as a managed low-latency inference api between prompt + supported model and low-latency token stream; its defining mechanism is groq accelerator service.
\item[2. Structure and execution] Follow the data path Prompt + supported model → Groq accelerator service → Low-latency token stream. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when very low token latency for supported models is a dominant interactive requirement. Reject it when you need arbitrary model deployment, self-hosting, or broader accelerator portability. Compare the same representative workload with Cerebras Inference, Together AI, hosted GPU APIs.
\item[5. Production proof] Start with the failure hypothesis: A small supported catalog, rate limits, and provider-specific tool semantics can constrain portability. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed Groq accelerator cloud\\
Choose when & Very low token latency for supported models is a dominant interactive requirement.\\
Reject when & You need arbitrary model deployment, self-hosting, or broader accelerator portability.\\
Primary failure & A small supported catalog, rate limits, and provider-specific tool semantics can constrain portability.\\
Compare with & Cerebras Inference; Together AI; hosted GPU APIs\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install groq
from groq import Groq
\end{Verbatim}
\textbf{Primary sources.} \href{https://console.groq.com/docs/overview}{GroqCloud official documentation}.
\subsubsection{Cerebras Inference}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries CI}}}\quad \badge{Managed high-speed inference API} \quad \textbf{Languages:} Python / REST
\par\smallskip High-speed hosted inference for supported open models through OpenAI-compatible and native SDK patterns.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Cerebras Inference as a managed high-speed inference api between prompt + supported model and high-speed token stream; its defining mechanism is cerebras inference service.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Prompt + supported model};
\node[tech,right=of t0](t1){Cerebras inference service};
\node[tech,right=of t1](t2){High-speed token stream};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Prompt + supported model -> Cerebras inference service -> High-speed token stream\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] High-speed hosted inference for supported open models through OpenAI-compatible and native SDK patterns. Think of Cerebras Inference as a managed high-speed inference api between prompt + supported model and high-speed token stream; its defining mechanism is cerebras inference service.
\item[2. Structure and execution] Follow the data path Prompt + supported model → Cerebras inference service → High-speed token stream. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when token generation speed for supported models materially improves an interactive or agentic workload. Reject it when custom weights, self-hosting, or a broader deployment matrix is required. Compare the same representative workload with GroqCloud, Fireworks AI, Together AI.
\item[5. Production proof] Start with the failure hypothesis: Provider benchmarks may not reflect end-to-end prompt transfer, queuing, tool loops, or output validation. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed Cerebras cloud\\
Choose when & Token generation speed for supported models materially improves an interactive or agentic workload.\\
Reject when & Custom weights, self-hosting, or a broader deployment matrix is required.\\
Primary failure & Provider benchmarks may not reflect end-to-end prompt transfer, queuing, tool loops, or output validation.\\
Compare with & GroqCloud; Fireworks AI; Together AI\\
\end{tabularx}
\paragraph{Minimal use in Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install cerebras-cloud-sdk
from cerebras.cloud.sdk import Cerebras
\end{Verbatim}
\textbf{Primary sources.} \href{https://inference-docs.cerebras.ai/introduction}{Cerebras Inference official documentation}.
\subsubsection{Amazon Bedrock}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries AB}}}\quad \badge{Managed foundation-model platform} \quad \textbf{Languages:} Python / Java / REST
\par\smallskip Managed model catalog and APIs with provisioned or on-demand inference, knowledge bases, agents, guardrails, evaluation, and enterprise controls.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Amazon Bedrock as a managed foundation-model platform between authenticated request + policy and governed model response; its defining mechanism is bedrock model or workflow.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Authenticated request + policy};
\node[tech,right=of t0](t1){Bedrock model or workflow};
\node[tech,right=of t1](t2){Governed model response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Authenticated request + policy -> Bedrock model or workflow -> Governed model response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Managed model catalog and APIs with provisioned or on-demand inference, knowledge bases, agents, guardrails, evaluation, and enterprise controls. Think of Amazon Bedrock as a managed foundation-model platform between authenticated request + policy and governed model response; its defining mechanism is bedrock model or workflow.
\item[2. Structure and execution] Follow the data path Authenticated request + policy → Bedrock model or workflow → Governed model response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when aWS identity, networking, governance, and access to several model providers in one service are priorities. Reject it when self-hosted open models, maximum kernel control, or another cloud boundary is required. Compare the same representative workload with Vertex AI, Azure AI Foundry, direct provider APIs.
\item[5. Production proof] Start with the failure hypothesis: Model capabilities, quotas, regions, logging, and fallback semantics differ even behind one platform. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & AWS managed regional service\\
Choose when & AWS identity, networking, governance, and access to several model providers in one service are priorities.\\
Reject when & Self-hosted open models, maximum kernel control, or another cloud boundary is required.\\
Primary failure & Model capabilities, quotas, regions, logging, and fallback semantics differ even behind one platform.\\
Compare with & Vertex AI; Azure AI Foundry; direct provider APIs\\
\end{tabularx}
\paragraph{Minimal use in Python / Java / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install boto3
boto3.client('bedrock-runtime').converse(...)
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.aws.amazon.com/bedrock/latest/userguide/what-is-bedrock.html}{Amazon Bedrock official documentation}.
\subsubsection{Cloudflare Workers AI}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries CWA}}}\quad \badge{Edge inference platform} \quad \textbf{Languages:} TypeScript / Python / REST
\par\smallskip Serverless inference bound to Workers with catalog models, embeddings, image and audio tasks, AI Gateway, and edge application primitives.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Cloudflare Workers AI as a edge inference platform between worker request + binding and edge application response; its defining mechanism is managed model execution.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Worker request + binding};
\node[tech,right=of t0](t1){Managed model execution};
\node[tech,right=of t1](t2){Edge application response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Worker request + binding -> Managed model execution -> Edge application response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Serverless inference bound to Workers with catalog models, embeddings, image and audio tasks, AI Gateway, and edge application primitives. Think of Cloudflare Workers AI as a edge inference platform between worker request + binding and edge application response; its defining mechanism is managed model execution.
\item[2. Structure and execution] Follow the data path Worker request + binding → Managed model execution → Edge application response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when edge applications need low-operations inference close to Workers, Durable Objects, or Vectorize. Reject it when custom large-model deployment, CUDA-level tuning, or cloud portability is mandatory. Compare the same representative workload with Amazon Bedrock, managed APIs, self-hosted vLLM.
\item[5. Production proof] Start with the failure hypothesis: Catalog, limits, regional behavior, and edge-to-accelerator routing must be tested under the actual workload. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Cloudflare Workers and managed accelerators\\
Choose when & Edge applications need low-operations inference close to Workers, Durable Objects, or Vectorize.\\
Reject when & Custom large-model deployment, CUDA-level tuning, or cloud portability is mandatory.\\
Primary failure & Catalog, limits, regional behavior, and edge-to-accelerator routing must be tested under the actual workload.\\
Compare with & Amazon Bedrock; managed APIs; self-hosted vLLM\\
\end{tabularx}
\paragraph{Minimal use in TypeScript / Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
npm install wrangler
npx wrangler ai models list
\end{Verbatim}
\textbf{Primary sources.} \href{https://developers.cloudflare.com/workers-ai/}{Cloudflare Workers AI official documentation}.
\subsubsection{Databricks Model Serving}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries DMS}}}\quad \badge{Managed model and agent serving} \quad \textbf{Languages:} Python / REST
\par\smallskip Managed endpoints for custom models, foundation models, AI agents, feature lookups, traffic splitting, monitoring, and governance.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Databricks Model Serving as a managed model and agent serving between registered model or agent and scored response; its defining mechanism is managed endpoint + governance.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Registered model or agent};
\node[tech,right=of t0](t1){Managed endpoint + governance};
\node[tech,right=of t1](t2){Scored response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Registered model or agent -> Managed endpoint + governance -> Scored response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Managed endpoints for custom models, foundation models, AI agents, feature lookups, traffic splitting, monitoring, and governance. Think of Databricks Model Serving as a managed model and agent serving between registered model or agent and scored response; its defining mechanism is managed endpoint + governance.
\item[2. Structure and execution] Follow the data path Registered model or agent → Managed endpoint + governance → Scored response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when models and features already live in Databricks and Unity Catalog governance is an organizational boundary. Reject it when a lightweight public API or independently operated Kubernetes engine is preferable. Compare the same representative workload with SageMaker endpoints, Vertex AI endpoints, KServe.
\item[5. Production proof] Start with the failure hypothesis: Workspace identity, feature freshness, scale-to-zero, model environment, and quota can dominate runtime behavior. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Databricks managed endpoints\\
Choose when & Models and features already live in Databricks and Unity Catalog governance is an organizational boundary.\\
Reject when & A lightweight public API or independently operated Kubernetes engine is preferable.\\
Primary failure & Workspace identity, feature freshness, scale-to-zero, model environment, and quota can dominate runtime behavior.\\
Compare with & SageMaker endpoints; Vertex AI endpoints; KServe\\
\end{tabularx}
\paragraph{Minimal use in Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
databricks serving-endpoints create --json config.json
curl -X POST endpoint/invocations
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.databricks.com/aws/en/machine-learning/model-serving/}{Databricks Model Serving official documentation}.
\clearpage\subsection{04. Retrieval, vector, and graph data}
\textbf{Selection question.} Which query shape earns which index?
\begin{center}\begin{tikzpicture}[tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]
\node[tech](c0){Query + governed corpus};
\node[tech,right=of c0](c1){Index, filter, traverse, rank};
\node[tech,right=of c1](c2){Traceable evidence};
\draw[arr](c0)--(c1);\draw[arr](c1)--(c2);
\end{tikzpicture}\end{center}
\paragraph{Comparison matrix.}
\begin{longtable}{@{}p{20mm}p{22mm}p{26mm}p{46mm}p{46mm}@{}}
\toprule\textbf{Tool} & \textbf{Kind} & \textbf{Language / boundary} & \textbf{Choose when} & \textbf{Reject when}\\\midrule\endhead
pgvector & PostgreSQL vector extension & SQL, C, client languages; Inside PostgreSQL & Relational truth, ACL joins, and operational consolidation matter more than independently scaling vector traffic. & A massive vector-only workload needs specialized distributed indexes or managed isolation from OLTP.\\
Pinecone & Managed vector database & Python, TypeScript, REST; Fully managed service & A team wants production vector operations without owning a distributed database. & On-premises control, complex relational joins, or avoiding service lock-in is a hard requirement.\\
Qdrant & Vector database & Rust, REST, gRPC, client SDKs; Self-hosted or Qdrant Cloud & Rich metadata filtering and open-source operational control are central to the workload. & Relational transactions dominate or the team cannot operate another stateful service.\\
Weaviate & Vector database and search platform & Go, GraphQL, REST, client SDKs; Self-hosted or Weaviate Cloud & Teams want a schema-aware integrated search system with managed or self-hosted choices. & You need a tiny embedded store or strict separation between embedding inference and storage.\\
Milvus and Zilliz & Distributed vector database & Go, C++, Python, REST, SDKs; Self-hosted Milvus or managed Zilliz Cloud & Billion-scale vector workloads, index diversity, and independent compute scaling justify the operational surface. & A moderate corpus fits an existing SQL/search platform or the team needs minimal infrastructure.\\
Chroma & Developer vector store & Python, TypeScript; Embedded, client-server, or cloud & You need a fast local prototype, notebook workflow, or small application with minimal setup. & Strict multi-tenant production operations, complex filters, or large distributed scale dominate.\\
LanceDB & Embedded and cloud multimodal database & Python, TypeScript, Rust; Embedded files/object storage or cloud & Vectors should live near analytical data or an embedded/object-storage workflow needs zero server management. & You need a traditional always-on distributed database control plane with mature cross-region operations.\\
FAISS & Vector search library & C++, Python; Embedded in one process & You need algorithm control, offline experimentation, or an embedded index and will build persistence and serving yourself. & You need a database with updates, metadata filters, replication, authorization, and operational APIs.\\
Elasticsearch & Distributed search engine & Java, REST, client SDKs; Self-hosted or Elastic Cloud & Existing Elastic operations or hybrid lexical-vector search and analytics are decisive. & The workload is vector-only, embedded, or requires simple relational transactions.\\
OpenSearch & Open-source search engine & Java, REST, client SDKs; Self-hosted or Amazon OpenSearch Service & Open governance, AWS integration, or an existing OpenSearch estate guides the platform choice. & Engine-specific ANN tuning is unwanted or a smaller purpose-built store is sufficient.\\
Vespa & Search and recommendation engine & Java, C++, YQL, tensor expressions; Self-hosted or Vespa Cloud & A sophisticated search or recommendation system needs custom ranking stages over structured and vector features. & A basic CRUD vector service is enough and the team cannot absorb schema and ranking-language complexity.\\
Redis Search and Vector Sets & In-memory search data platform & C, Lua, client languages; Self-hosted or Redis Cloud & Hot retrieval, session memory, caching, and search benefit from one low-latency data service. & The corpus exceeds affordable RAM or disk-first analytical durability is primary.\\
Neo4j & Property graph database & Java, Cypher, Python, JavaScript; Self-hosted or Neo4j Aura & Queries are relationship-heavy, multi-hop, path, community, or provenance questions that graphs express directly. & Most access is simple similarity search or tabular joins and graph construction quality is unproven.\\
Memgraph & In-memory property graph database & C++, Cypher, Python; Self-hosted, Docker, or cloud & Operational or streaming graph workloads need fast updates and familiar Cypher-style traversal. & Disk-scale managed graph durability or RDF/SPARQL standards are primary requirements.\\
FalkorDB & Graph database & C, Cypher, Python, client SDKs; Self-hosted or cloud & A lightweight low-latency graph or GraphRAG service fits the Redis operational model. & You require the largest enterprise graph ecosystem, RDF semantics, or mature multi-region managed controls.\\
Amazon Neptune & Managed graph database & Gremlin, SPARQL, openCypher, SDKs; AWS managed service & AWS governance, managed availability, and Gremlin, openCypher, or SPARQL workloads dominate. & Cloud portability, local development simplicity, or custom in-memory graph performance is primary.\\
Kuzu & Embedded property graph database & C++, Python, Node.js, Rust, Java; Embedded in-process database & Local analytics, desktop tools, notebooks, or application-embedded graph traversal need low operational overhead. & Many remote clients, managed high availability, or independent graph-service scaling is required.\\
MongoDB Atlas Vector Search & Managed document and vector search & JavaScript, Python, client SDKs; MongoDB Atlas managed clusters & Application documents already live in MongoDB and vector retrieval must share filters and operational updates. & A vector-only service, advanced lexical engine, or relational joins dominate.\\
Couchbase Vector Search & Document database vector search & SQL++, REST, client SDKs; Couchbase Server or Capella & Couchbase is the system of record and retrieval should remain near mutable JSON documents. & A specialized vector database or mature graph traversal is the core need.\\
Databricks Mosaic AI Vector Search & Managed vector search service & Python, REST, Spark; Databricks managed endpoints and indexes & Source data and governance live in Delta and Unity Catalog and synchronization should be managed inside Databricks. & A portable database, custom ANN control, or non-Databricks operating model is required.\\
Snowflake Cortex Search & Managed hybrid search service & SQL, Python, REST; Snowflake managed service & Governed enterprise data already lives in Snowflake and teams need low-operations hybrid retrieval. & Subsecond mutable operational search or infrastructure portability is essential.\\
Oracle AI Vector Search & Relational database vector search & SQL, Python, client SDKs; Oracle Database & Enterprise records already live in Oracle and semantic search must join governed relational data. & A cloud-native vector service or open lightweight stack is the operating standard.\\
TigerGraph & Distributed graph database and analytics & GSQL, Python, REST; Self-managed or managed cloud & Large relationship-heavy workloads need parallel multi-hop analytics and an integrated graph application platform. & Open Cypher portability, embedded analytics, or simple managed RDF is more important.\\
ArangoDB & Multi-model document and graph database & C++, JavaScript, AQL, client SDKs; Self-hosted cluster or managed cloud & One database should support document, graph, and search patterns with flexible joins between them. & A specialized graph, vector, or relational engine provides clearer scaling and expertise.\\
Apache AGE & PostgreSQL graph extension & C, SQL, openCypher; Inside PostgreSQL & A PostgreSQL estate needs graph traversal without introducing a separate graph service. & Graph scale, tooling, or algorithms require a dedicated graph database.\\
JanusGraph & Distributed property graph database & Java, Gremlin; Self-hosted over distributed storage backends & A team already operates compatible storage and search backends and needs Gremlin-scale graph traversal. & Operational simplicity, managed service, or Cypher-first tooling is required.\\
\bottomrule\end{longtable}
\subsubsection{pgvector}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries P}}}\quad \badge{PostgreSQL vector extension} \quad \textbf{Languages:} SQL / C / client languages
\par\smallskip Vector columns, exact distance, HNSW, and IVFFlat beside relational transactions, joins, filters, and backups.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of pgvector as a postgresql vector extension between rows + vector query and transactional ranked rows; its defining mechanism is sql filter + vector index.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Rows + vector query};
\node[tech,right=of t0](t1){SQL filter + vector index};
\node[tech,right=of t1](t2){Transactional ranked rows};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Rows + vector query -> SQL filter + vector index -> Transactional ranked rows\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Vector columns, exact distance, HNSW, and IVFFlat beside relational transactions, joins, filters, and backups. Think of pgvector as a postgresql vector extension between rows + vector query and transactional ranked rows; its defining mechanism is sql filter + vector index.
\item[2. Structure and execution] Follow the data path Rows + vector query → SQL filter + vector index → Transactional ranked rows. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when relational truth, ACL joins, and operational consolidation matter more than independently scaling vector traffic. Reject it when a massive vector-only workload needs specialized distributed indexes or managed isolation from OLTP. Compare the same representative workload with Pinecone, Qdrant, Milvus.
\item[5. Production proof] Start with the failure hypothesis: Selective filters and planner choices can underfill ANN results or choose an unexpectedly expensive plan. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Inside PostgreSQL\\
Choose when & Relational truth, ACL joins, and operational consolidation matter more than independently scaling vector traffic.\\
Reject when & A massive vector-only workload needs specialized distributed indexes or managed isolation from OLTP.\\
Primary failure & Selective filters and planner choices can underfill ANN results or choose an unexpectedly expensive plan.\\
Compare with & Pinecone; Qdrant; Milvus\\
\end{tabularx}
\paragraph{Minimal use in SQL / C / client languages.}
\begin{Verbatim}[fontsize=\scriptsize]
CREATE EXTENSION vector;
SELECT id FROM docs ORDER BY embedding <=> $1 LIMIT 10;
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/pgvector/pgvector}{pgvector official documentation}.
\subsubsection{Pinecone}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries P}}}\quad \badge{Managed vector database} \quad \textbf{Languages:} Python / TypeScript / REST
\par\smallskip Managed dense and sparse vector search with namespaces, metadata filters, serverless or dedicated deployment choices, and integrated inference features.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Pinecone as a managed vector database between vectors + metadata and ranked matches; its defining mechanism is managed ann + filters.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Vectors + metadata};
\node[tech,right=of t0](t1){Managed ANN + filters};
\node[tech,right=of t1](t2){Ranked matches};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Vectors + metadata -> Managed ANN + filters -> Ranked matches\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Managed dense and sparse vector search with namespaces, metadata filters, serverless or dedicated deployment choices, and integrated inference features. Think of Pinecone as a managed vector database between vectors + metadata and ranked matches; its defining mechanism is managed ann + filters.
\item[2. Structure and execution] Follow the data path Vectors + metadata → Managed ANN + filters → Ranked matches. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when a team wants production vector operations without owning a distributed database. Reject it when on-premises control, complex relational joins, or avoiding service lock-in is a hard requirement. Compare the same representative workload with Qdrant, Weaviate, pgvector.
\item[5. Production proof] Start with the failure hypothesis: Poor namespace, metadata, and deletion design can create cross-tenant noise, expensive scans, or stale evidence. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Fully managed service\\
Choose when & A team wants production vector operations without owning a distributed database.\\
Reject when & On-premises control, complex relational joins, or avoiding service lock-in is a hard requirement.\\
Primary failure & Poor namespace, metadata, and deletion design can create cross-tenant noise, expensive scans, or stale evidence.\\
Compare with & Qdrant; Weaviate; pgvector\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install pinecone
index.query(vector=q, top_k=10, filter=metadata)
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.pinecone.io/}{Pinecone official documentation}.
\subsubsection{Qdrant}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries Q}}}\quad \badge{Vector database} \quad \textbf{Languages:} Rust / REST / gRPC / client SDKs
\par\smallskip HNSW-centered collections with payload indexes, filtering, quantization, sparse vectors, replication, and distributed deployment.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Qdrant as a vector database between points + payload and filtered nearest neighbors; its defining mechanism is hnsw + payload index.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Points + payload};
\node[tech,right=of t0](t1){HNSW + payload index};
\node[tech,right=of t1](t2){Filtered nearest neighbors};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Points + payload -> HNSW + payload index -> Filtered nearest neighbors\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] HNSW-centered collections with payload indexes, filtering, quantization, sparse vectors, replication, and distributed deployment. Think of Qdrant as a vector database between points + payload and filtered nearest neighbors; its defining mechanism is hnsw + payload index.
\item[2. Structure and execution] Follow the data path Points + payload → HNSW + payload index → Filtered nearest neighbors. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when rich metadata filtering and open-source operational control are central to the workload. Reject it when relational transactions dominate or the team cannot operate another stateful service. Compare the same representative workload with Pinecone, Weaviate, pgvector.
\item[5. Production proof] Start with the failure hypothesis: Post-filtering or unindexed payload conditions can lose recall and create unstable tail latency. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Qdrant Cloud\\
Choose when & Rich metadata filtering and open-source operational control are central to the workload.\\
Reject when & Relational transactions dominate or the team cannot operate another stateful service.\\
Primary failure & Post-filtering or unindexed payload conditions can lose recall and create unstable tail latency.\\
Compare with & Pinecone; Weaviate; pgvector\\
\end{tabularx}
\paragraph{Minimal use in Rust / REST / gRPC / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install qdrant-client
client.query_points(collection_name='docs', query=q)
\end{Verbatim}
\textbf{Primary sources.} \href{https://qdrant.tech/documentation/}{Qdrant official documentation}.
\subsubsection{Weaviate}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries W}}}\quad \badge{Vector database and search platform} \quad \textbf{Languages:} Go / GraphQL / REST / client SDKs
\par\smallskip Collections, hybrid retrieval, filters, multi-tenancy, replication, reranking, and optional vectorization modules.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Weaviate as a vector database and search platform between objects + schema and ranked objects; its defining mechanism is hybrid index + modules.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Objects + schema};
\node[tech,right=of t0](t1){Hybrid index + modules};
\node[tech,right=of t1](t2){Ranked objects};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Objects + schema -> Hybrid index + modules -> Ranked objects\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Collections, hybrid retrieval, filters, multi-tenancy, replication, reranking, and optional vectorization modules. Think of Weaviate as a vector database and search platform between objects + schema and ranked objects; its defining mechanism is hybrid index + modules.
\item[2. Structure and execution] Follow the data path Objects + schema → Hybrid index + modules → Ranked objects. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when teams want a schema-aware integrated search system with managed or self-hosted choices. Reject it when you need a tiny embedded store or strict separation between embedding inference and storage. Compare the same representative workload with Qdrant, Pinecone, Elasticsearch.
\item[5. Production proof] Start with the failure hypothesis: Implicit vectorizer versions can make reindexing, reproducibility, and rollback ambiguous. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Weaviate Cloud\\
Choose when & Teams want a schema-aware integrated search system with managed or self-hosted choices.\\
Reject when & You need a tiny embedded store or strict separation between embedding inference and storage.\\
Primary failure & Implicit vectorizer versions can make reindexing, reproducibility, and rollback ambiguous.\\
Compare with & Qdrant; Pinecone; Elasticsearch\\
\end{tabularx}
\paragraph{Minimal use in Go / GraphQL / REST / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install weaviate-client
collection.query.hybrid(query='term', limit=10)
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.weaviate.io/weaviate}{Weaviate official documentation}.
\subsubsection{Milvus and Zilliz}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries MAZ}}}\quad \badge{Distributed vector database} \quad \textbf{Languages:} Go / C++ / Python / REST / SDKs
\par\smallskip Compute-storage-separated vector system with collections, partitions, scalar filters, multiple ANN indexes, and large-scale operations.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Milvus and Zilliz as a distributed vector database between entities + vectors and distributed vector results; its defining mechanism is segments + ann indexes.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Entities + vectors};
\node[tech,right=of t0](t1){Segments + ANN indexes};
\node[tech,right=of t1](t2){Distributed vector results};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Entities + vectors -> Segments + ANN indexes -> Distributed vector results\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Compute-storage-separated vector system with collections, partitions, scalar filters, multiple ANN indexes, and large-scale operations. Think of Milvus and Zilliz as a distributed vector database between entities + vectors and distributed vector results; its defining mechanism is segments + ann indexes.
\item[2. Structure and execution] Follow the data path Entities + vectors → Segments + ANN indexes → Distributed vector results. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when billion-scale vector workloads, index diversity, and independent compute scaling justify the operational surface. Reject it when a moderate corpus fits an existing SQL/search platform or the team needs minimal infrastructure. Compare the same representative workload with Qdrant, Pinecone, pgvector.
\item[5. Production proof] Start with the failure hypothesis: Index choice, segment lifecycle, and compaction can dominate memory, build time, and filtered recall. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted Milvus or managed Zilliz Cloud\\
Choose when & Billion-scale vector workloads, index diversity, and independent compute scaling justify the operational surface.\\
Reject when & A moderate corpus fits an existing SQL/search platform or the team needs minimal infrastructure.\\
Primary failure & Index choice, segment lifecycle, and compaction can dominate memory, build time, and filtered recall.\\
Compare with & Qdrant; Pinecone; pgvector\\
\end{tabularx}
\paragraph{Minimal use in Go / C++ / Python / REST / SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install pymilvus
MilvusClient(uri=URI).search(collection_name='docs', data=[q])
\end{Verbatim}
\textbf{Primary sources.} \href{https://milvus.io/docs}{Milvus and Zilliz official documentation}.
\subsubsection{Chroma}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries C}}}\quad \badge{Developer vector store} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Collection-oriented embedding storage and similarity search optimized for local development and simple AI applications.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Chroma as a developer vector store between documents + embeddings and nearest documents; its defining mechanism is local collection.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Documents + embeddings};
\node[tech,right=of t0](t1){Local collection};
\node[tech,right=of t1](t2){Nearest documents};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Documents + embeddings -> Local collection -> Nearest documents\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Collection-oriented embedding storage and similarity search optimized for local development and simple AI applications. Think of Chroma as a developer vector store between documents + embeddings and nearest documents; its defining mechanism is local collection.
\item[2. Structure and execution] Follow the data path Documents + embeddings → Local collection → Nearest documents. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when you need a fast local prototype, notebook workflow, or small application with minimal setup. Reject it when strict multi-tenant production operations, complex filters, or large distributed scale dominate. Compare the same representative workload with LanceDB, pgvector, Qdrant.
\item[5. Production proof] Start with the failure hypothesis: A local persistence layout can be mistaken for a production backup, tenancy, and disaster-recovery design. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Embedded, client-server, or cloud\\
Choose when & You need a fast local prototype, notebook workflow, or small application with minimal setup.\\
Reject when & Strict multi-tenant production operations, complex filters, or large distributed scale dominate.\\
Primary failure & A local persistence layout can be mistaken for a production backup, tenancy, and disaster-recovery design.\\
Compare with & LanceDB; pgvector; Qdrant\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install chromadb
collection.add(ids=['1'], documents=['text'])
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.trychroma.com/}{Chroma official documentation}.
\subsubsection{LanceDB}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Embedded and cloud multimodal database} \quad \textbf{Languages:} Python / TypeScript / Rust
\par\smallskip Lance columnar format with vector and full-text search, versioned data, multimodal support, and data-lake-friendly operation.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of LanceDB as a embedded and cloud multimodal database between tables + embeddings and versioned search results; its defining mechanism is columnar fragments + indexes.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Tables + embeddings};
\node[tech,right=of t0](t1){Columnar fragments + indexes};
\node[tech,right=of t1](t2){Versioned search results};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Tables + embeddings -> Columnar fragments + indexes -> Versioned search results\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Lance columnar format with vector and full-text search, versioned data, multimodal support, and data-lake-friendly operation. Think of LanceDB as a embedded and cloud multimodal database between tables + embeddings and versioned search results; its defining mechanism is columnar fragments + indexes.
\item[2. Structure and execution] Follow the data path Tables + embeddings → Columnar fragments + indexes → Versioned search results. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when vectors should live near analytical data or an embedded/object-storage workflow needs zero server management. Reject it when you need a traditional always-on distributed database control plane with mature cross-region operations. Compare the same representative workload with Chroma, pgvector, Milvus.
\item[5. Production proof] Start with the failure hypothesis: Ignoring fragment compaction and version retention can inflate storage and degrade scans. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Embedded files/object storage or cloud\\
Choose when & Vectors should live near analytical data or an embedded/object-storage workflow needs zero server management.\\
Reject when & You need a traditional always-on distributed database control plane with mature cross-region operations.\\
Primary failure & Ignoring fragment compaction and version retention can inflate storage and degrade scans.\\
Compare with & Chroma; pgvector; Milvus\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript / Rust.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install lancedb
db.create_table('docs', data=rows)
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.lancedb.com/}{LanceDB official documentation}.
\subsubsection{FAISS}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries F}}}\quad \badge{Vector search library} \quad \textbf{Languages:} C++ / Python
\par\smallskip Meta library for exact and approximate dense-vector indexes, GPU acceleration, clustering, IVF, and product quantization.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of FAISS as a vector search library between dense matrix + query and neighbor ids + distances; its defining mechanism is exact or ann library index.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Dense matrix + query};
\node[tech,right=of t0](t1){Exact or ANN library index};
\node[tech,right=of t1](t2){Neighbor ids + distances};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Dense matrix + query -> Exact or ANN library index -> Neighbor ids + distances\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Meta library for exact and approximate dense-vector indexes, GPU acceleration, clustering, IVF, and product quantization. Think of FAISS as a vector search library between dense matrix + query and neighbor ids + distances; its defining mechanism is exact or ann library index.
\item[2. Structure and execution] Follow the data path Dense matrix + query → Exact or ANN library index → Neighbor ids + distances. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when you need algorithm control, offline experimentation, or an embedded index and will build persistence and serving yourself. Reject it when you need a database with updates, metadata filters, replication, authorization, and operational APIs. Compare the same representative workload with ScaNN, hnswlib, vector databases.
\item[5. Production proof] Start with the failure hypothesis: Calling FAISS a database hides missing durability, metadata, concurrency, and deletion guarantees. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Embedded in one process\\
Choose when & You need algorithm control, offline experimentation, or an embedded index and will build persistence and serving yourself.\\
Reject when & You need a database with updates, metadata filters, replication, authorization, and operational APIs.\\
Primary failure & Calling FAISS a database hides missing durability, metadata, concurrency, and deletion guarantees.\\
Compare with & ScaNN; hnswlib; vector databases\\
\end{tabularx}
\paragraph{Minimal use in C++ / Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install faiss-cpu
index.add(vectors); D, I = index.search(query, k)
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/facebookresearch/faiss}{FAISS official documentation}.
\subsubsection{Elasticsearch}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries E}}}\quad \badge{Distributed search engine} \quad \textbf{Languages:} Java / REST / client SDKs
\par\smallskip Inverted indexes, BM25, aggregations, filters, approximate kNN, ingest pipelines, and multi-stage retrieval in one platform.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Elasticsearch as a distributed search engine between documents + mappings and hybrid ranked hits; its defining mechanism is shards, bm25, knn.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Documents + mappings};
\node[tech,right=of t0](t1){Shards, BM25, kNN};
\node[tech,right=of t1](t2){Hybrid ranked hits};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Documents + mappings -> Shards, BM25, kNN -> Hybrid ranked hits\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Inverted indexes, BM25, aggregations, filters, approximate kNN, ingest pipelines, and multi-stage retrieval in one platform. Think of Elasticsearch as a distributed search engine between documents + mappings and hybrid ranked hits; its defining mechanism is shards, bm25, knn.
\item[2. Structure and execution] Follow the data path Documents + mappings → Shards, BM25, kNN → Hybrid ranked hits. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when existing Elastic operations or hybrid lexical-vector search and analytics are decisive. Reject it when the workload is vector-only, embedded, or requires simple relational transactions. Compare the same representative workload with OpenSearch, Vespa, Weaviate.
\item[5. Production proof] Start with the failure hypothesis: Oversharding fragments candidates, consumes heap, and makes hybrid relevance and coordination harder to tune. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Elastic Cloud\\
Choose when & Existing Elastic operations or hybrid lexical-vector search and analytics are decisive.\\
Reject when & The workload is vector-only, embedded, or requires simple relational transactions.\\
Primary failure & Oversharding fragments candidates, consumes heap, and makes hybrid relevance and coordination harder to tune.\\
Compare with & OpenSearch; Vespa; Weaviate\\
\end{tabularx}
\paragraph{Minimal use in Java / REST / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
PUT /docs/_search
{"knn":{"field":"vector","query_vector":[...]}}
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.elastic.co/docs/solutions/search}{Elasticsearch official documentation}.
\subsubsection{OpenSearch}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries O}}}\quad \badge{Open-source search engine} \quad \textbf{Languages:} Java / REST / client SDKs
\par\smallskip Lexical, vector, neural, filtering, aggregation, and search-pipeline capabilities with several kNN engines.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of OpenSearch as a open-source search engine between documents + pipeline and fused search hits; its defining mechanism is lexical and vector engines.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Documents + pipeline};
\node[tech,right=of t0](t1){Lexical and vector engines};
\node[tech,right=of t1](t2){Fused search hits};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Documents + pipeline -> Lexical and vector engines -> Fused search hits\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Lexical, vector, neural, filtering, aggregation, and search-pipeline capabilities with several kNN engines. Think of OpenSearch as a open-source search engine between documents + pipeline and fused search hits; its defining mechanism is lexical and vector engines.
\item[2. Structure and execution] Follow the data path Documents + pipeline → Lexical and vector engines → Fused search hits. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when open governance, AWS integration, or an existing OpenSearch estate guides the platform choice. Reject it when engine-specific ANN tuning is unwanted or a smaller purpose-built store is sufficient. Compare the same representative workload with Elasticsearch, Vespa, Qdrant.
\item[5. Production proof] Start with the failure hypothesis: Lucene, FAISS, and other engine settings are not interchangeable; copying parameters produces invalid comparisons. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Amazon OpenSearch Service\\
Choose when & Open governance, AWS integration, or an existing OpenSearch estate guides the platform choice.\\
Reject when & Engine-specific ANN tuning is unwanted or a smaller purpose-built store is sufficient.\\
Primary failure & Lucene, FAISS, and other engine settings are not interchangeable; copying parameters produces invalid comparisons.\\
Compare with & Elasticsearch; Vespa; Qdrant\\
\end{tabularx}
\paragraph{Minimal use in Java / REST / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
PUT /my-index/_search
{"query":{"neural":{...}}}
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.opensearch.org/latest/vector-search/}{OpenSearch official documentation}.
\subsubsection{Vespa}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries V}}}\quad \badge{Search and recommendation engine} \quad \textbf{Languages:} Java / C++ / YQL / tensor expressions
\par\smallskip Real-time document updates, structured filters, BM25, nearest-neighbor tensors, phased ranking, grouping, and online serving.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Vespa as a search and recommendation engine between query + tensors and ranked documents; its defining mechanism is candidate retrieval + phased ranking.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Query + tensors};
\node[tech,right=of t0](t1){Candidate retrieval + phased ranking};
\node[tech,right=of t1](t2){Ranked documents};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Query + tensors -> Candidate retrieval + phased ranking -> Ranked documents\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Real-time document updates, structured filters, BM25, nearest-neighbor tensors, phased ranking, grouping, and online serving. Think of Vespa as a search and recommendation engine between query + tensors and ranked documents; its defining mechanism is candidate retrieval + phased ranking.
\item[2. Structure and execution] Follow the data path Query + tensors → Candidate retrieval + phased ranking → Ranked documents. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when a sophisticated search or recommendation system needs custom ranking stages over structured and vector features. Reject it when a basic CRUD vector service is enough and the team cannot absorb schema and ranking-language complexity. Compare the same representative workload with Elasticsearch, OpenSearch, Milvus.
\item[5. Production proof] Start with the failure hypothesis: Putting expensive tensor or model expressions in first-phase ranking can collapse throughput. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Vespa Cloud\\
Choose when & A sophisticated search or recommendation system needs custom ranking stages over structured and vector features.\\
Reject when & A basic CRUD vector service is enough and the team cannot absorb schema and ranking-language complexity.\\
Primary failure & Putting expensive tensor or model expressions in first-phase ranking can collapse throughput.\\
Compare with & Elasticsearch; OpenSearch; Milvus\\
\end{tabularx}
\paragraph{Minimal use in Java / C++ / YQL / tensor expressions.}
\begin{Verbatim}[fontsize=\scriptsize]
vespa clone album-recommendation
vespa deploy --wait 300
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.vespa.ai/}{Vespa official documentation}.
\subsubsection{Redis Search and Vector Sets}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries RSA}}}\quad \badge{In-memory search data platform} \quad \textbf{Languages:} C / Lua / client languages
\par\smallskip Low-latency full-text, vector, JSON, filtering, and caching close to application state.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Redis Search and Vector Sets as a in-memory search data platform between json or hashes + vectors and low-latency matches; its defining mechanism is in-memory index.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){JSON or hashes + vectors};
\node[tech,right=of t0](t1){In-memory index};
\node[tech,right=of t1](t2){Low-latency matches};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & JSON or hashes + vectors -> In-memory index -> Low-latency matches\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Low-latency full-text, vector, JSON, filtering, and caching close to application state. Think of Redis Search and Vector Sets as a in-memory search data platform between json or hashes + vectors and low-latency matches; its defining mechanism is in-memory index.
\item[2. Structure and execution] Follow the data path JSON or hashes + vectors → In-memory index → Low-latency matches. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when hot retrieval, session memory, caching, and search benefit from one low-latency data service. Reject it when the corpus exceeds affordable RAM or disk-first analytical durability is primary. Compare the same representative workload with pgvector, Qdrant, Elasticsearch.
\item[5. Production proof] Start with the failure hypothesis: Memory pressure or eviction policy can remove critical state and make recall depend on unrelated cache traffic. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Redis Cloud\\
Choose when & Hot retrieval, session memory, caching, and search benefit from one low-latency data service.\\
Reject when & The corpus exceeds affordable RAM or disk-first analytical durability is primary.\\
Primary failure & Memory pressure or eviction policy can remove critical state and make recall depend on unrelated cache traffic.\\
Compare with & pgvector; Qdrant; Elasticsearch\\
\end{tabularx}
\paragraph{Minimal use in C / Lua / client languages.}
\begin{Verbatim}[fontsize=\scriptsize]
FT.CREATE idx ON JSON SCHEMA $.v AS v VECTOR HNSW 6 TYPE FLOAT32 DIM 768 DISTANCE_METRIC COSINE
\end{Verbatim}
\textbf{Primary sources.} \href{https://redis.io/docs/latest/develop/ai/}{Redis Search and Vector Sets official documentation}.
\subsubsection{Neo4j}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries N}}}\quad \badge{Property graph database} \quad \textbf{Languages:} Java / Cypher / Python / JavaScript
\par\smallskip Transactional property graph with Cypher, vector and full-text indexes, graph algorithms, security, and GraphRAG integrations.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Neo4j as a property graph database between entities + relationships and paths with evidence; its defining mechanism is cypher traversal + vector entry.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Entities + relationships};
\node[tech,right=of t0](t1){Cypher traversal + vector entry};
\node[tech,right=of t1](t2){Paths with evidence};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Entities + relationships -> Cypher traversal + vector entry -> Paths with evidence\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Transactional property graph with Cypher, vector and full-text indexes, graph algorithms, security, and GraphRAG integrations. Think of Neo4j as a property graph database between entities + relationships and paths with evidence; its defining mechanism is cypher traversal + vector entry.
\item[2. Structure and execution] Follow the data path Entities + relationships → Cypher traversal + vector entry → Paths with evidence. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when queries are relationship-heavy, multi-hop, path, community, or provenance questions that graphs express directly. Reject it when most access is simple similarity search or tabular joins and graph construction quality is unproven. Compare the same representative workload with Memgraph, Amazon Neptune, pgvector.
\item[5. Production proof] Start with the failure hypothesis: Hallucinated or unresolved edges can become authoritative unless every graph fact retains source and extraction provenance. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Neo4j Aura\\
Choose when & Queries are relationship-heavy, multi-hop, path, community, or provenance questions that graphs express directly.\\
Reject when & Most access is simple similarity search or tabular joins and graph construction quality is unproven.\\
Primary failure & Hallucinated or unresolved edges can become authoritative unless every graph fact retains source and extraction provenance.\\
Compare with & Memgraph; Amazon Neptune; pgvector\\
\end{tabularx}
\paragraph{Minimal use in Java / Cypher / Python / JavaScript.}
\begin{Verbatim}[fontsize=\scriptsize]
CREATE VECTOR INDEX doc_embeddings FOR (n:Document) ON n.embedding;
MATCH p=(a)-[*1..3]->(b) RETURN p;
\end{Verbatim}
\textbf{Primary sources.} \href{https://neo4j.com/docs/}{Neo4j official documentation} \quad \href{https://neo4j.com/docs/getting-started/appendix/graphdb-concepts/}{Neo4j graph database concepts} \quad \href{https://neo4j.com/docs/cypher-manual/current/}{Cypher manual} \quad \href{https://neo4j.com/docs/neo4j-graphrag-python/current/}{Neo4j GraphRAG for Python}.
\subsubsection{Memgraph}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries M}}}\quad \badge{In-memory property graph database} \quad \textbf{Languages:} C++ / Cypher / Python
\par\smallskip Cypher-compatible streaming graph database with in-memory execution, triggers, procedures, and graph algorithms.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Memgraph as a in-memory property graph database between events + graph updates and real-time paths and analytics; its defining mechanism is in-memory cypher engine.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Events + graph updates};
\node[tech,right=of t0](t1){In-memory Cypher engine};
\node[tech,right=of t1](t2){Real-time paths and analytics};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Events + graph updates -> In-memory Cypher engine -> Real-time paths and analytics\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Cypher-compatible streaming graph database with in-memory execution, triggers, procedures, and graph algorithms. Think of Memgraph as a in-memory property graph database between events + graph updates and real-time paths and analytics; its defining mechanism is in-memory cypher engine.
\item[2. Structure and execution] Follow the data path Events + graph updates → In-memory Cypher engine → Real-time paths and analytics. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when operational or streaming graph workloads need fast updates and familiar Cypher-style traversal. Reject it when disk-scale managed graph durability or RDF/SPARQL standards are primary requirements. Compare the same representative workload with Neo4j, FalkorDB, TigerGraph.
\item[5. Production proof] Start with the failure hypothesis: In-memory performance claims must be tested with persistence, replication, graph size, and recovery enabled. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted, Docker, or cloud\\
Choose when & Operational or streaming graph workloads need fast updates and familiar Cypher-style traversal.\\
Reject when & Disk-scale managed graph durability or RDF/SPARQL standards are primary requirements.\\
Primary failure & In-memory performance claims must be tested with persistence, replication, graph size, and recovery enabled.\\
Compare with & Neo4j; FalkorDB; TigerGraph\\
\end{tabularx}
\paragraph{Minimal use in C++ / Cypher / Python.}
\begin{Verbatim}[fontsize=\scriptsize]
docker run memgraph/memgraph-platform
MATCH (n) RETURN n LIMIT 10;
\end{Verbatim}
\textbf{Primary sources.} \href{https://memgraph.com/docs}{Memgraph official documentation}.
\subsubsection{FalkorDB}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries F}}}\quad \badge{Graph database} \quad \textbf{Languages:} C / Cypher / Python / client SDKs
\par\smallskip Low-latency property graph built on Redis concepts with Cypher queries, graph algorithms, and GraphRAG positioning.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of FalkorDB as a graph database between entities + edges and subgraphs and paths; its defining mechanism is cypher graph engine.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Entities + edges};
\node[tech,right=of t0](t1){Cypher graph engine};
\node[tech,right=of t1](t2){Subgraphs and paths};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Entities + edges -> Cypher graph engine -> Subgraphs and paths\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Low-latency property graph built on Redis concepts with Cypher queries, graph algorithms, and GraphRAG positioning. Think of FalkorDB as a graph database between entities + edges and subgraphs and paths; its defining mechanism is cypher graph engine.
\item[2. Structure and execution] Follow the data path Entities + edges → Cypher graph engine → Subgraphs and paths. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when a lightweight low-latency graph or GraphRAG service fits the Redis operational model. Reject it when you require the largest enterprise graph ecosystem, RDF semantics, or mature multi-region managed controls. Compare the same representative workload with Neo4j, Memgraph, Amazon Neptune.
\item[5. Production proof] Start with the failure hypothesis: Assuming Redis-like familiarity covers graph modeling, persistence, and query-plan behavior is unsafe. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or cloud\\
Choose when & A lightweight low-latency graph or GraphRAG service fits the Redis operational model.\\
Reject when & You require the largest enterprise graph ecosystem, RDF semantics, or mature multi-region managed controls.\\
Primary failure & Assuming Redis-like familiarity covers graph modeling, persistence, and query-plan behavior is unsafe.\\
Compare with & Neo4j; Memgraph; Amazon Neptune\\
\end{tabularx}
\paragraph{Minimal use in C / Cypher / Python / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
docker run falkordb/falkordb
GRAPH.QUERY g "MATCH (n) RETURN n"
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.falkordb.com/}{FalkorDB official documentation}.
\subsubsection{Amazon Neptune}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries AN}}}\quad \badge{Managed graph database} \quad \textbf{Languages:} Gremlin / SPARQL / openCypher / SDKs
\par\smallskip Managed graph service for property-graph and RDF workloads with AWS identity, backup, analytics, and high-availability integration.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Amazon Neptune as a managed graph database between graph or rdf data and traversal or semantic result; its defining mechanism is managed graph engine.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Graph or RDF data};
\node[tech,right=of t0](t1){Managed graph engine};
\node[tech,right=of t1](t2){Traversal or semantic result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Graph or RDF data -> Managed graph engine -> Traversal or semantic result\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Managed graph service for property-graph and RDF workloads with AWS identity, backup, analytics, and high-availability integration. Think of Amazon Neptune as a managed graph database between graph or rdf data and traversal or semantic result; its defining mechanism is managed graph engine.
\item[2. Structure and execution] Follow the data path Graph or RDF data → Managed graph engine → Traversal or semantic result. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when aWS governance, managed availability, and Gremlin, openCypher, or SPARQL workloads dominate. Reject it when cloud portability, local development simplicity, or custom in-memory graph performance is primary. Compare the same representative workload with Neo4j Aura, TigerGraph, Memgraph.
\item[5. Production proof] Start with the failure hypothesis: Choosing a graph model or query language late can create expensive migrations and incompatible semantics. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & AWS managed service\\
Choose when & AWS governance, managed availability, and Gremlin, openCypher, or SPARQL workloads dominate.\\
Reject when & Cloud portability, local development simplicity, or custom in-memory graph performance is primary.\\
Primary failure & Choosing a graph model or query language late can create expensive migrations and incompatible semantics.\\
Compare with & Neo4j Aura; TigerGraph; Memgraph\\
\end{tabularx}
\paragraph{Minimal use in Gremlin / SPARQL / openCypher / SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
g.V().has('account','id',id).repeat(out()).times(3).path()
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.aws.amazon.com/neptune/}{Amazon Neptune official documentation}.
\subsubsection{Kuzu}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries K}}}\quad \badge{Embedded property graph database} \quad \textbf{Languages:} C++ / Python / Node.js / Rust / Java
\par\smallskip Column-oriented embedded property graph for analytical Cypher-style queries without a separate server.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Kuzu as a embedded property graph database between local files + graph schema and cypher query result; its defining mechanism is embedded columnar graph.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Local files + graph schema};
\node[tech,right=of t0](t1){Embedded columnar graph};
\node[tech,right=of t1](t2){Cypher query result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Local files + graph schema -> Embedded columnar graph -> Cypher query result\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Column-oriented embedded property graph for analytical Cypher-style queries without a separate server. Think of Kuzu as a embedded property graph database between local files + graph schema and cypher query result; its defining mechanism is embedded columnar graph.
\item[2. Structure and execution] Follow the data path Local files + graph schema → Embedded columnar graph → Cypher query result. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when local analytics, desktop tools, notebooks, or application-embedded graph traversal need low operational overhead. Reject it when many remote clients, managed high availability, or independent graph-service scaling is required. Compare the same representative workload with DuckDB plus graph logic, Neo4j, Memgraph.
\item[5. Production proof] Start with the failure hypothesis: An embedded database shares the host process failure, resource, and authorization boundary. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Embedded in-process database\\
Choose when & Local analytics, desktop tools, notebooks, or application-embedded graph traversal need low operational overhead.\\
Reject when & Many remote clients, managed high availability, or independent graph-service scaling is required.\\
Primary failure & An embedded database shares the host process failure, resource, and authorization boundary.\\
Compare with & DuckDB plus graph logic; Neo4j; Memgraph\\
\end{tabularx}
\paragraph{Minimal use in C++ / Python / Node.js / Rust / Java.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install kuzu
db = kuzu.Database('graph.kuzu')
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.kuzudb.com/}{Kuzu official documentation}.
\subsubsection{MongoDB Atlas Vector Search}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries MAV}}}\quad \badge{Managed document and vector search} \quad \textbf{Languages:} JavaScript / Python / client SDKs
\par\smallskip Vector search indexes inside Atlas alongside document filters, transactions, operational data, and aggregation pipelines.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of MongoDB Atlas Vector Search as a managed document and vector search between documents + vectors and ranked documents; its defining mechanism is atlas vector index + filters.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Documents + vectors};
\node[tech,right=of t0](t1){Atlas vector index + filters};
\node[tech,right=of t1](t2){Ranked documents};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Documents + vectors -> Atlas vector index + filters -> Ranked documents\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Vector search indexes inside Atlas alongside document filters, transactions, operational data, and aggregation pipelines. Think of MongoDB Atlas Vector Search as a managed document and vector search between documents + vectors and ranked documents; its defining mechanism is atlas vector index + filters.
\item[2. Structure and execution] Follow the data path Documents + vectors → Atlas vector index + filters → Ranked documents. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when application documents already live in MongoDB and vector retrieval must share filters and operational updates. Reject it when a vector-only service, advanced lexical engine, or relational joins dominate. Compare the same representative workload with pgvector, Pinecone, Elasticsearch.
\item[5. Production proof] Start with the failure hypothesis: Index freshness, filter selectivity, dimension limits, and cluster sizing can couple OLTP and retrieval tails. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & MongoDB Atlas managed clusters\\
Choose when & Application documents already live in MongoDB and vector retrieval must share filters and operational updates.\\
Reject when & A vector-only service, advanced lexical engine, or relational joins dominate.\\
Primary failure & Index freshness, filter selectivity, dimension limits, and cluster sizing can couple OLTP and retrieval tails.\\
Compare with & pgvector; Pinecone; Elasticsearch\\
\end{tabularx}
\paragraph{Minimal use in JavaScript / Python / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
db.collection.aggregate([{ $vectorSearch: { index: 'v', path: 'embedding' } }])
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.mongodb.com/docs/atlas/atlas-vector-search/}{MongoDB Atlas Vector Search official documentation}.
\subsubsection{Couchbase Vector Search}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries CVS}}}\quad \badge{Document database vector search} \quad \textbf{Languages:} SQL++ / REST / client SDKs
\par\smallskip Vector and hybrid search integrated with distributed JSON documents, full-text search, SQL++, and operational key-value access.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Couchbase Vector Search as a document database vector search between json documents + vectors and ranked document hits; its defining mechanism is search service + metadata filters.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){JSON documents + vectors};
\node[tech,right=of t0](t1){Search service + metadata filters};
\node[tech,right=of t1](t2){Ranked document hits};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & JSON documents + vectors -> Search service + metadata filters -> Ranked document hits\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Vector and hybrid search integrated with distributed JSON documents, full-text search, SQL++, and operational key-value access. Think of Couchbase Vector Search as a document database vector search between json documents + vectors and ranked document hits; its defining mechanism is search service + metadata filters.
\item[2. Structure and execution] Follow the data path JSON documents + vectors → Search service + metadata filters → Ranked document hits. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when couchbase is the system of record and retrieval should remain near mutable JSON documents. Reject it when a specialized vector database or mature graph traversal is the core need. Compare the same representative workload with MongoDB Atlas Vector Search, Elasticsearch, pgvector.
\item[5. Production proof] Start with the failure hypothesis: Search-index consistency and resource contention with operational traffic must be measured. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Couchbase Server or Capella\\
Choose when & Couchbase is the system of record and retrieval should remain near mutable JSON documents.\\
Reject when & A specialized vector database or mature graph traversal is the core need.\\
Primary failure & Search-index consistency and resource contention with operational traffic must be measured.\\
Compare with & MongoDB Atlas Vector Search; Elasticsearch; pgvector\\
\end{tabularx}
\paragraph{Minimal use in SQL++ / REST / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
CREATE INDEX idx ON bucket(scope.collection)(embedding) USING GSI;
SELECT META().id FROM bucket ORDER BY APPROX_VECTOR_DISTANCE(embedding, $q);
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.couchbase.com/server/current/vector-search/vector-search.html}{Couchbase Vector Search official documentation}.
\subsubsection{Databricks Mosaic AI Vector Search}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries DMA}}}\quad \badge{Managed vector search service} \quad \textbf{Languages:} Python / REST / Spark
\par\smallskip Creates governed vector indexes from Delta tables or direct writes with managed embeddings, filtering, and endpoint lifecycle.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Databricks Mosaic AI Vector Search as a managed vector search service between delta table or direct vectors and governed ranked rows; its defining mechanism is managed sync + ann endpoint.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Delta table or direct vectors};
\node[tech,right=of t0](t1){Managed sync + ANN endpoint};
\node[tech,right=of t1](t2){Governed ranked rows};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Delta table or direct vectors -> Managed sync + ANN endpoint -> Governed ranked rows\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Creates governed vector indexes from Delta tables or direct writes with managed embeddings, filtering, and endpoint lifecycle. Think of Databricks Mosaic AI Vector Search as a managed vector search service between delta table or direct vectors and governed ranked rows; its defining mechanism is managed sync + ann endpoint.
\item[2. Structure and execution] Follow the data path Delta table or direct vectors → Managed sync + ANN endpoint → Governed ranked rows. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when source data and governance live in Delta and Unity Catalog and synchronization should be managed inside Databricks. Reject it when a portable database, custom ANN control, or non-Databricks operating model is required. Compare the same representative workload with Pinecone, pgvector, Elasticsearch.
\item[5. Production proof] Start with the failure hypothesis: Delta sync lag, endpoint capacity, embedding identity, and workspace permissions can produce stale or inaccessible evidence. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Databricks managed endpoints and indexes\\
Choose when & Source data and governance live in Delta and Unity Catalog and synchronization should be managed inside Databricks.\\
Reject when & A portable database, custom ANN control, or non-Databricks operating model is required.\\
Primary failure & Delta sync lag, endpoint capacity, embedding identity, and workspace permissions can produce stale or inaccessible evidence.\\
Compare with & Pinecone; pgvector; Elasticsearch\\
\end{tabularx}
\paragraph{Minimal use in Python / REST / Spark.}
\begin{Verbatim}[fontsize=\scriptsize]
from databricks.vector_search.client import VectorSearchClient
vsc = VectorSearchClient()
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.databricks.com/aws/en/generative-ai/vector-search/}{Databricks Mosaic AI Vector Search official documentation}.
\subsubsection{Snowflake Cortex Search}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries SCS}}}\quad \badge{Managed hybrid search service} \quad \textbf{Languages:} SQL / Python / REST
\par\smallskip Managed hybrid lexical and vector search over Snowflake data with service refresh, filters, and Cortex integration.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Snowflake Cortex Search as a managed hybrid search service between snowflake table + query and filtered ranked rows; its defining mechanism is managed hybrid search service.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Snowflake table + query};
\node[tech,right=of t0](t1){Managed hybrid search service};
\node[tech,right=of t1](t2){Filtered ranked rows};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Snowflake table + query -> Managed hybrid search service -> Filtered ranked rows\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Managed hybrid lexical and vector search over Snowflake data with service refresh, filters, and Cortex integration. Think of Snowflake Cortex Search as a managed hybrid search service between snowflake table + query and filtered ranked rows; its defining mechanism is managed hybrid search service.
\item[2. Structure and execution] Follow the data path Snowflake table + query → Managed hybrid search service → Filtered ranked rows. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when governed enterprise data already lives in Snowflake and teams need low-operations hybrid retrieval. Reject it when subsecond mutable operational search or infrastructure portability is essential. Compare the same representative workload with Databricks Vector Search, Elasticsearch, Pinecone.
\item[5. Production proof] Start with the failure hypothesis: Target lag, warehouse cost, access policy, and service refresh behavior can make evidence stale or expensive. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Snowflake managed service\\
Choose when & Governed enterprise data already lives in Snowflake and teams need low-operations hybrid retrieval.\\
Reject when & Subsecond mutable operational search or infrastructure portability is essential.\\
Primary failure & Target lag, warehouse cost, access policy, and service refresh behavior can make evidence stale or expensive.\\
Compare with & Databricks Vector Search; Elasticsearch; Pinecone\\
\end{tabularx}
\paragraph{Minimal use in SQL / Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
CREATE CORTEX SEARCH SERVICE docs ON text ATTRIBUTES category WAREHOUSE = wh TARGET_LAG = '1 hour';
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-search/cortex-search-overview}{Snowflake Cortex Search official documentation}.
\subsubsection{Oracle AI Vector Search}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries OAV}}}\quad \badge{Relational database vector search} \quad \textbf{Languages:} SQL / Python / client SDKs
\par\smallskip Vector data types, indexes, distance functions, hybrid SQL queries, and embedding integrations inside Oracle Database.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Oracle AI Vector Search as a relational database vector search between rows + vector query and governed ranked rows; its defining mechanism is sql joins + vector index.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Rows + vector query};
\node[tech,right=of t0](t1){SQL joins + vector index};
\node[tech,right=of t1](t2){Governed ranked rows};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Rows + vector query -> SQL joins + vector index -> Governed ranked rows\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Vector data types, indexes, distance functions, hybrid SQL queries, and embedding integrations inside Oracle Database. Think of Oracle AI Vector Search as a relational database vector search between rows + vector query and governed ranked rows; its defining mechanism is sql joins + vector index.
\item[2. Structure and execution] Follow the data path Rows + vector query → SQL joins + vector index → Governed ranked rows. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when enterprise records already live in Oracle and semantic search must join governed relational data. Reject it when a cloud-native vector service or open lightweight stack is the operating standard. Compare the same representative workload with pgvector, AlloyDB AI, Pinecone.
\item[5. Production proof] Start with the failure hypothesis: Index parameters, optimizer choices, and mixed relational-vector predicates can produce unstable plans. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Oracle Database\\
Choose when & Enterprise records already live in Oracle and semantic search must join governed relational data.\\
Reject when & A cloud-native vector service or open lightweight stack is the operating standard.\\
Primary failure & Index parameters, optimizer choices, and mixed relational-vector predicates can produce unstable plans.\\
Compare with & pgvector; AlloyDB AI; Pinecone\\
\end{tabularx}
\paragraph{Minimal use in SQL / Python / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
SELECT id FROM docs ORDER BY VECTOR_DISTANCE(embedding, :q) FETCH FIRST 10 ROWS ONLY;
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.oracle.com/en/database/oracle/oracle-database/23/vecse/}{Oracle AI Vector Search official documentation}.
\subsubsection{TigerGraph}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries T}}}\quad \badge{Distributed graph database and analytics} \quad \textbf{Languages:} GSQL / Python / REST
\par\smallskip Distributed property graph platform with parallel query execution, graph algorithms, loading jobs, and real-time analytics.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of TigerGraph as a distributed graph database and analytics between events + graph schema and paths, scores, or features; its defining mechanism is distributed graph engine + gsql.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Events + graph schema};
\node[tech,right=of t0](t1){Distributed graph engine + GSQL};
\node[tech,right=of t1](t2){Paths, scores, or features};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Events + graph schema -> Distributed graph engine + GSQL -> Paths, scores, or features\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Distributed property graph platform with parallel query execution, graph algorithms, loading jobs, and real-time analytics. Think of TigerGraph as a distributed graph database and analytics between events + graph schema and paths, scores, or features; its defining mechanism is distributed graph engine + gsql.
\item[2. Structure and execution] Follow the data path Events + graph schema → Distributed graph engine + GSQL → Paths, scores, or features. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when large relationship-heavy workloads need parallel multi-hop analytics and an integrated graph application platform. Reject it when open Cypher portability, embedded analytics, or simple managed RDF is more important. Compare the same representative workload with Neo4j, Amazon Neptune, Memgraph.
\item[5. Production proof] Start with the failure hypothesis: Graph partitioning, supernodes, query design, and proprietary language skills can dominate operations. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-managed or managed cloud\\
Choose when & Large relationship-heavy workloads need parallel multi-hop analytics and an integrated graph application platform.\\
Reject when & Open Cypher portability, embedded analytics, or simple managed RDF is more important.\\
Primary failure & Graph partitioning, supernodes, query design, and proprietary language skills can dominate operations.\\
Compare with & Neo4j; Amazon Neptune; Memgraph\\
\end{tabularx}
\paragraph{Minimal use in GSQL / Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
CREATE QUERY neighbors(VERTEX<Person> p) FOR GRAPH MyGraph { PRINT p; }
gsql query.gsql
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.tigergraph.com/}{TigerGraph official documentation}.
\subsubsection{ArangoDB}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries A}}}\quad \badge{Multi-model document and graph database} \quad \textbf{Languages:} C++ / JavaScript / AQL / client SDKs
\par\smallskip Combines documents, key-value access, graph traversals, search, and vector indexes through AQL and one operational platform.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of ArangoDB as a multi-model document and graph database between documents + edges + query and joined graph or ranked result; its defining mechanism is aql execution across indexes.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Documents + edges + query};
\node[tech,right=of t0](t1){AQL execution across indexes};
\node[tech,right=of t1](t2){Joined graph or ranked result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Documents + edges + query -> AQL execution across indexes -> Joined graph or ranked result\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Combines documents, key-value access, graph traversals, search, and vector indexes through AQL and one operational platform. Think of ArangoDB as a multi-model document and graph database between documents + edges + query and joined graph or ranked result; its defining mechanism is aql execution across indexes.
\item[2. Structure and execution] Follow the data path Documents + edges + query → AQL execution across indexes → Joined graph or ranked result. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when one database should support document, graph, and search patterns with flexible joins between them. Reject it when a specialized graph, vector, or relational engine provides clearer scaling and expertise. Compare the same representative workload with Neo4j, MongoDB, Elasticsearch.
\item[5. Production proof] Start with the failure hypothesis: Multi-model convenience can mask different scaling, indexing, and consistency needs inside one cluster. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted cluster or managed cloud\\
Choose when & One database should support document, graph, and search patterns with flexible joins between them.\\
Reject when & A specialized graph, vector, or relational engine provides clearer scaling and expertise.\\
Primary failure & Multi-model convenience can mask different scaling, indexing, and consistency needs inside one cluster.\\
Compare with & Neo4j; MongoDB; Elasticsearch\\
\end{tabularx}
\paragraph{Minimal use in C++ / JavaScript / AQL / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
FOR v IN 1..3 OUTBOUND @start GRAPH 'g' RETURN v
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.arangodb.com/}{ArangoDB official documentation}.
\subsubsection{Apache AGE}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries AA}}}\quad \badge{PostgreSQL graph extension} \quad \textbf{Languages:} C / SQL / openCypher
\par\smallskip Adds property-graph modeling and openCypher queries to PostgreSQL while retaining SQL storage and transactions.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Apache AGE as a postgresql graph extension between relational rows + graph and graph paths or joined sql; its defining mechanism is opencypher extension in postgresql.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Relational rows + graph};
\node[tech,right=of t0](t1){openCypher extension in PostgreSQL};
\node[tech,right=of t1](t2){Graph paths or joined SQL};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Relational rows + graph -> openCypher extension in PostgreSQL -> Graph paths or joined SQL\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Adds property-graph modeling and openCypher queries to PostgreSQL while retaining SQL storage and transactions. Think of Apache AGE as a postgresql graph extension between relational rows + graph and graph paths or joined sql; its defining mechanism is opencypher extension in postgresql.
\item[2. Structure and execution] Follow the data path Relational rows + graph → openCypher extension in PostgreSQL → Graph paths or joined SQL. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when a PostgreSQL estate needs graph traversal without introducing a separate graph service. Reject it when graph scale, tooling, or algorithms require a dedicated graph database. Compare the same representative workload with Neo4j, pgvector, Kuzu.
\item[5. Production proof] Start with the failure hypothesis: Planner, extension version, graph modeling, and SQL-Cypher boundary behavior may lag specialized engines. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Inside PostgreSQL\\
Choose when & A PostgreSQL estate needs graph traversal without introducing a separate graph service.\\
Reject when & Graph scale, tooling, or algorithms require a dedicated graph database.\\
Primary failure & Planner, extension version, graph modeling, and SQL-Cypher boundary behavior may lag specialized engines.\\
Compare with & Neo4j; pgvector; Kuzu\\
\end{tabularx}
\paragraph{Minimal use in C / SQL / openCypher.}
\begin{Verbatim}[fontsize=\scriptsize]
CREATE EXTENSION age;
SELECT * FROM cypher('g', $$ MATCH (n) RETURN n $$) AS (n agtype);
\end{Verbatim}
\textbf{Primary sources.} \href{https://age.apache.org/age-manual/master/intro/overview.html}{Apache AGE official documentation}.
\subsubsection{JanusGraph}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries J}}}\quad \badge{Distributed property graph database} \quad \textbf{Languages:} Java / Gremlin
\par\smallskip Graph database layer using Apache TinkerPop and external storage and index backends for very large distributed graphs.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of JanusGraph as a distributed property graph database between graph mutations + gremlin and distributed traversal result; its defining mechanism is janusgraph over storage and index.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Graph mutations + Gremlin};
\node[tech,right=of t0](t1){JanusGraph over storage and index};
\node[tech,right=of t1](t2){Distributed traversal result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Graph mutations + Gremlin -> JanusGraph over storage and index -> Distributed traversal result\\
Control plane & Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.\\
State and trust boundary & Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Graph database layer using Apache TinkerPop and external storage and index backends for very large distributed graphs. Think of JanusGraph as a distributed property graph database between graph mutations + gremlin and distributed traversal result; its defining mechanism is janusgraph over storage and index.
\item[2. Structure and execution] Follow the data path Graph mutations + Gremlin → JanusGraph over storage and index → Distributed traversal result. Query routing selects lexical, dense, sparse, relational, or graph operators before fusion and reranking.
\item[3. State, security, and operations] Canonical source versions, stable ids, ACLs, embedding identity, graph provenance, index configuration, and deletion state must remain traceable.
\item[4. Selection decision] Choose it when a team already operates compatible storage and search backends and needs Gremlin-scale graph traversal. Reject it when operational simplicity, managed service, or Cypher-first tooling is required. Compare the same representative workload with Amazon Neptune, TigerGraph, Neo4j.
\item[5. Production proof] Start with the failure hypothesis: Correctness and performance span the graph layer, storage backend, index backend, and consistency configuration. Evaluate recall and ranking quality by query slice alongside filter selectivity, freshness, update cost, tail latency, recovery, and migration.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted over distributed storage backends\\
Choose when & A team already operates compatible storage and search backends and needs Gremlin-scale graph traversal.\\
Reject when & Operational simplicity, managed service, or Cypher-first tooling is required.\\
Primary failure & Correctness and performance span the graph layer, storage backend, index backend, and consistency configuration.\\
Compare with & Amazon Neptune; TigerGraph; Neo4j\\
\end{tabularx}
\paragraph{Minimal use in Java / Gremlin.}
\begin{Verbatim}[fontsize=\scriptsize]
graph = JanusGraphFactory.open('conf/janusgraph-cql.properties')
g.V().limit(10)
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.janusgraph.org/}{JanusGraph official documentation}.
\clearpage\subsection{05. Observability and evaluation}
\textbf{Selection question.} Can a regression be localized, not merely noticed?
\begin{center}\begin{tikzpicture}[tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]
\node[tech](c0){Runs + expected behavior};
\node[tech,right=of c0](c1){Trace, score, slice, compare};
\node[tech,right=of c1](c2){Release and incident evidence};
\draw[arr](c0)--(c1);\draw[arr](c1)--(c2);
\end{tikzpicture}\end{center}
\paragraph{Comparison matrix.}
\begin{longtable}{@{}p{20mm}p{22mm}p{26mm}p{46mm}p{46mm}@{}}
\toprule\textbf{Tool} & \textbf{Kind} & \textbf{Language / boundary} & \textbf{Choose when} & \textbf{Reject when}\\\midrule\endhead
LangSmith & LLM operations platform & Python, TypeScript, REST; Managed SaaS and hybrid options & Deep LangChain or LangGraph integration and one platform for debugging, evaluation, and deployment are valuable. & Vendor-neutral telemetry ownership or fully self-hosted control is mandatory.\\
Langfuse & Open-source LLM engineering platform & TypeScript, Python SDKs; Self-hosted or Langfuse Cloud & Open-source control, self-hosting, and product analytics around prompts and traces are important. & You need a broader traditional APM suite or a tightly integrated managed agent deployment platform.\\
Arize Phoenix & Open-source AI observability & Python, OpenTelemetry; Self-hosted or Arize AX & You need open-source local debugging, retrieval analysis, and standards-based traces. & A proxy-only cost dashboard is sufficient or existing APM already covers the required semantics.\\
Helicone & LLM gateway and observability platform & TypeScript, Python, HTTP; Cloud or self-hosted gateway & A drop-in gateway can instrument many provider calls with minimal application changes. & You cannot route sensitive traffic through a gateway or need deep internal agent-span semantics.\\
OpenTelemetry & Telemetry standard and SDKs & Many languages; Vendor-neutral libraries, collectors, and backends & Telemetry must remain backend-portable and correlate AI spans with the rest of a distributed system. & You expect an out-of-box LLM product dashboard without defining conventions, sampling, and a backend.\\
MLflow & ML and GenAI lifecycle platform & Python, Java, R, REST; Self-hosted or managed platforms & Traditional ML artifacts and GenAI traces or evaluations need one open lifecycle record. & You only need a lightweight LLM proxy or specialized retrieval-debugging UI.\\
Weights \& Biases Weave & AI tracing and evaluation platform & Python, TypeScript; Managed W\&B with SDK instrumentation & Teams already use W\&B and want code-centric LLM evaluation tied to broader experiments. & Self-hosting or backend-neutral OpenTelemetry is a non-negotiable requirement.\\
Braintrust & Evaluation and observability platform & TypeScript, Python; Managed platform with data-plane options & Evaluation-driven development and side-by-side experiment analysis are central to the team's process. & A fully self-hosted general APM system or pure open standards are required.\\
Ragas & RAG and agent evaluation library & Python; Library; runs locally or with platforms & You want programmable RAG-specific metrics and can calibrate model-based evaluators to the domain. & You need tracing infrastructure, model registry, or product analytics rather than an evaluation library.\\
DeepEval & LLM testing framework & Python; Library, CLI, and Confident AI platform & AI assertions should run like tests in CI with custom and built-in evaluators. & You mainly need production tracing or a language-neutral telemetry standard.\\
TruLens & Evaluation and tracking library & Python; Open-source library and platform integrations & You want inspectable feedback functions and RAG evaluation close to a Python application. & A full lifecycle registry, managed gateway, or language-neutral tracing standard is the main need.\\
Evidently & ML and AI evaluation library & Python; Library, self-hosted service, or cloud & One evaluation layer should cover data drift, ML quality, and LLM outputs with batch or monitoring workflows. & Deep trace-level debugging or a gateway is the only requirement.\\
Promptfoo & Prompt and model testing CLI & TypeScript, YAML; Local CLI and CI; optional cloud & Teams need reproducible local or CI comparisons across prompts, models, and attack cases. & Rich production traces, experiment lineage, or a model registry is the primary need.\\
Opik & Open-source LLM evaluation and tracing platform & Python, TypeScript; Self-hosted or Comet cloud & You want an open-source combined evaluation and observability stack with self-hosting. & A framework-native managed tool or generic OpenTelemetry backend is the standard.\\
AgentOps & Agent observability platform & Python, JavaScript; SDK with managed service & A team wants quick instrumentation focused on agent runs and tool interactions. & Data cannot leave the boundary or an existing OTel-based backend already provides the required semantics.\\
Parea & LLM evaluation and observability platform & Python, TypeScript; Managed platform and SDKs & Product teams need iterative offline and online evaluation tied to prompt and trace versions. & Self-hosting or an existing experiment platform is mandatory.\\
HoneyHive & AI evaluation and observability platform & Python, TypeScript; Managed platform and SDKs & Teams need a collaborative evaluation workflow spanning development and production feedback. & An open-source self-hosted stack or generic telemetry pipeline is required.\\
Galileo & AI evaluation, monitoring, and guardrails platform & Python, REST; Managed enterprise platform & Enterprise teams want managed quality evaluation and production monitoring with packaged metrics. & Full self-hosting, minimal vendor surface, or custom evaluation code is preferred.\\
Giskard & AI testing and evaluation framework & Python; Open-source library and enterprise platform & Teams need structured quality and security testing beyond ad hoc prompt examples. & Low-level tracing or a simple deterministic assertion suite is the primary requirement.\\
Inspect AI & Model evaluation framework & Python; Local or distributed evaluation runner & You need code-defined, auditable model evaluations with agent tools and sandboxed tasks. & Production tracing, prompt management, or a no-code stakeholder UI is the main need.\\
Maxim AI & AI evaluation and observability platform & Python, TypeScript; Managed platform and SDKs & A product team wants one managed workflow for agent simulation, evaluation, and observability. & Self-hosting or a small code-only evaluator is required.\\
\bottomrule\end{longtable}
\subsubsection{LangSmith}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{LLM operations platform} \quad \textbf{Languages:} Python / TypeScript / REST
\par\smallskip Tracing, datasets, experiments, evaluators, prompt management, annotation, deployment, and an LLM gateway around agent applications.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of LangSmith as a llm operations platform between application runs and experiment and release evidence; its defining mechanism is trace + dataset + evaluator.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Application runs};
\node[tech,right=of t0](t1){Trace + dataset + evaluator};
\node[tech,right=of t1](t2){Experiment and release evidence};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Application runs -> Trace + dataset + evaluator -> Experiment and release evidence\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Tracing, datasets, experiments, evaluators, prompt management, annotation, deployment, and an LLM gateway around agent applications. Think of LangSmith as a llm operations platform between application runs and experiment and release evidence; its defining mechanism is trace + dataset + evaluator.
\item[2. Structure and execution] Follow the data path Application runs → Trace + dataset + evaluator → Experiment and release evidence. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when deep LangChain or LangGraph integration and one platform for debugging, evaluation, and deployment are valuable. Reject it when vendor-neutral telemetry ownership or fully self-hosted control is mandatory. Compare the same representative workload with Langfuse, Braintrust, Arize Phoenix.
\item[5. Production proof] Start with the failure hypothesis: UI-only annotations and hidden evaluators make release evidence impossible to reproduce outside the platform. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed SaaS and hybrid options\\
Choose when & Deep LangChain or LangGraph integration and one platform for debugging, evaluation, and deployment are valuable.\\
Reject when & Vendor-neutral telemetry ownership or fully self-hosted control is mandatory.\\
Primary failure & UI-only annotations and hidden evaluators make release evidence impossible to reproduce outside the platform.\\
Compare with & Langfuse; Braintrust; Arize Phoenix\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install langsmith
LANGSMITH_TRACING=true python app.py
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.langchain.com/langsmith/home}{LangSmith official documentation} \quad \href{https://docs.langchain.com/langsmith/observability}{LangSmith observability} \quad \href{https://docs.langchain.com/langsmith/evaluation}{LangSmith evaluation}.
\subsubsection{Langfuse}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Open-source LLM engineering platform} \quad \textbf{Languages:} TypeScript / Python SDKs
\par\smallskip Traces, prompt management, scores, datasets, experiments, cost, sessions, and OpenTelemetry-oriented integrations.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Langfuse as a open-source llm engineering platform between llm calls + spans and debug and experiment views; its defining mechanism is trace, score, aggregate.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){LLM calls + spans};
\node[tech,right=of t0](t1){Trace, score, aggregate};
\node[tech,right=of t1](t2){Debug and experiment views};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & LLM calls + spans -> Trace, score, aggregate -> Debug and experiment views\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Traces, prompt management, scores, datasets, experiments, cost, sessions, and OpenTelemetry-oriented integrations. Think of Langfuse as a open-source llm engineering platform between llm calls + spans and debug and experiment views; its defining mechanism is trace, score, aggregate.
\item[2. Structure and execution] Follow the data path LLM calls + spans → Trace, score, aggregate → Debug and experiment views. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when open-source control, self-hosting, and product analytics around prompts and traces are important. Reject it when you need a broader traditional APM suite or a tightly integrated managed agent deployment platform. Compare the same representative workload with LangSmith, Phoenix, Helicone.
\item[5. Production proof] Start with the failure hypothesis: Capturing raw prompts without redaction, retention, and tenant isolation can create a sensitive shadow datastore. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Langfuse Cloud\\
Choose when & Open-source control, self-hosting, and product analytics around prompts and traces are important.\\
Reject when & You need a broader traditional APM suite or a tightly integrated managed agent deployment platform.\\
Primary failure & Capturing raw prompts without redaction, retention, and tenant isolation can create a sensitive shadow datastore.\\
Compare with & LangSmith; Phoenix; Helicone\\
\end{tabularx}
\paragraph{Minimal use in TypeScript / Python SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install langfuse
from langfuse import Langfuse
\end{Verbatim}
\textbf{Primary sources.} \href{https://langfuse.com/docs}{Langfuse official documentation}.
\subsubsection{Arize Phoenix}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries AP}}}\quad \badge{Open-source AI observability} \quad \textbf{Languages:} Python / OpenTelemetry
\par\smallskip OpenTelemetry-based tracing, evaluation, datasets, experiments, and embedding or retrieval analysis.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Arize Phoenix as a open-source ai observability between spans + eval data and root-cause views; its defining mechanism is trace and evaluate.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Spans + eval data};
\node[tech,right=of t0](t1){Trace and evaluate};
\node[tech,right=of t1](t2){Root-cause views};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Spans + eval data -> Trace and evaluate -> Root-cause views\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] OpenTelemetry-based tracing, evaluation, datasets, experiments, and embedding or retrieval analysis. Think of Arize Phoenix as a open-source ai observability between spans + eval data and root-cause views; its defining mechanism is trace and evaluate.
\item[2. Structure and execution] Follow the data path Spans + eval data → Trace and evaluate → Root-cause views. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when you need open-source local debugging, retrieval analysis, and standards-based traces. Reject it when a proxy-only cost dashboard is sufficient or existing APM already covers the required semantics. Compare the same representative workload with Langfuse, LangSmith, MLflow.
\item[5. Production proof] Start with the failure hypothesis: Aggregate judge scores can look healthy while important retrieval slices or trace spans regress. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Arize AX\\
Choose when & You need open-source local debugging, retrieval analysis, and standards-based traces.\\
Reject when & A proxy-only cost dashboard is sufficient or existing APM already covers the required semantics.\\
Primary failure & Aggregate judge scores can look healthy while important retrieval slices or trace spans regress.\\
Compare with & Langfuse; LangSmith; MLflow\\
\end{tabularx}
\paragraph{Minimal use in Python / OpenTelemetry.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install arize-phoenix
phoenix serve
\end{Verbatim}
\textbf{Primary sources.} \href{https://arize.com/docs/phoenix}{Arize Phoenix official documentation}.
\subsubsection{Helicone}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries H}}}\quad \badge{LLM gateway and observability platform} \quad \textbf{Languages:} TypeScript / Python / HTTP
\par\smallskip Proxy-based request logging, cost and latency analytics, caching, rate limits, sessions, experiments, and provider routing.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Helicone as a llm gateway and observability platform between llm http traffic and cost and latency analytics; its defining mechanism is gateway logging + policy.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){LLM HTTP traffic};
\node[tech,right=of t0](t1){Gateway logging + policy};
\node[tech,right=of t1](t2){Cost and latency analytics};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & LLM HTTP traffic -> Gateway logging + policy -> Cost and latency analytics\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Proxy-based request logging, cost and latency analytics, caching, rate limits, sessions, experiments, and provider routing. Think of Helicone as a llm gateway and observability platform between llm http traffic and cost and latency analytics; its defining mechanism is gateway logging + policy.
\item[2. Structure and execution] Follow the data path LLM HTTP traffic → Gateway logging + policy → Cost and latency analytics. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when a drop-in gateway can instrument many provider calls with minimal application changes. Reject it when you cannot route sensitive traffic through a gateway or need deep internal agent-span semantics. Compare the same representative workload with Langfuse, LiteLLM, LangSmith.
\item[5. Production proof] Start with the failure hypothesis: A proxy sees provider calls but not automatically the business context, retriever decisions, or side effects around them. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Cloud or self-hosted gateway\\
Choose when & A drop-in gateway can instrument many provider calls with minimal application changes.\\
Reject when & You cannot route sensitive traffic through a gateway or need deep internal agent-span semantics.\\
Primary failure & A proxy sees provider calls but not automatically the business context, retriever decisions, or side effects around them.\\
Compare with & Langfuse; LiteLLM; LangSmith\\
\end{tabularx}
\paragraph{Minimal use in TypeScript / Python / HTTP.}
\begin{Verbatim}[fontsize=\scriptsize]
set OPENAI_BASE_URL=https://oai.helicone.ai/v1
set HELICONE_API_KEY=...
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.helicone.ai/}{Helicone official documentation}.
\subsubsection{OpenTelemetry}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries O}}}\quad \badge{Telemetry standard and SDKs} \quad \textbf{Languages:} Many languages
\par\smallskip Common APIs, SDKs, semantic conventions, context propagation, collectors, traces, metrics, and logs.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of OpenTelemetry as a telemetry standard and sdks between application signals and observability backend; its defining mechanism is sdk + collector.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Application signals};
\node[tech,right=of t0](t1){SDK + Collector};
\node[tech,right=of t1](t2){Observability backend};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Application signals -> SDK + Collector -> Observability backend\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Common APIs, SDKs, semantic conventions, context propagation, collectors, traces, metrics, and logs. Think of OpenTelemetry as a telemetry standard and sdks between application signals and observability backend; its defining mechanism is sdk + collector.
\item[2. Structure and execution] Follow the data path Application signals → SDK + Collector → Observability backend. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when telemetry must remain backend-portable and correlate AI spans with the rest of a distributed system. Reject it when you expect an out-of-box LLM product dashboard without defining conventions, sampling, and a backend. Compare the same representative workload with vendor APM, Langfuse, Phoenix.
\item[5. Production proof] Start with the failure hypothesis: High-cardinality prompt data in metrics or unredacted spans causes cost, privacy, and operability failures. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Vendor-neutral libraries, collectors, and backends\\
Choose when & Telemetry must remain backend-portable and correlate AI spans with the rest of a distributed system.\\
Reject when & You expect an out-of-box LLM product dashboard without defining conventions, sampling, and a backend.\\
Primary failure & High-cardinality prompt data in metrics or unredacted spans causes cost, privacy, and operability failures.\\
Compare with & vendor APM; Langfuse; Phoenix\\
\end{tabularx}
\paragraph{Minimal use in Many languages.}
\begin{Verbatim}[fontsize=\scriptsize]
opentelemetry-instrument python app.py
otelcol --config config.yaml
\end{Verbatim}
\textbf{Primary sources.} \href{https://opentelemetry.io/docs/}{OpenTelemetry official documentation}.
\subsubsection{MLflow}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries M}}}\quad \badge{ML and GenAI lifecycle platform} \quad \textbf{Languages:} Python / Java / R / REST
\par\smallskip Experiment tracking, model registry, packaging, evaluation, tracing, prompt registry, and deployment integrations.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of MLflow as a ml and genai lifecycle platform between runs + artifacts and versioned release evidence; its defining mechanism is track, evaluate, register.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Runs + artifacts};
\node[tech,right=of t0](t1){Track, evaluate, register};
\node[tech,right=of t1](t2){Versioned release evidence};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Runs + artifacts -> Track, evaluate, register -> Versioned release evidence\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Experiment tracking, model registry, packaging, evaluation, tracing, prompt registry, and deployment integrations. Think of MLflow as a ml and genai lifecycle platform between runs + artifacts and versioned release evidence; its defining mechanism is track, evaluate, register.
\item[2. Structure and execution] Follow the data path Runs + artifacts → Track, evaluate, register → Versioned release evidence. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when traditional ML artifacts and GenAI traces or evaluations need one open lifecycle record. Reject it when you only need a lightweight LLM proxy or specialized retrieval-debugging UI. Compare the same representative workload with Weights \& Biases, LangSmith, Phoenix.
\item[5. Production proof] Start with the failure hypothesis: Registering a model without immutable data, environment, prompt, and evaluator lineage does not make a release reproducible. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or managed platforms\\
Choose when & Traditional ML artifacts and GenAI traces or evaluations need one open lifecycle record.\\
Reject when & You only need a lightweight LLM proxy or specialized retrieval-debugging UI.\\
Primary failure & Registering a model without immutable data, environment, prompt, and evaluator lineage does not make a release reproducible.\\
Compare with & Weights \& Biases; LangSmith; Phoenix\\
\end{tabularx}
\paragraph{Minimal use in Python / Java / R / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install mlflow
mlflow server --host 0.0.0.0
\end{Verbatim}
\textbf{Primary sources.} \href{https://mlflow.org/docs/latest/genai/}{MLflow official documentation}.
\subsubsection{Weights \& Biases Weave}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries W\&B}}}\quad \badge{AI tracing and evaluation platform} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Tracing, evaluations, datasets, scorers, object versioning, and dashboards integrated with W\&B experiments.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Weights \& Biases Weave as a ai tracing and evaluation platform between calls + dataset and evaluation dashboard; its defining mechanism is trace + score + version.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Calls + dataset};
\node[tech,right=of t0](t1){Trace + score + version};
\node[tech,right=of t1](t2){Evaluation dashboard};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Calls + dataset -> Trace + score + version -> Evaluation dashboard\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Tracing, evaluations, datasets, scorers, object versioning, and dashboards integrated with W\&B experiments. Think of Weights \& Biases Weave as a ai tracing and evaluation platform between calls + dataset and evaluation dashboard; its defining mechanism is trace + score + version.
\item[2. Structure and execution] Follow the data path Calls + dataset → Trace + score + version → Evaluation dashboard. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when teams already use W\&B and want code-centric LLM evaluation tied to broader experiments. Reject it when self-hosting or backend-neutral OpenTelemetry is a non-negotiable requirement. Compare the same representative workload with MLflow, LangSmith, Braintrust.
\item[5. Production proof] Start with the failure hypothesis: Unversioned scorer code or silently changed datasets can make experiment comparisons meaningless. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed W\&B with SDK instrumentation\\
Choose when & Teams already use W\&B and want code-centric LLM evaluation tied to broader experiments.\\
Reject when & Self-hosting or backend-neutral OpenTelemetry is a non-negotiable requirement.\\
Primary failure & Unversioned scorer code or silently changed datasets can make experiment comparisons meaningless.\\
Compare with & MLflow; LangSmith; Braintrust\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install weave
weave.init('project')
\end{Verbatim}
\textbf{Primary sources.} \href{https://weave-docs.wandb.ai/}{Weights \& Biases Weave official documentation}.
\subsubsection{Braintrust}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries B}}}\quad \badge{Evaluation and observability platform} \quad \textbf{Languages:} TypeScript / Python
\par\smallskip Datasets, experiments, scorers, tracing, prompt playgrounds, production logs, and evaluation workflows.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Braintrust as a evaluation and observability platform between dataset + candidates and comparable cases and scores; its defining mechanism is run experiments + scorers.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Dataset + candidates};
\node[tech,right=of t0](t1){Run experiments + scorers};
\node[tech,right=of t1](t2){Comparable cases and scores};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Dataset + candidates -> Run experiments + scorers -> Comparable cases and scores\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Datasets, experiments, scorers, tracing, prompt playgrounds, production logs, and evaluation workflows. Think of Braintrust as a evaluation and observability platform between dataset + candidates and comparable cases and scores; its defining mechanism is run experiments + scorers.
\item[2. Structure and execution] Follow the data path Dataset + candidates → Run experiments + scorers → Comparable cases and scores. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when evaluation-driven development and side-by-side experiment analysis are central to the team's process. Reject it when a fully self-hosted general APM system or pure open standards are required. Compare the same representative workload with LangSmith, Weave, Langfuse.
\item[5. Production proof] Start with the failure hypothesis: A polished aggregate score can encourage overfitting unless cases, slices, and scorer disagreements stay visible. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed platform with data-plane options\\
Choose when & Evaluation-driven development and side-by-side experiment analysis are central to the team's process.\\
Reject when & A fully self-hosted general APM system or pure open standards are required.\\
Primary failure & A polished aggregate score can encourage overfitting unless cases, slices, and scorer disagreements stay visible.\\
Compare with & LangSmith; Weave; Langfuse\\
\end{tabularx}
\paragraph{Minimal use in TypeScript / Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install braintrust
from braintrust import Eval
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.braintrust.dev/docs}{Braintrust official documentation}.
\subsubsection{Ragas}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries R}}}\quad \badge{RAG and agent evaluation library} \quad \textbf{Languages:} Python
\par\smallskip Metrics, test generation, experiments, and evaluation workflows focused on RAG and agent systems.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Ragas as a rag and agent evaluation library between questions + contexts + answers and per-case scores; its defining mechanism is rag metrics.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Questions + contexts + answers};
\node[tech,right=of t0](t1){RAG metrics};
\node[tech,right=of t1](t2){Per-case scores};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Questions + contexts + answers -> RAG metrics -> Per-case scores\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Metrics, test generation, experiments, and evaluation workflows focused on RAG and agent systems. Think of Ragas as a rag and agent evaluation library between questions + contexts + answers and per-case scores; its defining mechanism is rag metrics.
\item[2. Structure and execution] Follow the data path Questions + contexts + answers → RAG metrics → Per-case scores. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when you want programmable RAG-specific metrics and can calibrate model-based evaluators to the domain. Reject it when you need tracing infrastructure, model registry, or product analytics rather than an evaluation library. Compare the same representative workload with DeepEval, TruLens, Phoenix.
\item[5. Production proof] Start with the failure hypothesis: Using faithfulness or context metrics without human calibration can turn judge bias into a release gate. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; runs locally or with platforms\\
Choose when & You want programmable RAG-specific metrics and can calibrate model-based evaluators to the domain.\\
Reject when & You need tracing infrastructure, model registry, or product analytics rather than an evaluation library.\\
Primary failure & Using faithfulness or context metrics without human calibration can turn judge bias into a release gate.\\
Compare with & DeepEval; TruLens; Phoenix\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install ragas
from ragas import evaluate
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.ragas.io/}{Ragas official documentation}.
\subsubsection{DeepEval}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries D}}}\quad \badge{LLM testing framework} \quad \textbf{Languages:} Python
\par\smallskip Pytest-style LLM tests, metrics, synthetic data, red teaming, component evaluation, and CI integration.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of DeepEval as a llm testing framework between test cases + metrics and pass, fail, diagnostics; its defining mechanism is run ai assertions.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Test cases + metrics};
\node[tech,right=of t0](t1){Run AI assertions};
\node[tech,right=of t1](t2){Pass, fail, diagnostics};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Test cases + metrics -> Run AI assertions -> Pass, fail, diagnostics\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Pytest-style LLM tests, metrics, synthetic data, red teaming, component evaluation, and CI integration. Think of DeepEval as a llm testing framework between test cases + metrics and pass, fail, diagnostics; its defining mechanism is run ai assertions.
\item[2. Structure and execution] Follow the data path Test cases + metrics → Run AI assertions → Pass, fail, diagnostics. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when aI assertions should run like tests in CI with custom and built-in evaluators. Reject it when you mainly need production tracing or a language-neutral telemetry standard. Compare the same representative workload with Ragas, Promptfoo, Giskard.
\item[5. Production proof] Start with the failure hypothesis: Nondeterministic judge tests without thresholds, caching, and retry classification create flaky CI. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library, CLI, and Confident AI platform\\
Choose when & AI assertions should run like tests in CI with custom and built-in evaluators.\\
Reject when & You mainly need production tracing or a language-neutral telemetry standard.\\
Primary failure & Nondeterministic judge tests without thresholds, caching, and retry classification create flaky CI.\\
Compare with & Ragas; Promptfoo; Giskard\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install deepeval
deepeval test run test_app.py
\end{Verbatim}
\textbf{Primary sources.} \href{https://deepeval.com/docs/}{DeepEval official documentation}.
\subsubsection{TruLens}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries T}}}\quad \badge{Evaluation and tracking library} \quad \textbf{Languages:} Python
\par\smallskip Feedback functions, RAG triad evaluation, instrumentation, records, dashboards, and groundedness analysis.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of TruLens as a evaluation and tracking library between app records + feedback and rag diagnostics; its defining mechanism is instrument + evaluate.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){App records + feedback};
\node[tech,right=of t0](t1){Instrument + evaluate};
\node[tech,right=of t1](t2){RAG diagnostics};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & App records + feedback -> Instrument + evaluate -> RAG diagnostics\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Feedback functions, RAG triad evaluation, instrumentation, records, dashboards, and groundedness analysis. Think of TruLens as a evaluation and tracking library between app records + feedback and rag diagnostics; its defining mechanism is instrument + evaluate.
\item[2. Structure and execution] Follow the data path App records + feedback → Instrument + evaluate → RAG diagnostics. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when you want inspectable feedback functions and RAG evaluation close to a Python application. Reject it when a full lifecycle registry, managed gateway, or language-neutral tracing standard is the main need. Compare the same representative workload with Ragas, DeepEval, Phoenix.
\item[5. Production proof] Start with the failure hypothesis: A feedback function is still a model or heuristic with failure modes; its score needs calibration and provenance. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Open-source library and platform integrations\\
Choose when & You want inspectable feedback functions and RAG evaluation close to a Python application.\\
Reject when & A full lifecycle registry, managed gateway, or language-neutral tracing standard is the main need.\\
Primary failure & A feedback function is still a model or heuristic with failure modes; its score needs calibration and provenance.\\
Compare with & Ragas; DeepEval; Phoenix\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install trulens
from trulens.apps.custom import TruCustomApp
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.trulens.org/}{TruLens official documentation}.
\subsubsection{Evidently}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries E}}}\quad \badge{ML and AI evaluation library} \quad \textbf{Languages:} Python
\par\smallskip Test suites, reports, dashboards, monitoring, and evaluation for tabular ML, text, and LLM systems.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Evidently as a ml and ai evaluation library between reference + current data and quality or drift decision; its defining mechanism is tests and reports.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Reference + current data};
\node[tech,right=of t0](t1){Tests and reports};
\node[tech,right=of t1](t2){Quality or drift decision};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Reference + current data -> Tests and reports -> Quality or drift decision\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Test suites, reports, dashboards, monitoring, and evaluation for tabular ML, text, and LLM systems. Think of Evidently as a ml and ai evaluation library between reference + current data and quality or drift decision; its defining mechanism is tests and reports.
\item[2. Structure and execution] Follow the data path Reference + current data → Tests and reports → Quality or drift decision. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when one evaluation layer should cover data drift, ML quality, and LLM outputs with batch or monitoring workflows. Reject it when deep trace-level debugging or a gateway is the only requirement. Compare the same representative workload with MLflow, Phoenix, WhyLabs.
\item[5. Production proof] Start with the failure hypothesis: Drift alerts without cohort, label-delay, and action thresholds create noise rather than diagnosis. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library, self-hosted service, or cloud\\
Choose when & One evaluation layer should cover data drift, ML quality, and LLM outputs with batch or monitoring workflows.\\
Reject when & Deep trace-level debugging or a gateway is the only requirement.\\
Primary failure & Drift alerts without cohort, label-delay, and action thresholds create noise rather than diagnosis.\\
Compare with & MLflow; Phoenix; WhyLabs\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install evidently
from evidently import Report
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.evidentlyai.com/}{Evidently official documentation}.
\subsubsection{Promptfoo}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries P}}}\quad \badge{Prompt and model testing CLI} \quad \textbf{Languages:} TypeScript / YAML
\par\smallskip Configuration-driven prompt/model matrices, assertions, red teaming, caching, and CI reports.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Promptfoo as a prompt and model testing cli between prompts + providers + assertions and diff and gate; its defining mechanism is evaluation matrix.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Prompts + providers + assertions};
\node[tech,right=of t0](t1){Evaluation matrix};
\node[tech,right=of t1](t2){Diff and gate};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Prompts + providers + assertions -> Evaluation matrix -> Diff and gate\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Configuration-driven prompt/model matrices, assertions, red teaming, caching, and CI reports. Think of Promptfoo as a prompt and model testing cli between prompts + providers + assertions and diff and gate; its defining mechanism is evaluation matrix.
\item[2. Structure and execution] Follow the data path Prompts + providers + assertions → Evaluation matrix → Diff and gate. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when teams need reproducible local or CI comparisons across prompts, models, and attack cases. Reject it when rich production traces, experiment lineage, or a model registry is the primary need. Compare the same representative workload with DeepEval, Braintrust, Giskard.
\item[5. Production proof] Start with the failure hypothesis: A broad red-team template set can create false confidence if threat models and application tools are not represented. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local CLI and CI; optional cloud\\
Choose when & Teams need reproducible local or CI comparisons across prompts, models, and attack cases.\\
Reject when & Rich production traces, experiment lineage, or a model registry is the primary need.\\
Primary failure & A broad red-team template set can create false confidence if threat models and application tools are not represented.\\
Compare with & DeepEval; Braintrust; Giskard\\
\end{tabularx}
\paragraph{Minimal use in TypeScript / YAML.}
\begin{Verbatim}[fontsize=\scriptsize]
npx promptfoo eval
promptfoo view
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.promptfoo.dev/docs/}{Promptfoo official documentation}.
\subsubsection{Opik}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries O}}}\quad \badge{Open-source LLM evaluation and tracing platform} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Tracing, datasets, experiments, prompt management, LLM-as-judge metrics, online evaluation, and production monitoring.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Opik as a open-source llm evaluation and tracing platform between application traces + datasets and regression and incident evidence; its defining mechanism is experiment and online scoring.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Application traces + datasets};
\node[tech,right=of t0](t1){Experiment and online scoring};
\node[tech,right=of t1](t2){Regression and incident evidence};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Application traces + datasets -> Experiment and online scoring -> Regression and incident evidence\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Tracing, datasets, experiments, prompt management, LLM-as-judge metrics, online evaluation, and production monitoring. Think of Opik as a open-source llm evaluation and tracing platform between application traces + datasets and regression and incident evidence; its defining mechanism is experiment and online scoring.
\item[2. Structure and execution] Follow the data path Application traces + datasets → Experiment and online scoring → Regression and incident evidence. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when you want an open-source combined evaluation and observability stack with self-hosting. Reject it when a framework-native managed tool or generic OpenTelemetry backend is the standard. Compare the same representative workload with Langfuse, LangSmith, Phoenix.
\item[5. Production proof] Start with the failure hypothesis: Judge and trace coverage can look complete while business outcomes and tool side effects remain unobserved. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted or Comet cloud\\
Choose when & You want an open-source combined evaluation and observability stack with self-hosting.\\
Reject when & A framework-native managed tool or generic OpenTelemetry backend is the standard.\\
Primary failure & Judge and trace coverage can look complete while business outcomes and tool side effects remain unobserved.\\
Compare with & Langfuse; LangSmith; Phoenix\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install opik
from opik import track
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.comet.com/docs/opik/}{Opik official documentation}.
\subsubsection{AgentOps}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries A}}}\quad \badge{Agent observability platform} \quad \textbf{Languages:} Python / JavaScript
\par\smallskip Agent-oriented sessions, traces, tool and LLM events, cost tracking, replay, and monitoring integrations.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of AgentOps as a agent observability platform between agent events + session and run timeline + metrics; its defining mechanism is instrumentation and trace backend.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Agent events + session};
\node[tech,right=of t0](t1){Instrumentation and trace backend};
\node[tech,right=of t1](t2){Run timeline + metrics};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Agent events + session -> Instrumentation and trace backend -> Run timeline + metrics\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Agent-oriented sessions, traces, tool and LLM events, cost tracking, replay, and monitoring integrations. Think of AgentOps as a agent observability platform between agent events + session and run timeline + metrics; its defining mechanism is instrumentation and trace backend.
\item[2. Structure and execution] Follow the data path Agent events + session → Instrumentation and trace backend → Run timeline + metrics. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when a team wants quick instrumentation focused on agent runs and tool interactions. Reject it when data cannot leave the boundary or an existing OTel-based backend already provides the required semantics. Compare the same representative workload with LangSmith, Langfuse, Opik.
\item[5. Production proof] Start with the failure hypothesis: SDK auto-instrumentation can miss custom state changes and external side effects or collect sensitive arguments. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & SDK with managed service\\
Choose when & A team wants quick instrumentation focused on agent runs and tool interactions.\\
Reject when & Data cannot leave the boundary or an existing OTel-based backend already provides the required semantics.\\
Primary failure & SDK auto-instrumentation can miss custom state changes and external side effects or collect sensitive arguments.\\
Compare with & LangSmith; Langfuse; Opik\\
\end{tabularx}
\paragraph{Minimal use in Python / JavaScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install agentops
agentops.init()
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.agentops.ai/}{AgentOps official documentation}.
\subsubsection{Parea}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries P}}}\quad \badge{LLM evaluation and observability platform} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Experiment tracking, traces, datasets, prompt management, evaluators, human feedback, and production monitoring.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Parea as a llm evaluation and observability platform between dataset + application trace and version comparison and monitor; its defining mechanism is experiment + evaluators.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Dataset + application trace};
\node[tech,right=of t0](t1){Experiment + evaluators};
\node[tech,right=of t1](t2){Version comparison and monitor};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Dataset + application trace -> Experiment + evaluators -> Version comparison and monitor\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Experiment tracking, traces, datasets, prompt management, evaluators, human feedback, and production monitoring. Think of Parea as a llm evaluation and observability platform between dataset + application trace and version comparison and monitor; its defining mechanism is experiment + evaluators.
\item[2. Structure and execution] Follow the data path Dataset + application trace → Experiment + evaluators → Version comparison and monitor. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when product teams need iterative offline and online evaluation tied to prompt and trace versions. Reject it when self-hosting or an existing experiment platform is mandatory. Compare the same representative workload with Braintrust, LangSmith, HoneyHive.
\item[5. Production proof] Start with the failure hypothesis: Aggregate scores can hide slice regressions and evaluator-model changes unless cases and judges are versioned. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed platform and SDKs\\
Choose when & Product teams need iterative offline and online evaluation tied to prompt and trace versions.\\
Reject when & Self-hosting or an existing experiment platform is mandatory.\\
Primary failure & Aggregate scores can hide slice regressions and evaluator-model changes unless cases and judges are versioned.\\
Compare with & Braintrust; LangSmith; HoneyHive\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install parea-ai
from parea import Parea
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.parea.ai/}{Parea official documentation}.
\subsubsection{HoneyHive}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries H}}}\quad \badge{AI evaluation and observability platform} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Traces, datasets, prompt and model experiments, evaluators, feedback, and production monitoring for AI applications.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of HoneyHive as a ai evaluation and observability platform between runs + datasets and release and monitoring evidence; its defining mechanism is trace, evaluate, compare.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Runs + datasets};
\node[tech,right=of t0](t1){Trace, evaluate, compare};
\node[tech,right=of t1](t2){Release and monitoring evidence};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Runs + datasets -> Trace, evaluate, compare -> Release and monitoring evidence\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Traces, datasets, prompt and model experiments, evaluators, feedback, and production monitoring for AI applications. Think of HoneyHive as a ai evaluation and observability platform between runs + datasets and release and monitoring evidence; its defining mechanism is trace, evaluate, compare.
\item[2. Structure and execution] Follow the data path Runs + datasets → Trace, evaluate, compare → Release and monitoring evidence. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when teams need a collaborative evaluation workflow spanning development and production feedback. Reject it when an open-source self-hosted stack or generic telemetry pipeline is required. Compare the same representative workload with Braintrust, Parea, LangSmith.
\item[5. Production proof] Start with the failure hypothesis: Changing evaluators, samples, or trace schemas can invalidate trend lines without explicit versioning. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed platform and SDKs\\
Choose when & Teams need a collaborative evaluation workflow spanning development and production feedback.\\
Reject when & An open-source self-hosted stack or generic telemetry pipeline is required.\\
Primary failure & Changing evaluators, samples, or trace schemas can invalidate trend lines without explicit versioning.\\
Compare with & Braintrust; Parea; LangSmith\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install honeyhive
from honeyhive import HoneyHive
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.honeyhive.ai/}{HoneyHive official documentation}.
\subsubsection{Galileo}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries G}}}\quad \badge{AI evaluation, monitoring, and guardrails platform} \quad \textbf{Languages:} Python / REST
\par\smallskip Evaluation and observability platform with trace analysis, quality metrics, experiments, monitoring, and runtime guardrail signals.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Galileo as a ai evaluation, monitoring, and guardrails platform between application runs + labels and alerts and release evidence; its defining mechanism is quality metrics + monitor.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Application runs + labels};
\node[tech,right=of t0](t1){Quality metrics + monitor};
\node[tech,right=of t1](t2){Alerts and release evidence};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Application runs + labels -> Quality metrics + monitor -> Alerts and release evidence\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Evaluation and observability platform with trace analysis, quality metrics, experiments, monitoring, and runtime guardrail signals. Think of Galileo as a ai evaluation, monitoring, and guardrails platform between application runs + labels and alerts and release evidence; its defining mechanism is quality metrics + monitor.
\item[2. Structure and execution] Follow the data path Application runs + labels → Quality metrics + monitor → Alerts and release evidence. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when enterprise teams want managed quality evaluation and production monitoring with packaged metrics. Reject it when full self-hosting, minimal vendor surface, or custom evaluation code is preferred. Compare the same representative workload with Arize Phoenix, Fiddler, Opik.
\item[5. Production proof] Start with the failure hypothesis: Packaged metrics must be calibrated against domain labels and can drift with model or evaluator changes. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed enterprise platform\\
Choose when & Enterprise teams want managed quality evaluation and production monitoring with packaged metrics.\\
Reject when & Full self-hosting, minimal vendor surface, or custom evaluation code is preferred.\\
Primary failure & Packaged metrics must be calibrated against domain labels and can drift with model or evaluator changes.\\
Compare with & Arize Phoenix; Fiddler; Opik\\
\end{tabularx}
\paragraph{Minimal use in Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install promptquality
from promptquality import Scorers
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.galileo.ai/}{Galileo official documentation}.
\subsubsection{Giskard}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries G}}}\quad \badge{AI testing and evaluation framework} \quad \textbf{Languages:} Python
\par\smallskip Testing and scanning for ML and LLM applications, RAG evaluation, vulnerability discovery, datasets, and test suites.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Giskard as a ai testing and evaluation framework between model app + dataset and test suite + findings; its defining mechanism is scan, generate tests, evaluate.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model app + dataset};
\node[tech,right=of t0](t1){Scan, generate tests, evaluate};
\node[tech,right=of t1](t2){Test suite + findings};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model app + dataset -> Scan, generate tests, evaluate -> Test suite + findings\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Testing and scanning for ML and LLM applications, RAG evaluation, vulnerability discovery, datasets, and test suites. Think of Giskard as a ai testing and evaluation framework between model app + dataset and test suite + findings; its defining mechanism is scan, generate tests, evaluate.
\item[2. Structure and execution] Follow the data path Model app + dataset → Scan, generate tests, evaluate → Test suite + findings. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when teams need structured quality and security testing beyond ad hoc prompt examples. Reject it when low-level tracing or a simple deterministic assertion suite is the primary requirement. Compare the same representative workload with DeepEval, Promptfoo, garak.
\item[5. Production proof] Start with the failure hypothesis: Automated probes are only as representative as the application wrapper, datasets, and threat model they exercise. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Open-source library and enterprise platform\\
Choose when & Teams need structured quality and security testing beyond ad hoc prompt examples.\\
Reject when & Low-level tracing or a simple deterministic assertion suite is the primary requirement.\\
Primary failure & Automated probes are only as representative as the application wrapper, datasets, and threat model they exercise.\\
Compare with & DeepEval; Promptfoo; garak\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install 'giskard[llm]'
import giskard
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.giskard.ai/}{Giskard official documentation}.
\subsubsection{Inspect AI}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries IA}}}\quad \badge{Model evaluation framework} \quad \textbf{Languages:} Python
\par\smallskip UK AI Security Institute framework for reproducible evaluations using datasets, solvers, tools, sandboxes, scorers, logs, and task registries.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Inspect AI as a model evaluation framework between dataset + solver + tools and scored log + artifacts; its defining mechanism is evaluation task in sandbox.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Dataset + solver + tools};
\node[tech,right=of t0](t1){Evaluation task in sandbox};
\node[tech,right=of t1](t2){Scored log + artifacts};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Dataset + solver + tools -> Evaluation task in sandbox -> Scored log + artifacts\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] UK AI Security Institute framework for reproducible evaluations using datasets, solvers, tools, sandboxes, scorers, logs, and task registries. Think of Inspect AI as a model evaluation framework between dataset + solver + tools and scored log + artifacts; its defining mechanism is evaluation task in sandbox.
\item[2. Structure and execution] Follow the data path Dataset + solver + tools → Evaluation task in sandbox → Scored log + artifacts. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when you need code-defined, auditable model evaluations with agent tools and sandboxed tasks. Reject it when production tracing, prompt management, or a no-code stakeholder UI is the main need. Compare the same representative workload with lm-evaluation-harness, Promptfoo, DeepEval.
\item[5. Production proof] Start with the failure hypothesis: A reproducible harness can still produce invalid conclusions from contaminated tasks, weak scorers, or unsafe tools. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local or distributed evaluation runner\\
Choose when & You need code-defined, auditable model evaluations with agent tools and sandboxed tasks.\\
Reject when & Production tracing, prompt management, or a no-code stakeholder UI is the main need.\\
Primary failure & A reproducible harness can still produce invalid conclusions from contaminated tasks, weak scorers, or unsafe tools.\\
Compare with & lm-evaluation-harness; Promptfoo; DeepEval\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install inspect-ai
inspect eval task.py
\end{Verbatim}
\textbf{Primary sources.} \href{https://inspect.aisi.org.uk/}{Inspect AI official documentation}.
\subsubsection{Maxim AI}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries MA}}}\quad \badge{AI evaluation and observability platform} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Simulation, evaluation, experiments, prompt management, traces, and production monitoring for agents and AI applications.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Maxim AI as a ai evaluation and observability platform between scenario + agent trace and quality report + monitor; its defining mechanism is simulate, score, compare.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Scenario + agent trace};
\node[tech,right=of t0](t1){Simulate, score, compare};
\node[tech,right=of t1](t2){Quality report + monitor};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Scenario + agent trace -> Simulate, score, compare -> Quality report + monitor\\
Control plane & A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.\\
State and trust boundary & Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Simulation, evaluation, experiments, prompt management, traces, and production monitoring for agents and AI applications. Think of Maxim AI as a ai evaluation and observability platform between scenario + agent trace and quality report + monitor; its defining mechanism is simulate, score, compare.
\item[2. Structure and execution] Follow the data path Scenario + agent trace → Simulate, score, compare → Quality report + monitor. A stable trace schema and evaluation policy decide what is captured, sampled, scored, compared, and allowed to ship.
\item[3. State, security, and operations] Dataset cases, expected behavior, prompts, models, tools, traces, evaluator versions, human labels, and releases need durable identity.
\item[4. Selection decision] Choose it when a product team wants one managed workflow for agent simulation, evaluation, and observability. Reject it when self-hosting or a small code-only evaluator is required. Compare the same representative workload with LangSmith, Braintrust, Parea.
\item[5. Production proof] Start with the failure hypothesis: Simulated users and judges can encode unrealistic behavior or reward proxy metrics rather than outcomes. Require per-case diffs, slice metrics, judge calibration, inter-rater checks, privacy controls, cost, and incident-localizing spans.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed platform and SDKs\\
Choose when & A product team wants one managed workflow for agent simulation, evaluation, and observability.\\
Reject when & Self-hosting or a small code-only evaluator is required.\\
Primary failure & Simulated users and judges can encode unrealistic behavior or reward proxy metrics rather than outcomes.\\
Compare with & LangSmith; Braintrust; Parea\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install maxim-py
from maxim import Maxim
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.getmaxim.ai/docs/}{Maxim AI official documentation}.
\clearpage\subsection{06. Safety, validation, and policy guardrails}
\textbf{Selection question.} Which control is deterministic, model-based, or policy-based?
\begin{center}\begin{tikzpicture}[tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]
\node[tech](c0){Untrusted input + proposed action};
\node[tech,right=of c0](c1){Detect, validate, authorize};
\node[tech,right=of c1](c2){Bounded output or denial};
\draw[arr](c0)--(c1);\draw[arr](c1)--(c2);
\end{tikzpicture}\end{center}
\paragraph{Comparison matrix.}
\begin{longtable}{@{}p{20mm}p{22mm}p{26mm}p{46mm}p{46mm}@{}}
\toprule\textbf{Tool} & \textbf{Kind} & \textbf{Language / boundary} & \textbf{Choose when} & \textbf{Reject when}\\\midrule\endhead
NVIDIA NeMo Guardrails & Programmable conversation guardrails & Python, Colang; Library or service around model applications & Conversation policy needs explicit flows and model-assisted rails integrated around a Python application. & Simple output schema validation or a deterministic authorization service is the real need.\\
Guardrails AI & Output validation framework & Python; Library; local or Guardrails Hub components & Outputs must satisfy typed, regex, domain, or custom checks before application use. & A provider schema alone is enough or business authorization must be enforced outside model generation.\\
Llama Guard & Safety classification model family & Python, Transformers; Model checkpoint in local or hosted inference & A model-based content safety signal is needed and its taxonomy can be calibrated to the product population. & Deterministic secrets, authorization, or jurisdictional policy cannot be delegated to a classifier.\\
Protect AI LLM Guard & Input and output scanner library & Python; Library or API service & A self-hosted Python application needs layered pre- and post-generation scanners with explicit findings. & A single managed moderation API or organization-wide policy plane is already mandated.\\
Microsoft Presidio & PII detection and anonymization framework & Python, .NET; Library, containers, or Kubernetes & Sensitive data must be identified, redacted, pseudonymized, or deanonymized under controlled application policy. & General content safety or access authorization is the primary problem rather than PII handling.\\
Open Policy Agent & General policy engine & Go, Rego, REST; Sidecar, daemon, library, Kubernetes, or managed integrations & Tool calls, model routes, tenant actions, or infrastructure requests need deterministic reviewable policy. & A tiny local check is clearer or natural-language content classification is the problem.\\
Cedar & Authorization policy language and engine & Rust, Java, Cedar policy; Embedded engine or Amazon Verified Permissions & Fine-grained application authorization should be centralized with explicit entity relationships and analyzable policy. & Infrastructure admission, content classification, or a broad general policy language is the main use case.\\
Lakera Guard & Managed AI security API & REST, Python; Managed security service & A managed, rapidly updated prompt-attack signal is acceptable within latency, privacy, and regional constraints. & Prompts cannot leave the trust boundary or deterministic policy and local scanners already satisfy the threat model.\\
garak & LLM vulnerability scanner & Python; Local CLI against local or remote generators & You need repeatable adversarial probes across models or endpoints as one security-testing layer. & You need deterministic authorization, production inline blocking, or a complete product threat model.\\
PyRIT & Generative AI red-team framework & Python; Local or orchestrated attack runner & Security teams need programmable multi-turn adversarial campaigns and reproducible attack histories. & Inline safety filtering or simple fixed prompt tests are the actual need.\\
Rebuff & Prompt-injection detection toolkit & Python, JavaScript; Library or service integrations & A team wants a reference implementation of multiple prompt-injection signals around an application. & A maintained managed detector or deterministic tool authorization already covers the required boundary.\\
Amazon Bedrock Guardrails & Managed model guardrail service & Python, Java, REST; AWS managed policy and evaluation service & AWS-hosted applications need centrally managed policy signals across supported models and flows. & Prompts cannot leave AWS or application authorization and domain validation are the central risks.\\
Azure AI Content Safety & Managed content safety service & Python, C\#, Java, REST; Azure managed APIs & Azure governance and managed multi-modal safety classifiers fit the application boundary. & Self-hosting, deterministic policy, or highly specialized domain harm detection is required.\\
Google Cloud Model Armor & Managed AI security service & REST, client SDKs; Google Cloud managed service & Google Cloud applications need centralized managed inspection across supported model and agent paths. & A local open-source control or business authorization is the actual requirement.\\
\bottomrule\end{longtable}
\subsubsection{NVIDIA NeMo Guardrails}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries NNG}}}\quad \badge{Programmable conversation guardrails} \quad \textbf{Languages:} Python / Colang
\par\smallskip Defines topical, safety, retrieval, execution, and dialog rails using Colang flows plus configurable model and action integrations.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of NVIDIA NeMo Guardrails as a programmable conversation guardrails between input + policy flows and allowed response or refusal; its defining mechanism is detect, route, constrain.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Input + policy flows};
\node[tech,right=of t0](t1){Detect, route, constrain};
\node[tech,right=of t1](t2){Allowed response or refusal};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Input + policy flows -> Detect, route, constrain -> Allowed response or refusal\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Defines topical, safety, retrieval, execution, and dialog rails using Colang flows plus configurable model and action integrations. Think of NVIDIA NeMo Guardrails as a programmable conversation guardrails between input + policy flows and allowed response or refusal; its defining mechanism is detect, route, constrain.
\item[2. Structure and execution] Follow the data path Input + policy flows → Detect, route, constrain → Allowed response or refusal. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when conversation policy needs explicit flows and model-assisted rails integrated around a Python application. Reject it when simple output schema validation or a deterministic authorization service is the real need. Compare the same representative workload with Guardrails AI, Llama Guard, custom policy engine.
\item[5. Production proof] Start with the failure hypothesis: Model-based rails can be bypassed, conflict, add latency, or create a false sense that authorization is solved. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library or service around model applications\\
Choose when & Conversation policy needs explicit flows and model-assisted rails integrated around a Python application.\\
Reject when & Simple output schema validation or a deterministic authorization service is the real need.\\
Primary failure & Model-based rails can be bypassed, conflict, add latency, or create a false sense that authorization is solved.\\
Compare with & Guardrails AI; Llama Guard; custom policy engine\\
\end{tabularx}
\paragraph{Minimal use in Python / Colang.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install nemoguardrails
nemoguardrails chat --config config
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.nvidia.com/nemo/guardrails/latest/}{NVIDIA NeMo Guardrails official documentation}.
\subsubsection{Guardrails AI}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries GA}}}\quad \badge{Output validation framework} \quad \textbf{Languages:} Python
\par\smallskip Validates and repairs structured or natural-language model output through schemas, validators, and configurable reasks.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Guardrails AI as a output validation framework between model output + validators and accepted value or error; its defining mechanism is validate, reask, reject.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model output + validators};
\node[tech,right=of t0](t1){Validate, reask, reject};
\node[tech,right=of t1](t2){Accepted value or error};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model output + validators -> Validate, reask, reject -> Accepted value or error\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Validates and repairs structured or natural-language model output through schemas, validators, and configurable reasks. Think of Guardrails AI as a output validation framework between model output + validators and accepted value or error; its defining mechanism is validate, reask, reject.
\item[2. Structure and execution] Follow the data path Model output + validators → Validate, reask, reject → Accepted value or error. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when outputs must satisfy typed, regex, domain, or custom checks before application use. Reject it when a provider schema alone is enough or business authorization must be enforced outside model generation. Compare the same representative workload with Instructor, PydanticAI, NeMo Guardrails.
\item[5. Production proof] Start with the failure hypothesis: Repeated reasks can increase latency and still turn a semantic error into schema-valid misinformation. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; local or Guardrails Hub components\\
Choose when & Outputs must satisfy typed, regex, domain, or custom checks before application use.\\
Reject when & A provider schema alone is enough or business authorization must be enforced outside model generation.\\
Primary failure & Repeated reasks can increase latency and still turn a semantic error into schema-valid misinformation.\\
Compare with & Instructor; PydanticAI; NeMo Guardrails\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install guardrails-ai
from guardrails import Guard
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.guardrailsai.com/docs}{Guardrails AI official documentation}.
\subsubsection{Llama Guard}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries LG}}}\quad \badge{Safety classification model family} \quad \textbf{Languages:} Python / Transformers
\par\smallskip Instruction-tuned classifiers for categorizing unsafe model inputs and outputs against a configurable taxonomy.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Llama Guard as a safety classification model family between prompt or response and category and safe or unsafe label; its defining mechanism is safety classifier + taxonomy.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Prompt or response};
\node[tech,right=of t0](t1){Safety classifier + taxonomy};
\node[tech,right=of t1](t2){Category and safe or unsafe label};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Prompt or response -> Safety classifier + taxonomy -> Category and safe or unsafe label\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Instruction-tuned classifiers for categorizing unsafe model inputs and outputs against a configurable taxonomy. Think of Llama Guard as a safety classification model family between prompt or response and category and safe or unsafe label; its defining mechanism is safety classifier + taxonomy.
\item[2. Structure and execution] Follow the data path Prompt or response → Safety classifier + taxonomy → Category and safe or unsafe label. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when a model-based content safety signal is needed and its taxonomy can be calibrated to the product population. Reject it when deterministic secrets, authorization, or jurisdictional policy cannot be delegated to a classifier. Compare the same representative workload with provider moderation APIs, NeMo Guardrails, LLM Guard.
\item[5. Production proof] Start with the failure hypothesis: Classifier false negatives, false positives, language gaps, and taxonomy drift require human-calibrated thresholds. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Model checkpoint in local or hosted inference\\
Choose when & A model-based content safety signal is needed and its taxonomy can be calibrated to the product population.\\
Reject when & Deterministic secrets, authorization, or jurisdictional policy cannot be delegated to a classifier.\\
Primary failure & Classifier false negatives, false positives, language gaps, and taxonomy drift require human-calibrated thresholds.\\
Compare with & provider moderation APIs; NeMo Guardrails; LLM Guard\\
\end{tabularx}
\paragraph{Minimal use in Python / Transformers.}
\begin{Verbatim}[fontsize=\scriptsize]
from transformers import pipeline
classifier = pipeline('text-classification', model='meta-llama/Llama-Guard')
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.llama.com/docs/model-cards-and-prompt-formats/llama-guard-4/}{Llama Guard official documentation}.
\subsubsection{Protect AI LLM Guard}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries PAL}}}\quad \badge{Input and output scanner library} \quad \textbf{Languages:} Python
\par\smallskip Composable scanners for prompt injection, secrets, toxicity, sensitive data, banned topics, URLs, and output risks.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Protect AI LLM Guard as a input and output scanner library between prompt or response and sanitized text + risk scores; its defining mechanism is run selected scanners.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Prompt or response};
\node[tech,right=of t0](t1){Run selected scanners};
\node[tech,right=of t1](t2){Sanitized text + risk scores};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Prompt or response -> Run selected scanners -> Sanitized text + risk scores\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Composable scanners for prompt injection, secrets, toxicity, sensitive data, banned topics, URLs, and output risks. Think of Protect AI LLM Guard as a input and output scanner library between prompt or response and sanitized text + risk scores; its defining mechanism is run selected scanners.
\item[2. Structure and execution] Follow the data path Prompt or response → Run selected scanners → Sanitized text + risk scores. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when a self-hosted Python application needs layered pre- and post-generation scanners with explicit findings. Reject it when a single managed moderation API or organization-wide policy plane is already mandated. Compare the same representative workload with Lakera Guard, NeMo Guardrails, Presidio.
\item[5. Production proof] Start with the failure hypothesis: Scanner order, thresholds, language coverage, and model dependencies can create bypasses or unusable false positives. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library or API service\\
Choose when & A self-hosted Python application needs layered pre- and post-generation scanners with explicit findings.\\
Reject when & A single managed moderation API or organization-wide policy plane is already mandated.\\
Primary failure & Scanner order, thresholds, language coverage, and model dependencies can create bypasses or unusable false positives.\\
Compare with & Lakera Guard; NeMo Guardrails; Presidio\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install llm-guard
from llm_guard import scan_prompt, scan_output
\end{Verbatim}
\textbf{Primary sources.} \href{https://protectai.github.io/llm-guard/}{Protect AI LLM Guard official documentation}.
\subsubsection{Microsoft Presidio}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries MP}}}\quad \badge{PII detection and anonymization framework} \quad \textbf{Languages:} Python / .NET
\par\smallskip Detects and anonymizes personally identifiable information using recognizers, NLP engines, and configurable operators for text, images, and structured data.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Microsoft Presidio as a pii detection and anonymization framework between raw content and redacted or tokenized content; its defining mechanism is recognize entities + apply operators.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Raw content};
\node[tech,right=of t0](t1){Recognize entities + apply operators};
\node[tech,right=of t1](t2){Redacted or tokenized content};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Raw content -> Recognize entities + apply operators -> Redacted or tokenized content\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Detects and anonymizes personally identifiable information using recognizers, NLP engines, and configurable operators for text, images, and structured data. Think of Microsoft Presidio as a pii detection and anonymization framework between raw content and redacted or tokenized content; its defining mechanism is recognize entities + apply operators.
\item[2. Structure and execution] Follow the data path Raw content → Recognize entities + apply operators → Redacted or tokenized content. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when sensitive data must be identified, redacted, pseudonymized, or deanonymized under controlled application policy. Reject it when general content safety or access authorization is the primary problem rather than PII handling. Compare the same representative workload with cloud DLP services, LLM Guard, custom recognizers.
\item[5. Production proof] Start with the failure hypothesis: Entity detection is probabilistic and locale dependent; missed identifiers or irreversible transformations can create incidents. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library, containers, or Kubernetes\\
Choose when & Sensitive data must be identified, redacted, pseudonymized, or deanonymized under controlled application policy.\\
Reject when & General content safety or access authorization is the primary problem rather than PII handling.\\
Primary failure & Entity detection is probabilistic and locale dependent; missed identifiers or irreversible transformations can create incidents.\\
Compare with & cloud DLP services; LLM Guard; custom recognizers\\
\end{tabularx}
\paragraph{Minimal use in Python / .NET.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install presidio-analyzer presidio-anonymizer
from presidio_analyzer import AnalyzerEngine
\end{Verbatim}
\textbf{Primary sources.} \href{https://microsoft.github.io/presidio/}{Microsoft Presidio official documentation}.
\subsubsection{Open Policy Agent}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries OPA}}}\quad \badge{General policy engine} \quad \textbf{Languages:} Go / Rego / REST
\par\smallskip Evaluates declarative policy over structured input and data, separating authorization decisions from application code.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Open Policy Agent as a general policy engine between trusted identity + action + resource and allow, deny, or obligations; its defining mechanism is evaluate rego policy.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Trusted identity + action + resource};
\node[tech,right=of t0](t1){Evaluate Rego policy};
\node[tech,right=of t1](t2){Allow, deny, or obligations};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Trusted identity + action + resource -> Evaluate Rego policy -> Allow, deny, or obligations\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Evaluates declarative policy over structured input and data, separating authorization decisions from application code. Think of Open Policy Agent as a general policy engine between trusted identity + action + resource and allow, deny, or obligations; its defining mechanism is evaluate rego policy.
\item[2. Structure and execution] Follow the data path Trusted identity + action + resource → Evaluate Rego policy → Allow, deny, or obligations. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when tool calls, model routes, tenant actions, or infrastructure requests need deterministic reviewable policy. Reject it when a tiny local check is clearer or natural-language content classification is the problem. Compare the same representative workload with Cedar, Casbin, cloud policy services.
\item[5. Production proof] Start with the failure hypothesis: Passing model text instead of trusted identity and resource attributes into policy creates an authorization illusion. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Sidecar, daemon, library, Kubernetes, or managed integrations\\
Choose when & Tool calls, model routes, tenant actions, or infrastructure requests need deterministic reviewable policy.\\
Reject when & A tiny local check is clearer or natural-language content classification is the problem.\\
Primary failure & Passing model text instead of trusted identity and resource attributes into policy creates an authorization illusion.\\
Compare with & Cedar; Casbin; cloud policy services\\
\end{tabularx}
\paragraph{Minimal use in Go / Rego / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
opa eval --data policy.rego --input input.json 'data.app.allow'
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.openpolicyagent.org/docs/latest/}{Open Policy Agent official documentation}.
\subsubsection{Cedar}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries C}}}\quad \badge{Authorization policy language and engine} \quad \textbf{Languages:} Rust / Java / Cedar policy
\par\smallskip Schema-aware authorization language built around principals, actions, resources, policies, and formal analysis-oriented semantics.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Cedar as a authorization policy language and engine between principal + action + resource and permit or forbid decision; its defining mechanism is evaluate cedar policies.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Principal + action + resource};
\node[tech,right=of t0](t1){Evaluate Cedar policies};
\node[tech,right=of t1](t2){Permit or forbid decision};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Principal + action + resource -> Evaluate Cedar policies -> Permit or forbid decision\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Schema-aware authorization language built around principals, actions, resources, policies, and formal analysis-oriented semantics. Think of Cedar as a authorization policy language and engine between principal + action + resource and permit or forbid decision; its defining mechanism is evaluate cedar policies.
\item[2. Structure and execution] Follow the data path Principal + action + resource → Evaluate Cedar policies → Permit or forbid decision. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when fine-grained application authorization should be centralized with explicit entity relationships and analyzable policy. Reject it when infrastructure admission, content classification, or a broad general policy language is the main use case. Compare the same representative workload with Open Policy Agent, Casbin, Zanzibar-style service.
\item[5. Production proof] Start with the failure hypothesis: Incorrect entity graphs, schema evolution, or missing context can deny valid work or grant unintended access. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Embedded engine or Amazon Verified Permissions\\
Choose when & Fine-grained application authorization should be centralized with explicit entity relationships and analyzable policy.\\
Reject when & Infrastructure admission, content classification, or a broad general policy language is the main use case.\\
Primary failure & Incorrect entity graphs, schema evolution, or missing context can deny valid work or grant unintended access.\\
Compare with & Open Policy Agent; Casbin; Zanzibar-style service\\
\end{tabularx}
\paragraph{Minimal use in Rust / Java / Cedar policy.}
\begin{Verbatim}[fontsize=\scriptsize]
cargo add cedar-policy
cedar authorize --policies policies.cedar --entities entities.json
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.cedarpolicy.com/}{Cedar official documentation}.
\subsubsection{Lakera Guard}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries LG}}}\quad \badge{Managed AI security API} \quad \textbf{Languages:} REST / Python
\par\smallskip Detects prompt attacks and other model-input risks through an API intended to sit in front of LLM applications and agents.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Lakera Guard as a managed ai security api between untrusted prompt and risk signal for policy; its defining mechanism is managed attack detection.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Untrusted prompt};
\node[tech,right=of t0](t1){Managed attack detection};
\node[tech,right=of t1](t2){Risk signal for policy};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Untrusted prompt -> Managed attack detection -> Risk signal for policy\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Detects prompt attacks and other model-input risks through an API intended to sit in front of LLM applications and agents. Think of Lakera Guard as a managed ai security api between untrusted prompt and risk signal for policy; its defining mechanism is managed attack detection.
\item[2. Structure and execution] Follow the data path Untrusted prompt → Managed attack detection → Risk signal for policy. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when a managed, rapidly updated prompt-attack signal is acceptable within latency, privacy, and regional constraints. Reject it when prompts cannot leave the trust boundary or deterministic policy and local scanners already satisfy the threat model. Compare the same representative workload with LLM Guard, Rebuff, custom classifiers.
\item[5. Production proof] Start with the failure hypothesis: Treating one detector score as complete prompt-injection defense leaves tool authority and data exfiltration paths open. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed security service\\
Choose when & A managed, rapidly updated prompt-attack signal is acceptable within latency, privacy, and regional constraints.\\
Reject when & Prompts cannot leave the trust boundary or deterministic policy and local scanners already satisfy the threat model.\\
Primary failure & Treating one detector score as complete prompt-injection defense leaves tool authority and data exfiltration paths open.\\
Compare with & LLM Guard; Rebuff; custom classifiers\\
\end{tabularx}
\paragraph{Minimal use in REST / Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install lakera
from lakera import Lakera
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.lakera.ai/docs}{Lakera Guard official documentation}.
\subsubsection{garak}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries G}}}\quad \badge{LLM vulnerability scanner} \quad \textbf{Languages:} Python
\par\smallskip Open-source scanner with probes, detectors, generators, harnesses, and reports for testing model and application vulnerabilities.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of garak as a llm vulnerability scanner between target model + probe set and vulnerability report; its defining mechanism is generate attacks + detectors.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Target model + probe set};
\node[tech,right=of t0](t1){Generate attacks + detectors};
\node[tech,right=of t1](t2){Vulnerability report};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Target model + probe set -> Generate attacks + detectors -> Vulnerability report\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Open-source scanner with probes, detectors, generators, harnesses, and reports for testing model and application vulnerabilities. Think of garak as a llm vulnerability scanner between target model + probe set and vulnerability report; its defining mechanism is generate attacks + detectors.
\item[2. Structure and execution] Follow the data path Target model + probe set → Generate attacks + detectors → Vulnerability report. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when you need repeatable adversarial probes across models or endpoints as one security-testing layer. Reject it when you need deterministic authorization, production inline blocking, or a complete product threat model. Compare the same representative workload with PyRIT, Giskard, Promptfoo red teaming.
\item[5. Production proof] Start with the failure hypothesis: A high pass rate can reflect probe coverage or detector weaknesses rather than real resistance to attacks. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local CLI against local or remote generators\\
Choose when & You need repeatable adversarial probes across models or endpoints as one security-testing layer.\\
Reject when & You need deterministic authorization, production inline blocking, or a complete product threat model.\\
Primary failure & A high pass rate can reflect probe coverage or detector weaknesses rather than real resistance to attacks.\\
Compare with & PyRIT; Giskard; Promptfoo red teaming\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install garak
garak --model_type openai --model_name MODEL
\end{Verbatim}
\textbf{Primary sources.} \href{https://reference.garak.ai/en/latest/}{garak official documentation}.
\subsubsection{PyRIT}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries P}}}\quad \badge{Generative AI red-team framework} \quad \textbf{Languages:} Python
\par\smallskip Microsoft framework for multi-turn attack orchestration, prompt conversion, targets, scoring, memory, and red-team automation.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of PyRIT as a generative ai red-team framework between attack objective + target and attack transcript + score; its defining mechanism is converters, orchestrator, scorer.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Attack objective + target};
\node[tech,right=of t0](t1){Converters, orchestrator, scorer};
\node[tech,right=of t1](t2){Attack transcript + score};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Attack objective + target -> Converters, orchestrator, scorer -> Attack transcript + score\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Microsoft framework for multi-turn attack orchestration, prompt conversion, targets, scoring, memory, and red-team automation. Think of PyRIT as a generative ai red-team framework between attack objective + target and attack transcript + score; its defining mechanism is converters, orchestrator, scorer.
\item[2. Structure and execution] Follow the data path Attack objective + target → Converters, orchestrator, scorer → Attack transcript + score. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when security teams need programmable multi-turn adversarial campaigns and reproducible attack histories. Reject it when inline safety filtering or simple fixed prompt tests are the actual need. Compare the same representative workload with garak, Promptfoo, Giskard.
\item[5. Production proof] Start with the failure hypothesis: Powerful attack automation can target real systems or retain sensitive outputs unless scope, credentials, and storage are controlled. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local or orchestrated attack runner\\
Choose when & Security teams need programmable multi-turn adversarial campaigns and reproducible attack histories.\\
Reject when & Inline safety filtering or simple fixed prompt tests are the actual need.\\
Primary failure & Powerful attack automation can target real systems or retain sensitive outputs unless scope, credentials, and storage are controlled.\\
Compare with & garak; Promptfoo; Giskard\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install pyrit
from pyrit.orchestrator import PromptSendingOrchestrator
\end{Verbatim}
\textbf{Primary sources.} \href{https://azure.github.io/PyRIT/}{PyRIT official documentation}.
\subsubsection{Rebuff}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries R}}}\quad \badge{Prompt-injection detection toolkit} \quad \textbf{Languages:} Python / JavaScript
\par\smallskip Layered prompt-injection detection using heuristics, vector similarity, model checks, and canary-style signals.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Rebuff as a prompt-injection detection toolkit between prompt + canary context and risk decision + evidence; its defining mechanism is layered injection detectors.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Prompt + canary context};
\node[tech,right=of t0](t1){Layered injection detectors};
\node[tech,right=of t1](t2){Risk decision + evidence};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Prompt + canary context -> Layered injection detectors -> Risk decision + evidence\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Layered prompt-injection detection using heuristics, vector similarity, model checks, and canary-style signals. Think of Rebuff as a prompt-injection detection toolkit between prompt + canary context and risk decision + evidence; its defining mechanism is layered injection detectors.
\item[2. Structure and execution] Follow the data path Prompt + canary context → Layered injection detectors → Risk decision + evidence. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when a team wants a reference implementation of multiple prompt-injection signals around an application. Reject it when a maintained managed detector or deterministic tool authorization already covers the required boundary. Compare the same representative workload with Lakera Guard, LLM Guard, custom policy.
\item[5. Production proof] Start with the failure hypothesis: Detection cannot make untrusted content safe to execute and project maintenance must be assessed. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library or service integrations\\
Choose when & A team wants a reference implementation of multiple prompt-injection signals around an application.\\
Reject when & A maintained managed detector or deterministic tool authorization already covers the required boundary.\\
Primary failure & Detection cannot make untrusted content safe to execute and project maintenance must be assessed.\\
Compare with & Lakera Guard; LLM Guard; custom policy\\
\end{tabularx}
\paragraph{Minimal use in Python / JavaScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install rebuff
from rebuff import Rebuff
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/protectai/rebuff}{Rebuff official documentation}.
\subsubsection{Amazon Bedrock Guardrails}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries ABG}}}\quad \badge{Managed model guardrail service} \quad \textbf{Languages:} Python / Java / REST
\par\smallskip Configurable content filters, denied topics, word filters, sensitive-information handling, contextual grounding, and automated reasoning checks around model calls.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Amazon Bedrock Guardrails as a managed model guardrail service between prompt or response + guardrail and filtered content + assessments; its defining mechanism is managed policy evaluation.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Prompt or response + guardrail};
\node[tech,right=of t0](t1){Managed policy evaluation};
\node[tech,right=of t1](t2){Filtered content + assessments};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Prompt or response + guardrail -> Managed policy evaluation -> Filtered content + assessments\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Configurable content filters, denied topics, word filters, sensitive-information handling, contextual grounding, and automated reasoning checks around model calls. Think of Amazon Bedrock Guardrails as a managed model guardrail service between prompt or response + guardrail and filtered content + assessments; its defining mechanism is managed policy evaluation.
\item[2. Structure and execution] Follow the data path Prompt or response + guardrail → Managed policy evaluation → Filtered content + assessments. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when aWS-hosted applications need centrally managed policy signals across supported models and flows. Reject it when prompts cannot leave AWS or application authorization and domain validation are the central risks. Compare the same representative workload with NeMo Guardrails, Azure AI Content Safety, Llama Guard.
\item[5. Production proof] Start with the failure hypothesis: Managed filters can overblock, miss domain-specific harm, or be mistaken for tool authorization. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & AWS managed policy and evaluation service\\
Choose when & AWS-hosted applications need centrally managed policy signals across supported models and flows.\\
Reject when & Prompts cannot leave AWS or application authorization and domain validation are the central risks.\\
Primary failure & Managed filters can overblock, miss domain-specific harm, or be mistaken for tool authorization.\\
Compare with & NeMo Guardrails; Azure AI Content Safety; Llama Guard\\
\end{tabularx}
\paragraph{Minimal use in Python / Java / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
boto3.client('bedrock-runtime').apply_guardrail(...)
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.aws.amazon.com/bedrock/latest/userguide/guardrails.html}{Amazon Bedrock Guardrails official documentation}.
\subsubsection{Azure AI Content Safety}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries AAC}}}\quad \badge{Managed content safety service} \quad \textbf{Languages:} Python / C\# / Java / REST
\par\smallskip Text and image harm classification, blocklists, prompt-shield signals, and groundedness-related safety capabilities for applications.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Azure AI Content Safety as a managed content safety service between text or image and categories, severities, and policy action; its defining mechanism is managed safety classifiers.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Text or image};
\node[tech,right=of t0](t1){Managed safety classifiers};
\node[tech,right=of t1](t2){Categories, severities, and policy action};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Text or image -> Managed safety classifiers -> Categories, severities, and policy action\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Text and image harm classification, blocklists, prompt-shield signals, and groundedness-related safety capabilities for applications. Think of Azure AI Content Safety as a managed content safety service between text or image and categories, severities, and policy action; its defining mechanism is managed safety classifiers.
\item[2. Structure and execution] Follow the data path Text or image → Managed safety classifiers → Categories, severities, and policy action. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when azure governance and managed multi-modal safety classifiers fit the application boundary. Reject it when self-hosting, deterministic policy, or highly specialized domain harm detection is required. Compare the same representative workload with Bedrock Guardrails, Llama Guard, Lakera Guard.
\item[5. Production proof] Start with the failure hypothesis: Severity thresholds and language coverage must be calibrated; classifiers do not establish identity or permission. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Azure managed APIs\\
Choose when & Azure governance and managed multi-modal safety classifiers fit the application boundary.\\
Reject when & Self-hosting, deterministic policy, or highly specialized domain harm detection is required.\\
Primary failure & Severity thresholds and language coverage must be calibrated; classifiers do not establish identity or permission.\\
Compare with & Bedrock Guardrails; Llama Guard; Lakera Guard\\
\end{tabularx}
\paragraph{Minimal use in Python / C\# / Java / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install azure-ai-contentsafety
from azure.ai.contentsafety import ContentSafetyClient
\end{Verbatim}
\textbf{Primary sources.} \href{https://learn.microsoft.com/en-us/azure/ai-services/content-safety/overview}{Azure AI Content Safety official documentation}.
\subsubsection{Google Cloud Model Armor}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries GCM}}}\quad \badge{Managed AI security service} \quad \textbf{Languages:} REST / client SDKs
\par\smallskip Screens prompts and responses for prompt injection, sensitive data, malicious URLs, and content-safety risks around model applications.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Google Cloud Model Armor as a managed ai security service between prompt or response and sanitized content + findings; its defining mechanism is managed security screening.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Prompt or response};
\node[tech,right=of t0](t1){Managed security screening};
\node[tech,right=of t1](t2){Sanitized content + findings};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Prompt or response -> Managed security screening -> Sanitized content + findings\\
Control plane & Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.\\
State and trust boundary & Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Screens prompts and responses for prompt injection, sensitive data, malicious URLs, and content-safety risks around model applications. Think of Google Cloud Model Armor as a managed ai security service between prompt or response and sanitized content + findings; its defining mechanism is managed security screening.
\item[2. Structure and execution] Follow the data path Prompt or response → Managed security screening → Sanitized content + findings. Layer deterministic validation and authorization with calibrated model-based detectors at the boundary each can actually defend.
\item[3. State, security, and operations] Trusted identity, resource, consent, policy version, detector version, redacted evidence, and override decisions belong outside model text.
\item[4. Selection decision] Choose it when google Cloud applications need centralized managed inspection across supported model and agent paths. Reject it when a local open-source control or business authorization is the actual requirement. Compare the same representative workload with Bedrock Guardrails, Azure AI Content Safety, LLM Guard.
\item[5. Production proof] Start with the failure hypothesis: Detection coverage, false positives, region, and latency must be tested, and screening is not authorization. Test attack success, false positives, false negatives, over-refusal, language and cohort slices, latency, bypass chains, and rollback.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Google Cloud managed service\\
Choose when & Google Cloud applications need centralized managed inspection across supported model and agent paths.\\
Reject when & A local open-source control or business authorization is the actual requirement.\\
Primary failure & Detection coverage, false positives, region, and latency must be tested, and screening is not authorization.\\
Compare with & Bedrock Guardrails; Azure AI Content Safety; LLM Guard\\
\end{tabularx}
\paragraph{Minimal use in REST / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
gcloud model-armor templates create TEMPLATE
gcloud model-armor templates sanitize-user-prompt TEMPLATE
\end{Verbatim}
\textbf{Primary sources.} \href{https://cloud.google.com/security-command-center/docs/model-armor-overview}{Google Cloud Model Armor official documentation}.
\clearpage\subsection{07. MCP and tool connectivity}
\textbf{Selection question.} Where is authority enforced?
\begin{center}\begin{tikzpicture}[tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]
\node[tech](c0){Model intent + capability};
\node[tech,right=of c0](c1){Discover, authorize, invoke};
\node[tech,right=of c1](c2){Typed tool result};
\draw[arr](c0)--(c1);\draw[arr](c1)--(c2);
\end{tikzpicture}\end{center}
\paragraph{Comparison matrix.}
\begin{longtable}{@{}p{20mm}p{22mm}p{26mm}p{46mm}p{46mm}@{}}
\toprule\textbf{Tool} & \textbf{Kind} & \textbf{Language / boundary} & \textbf{Choose when} & \textbf{Reject when}\\\midrule\endhead
Model Context Protocol & Open protocol & JSON-RPC, TypeScript, Python, C\#, Java and more; Local stdio or remote HTTP transports & Independent applications and capability providers need a standard integration boundary with explicit host control. & A private in-process function call is simpler, or the integration cannot implement authentication and authorization safely.\\
Official MCP SDKs & Protocol SDKs & TypeScript, Python, C\#, Java, Kotlin, Swift, Rust, Go, Ruby, PHP; Embedded in MCP clients and servers & You are implementing an MCP client or server and want protocol-correct primitives instead of hand-written JSON-RPC. & You only consume a stable product SDK and do not need MCP interoperability.\\
FastMCP & MCP framework & Python, TypeScript; Library; local or remote server & Developers need a concise typed API for production Python or TypeScript MCP services. & You require only low-level protocol control or an existing framework already owns routing and auth.\\
MCP Inspector & Developer inspection tool & TypeScript; Local web UI and proxy & You are developing or debugging a server and need to see exact schemas, messages, and responses. & You need production monitoring, authorization, or a security boundary.\\
Docker MCP Toolkit and Gateway & Tool catalog and gateway & Go, Docker configuration; Docker Desktop and containerized gateway & Desktop or enterprise teams want curated containerized servers and centralized client configuration. & Container trust, image provenance, or Docker Desktop availability cannot be accepted.\\
Smithery & MCP registry and deployment platform & TypeScript, Python, CLI; Hosted registry and remote server infrastructure & Teams need to find and connect interoperable servers quickly with hosted deployment options. & Strict private catalogs, on-premises execution, or bespoke authorization dominate.\\
Composio & Managed tool integration platform & Python, TypeScript, REST; Managed service and SDKs & Many SaaS integrations and per-user OAuth connections must be delivered quickly. & On-premises secrets, custom APIs, or avoiding an external auth and tool data plane is mandatory.\\
Arcade & Agent tool and authorization platform & Python, TypeScript, REST; Managed or gateway-oriented platform & Agents need end-user authorization and a curated tool layer without rebuilding OAuth for every integration. & The organization requires fully self-hosted bespoke connectors or direct provider APIs only.\\
Official MCP Registry & MCP metadata registry & REST, JSON; Hosted registry API with ecosystem clients & Clients or catalogs need a common source of server identity and distribution metadata. & You need runtime authorization, trust decisions, or private internal discovery by itself.\\
GitHub MCP Server & Repository tool server & Go, Docker; Local stdio, containers, or remote GitHub service & Agents need typed GitHub operations with token scopes and explicit toolsets. & Direct REST or GraphQL clients are simpler, or broad repository mutation is too risky.\\
AWS MCP Servers & Cloud service MCP server collection & Python, TypeScript; Local or remote servers for AWS services & Agents need typed access to selected AWS services under existing IAM and audit controls. & Direct SDK calls are clearer or a centralized gateway must own every tool schema and policy.\\
MCP Toolbox for Databases & Database tool server & Go, YAML, client SDKs; Local binary, container, or cloud deployment & Agents need governed, reusable database operations rather than arbitrary SQL generation. & A direct application data layer or unrestricted analyst SQL is the actual boundary.\\
Azure MCP Server & Cloud service MCP server & TypeScript, Docker; Local stdio or container with Azure identity & Agents need governed access to Azure resources through familiar identity and service-specific tools. & Direct Azure SDKs are clearer or remote shared tool execution violates the trust boundary.\\
Zapier MCP & Managed application tool gateway & MCP, REST; Managed remote MCP server & A user wants quick authenticated access to many SaaS actions without building each OAuth connector. & High-impact actions, self-hosting, or deeply customized schemas require an owned integration service.\\
mcp-use & MCP client and agent library & Python, TypeScript; Library and optional cloud services & You need a focused programmatic MCP client layer across models and server transports. & A framework already supplies adequate MCP integration or an enterprise gateway must mediate every call.\\
\bottomrule\end{longtable}
