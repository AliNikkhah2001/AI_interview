\begin{titlepage}\pagecolor{navy}\color{white}\vspace*{1.1in}{\Huge\bfseries AI Engineering\\Interview Atlas\par}\vspace{.35in}{\Large Mathematical tutorials, technology field guide, systems, and 2,000+ questions\par}\vspace{.35in}{\large 33 derivations \textbullet\ 15 visual models \textbullet\ 194 technology profiles \textbullet\ 192 deep lessons\par}\vfill{\large Research snapshot: 6 September 2026\par}\vspace{.15in}{\normalsize Primary papers, official documentation, specifications, and standards\par}\vspace{.7in}{\color{cyan}\rule{\textwidth}{3pt}}\end{titlepage}\nopagecolor\color{black}
\tableofcontents\newpage
\section{How to use this handbook}
This handbook contains 192 complete topic tutorials, 194 workload-oriented technology profiles, 2696 generated practice questions, 50 core interview questions, 32 progressive system-design challenges, 15 visual intuition models, and 33 mathematical modules. The browser resumes the last unfinished chapter, records five reading checkpoints, unlocks a five-question exam, and advances after a score of at least 80 percent.
\subsection{The interview answer loop}
Answer in seven moves: \textbf{mechanism $\rightarrow$ assumptions $\rightarrow$ quantitative model $\rightarrow$ alternative $\rightarrow$ experiment $\rightarrow$ operation $\rightarrow$ recovery}. For a short answer, compress the same structure rather than replacing it with product slogans.
\subsection{How to study a derivation}
For every equation: (1) define the tensor or random-variable shapes; (2) state assumptions; (3) derive rather than memorize; (4) check units or limiting cases; (5) connect each parameter to quality, latency, memory, or cost; and (6) identify where the approximation stops being valid.
\clearpage\section{Ordered study roadmap and progress model}
The online atlas stores completed phases, mastered lessons, and reviewed questions in browser storage. The first incomplete phase is highlighted as the current phase. In print, mark the boxes and write a date beside each milestone.
\begin{center}\begin{tikzpicture}[node distance=6mm and 18mm,phase/.style={draw=cyan,fill=paper,rounded corners,align=left,text width=66mm,minimum height=10mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[phase](p0){\textbf{00. Diagnostic and study system}\hfill 3h};
\node[phase,below=of p0](p1){\textbf{01. Python, APIs, and distributed foundations}\hfill 22h};
\node[phase,below=of p1](p2){\textbf{02. Transformer mathematics and training}\hfill 26h};
\node[phase,below=of p2](p3){\textbf{03. Retrieval theory and vector systems}\hfill 28h};
\node[phase,below=of p3](p4){\textbf{04. RAG construction, graphs, and evaluation}\hfill 30h};
\node[phase,below=of p4](p5){\textbf{05. Agents, MCP, orchestration, and sandbox control}\hfill 28h};
\node[phase,right=of p5](p6){\textbf{06. Serving and inference optimization}\hfill 28h};
\node[phase,above=of p6](p7){\textbf{07. Observability, evaluation, and quality monitoring}\hfill 20h};
\node[phase,above=of p7](p8){\textbf{08. Data, experiments, and ML lifecycle}\hfill 18h};
\node[phase,above=of p8](p9){\textbf{09. Security, safety, and governance}\hfill 18h};
\node[phase,above=of p9](p10){\textbf{10. Integrated AI system design}\hfill 26h};
\node[phase,above=of p10](p11){\textbf{11. Interview simulation and targeted repair}\hfill 18h};
\draw[arr](p0)--(p1);
\draw[arr](p1)--(p2);
\draw[arr](p2)--(p3);
\draw[arr](p3)--(p4);
\draw[arr](p4)--(p5);
\draw[arr](p5)--(p6);
\draw[arr](p6)--(p7);
\draw[arr](p7)--(p8);
\draw[arr](p8)--(p9);
\draw[arr](p9)--(p10);
\draw[arr](p10)--(p11);
\end{tikzpicture}\end{center}
\subsection{The guided learning loop}
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=30mm,minimum height=11mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]\node[visual](resume){Resume last\\unfinished};\node[visual,right=of resume](teach){Read five\\lesson parts};\node[visual,right=of teach](exam){Pass topic exam\\at 80\%};\node[visual,below=of exam](mark){Mark chapter\\and phase};\node[visual,left=of mark](next){Open next\\chapter};\draw[arr](resume)--(teach);\draw[arr](teach)--(exam);\draw[arr](exam)--(mark);\draw[arr](mark)--(next);\draw[arr](next.west) to[bend left=26] (resume.south);\end{tikzpicture}\end{center}
The website stores the last opened chapter, reading checkpoints, exam attempts, best scores, chapter mastery, and automatically completed lesson-bearing phases in browser storage. A chapter is learned only after all five parts have been reached and its exam has been passed.
\subsection{00. Diagnostic and study system}
\badge{3 focused hours} \quad \textbf{Prerequisites:} none
\begin{itemize}
\item Benchmark current recall across all domains
\item Set a weekly active-recall cadence
\item Create an error log organized by mechanism, tradeoff, metric, and failure
\end{itemize}
\textbf{Milestone.} Complete 50 mixed questions and label every miss by cause.
\textbf{Practice.} 50 mixed questions; do not study before the diagnostic.
\subsection{01. Python, APIs, and distributed foundations}
\badge{22 focused hours} \quad \textbf{Prerequisites:} phase-00
\begin{itemize}
\item Explain Python execution and concurrency
\item Design typed streaming APIs
\item Reason about queues, retries, caching, consensus, and capacity
\end{itemize}
\textbf{Milestone.} Design a backpressured streaming API and defend its failure semantics.
\textbf{Practice.} 180 questions plus system designs 8 and 12.
\subsection{02. Transformer mathematics and training}
\badge{26 focused hours} \quad \textbf{Prerequisites:} phase-01
\begin{itemize}
\item Derive scaled dot-product attention
\item Explain normalization, residuals, objectives, and preference tuning
\item Estimate training and adaptation memory
\end{itemize}
\textbf{Milestone.} Derive attention, cross-entropy, LoRA, and DPO without notes.
\textbf{Practice.} 120 questions and one whiteboard derivation session.
\subsection{03. Retrieval theory and vector systems}
\badge{28 focused hours} \quad \textbf{Prerequisites:} phase-02
\begin{itemize}
\item Compare lexical, dense, sparse, and hybrid retrieval
\item Derive ranking metrics and fusion
\item Select and tune exact, HNSW, IVF, and PQ indexes
\end{itemize}
\textbf{Milestone.} Choose pgvector or a dedicated vector service from a stated workload and prove the choice with filtered recall tests.
\textbf{Practice.} 260 questions plus a retrieval benchmark design.
\subsection{04. RAG construction, graphs, and evaluation}
\badge{30 focused hours} \quad \textbf{Prerequisites:} phase-03
\begin{itemize}
\item Build traceable ingestion-to-answer pipelines
\item Separate retrieval, generation, citation, and business evaluation
\item Know when graphs add value over vector retrieval
\end{itemize}
\textbf{Milestone.} Localize a RAG regression to ingestion, retrieval, packing, generation, or judging.
\textbf{Practice.} 270 questions plus system designs 1, 2, and 3.
\subsection{05. Agents, MCP, orchestration, and sandbox control}
\badge{28 focused hours} \quad \textbf{Prerequisites:} phase-04
\begin{itemize}
\item Separate agentic decisions from deterministic workflow boundaries
\item Design durable state, retries, approvals, and compensation
\item Apply least privilege to tools, sandboxes, and MCP servers
\end{itemize}
\textbf{Milestone.} Design a resumable agent that cannot duplicate a high-impact side effect.
\textbf{Practice.} 230 questions plus system designs 4 and 5.
\subsection{06. Serving and inference optimization}
\badge{28 focused hours} \quad \textbf{Prerequisites:} phase-02, phase-01
\begin{itemize}
\item Calculate KV-cache and batching capacity
\item Explain quantization, FlashAttention, GQA, and speculative decoding
\item Design model routing, rollout, autoscaling, and fallback
\end{itemize}
\textbf{Milestone.} Meet a p95 TTFT target under a token-distribution workload and explain the cost model.
\textbf{Practice.} 250 questions plus system designs 7 and 10.
\subsection{07. Observability, evaluation, and quality monitoring}
\badge{20 focused hours} \quad \textbf{Prerequisites:} phase-04, phase-05, phase-06
\begin{itemize}
\item Instrument traces across retrieval, model, and tool spans
\item Define semantic SLOs and calibrated evaluators
\item Detect cost, latency, and quality drift by cohort
\end{itemize}
\textbf{Milestone.} Write an incident runbook that ties alerts to traces, evaluation slices, and rollback.
\textbf{Practice.} 130 questions plus system design 6.
\subsection{08. Data, experiments, and ML lifecycle}
\badge{18 focused hours} \quad \textbf{Prerequisites:} phase-07
\begin{itemize}
\item Version code, data, prompts, models, and environments together
\item Prevent leakage with point-in-time joins
\item Promote artifacts using reproducible evaluation evidence
\end{itemize}
\textbf{Milestone.} Reproduce an evaluation run from immutable lineage and promote or roll back its artifacts.
\textbf{Practice.} 120 questions plus system design 11.
\subsection{09. Security, safety, and governance}
\badge{18 focused hours} \quad \textbf{Prerequisites:} phase-05, phase-07
\begin{itemize}
\item Threat-model prompt, retrieval, tool, and model supply chains
\item Design tenant isolation and auditable policy enforcement
\item Connect evaluations to release gates and incident response
\end{itemize}
\textbf{Milestone.} Threat-model one agentic RAG architecture and close its three highest-risk paths.
\textbf{Practice.} 120 questions plus system design 9.
\subsection{10. Integrated AI system design}
\badge{26 focused hours} \quad \textbf{Prerequisites:} phase-06, phase-08, phase-09
\begin{itemize}
\item Translate ambiguous requirements into SLOs and workload models
\item Build multi-stage architectures with explicit data and control planes
\item Defend tradeoffs, degradation, migration, and rollback
\end{itemize}
\textbf{Milestone.} Complete all 12 designs aloud in 45 minutes each, including every twist.
\textbf{Practice.} All 12 system designs; repeat the weakest four after 72 hours.
\subsection{11. Interview simulation and targeted repair}
\badge{18 focused hours} \quad \textbf{Prerequisites:} phase-10
\begin{itemize}
\item Answer at medium and hard depth under time pressure
\item Use clarifying questions and quantitative estimates
\item Convert misses into a short repair loop
\end{itemize}
\textbf{Milestone.} Score at least 80\% on 200 unseen mixed questions and pass three timed mock loops.
\textbf{Practice.} 200 mixed questions, 3 mocks, and spaced repetition of every miss.
\clearpage\section{Visual intuition atlas}
Use each picture to predict behavior before memorizing terminology. Curves are normalized teaching models unless a unit is stated; benchmark real systems on representative workloads.
\subsection{Why BM25 saturates repeated terms}
\badge{RANKING INTUITION} \quad The first matching term is strong evidence. Each repetition adds less information, so the score bends toward a ceiling instead of growing forever.
\begin{center}\begin{tikzpicture}[x=1.32cm,y=0.047cm]
\draw[->,draw=navy] (0,0)--(5.45,0) node[right]{\scriptsize term frequency};
\draw[->,draw=navy] (0,0)--(0,108) node[above]{\scriptsize relative contribution};
\draw[draw=gray!25,dashed] (0,25)--(5,25) node[left]{\scriptsize 25};
\draw[draw=gray!25,dashed] (0,50)--(5,50) node[left]{\scriptsize 50};
\draw[draw=gray!25,dashed] (0,75)--(5,75) node[left]{\scriptsize 75};
\draw[draw=gray!25,dashed] (0,100)--(5,100) node[left]{\scriptsize 100};
\draw[very thick,draw=cyan] plot coordinates {(0,3) (1,6) (2,13) (3,25) (4,50) (5,100)};
\fill[cyan] (0,3) circle (1.5pt);
\fill[cyan] (1,6) circle (1.5pt);
\fill[cyan] (2,13) circle (1.5pt);
\fill[cyan] (3,25) circle (1.5pt);
\fill[cyan] (4,50) circle (1.5pt);
\fill[cyan] (5,100) circle (1.5pt);
\draw[very thick,draw=amber] plot coordinates {(0,47) (1,65) (2,80) (3,90) (4,96) (5,100)};
\fill[amber] (0,47) circle (1.5pt);
\fill[amber] (1,65) circle (1.5pt);
\fill[amber] (2,80) circle (1.5pt);
\fill[amber] (3,90) circle (1.5pt);
\fill[amber] (4,96) circle (1.5pt);
\fill[amber] (5,100) circle (1.5pt);
\node[below,font=\scriptsize] at (0,0) {1};
\node[below,font=\scriptsize] at (1,0) {2};
\node[below,font=\scriptsize] at (2,0) {4};
\node[below,font=\scriptsize] at (3,0) {8};
\node[below,font=\scriptsize] at (4,0) {16};
\node[below,font=\scriptsize] at (5,0) {32};
\end{tikzpicture}\par\smallskip \textcolor{cyan}{\rule{5mm}{1.2pt}} linear count\quad \textcolor{amber}{\rule{5mm}{1.2pt}} BM25-like saturation\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} If a document repeats a query token 32 times, it should not automatically outrank a concise document with better coverage.}}
\subsection{ANN search moves along a frontier}
\badge{INDEX TUNING} \quad Searching more candidates usually improves recall, but tail latency and compute rise. The useful operating point is a measured frontier, not a universal index setting.
\begin{center}\begin{tikzpicture}[x=1.32cm,y=0.047cm]
\draw[->,draw=navy] (0,0)--(4.45,0) node[right]{\scriptsize search effort};
\draw[->,draw=navy] (0,0)--(0,108) node[above]{\scriptsize normalized percent};
\draw[draw=gray!25,dashed] (0,25)--(4,25) node[left]{\scriptsize 25};
\draw[draw=gray!25,dashed] (0,50)--(4,50) node[left]{\scriptsize 50};
\draw[draw=gray!25,dashed] (0,75)--(4,75) node[left]{\scriptsize 75};
\draw[draw=gray!25,dashed] (0,100)--(4,100) node[left]{\scriptsize 100};
\draw[very thick,draw=cyan] plot coordinates {(0,58) (1,74) (2,86) (3,94) (4,98)};
\fill[cyan] (0,58) circle (1.5pt);
\fill[cyan] (1,74) circle (1.5pt);
\fill[cyan] (2,86) circle (1.5pt);
\fill[cyan] (3,94) circle (1.5pt);
\fill[cyan] (4,98) circle (1.5pt);
\draw[very thick,draw=amber] plot coordinates {(0,12) (1,21) (2,37) (3,66) (4,100)};
\fill[amber] (0,12) circle (1.5pt);
\fill[amber] (1,21) circle (1.5pt);
\fill[amber] (2,37) circle (1.5pt);
\fill[amber] (3,66) circle (1.5pt);
\fill[amber] (4,100) circle (1.5pt);
\node[below,font=\scriptsize] at (0,0) {tiny};
\node[below,font=\scriptsize] at (1,0) {low};
\node[below,font=\scriptsize] at (2,0) {medium};
\node[below,font=\scriptsize] at (3,0) {high};
\node[below,font=\scriptsize] at (4,0) {max};
\end{tikzpicture}\par\smallskip \textcolor{cyan}{\rule{5mm}{1.2pt}} recall\quad \textcolor{amber}{\rule{5mm}{1.2pt}} latency cost\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Tune filtered recall and p95 latency together on representative queries; either number alone can hide a bad index configuration.}}
\subsection{RAG is an evidence supply chain}
\badge{DATA + MODEL FLOW} \quad A correct-looking answer can fail in five different places. Preserve identifiers so a bad claim can be traced backward to retrieval, source version, and parsing.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{Source}\\{\scriptsize version + ACL}};
\node[visual,right=of v0] (v1) {\textbf{Chunk}\\{\scriptsize span + metadata}};
\node[visual,right=of v1] (v2) {\textbf{Retrieve}\\{\scriptsize candidate + score}};
\node[visual,right=of v2] (v3) {\textbf{Generate}\\{\scriptsize claim + citation}};
\node[visual,right=of v3] (v4) {\textbf{Evaluate}\\{\scriptsize localize failure}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Measure retriever recall, context quality, claim support, and answer usefulness separately.}}
\subsection{Graphs add structure in layers}
\badge{GRAPH INTUITION} \quad A knowledge graph is not merely another vector index. It turns mentions into resolved entities, explicit relationships, communities, and queryable summaries.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{Mentions}\\{\scriptsize raw spans}};
\node[visual,right=of v0] (v1) {\textbf{Entities}\\{\scriptsize resolved identity}};
\node[visual,right=of v1] (v2) {\textbf{Relations}\\{\scriptsize typed edges}};
\node[visual,right=of v2] (v3) {\textbf{Communities}\\{\scriptsize global structure}};
\node[visual,right=of v3] (v4) {\textbf{Answers}\\{\scriptsize traceable paths}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Each higher layer adds reasoning power and a new source of extraction or resolution error.}}
\subsection{An agent is a controlled feedback loop}
\badge{CONTROL FLOW} \quad The model proposes; policy authorizes; a typed tool changes the world; observation updates state. The loop must have budgets and deterministic stop conditions.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{Observe}\\{\scriptsize state + evidence}};
\node[visual,right=of v0] (v1) {\textbf{Propose}\\{\scriptsize structured action}};
\node[visual,right=of v1] (v2) {\textbf{Authorize}\\{\scriptsize policy + approval}};
\node[visual,right=of v2] (v3) {\textbf{Execute}\\{\scriptsize idempotent tool}};
\node[visual,right=of v3] (v4) {\textbf{Record}\\{\scriptsize result + audit}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\draw[arr] (v4.south) to[bend left=32] node[below,font=\scriptsize]{feedback} (v0.south);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Do not let natural-language intent bypass authorization or make an external effect invisible to the state machine.}}
\subsection{Durability separates decisions from effects}
\badge{WORKFLOW STATE} \quad Persist state before and after risky effects. A retry should replay a decision safely or reuse a recorded result, not duplicate the side effect.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{State}\\{\scriptsize checkpoint}};
\node[visual,right=of v0] (v1) {\textbf{Decision}\\{\scriptsize pure transition}};
\node[visual,right=of v1] (v2) {\textbf{Effect}\\{\scriptsize idempotency key}};
\node[visual,right=of v2] (v3) {\textbf{Result}\\{\scriptsize durable record}};
\node[visual,right=of v3] (v4) {\textbf{Resume}\\{\scriptsize continue safely}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\draw[arr] (v4.south) to[bend left=32] node[below,font=\scriptsize]{feedback} (v0.south);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Crash recovery is a data-model property before it is a framework feature.}}
\subsection{A trace explains where latency went}
\badge{REQUEST WATERFALL} \quad End-to-end latency is a composition of queueing, retrieval, model prefill, decoding, tools, and retries. A single aggregate hides the critical path.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{Queue}\\{\scriptsize 0-80 ms}};
\node[visual,right=of v0] (v1) {\textbf{Retrieve}\\{\scriptsize 80-210 ms}};
\node[visual,right=of v1] (v2) {\textbf{Prefill}\\{\scriptsize 210-430 ms}};
\node[visual,right=of v2] (v3) {\textbf{Decode}\\{\scriptsize 430-1250 ms}};
\node[visual,right=of v3] (v4) {\textbf{Tool}\\{\scriptsize overlap/retry}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Correlate traces with quality, cost, model version, prompt version, and user cohort.}}
\subsection{Batching trades latency for utilization}
\badge{SERVING FRONTIER} \quad Larger effective batches fill the accelerator and raise throughput, but waiting and head-of-line blocking eventually dominate request latency.
\begin{center}\begin{tikzpicture}[x=1.32cm,y=0.047cm]
\draw[->,draw=navy] (0,0)--(5.45,0) node[right]{\scriptsize effective batch};
\draw[->,draw=navy] (0,0)--(0,108) node[above]{\scriptsize normalized percent};
\draw[draw=gray!25,dashed] (0,25)--(5,25) node[left]{\scriptsize 25};
\draw[draw=gray!25,dashed] (0,50)--(5,50) node[left]{\scriptsize 50};
\draw[draw=gray!25,dashed] (0,75)--(5,75) node[left]{\scriptsize 75};
\draw[draw=gray!25,dashed] (0,100)--(5,100) node[left]{\scriptsize 100};
\draw[very thick,draw=cyan] plot coordinates {(0,12) (1,23) (2,43) (3,68) (4,86) (5,92)};
\fill[cyan] (0,12) circle (1.5pt);
\fill[cyan] (1,23) circle (1.5pt);
\fill[cyan] (2,43) circle (1.5pt);
\fill[cyan] (3,68) circle (1.5pt);
\fill[cyan] (4,86) circle (1.5pt);
\fill[cyan] (5,92) circle (1.5pt);
\draw[very thick,draw=amber] plot coordinates {(0,8) (1,12) (2,19) (3,31) (4,55) (5,94)};
\fill[amber] (0,8) circle (1.5pt);
\fill[amber] (1,12) circle (1.5pt);
\fill[amber] (2,19) circle (1.5pt);
\fill[amber] (3,31) circle (1.5pt);
\fill[amber] (4,55) circle (1.5pt);
\fill[amber] (5,94) circle (1.5pt);
\node[below,font=\scriptsize] at (0,0) {1};
\node[below,font=\scriptsize] at (1,0) {2};
\node[below,font=\scriptsize] at (2,0) {4};
\node[below,font=\scriptsize] at (3,0) {8};
\node[below,font=\scriptsize] at (4,0) {16};
\node[below,font=\scriptsize] at (5,0) {32};
\end{tikzpicture}\par\smallskip \textcolor{cyan}{\rule{5mm}{1.2pt}} throughput\quad \textcolor{amber}{\rule{5mm}{1.2pt}} p95 latency\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Choose a workload-specific knee using TTFT, decode rate, queue time, and quality-qualified cost.}}
\subsection{Reproducibility is a chain of identities}
\badge{LINEAGE} \quad A model version is insufficient. You need immutable links among data, code, parameters, environment, prompts, evaluations, and the deployed artifact.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{Data}\\{\scriptsize snapshot}};
\node[visual,right=of v0] (v1) {\textbf{Code}\\{\scriptsize commit}};
\node[visual,right=of v1] (v2) {\textbf{Run}\\{\scriptsize params + env}};
\node[visual,right=of v2] (v3) {\textbf{Artifact}\\{\scriptsize content hash}};
\node[visual,right=of v3] (v4) {\textbf{Release}\\{\scriptsize evidence + rollback}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} When one link is mutable, exact reproduction and impact analysis become guesses.}}
\subsection{Attention builds a token-to-token routing table}
\badge{TENSOR SHAPES} \quad Queries choose where to read; keys advertise what each position contains; softmax turns similarities into routing weights; values carry the information mixed into the output.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{QK\textasciicircum{}T}\\{\scriptsize n by n scores}};
\node[visual,right=of v0] (v1) {\textbf{Scale}\\{\scriptsize divide by sqrt(d)}};
\node[visual,right=of v1] (v2) {\textbf{Mask}\\{\scriptsize hide future tokens}};
\node[visual,right=of v2] (v3) {\textbf{Softmax}\\{\scriptsize row probabilities}};
\node[visual,right=of v3] (v4) {\textbf{Multiply V}\\{\scriptsize weighted content}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Track the n-by-n score matrix separately from the n-by-d token representation.}}
\subsection{KV memory grows linearly with live context}
\badge{MEMORY MODEL} \quad Caching avoids recomputing old keys and values, but every live token consumes memory in every layer. GQA lowers the slope; paging reduces fragmentation, not live tensor bytes.
\begin{center}\begin{tikzpicture}[x=1.32cm,y=0.047cm]
\draw[->,draw=navy] (0,0)--(4.45,0) node[right]{\scriptsize context tokens (thousands)};
\draw[->,draw=navy] (0,0)--(0,108) node[above]{\scriptsize relative KV memory};
\draw[draw=gray!25,dashed] (0,25)--(4,25) node[left]{\scriptsize 25};
\draw[draw=gray!25,dashed] (0,50)--(4,50) node[left]{\scriptsize 50};
\draw[draw=gray!25,dashed] (0,75)--(4,75) node[left]{\scriptsize 75};
\draw[draw=gray!25,dashed] (0,100)--(4,100) node[left]{\scriptsize 100};
\draw[very thick,draw=cyan] plot coordinates {(0,6) (1,13) (2,25) (3,50) (4,100)};
\fill[cyan] (0,6) circle (1.5pt);
\fill[cyan] (1,13) circle (1.5pt);
\fill[cyan] (2,25) circle (1.5pt);
\fill[cyan] (3,50) circle (1.5pt);
\fill[cyan] (4,100) circle (1.5pt);
\draw[very thick,draw=amber] plot coordinates {(0,2) (1,3) (2,6) (3,13) (4,25)};
\fill[amber] (0,2) circle (1.5pt);
\fill[amber] (1,3) circle (1.5pt);
\fill[amber] (2,6) circle (1.5pt);
\fill[amber] (3,13) circle (1.5pt);
\fill[amber] (4,25) circle (1.5pt);
\node[below,font=\scriptsize] at (0,0) {1};
\node[below,font=\scriptsize] at (1,0) {2};
\node[below,font=\scriptsize] at (2,0) {4};
\node[below,font=\scriptsize] at (3,0) {8};
\node[below,font=\scriptsize] at (4,0) {16};
\end{tikzpicture}\par\smallskip \textcolor{cyan}{\rule{5mm}{1.2pt}} multi-head KV\quad \textcolor{amber}{\rule{5mm}{1.2pt}} grouped-query KV\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Capacity planning must use the distribution of concurrent sequence lengths, not one advertised context maximum.}}
\subsection{Backpressure must travel toward the caller}
\badge{SERVICE FLOW} \quad When downstream work saturates, bounded queues, deadlines, admission control, and cancellation prevent hidden overload from becoming an outage.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{Client}\\{\scriptsize deadline}};
\node[visual,right=of v0] (v1) {\textbf{Gateway}\\{\scriptsize admit/reject}};
\node[visual,right=of v1] (v2) {\textbf{Queue}\\{\scriptsize bounded}};
\node[visual,right=of v2] (v3) {\textbf{Worker}\\{\scriptsize cancelable}};
\node[visual,right=of v3] (v4) {\textbf{Dependency}\\{\scriptsize timeout + breaker}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} An unbounded queue converts visible rejection into invisible latency and memory growth.}}
\subsection{Choose concurrency from the waiting pattern}
\badge{EXECUTION MODEL} \quad Asyncio overlaps many cooperative waits, threads overlap blocking I/O with shared memory, and processes provide CPU parallelism with serialization and coordination costs.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{Asyncio}\\{\scriptsize many waits, one loop}};
\node[visual,right=of v0] (v1) {\textbf{Threads}\\{\scriptsize blocking I/O}};
\node[visual,right=of v1] (v2) {\textbf{Processes}\\{\scriptsize CPU parallelism}};
\node[visual,right=of v2] (v3) {\textbf{Queues}\\{\scriptsize ownership boundary}};
\node[visual,right=of v3] (v4) {\textbf{Cancel}\\{\scriptsize structured cleanup}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Start with whether work is CPU-bound, blocking, or awaitable; syntax comes afterward.}}
\subsection{Authority narrows at every trust boundary}
\badge{DEFENSE IN DEPTH} \quad Prompts, retrieved text, and model outputs are untrusted data. Capabilities come from deterministic policy, scoped credentials, sandbox limits, and explicit approval.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{Untrusted text}\\{\scriptsize no authority}};
\node[visual,right=of v0] (v1) {\textbf{Typed proposal}\\{\scriptsize schema validation}};
\node[visual,right=of v1] (v2) {\textbf{Policy}\\{\scriptsize allowlist + limits}};
\node[visual,right=of v2] (v3) {\textbf{Sandbox}\\{\scriptsize isolated effect}};
\node[visual,right=of v3] (v4) {\textbf{Audit}\\{\scriptsize evidence + revocation}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} A stronger prompt is not a security boundary; enforce effects outside the model.}}
\subsection{Reliability controls form a stabilizing loop}
\badge{OVERLOAD CONTROL} \quad Measure saturation, shed excess work, isolate failures, recover gradually, and feed the outcome into safer limits. Retries without this loop amplify overload.
\begin{center}\begin{tikzpicture}[visual/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=25mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=7mm]
\node[visual] (v0) {\textbf{Measure}\\{\scriptsize queue + tails}};
\node[visual,right=of v0] (v1) {\textbf{Admit}\\{\scriptsize budgeted load}};
\node[visual,right=of v1] (v2) {\textbf{Isolate}\\{\scriptsize bulkhead}};
\node[visual,right=of v2] (v3) {\textbf{Recover}\\{\scriptsize backoff + probe}};
\node[visual,right=of v3] (v4) {\textbf{Learn}\\{\scriptsize update limits}};
\draw[arr] (v0)--(v1);
\draw[arr] (v1)--(v2);
\draw[arr] (v2)--(v3);
\draw[arr] (v3)--(v4);
\draw[arr] (v4.south) to[bend left=32] node[below,font=\scriptsize]{feedback} (v0.south);
\end{tikzpicture}\end{center}
\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Remember.} Design for bounded degradation rather than assuming every dependency remains healthy.}}
\clearpage\section{Libraries and technologies field guide}
This section separates technology choice from general theory. Each profile starts with the workload, shows the abstract execution path, states language and deployment boundaries, gives a minimal use pattern, and makes the rejection case explicit. The online version loads a recognizable brand mark for every profile; this print edition uses a compact vector monogram so it remains reproducible without network access.
\subsection{9-layer technology landscape}
\begin{center}\begin{tikzpicture}[layer/.style={draw=cyan,fill=paper,rounded corners,align=left,text width=63mm,minimum height=13mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=6mm and 15mm]
\node[layer] (l0) {\textbf{01. Agent and application frameworks}\hfill 30 profiles\\{\scriptsize Who owns state, retries, and side effects?}};
\node[layer,below=of l0] (l1) {\textbf{02. Model, embedding, and optimization libraries}\hfill 16 profiles\\{\scriptsize Which representation and runtime boundary does the workload require?}};
\node[layer,below=of l1] (l2) {\textbf{03. Model serving, gateways, and inference}\hfill 39 profiles\\{\scriptsize What is the latency-throughput-memory-reliability frontier?}};
\node[layer,below=of l2] (l3) {\textbf{04. Retrieval, vector, and graph data}\hfill 26 profiles\\{\scriptsize Which query shape earns which index?}};
\node[layer,right=of l3] (l4) {\textbf{05. Observability and evaluation}\hfill 21 profiles\\{\scriptsize Can a regression be localized, not merely noticed?}};
\node[layer,above=of l4] (l5) {\textbf{06. Safety, validation, and policy guardrails}\hfill 14 profiles\\{\scriptsize Which control is deterministic, model-based, or policy-based?}};
\node[layer,above=of l5] (l6) {\textbf{07. MCP and tool connectivity}\hfill 15 profiles\\{\scriptsize Where is authority enforced?}};
\node[layer,above=of l6] (l7) {\textbf{08. Sandbox and code execution}\hfill 15 profiles\\{\scriptsize What survives a guest escape attempt?}};
\node[layer,above=of l7] (l8) {\textbf{09. Data, workflow, and ML lifecycle}\hfill 18 profiles\\{\scriptsize Can every effect be replayed or reconciled?}};
\draw[arr](l0)--(l1);
\draw[arr](l1)--(l2);
\draw[arr](l2)--(l3);
\draw[arr](l3)--(l4);
\draw[arr](l4)--(l5);
\draw[arr](l5)--(l6);
\draw[arr](l6)--(l7);
\draw[arr](l7)--(l8);
\end{tikzpicture}\end{center}
\clearpage\subsection{01. Agent and application frameworks}
\textbf{Selection question.} Who owns state, retries, and side effects?
\begin{center}\begin{tikzpicture}[tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]
\node[tech](c0){Intent + context};
\node[tech,right=of c0](c1){State, tools, control};
\node[tech,right=of c1](c2){Agent or RAG application};
\draw[arr](c0)--(c1);\draw[arr](c1)--(c2);
\end{tikzpicture}\end{center}
\paragraph{Comparison matrix.}
\begin{longtable}{@{}p{20mm}p{22mm}p{26mm}p{46mm}p{46mm}@{}}
\toprule\textbf{Tool} & \textbf{Kind} & \textbf{Language / boundary} & \textbf{Choose when} & \textbf{Reject when}\\\midrule\endhead
LangChain & Application library & Python, TypeScript; Library; self-hosted or LangSmith & You need provider-neutral components, rapid RAG construction, or standard tool calling. & A small direct SDK call is clearer, or the workflow needs durable explicit state without LangGraph.\\
LangGraph & Stateful orchestration library & Python, TypeScript; Library; self-hosted or LangSmith Deployment & The application branches, loops, pauses for approval, or must resume from persisted state. & A linear one-shot chain is enough, or business effects require a general workflow engine outside the agent graph.\\
Deep Agents & Agent harness & Python, TypeScript; Library; self-hosted or managed & You are building a long-horizon research or coding agent and want a batteries-included harness. & You need a narrowly bounded single agent, or cannot accept filesystem and delegation complexity.\\
LlamaIndex & Data and RAG framework & Python, TypeScript; Library; self-hosted or LlamaCloud & The hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG. & You need a tiny retrieval loop, or want framework-independent ingestion contracts.\\
Haystack & Pipeline framework & Python; Library and deepset Cloud & You want explicit modular pipelines, strong search heritage, and inspectable component connections. & Your team depends on TypeScript-first application code or only needs a direct model SDK.\\
Semantic Kernel & Enterprise agent SDK & C\#, Python, Java; Library; Azure-friendly self-hosted & Your estate is .NET, Java, Microsoft 365, or Azure and plugins must fit established dependency-injection patterns. & You need the broadest open-source Python integration catalog or a very small provider-specific client.\\
AutoGen & Event-driven agent framework & Python, .NET; Library and Studio tooling & You need multi-agent messaging, distributed runtimes, or research-friendly team patterns. & A deterministic graph or ordinary function composition expresses the workflow more safely.\\
CrewAI & Role-based agent framework & Python; Library and hosted control plane & A role/task metaphor helps a team prototype coordinated research, operations, or content workflows. & You need low-level state-machine semantics, strict event schemas, or non-Python first-class support.\\
PydanticAI & Typed agent framework & Python; Library; self-hosted & Python typing and Pydantic validation should define the boundary between models and application code. & You need a visual multi-agent workbench or a TypeScript-native runtime.\\
DSPy & Program optimization framework & Python; Library; self-hosted & You have representative training/evaluation examples and want systematic optimization rather than manual prompt editing. & The task lacks a measurable objective, stable examples, or budget for repeated model calls.\\
OpenAI Agents SDK & Agent SDK & Python, TypeScript; Library; provider and tool integrations & You want an intentionally small agent API with first-class OpenAI platform integration and inspectable handoffs. & You require a provider-neutral high-level RAG ecosystem or durable graph checkpoints as the main abstraction.\\
Google Agent Development Kit & Agent development kit & Python, Java, Go; Library; local, Cloud Run, or Vertex AI & You use Gemini or Google Cloud but want a framework that can also express model-agnostic agent teams. & Your application is tightly aligned to another provider or needs LangGraph-style explicit checkpoint semantics.\\
Mastra & AI application framework & TypeScript; Library and Mastra Cloud & The product team is TypeScript-first and wants agents and workflows close to application code. & Python-only model research integrations dominate or a general durable workflow engine is already mandated.\\
Zep & Agent memory and context platform & Python, TypeScript, REST; Cloud service or self-hosted components & Agents need durable, evolving user or entity context with retrieval and provenance beyond a short chat buffer. & Simple session history, explicit relational facts, or application-owned event sourcing is sufficient.\\
Mem0 & Agent memory layer & Python, TypeScript; Library, self-hosted platform, or managed service & Personalization across sessions matters and memory extraction can be evaluated against explicit product policy. & A bounded conversation window or application-owned profile table is clearer and easier to govern.\\
Agno & Multi-agent framework and runtime & Python; Library, AgentOS, or cloud & You want a Python-first framework that joins agent construction with sessions, memory, knowledge, and serving. & You need an explicit durable state graph or a tiny direct model loop.\\
Hugging Face smolagents & Minimal agent framework & Python; Library with local, hosted, or sandboxed models & You want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem. & You require durable workflow state, a broad managed control plane, or complex multi-tenant orchestration.\\
Letta & Stateful agent platform & Python, TypeScript, REST; Self-hosted server or managed cloud & Persistent agent memory and inspectable state are central product requirements. & A stateless request pipeline or application-owned profile store is simpler and sufficient.\\
Langroid & Multi-agent programming framework & Python; Open-source library & You want Python-first multi-agent collaboration with relatively transparent message and task control. & A managed platform, TypeScript runtime, or durable graph checkpointing is required.\\
BeeAI Framework & Open-source agent framework & Python, TypeScript; Library and platform integrations & You want open multi-language agent components with middleware and production integration points. & A vendor-native SDK or explicit graph runtime already matches the product boundary.\\
Strands Agents SDK & Agent harness SDK & Python, TypeScript; Open-source library across models and clouds & You need an in-process model-agnostic agent loop with production lifecycle controls and strong extension points. & A managed control plane or explicit durable workflow graph is the dominant requirement.\\
Claude Agent SDK & Coding and computer-use agent SDK & Python, TypeScript; Library backed by Claude Code capabilities & You need a coding or research agent with mature filesystem, terminal, and MCP behavior. & A provider-neutral application framework or narrowly scoped tool caller is required.\\
Vercel AI SDK & TypeScript AI application toolkit & TypeScript; Library for web, Node.js, and edge runtimes & A TypeScript web product needs streaming model and tool interactions close to its UI framework. & Durable multi-step orchestration or Python data and ML libraries dominate.\\
Microsoft Agent Framework & Enterprise agent and workflow SDK & Python, .NET; Library and Azure integrations & A Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET. & Another established framework already owns orchestration or the application only needs direct model calls.\\
CAMEL-AI & Multi-agent research framework & Python; Open-source library & You are researching multi-agent coordination, role specialization, or agent-society behavior. & A small production workflow needs strict control, simple operations, and narrow authority.\\
Rasa & Conversational AI and dialogue framework & Python, YAML; Self-hosted services or managed platform & Deterministic business conversations, channel integration, and controlled dialogue policy matter alongside LLM flexibility. & A free-form research agent or simple single-turn assistant is the product.\\
Langflow & Visual AI workflow builder & Python, TypeScript; Desktop, self-hosted server, or cloud & Cross-functional teams need to prototype and inspect component graphs visually before hardening them. & Code review, typed contracts, or high-scale runtime control must be the primary authoring model.\\
Dify & LLM application platform & Python, TypeScript; Self-hosted containers or managed cloud & A product team wants an integrated low-code environment for RAG and agent applications. & Fine-grained runtime ownership, minimal infrastructure, or code-native composition is mandatory.\\
Flowise & Visual LLM application builder & TypeScript; Self-hosted Node.js service or cloud & A JavaScript team wants fast visual assembly of LangChain-style RAG and agents. & Strict typed application code or durable general workflow semantics outweigh visual speed.\\
Cloudflare Agents SDK & Durable edge agent SDK & TypeScript; Cloudflare Workers and Durable Objects & You need globally distributed stateful agents and already accept the Cloudflare runtime model. & Portable self-hosting, GPU-local inference, or Python-first ML integration is required.\\
\bottomrule\end{longtable}
\subsubsection{LangChain}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Application library} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Composable model, prompt, retriever, tool, and agent interfaces with a broad integration ecosystem.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of LangChain as a application library between user request and model or tool result; its defining mechanism is runnable composition.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){User request};
\node[tech,right=of t0](t1){Runnable composition};
\node[tech,right=of t1](t2){Model or tool result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & User request -> Runnable composition -> Model or tool result\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Composable model, prompt, retriever, tool, and agent interfaces with a broad integration ecosystem. Think of LangChain as a application library between user request and model or tool result; its defining mechanism is runnable composition.
\item[2. Structure and execution] Follow the data path User request → Runnable composition → Model or tool result. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you need provider-neutral components, rapid RAG construction, or standard tool calling. Reject it when a small direct SDK call is clearer, or the workflow needs durable explicit state without LangGraph. Compare the same representative workload with LlamaIndex, Haystack, PydanticAI.
\item[5. Production proof] Start with the failure hypothesis: Default abstractions can hide prompts, state growth, retries, and provider-specific semantics. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; self-hosted or LangSmith\\
Choose when & You need provider-neutral components, rapid RAG construction, or standard tool calling.\\
Reject when & A small direct SDK call is clearer, or the workflow needs durable explicit state without LangGraph.\\
Primary failure & Default abstractions can hide prompts, state growth, retries, and provider-specific semantics.\\
Compare with & LlamaIndex; Haystack; PydanticAI\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install langchain
from langchain.agents import create_agent
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.langchain.com/oss/python/langchain/overview}{LangChain official documentation} \quad \href{https://docs.langchain.com/oss/python/langchain/agents}{LangChain agents and runtime}.
\subsubsection{LangGraph}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Stateful orchestration library} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Graph-based agent runtime with typed state, checkpoints, interrupts, streaming, and durable execution patterns.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of LangGraph as a stateful orchestration library between durable state and checkpointed outcome; its defining mechanism is graph node + transition.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Durable state};
\node[tech,right=of t0](t1){Graph node + transition};
\node[tech,right=of t1](t2){Checkpointed outcome};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Durable state -> Graph node + transition -> Checkpointed outcome\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Graph-based agent runtime with typed state, checkpoints, interrupts, streaming, and durable execution patterns. Think of LangGraph as a stateful orchestration library between durable state and checkpointed outcome; its defining mechanism is graph node + transition.
\item[2. Structure and execution] Follow the data path Durable state → Graph node + transition → Checkpointed outcome. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when the application branches, loops, pauses for approval, or must resume from persisted state. Reject it when a linear one-shot chain is enough, or business effects require a general workflow engine outside the agent graph. Compare the same representative workload with Temporal, AutoGen, custom state machine.
\item[5. Production proof] Start with the failure hypothesis: A retried node can duplicate an external effect unless the effect is idempotent and separately recorded. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; self-hosted or LangSmith Deployment\\
Choose when & The application branches, loops, pauses for approval, or must resume from persisted state.\\
Reject when & A linear one-shot chain is enough, or business effects require a general workflow engine outside the agent graph.\\
Primary failure & A retried node can duplicate an external effect unless the effect is idempotent and separately recorded.\\
Compare with & Temporal; AutoGen; custom state machine\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install langgraph
from langgraph.graph import StateGraph
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.langchain.com/oss/python/langgraph/overview}{LangGraph official documentation} \quad \href{https://docs.langchain.com/oss/python/langgraph/graph-api}{LangGraph graph API} \quad \href{https://docs.langchain.com/oss/python/langgraph/persistence}{LangGraph persistence design}.
\subsubsection{Deep Agents}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries DA}}}\quad \badge{Agent harness} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip LangGraph-based harness with planning, filesystem context, subagents, memory, skills, and pluggable execution backends.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Deep Agents as a agent harness between task + files and verified artifact; its defining mechanism is plan, delegate, use skills.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Task + files};
\node[tech,right=of t0](t1){Plan, delegate, use skills};
\node[tech,right=of t1](t2){Verified artifact};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Task + files -> Plan, delegate, use skills -> Verified artifact\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] LangGraph-based harness with planning, filesystem context, subagents, memory, skills, and pluggable execution backends. Think of Deep Agents as a agent harness between task + files and verified artifact; its defining mechanism is plan, delegate, use skills.
\item[2. Structure and execution] Follow the data path Task + files → Plan, delegate, use skills → Verified artifact. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you are building a long-horizon research or coding agent and want a batteries-included harness. Reject it when you need a narrowly bounded single agent, or cannot accept filesystem and delegation complexity. Compare the same representative workload with LangGraph, Claude Agent SDK, OpenAI Agents SDK.
\item[5. Production proof] Start with the failure hypothesis: Giving the harness a powerful backend without matching permission rules turns convenience into broad ambient authority. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; self-hosted or managed\\
Choose when & You are building a long-horizon research or coding agent and want a batteries-included harness.\\
Reject when & You need a narrowly bounded single agent, or cannot accept filesystem and delegation complexity.\\
Primary failure & Giving the harness a powerful backend without matching permission rules turns convenience into broad ambient authority.\\
Compare with & LangGraph; Claude Agent SDK; OpenAI Agents SDK\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install deepagents
from deepagents import create_deep_agent
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.langchain.com/oss/python/deepagents/overview}{Deep Agents official documentation} \quad \href{https://docs.langchain.com/oss/python/deepagents/harness}{Deep Agents harness} \quad \href{https://docs.langchain.com/oss/python/deepagents/backends}{Deep Agents backends}.
\subsubsection{LlamaIndex}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Data and RAG framework} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Connectors, parsing, indexes, retrievers, query engines, workflows, and agents centered on private-data applications.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of LlamaIndex as a data and rag framework between documents and evidence-bound response; its defining mechanism is parse, index, retrieve.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Documents};
\node[tech,right=of t0](t1){Parse, index, retrieve};
\node[tech,right=of t1](t2){Evidence-bound response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Documents -> Parse, index, retrieve -> Evidence-bound response\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Connectors, parsing, indexes, retrievers, query engines, workflows, and agents centered on private-data applications. Think of LlamaIndex as a data and rag framework between documents and evidence-bound response; its defining mechanism is parse, index, retrieve.
\item[2. Structure and execution] Follow the data path Documents → Parse, index, retrieve → Evidence-bound response. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when the hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG. Reject it when you need a tiny retrieval loop, or want framework-independent ingestion contracts. Compare the same representative workload with Haystack, LangChain, custom retrieval services.
\item[5. Production proof] Start with the failure hypothesis: Convenient ingestion defaults can erase source structure, provenance, and version identity. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; self-hosted or LlamaCloud\\
Choose when & The hard part is ingesting heterogeneous data and composing advanced retrieval or GraphRAG.\\
Reject when & You need a tiny retrieval loop, or want framework-independent ingestion contracts.\\
Primary failure & Convenient ingestion defaults can erase source structure, provenance, and version identity.\\
Compare with & Haystack; LangChain; custom retrieval services\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install llama-index
from llama_index.core import VectorStoreIndex
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.llamaindex.ai/en/stable/}{LlamaIndex official documentation}.
\subsubsection{Haystack}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries H}}}\quad \badge{Pipeline framework} \quad \textbf{Languages:} Python
\par\smallskip Typed components and pipelines for search, RAG, agents, evaluation, and production integration.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Haystack as a pipeline framework between documents + query and answer + metadata; its defining mechanism is pipeline components.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Documents + query};
\node[tech,right=of t0](t1){Pipeline components};
\node[tech,right=of t1](t2){Answer + metadata};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Documents + query -> Pipeline components -> Answer + metadata\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Typed components and pipelines for search, RAG, agents, evaluation, and production integration. Think of Haystack as a pipeline framework between documents + query and answer + metadata; its defining mechanism is pipeline components.
\item[2. Structure and execution] Follow the data path Documents + query → Pipeline components → Answer + metadata. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you want explicit modular pipelines, strong search heritage, and inspectable component connections. Reject it when your team depends on TypeScript-first application code or only needs a direct model SDK. Compare the same representative workload with LlamaIndex, LangChain, DSPy.
\item[5. Production proof] Start with the failure hypothesis: A visually clean pipeline can still conceal incompatible document schemas or unmeasured retriever quality. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library and deepset Cloud\\
Choose when & You want explicit modular pipelines, strong search heritage, and inspectable component connections.\\
Reject when & Your team depends on TypeScript-first application code or only needs a direct model SDK.\\
Primary failure & A visually clean pipeline can still conceal incompatible document schemas or unmeasured retriever quality.\\
Compare with & LlamaIndex; LangChain; DSPy\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install haystack-ai
from haystack import Pipeline
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.haystack.deepset.ai/docs/intro}{Haystack official documentation}.
\subsubsection{Semantic Kernel}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries SK}}}\quad \badge{Enterprise agent SDK} \quad \textbf{Languages:} C\# / Python / Java
\par\smallskip Microsoft SDK for agents, plugins, process orchestration, memory integrations, and enterprise application embedding.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Semantic Kernel as a enterprise agent sdk between enterprise context and governed ai feature; its defining mechanism is kernel + plugins.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Enterprise context};
\node[tech,right=of t0](t1){Kernel + plugins};
\node[tech,right=of t1](t2){Governed AI feature};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Enterprise context -> Kernel + plugins -> Governed AI feature\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Microsoft SDK for agents, plugins, process orchestration, memory integrations, and enterprise application embedding. Think of Semantic Kernel as a enterprise agent sdk between enterprise context and governed ai feature; its defining mechanism is kernel + plugins.
\item[2. Structure and execution] Follow the data path Enterprise context → Kernel + plugins → Governed AI feature. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when your estate is .NET, Java, Microsoft 365, or Azure and plugins must fit established dependency-injection patterns. Reject it when you need the broadest open-source Python integration catalog or a very small provider-specific client. Compare the same representative workload with LangChain, AutoGen, OpenAI Agents SDK.
\item[5. Production proof] Start with the failure hypothesis: Treating natural-language planning as authorization can bypass the surrounding enterprise control plane. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; Azure-friendly self-hosted\\
Choose when & Your estate is .NET, Java, Microsoft 365, or Azure and plugins must fit established dependency-injection patterns.\\
Reject when & You need the broadest open-source Python integration catalog or a very small provider-specific client.\\
Primary failure & Treating natural-language planning as authorization can bypass the surrounding enterprise control plane.\\
Compare with & LangChain; AutoGen; OpenAI Agents SDK\\
\end{tabularx}
\paragraph{Minimal use in C\# / Python / Java.}
\begin{Verbatim}[fontsize=\scriptsize]
dotnet add package Microsoft.SemanticKernel
var kernel = Kernel.CreateBuilder().Build();
\end{Verbatim}
\textbf{Primary sources.} \href{https://learn.microsoft.com/en-us/semantic-kernel/overview/}{Semantic Kernel official documentation}.
\subsubsection{AutoGen}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries A}}}\quad \badge{Event-driven agent framework} \quad \textbf{Languages:} Python / .NET
\par\smallskip Microsoft framework for conversational, event-driven, single-agent and multi-agent systems with extensible runtimes.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of AutoGen as a event-driven agent framework between events + roles and team outcome; its defining mechanism is agent messages + tools.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Events + roles};
\node[tech,right=of t0](t1){Agent messages + tools};
\node[tech,right=of t1](t2){Team outcome};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Events + roles -> Agent messages + tools -> Team outcome\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Microsoft framework for conversational, event-driven, single-agent and multi-agent systems with extensible runtimes. Think of AutoGen as a event-driven agent framework between events + roles and team outcome; its defining mechanism is agent messages + tools.
\item[2. Structure and execution] Follow the data path Events + roles → Agent messages + tools → Team outcome. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you need multi-agent messaging, distributed runtimes, or research-friendly team patterns. Reject it when a deterministic graph or ordinary function composition expresses the workflow more safely. Compare the same representative workload with LangGraph, CrewAI, Semantic Kernel.
\item[5. Production proof] Start with the failure hypothesis: Free-form agent conversations can loop, amplify cost, and obscure which participant owns a decision. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library and Studio tooling\\
Choose when & You need multi-agent messaging, distributed runtimes, or research-friendly team patterns.\\
Reject when & A deterministic graph or ordinary function composition expresses the workflow more safely.\\
Primary failure & Free-form agent conversations can loop, amplify cost, and obscure which participant owns a decision.\\
Compare with & LangGraph; CrewAI; Semantic Kernel\\
\end{tabularx}
\paragraph{Minimal use in Python / .NET.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install autogen-agentchat
from autogen_agentchat.agents import AssistantAgent
\end{Verbatim}
\textbf{Primary sources.} \href{https://microsoft.github.io/autogen/stable/}{AutoGen official documentation}.
\subsubsection{CrewAI}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries C}}}\quad \badge{Role-based agent framework} \quad \textbf{Languages:} Python
\par\smallskip Agents, crews, tasks, and flows for role-oriented collaboration and event-driven automation.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of CrewAI as a role-based agent framework between goal + roles and delegated deliverable; its defining mechanism is crew or flow.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Goal + roles};
\node[tech,right=of t0](t1){Crew or flow};
\node[tech,right=of t1](t2){Delegated deliverable};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Goal + roles -> Crew or flow -> Delegated deliverable\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Agents, crews, tasks, and flows for role-oriented collaboration and event-driven automation. Think of CrewAI as a role-based agent framework between goal + roles and delegated deliverable; its defining mechanism is crew or flow.
\item[2. Structure and execution] Follow the data path Goal + roles → Crew or flow → Delegated deliverable. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when a role/task metaphor helps a team prototype coordinated research, operations, or content workflows. Reject it when you need low-level state-machine semantics, strict event schemas, or non-Python first-class support. Compare the same representative workload with AutoGen, LangGraph, PydanticAI.
\item[5. Production proof] Start with the failure hypothesis: Role labels can suggest organizational rigor while retries, authority, and shared memory remain underspecified. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library and hosted control plane\\
Choose when & A role/task metaphor helps a team prototype coordinated research, operations, or content workflows.\\
Reject when & You need low-level state-machine semantics, strict event schemas, or non-Python first-class support.\\
Primary failure & Role labels can suggest organizational rigor while retries, authority, and shared memory remain underspecified.\\
Compare with & AutoGen; LangGraph; PydanticAI\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install crewai
from crewai import Agent, Crew, Task
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.crewai.com/}{CrewAI official documentation}.
\subsubsection{PydanticAI}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries P}}}\quad \badge{Typed agent framework} \quad \textbf{Languages:} Python
\par\smallskip Model-agnostic agents with typed dependencies, tools, structured results, validation, testing, and graph support.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of PydanticAI as a typed agent framework between typed dependencies and typed result; its defining mechanism is agent + validators.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Typed dependencies};
\node[tech,right=of t0](t1){Agent + validators};
\node[tech,right=of t1](t2){Typed result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Typed dependencies -> Agent + validators -> Typed result\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Model-agnostic agents with typed dependencies, tools, structured results, validation, testing, and graph support. Think of PydanticAI as a typed agent framework between typed dependencies and typed result; its defining mechanism is agent + validators.
\item[2. Structure and execution] Follow the data path Typed dependencies → Agent + validators → Typed result. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when python typing and Pydantic validation should define the boundary between models and application code. Reject it when you need a visual multi-agent workbench or a TypeScript-native runtime. Compare the same representative workload with OpenAI Agents SDK, LangChain, Semantic Kernel.
\item[5. Production proof] Start with the failure hypothesis: Schema-valid output can still be false, unsafe, or unauthorized; semantic checks remain necessary. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; self-hosted\\
Choose when & Python typing and Pydantic validation should define the boundary between models and application code.\\
Reject when & You need a visual multi-agent workbench or a TypeScript-native runtime.\\
Primary failure & Schema-valid output can still be false, unsafe, or unauthorized; semantic checks remain necessary.\\
Compare with & OpenAI Agents SDK; LangChain; Semantic Kernel\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install pydantic-ai
from pydantic_ai import Agent
\end{Verbatim}
\textbf{Primary sources.} \href{https://ai.pydantic.dev/}{PydanticAI official documentation}.
\subsubsection{DSPy}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries D}}}\quad \badge{Program optimization framework} \quad \textbf{Languages:} Python
\par\smallskip Declarative LM modules and optimizers that compile prompts or weights against examples and metrics.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of DSPy as a program optimization framework between examples + metric and optimized predictor; its defining mechanism is compile lm program.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Examples + metric};
\node[tech,right=of t0](t1){Compile LM program};
\node[tech,right=of t1](t2){Optimized predictor};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Examples + metric -> Compile LM program -> Optimized predictor\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Declarative LM modules and optimizers that compile prompts or weights against examples and metrics. Think of DSPy as a program optimization framework between examples + metric and optimized predictor; its defining mechanism is compile lm program.
\item[2. Structure and execution] Follow the data path Examples + metric → Compile LM program → Optimized predictor. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you have representative training/evaluation examples and want systematic optimization rather than manual prompt editing. Reject it when the task lacks a measurable objective, stable examples, or budget for repeated model calls. Compare the same representative workload with manual prompting, LangChain, prompt optimizers.
\item[5. Production proof] Start with the failure hypothesis: Optimizing against a narrow metric or leaky development set produces impressive but brittle programs. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; self-hosted\\
Choose when & You have representative training/evaluation examples and want systematic optimization rather than manual prompt editing.\\
Reject when & The task lacks a measurable objective, stable examples, or budget for repeated model calls.\\
Primary failure & Optimizing against a narrow metric or leaky development set produces impressive but brittle programs.\\
Compare with & manual prompting; LangChain; prompt optimizers\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install dspy
import dspy; predictor = dspy.Predict('question -> answer')
\end{Verbatim}
\textbf{Primary sources.} \href{https://dspy.ai/}{DSPy official documentation}.
\subsubsection{OpenAI Agents SDK}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries OAS}}}\quad \badge{Agent SDK} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Small primitives for agents, handoffs, guardrails, sessions, tracing, and hosted or local tools.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of OpenAI Agents SDK as a agent sdk between input + tools and traced result; its defining mechanism is agent loop + handoffs.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Input + tools};
\node[tech,right=of t0](t1){Agent loop + handoffs};
\node[tech,right=of t1](t2){Traced result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Input + tools -> Agent loop + handoffs -> Traced result\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Small primitives for agents, handoffs, guardrails, sessions, tracing, and hosted or local tools. Think of OpenAI Agents SDK as a agent sdk between input + tools and traced result; its defining mechanism is agent loop + handoffs.
\item[2. Structure and execution] Follow the data path Input + tools → Agent loop + handoffs → Traced result. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you want an intentionally small agent API with first-class OpenAI platform integration and inspectable handoffs. Reject it when you require a provider-neutral high-level RAG ecosystem or durable graph checkpoints as the main abstraction. Compare the same representative workload with PydanticAI, LangGraph, Google ADK.
\item[5. Production proof] Start with the failure hypothesis: A handoff can silently expand capability scope unless tools and session state are narrowed per receiving agent. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; provider and tool integrations\\
Choose when & You want an intentionally small agent API with first-class OpenAI platform integration and inspectable handoffs.\\
Reject when & You require a provider-neutral high-level RAG ecosystem or durable graph checkpoints as the main abstraction.\\
Primary failure & A handoff can silently expand capability scope unless tools and session state are narrowed per receiving agent.\\
Compare with & PydanticAI; LangGraph; Google ADK\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install openai-agents
from agents import Agent, Runner
\end{Verbatim}
\textbf{Primary sources.} \href{https://openai.github.io/openai-agents-python/}{OpenAI Agents SDK official documentation}.
\subsubsection{Google Agent Development Kit}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries GAD}}}\quad \badge{Agent development kit} \quad \textbf{Languages:} Python / Java / Go
\par\smallskip Google framework for model-flexible agents, tools, sessions, evaluation, multi-agent patterns, and deployment.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Google Agent Development Kit as a agent development kit between user/session and deployable service; its defining mechanism is agent + tools + events.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){User/session};
\node[tech,right=of t0](t1){Agent + tools + events};
\node[tech,right=of t1](t2){Deployable service};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & User/session -> Agent + tools + events -> Deployable service\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Google framework for model-flexible agents, tools, sessions, evaluation, multi-agent patterns, and deployment. Think of Google Agent Development Kit as a agent development kit between user/session and deployable service; its defining mechanism is agent + tools + events.
\item[2. Structure and execution] Follow the data path User/session → Agent + tools + events → Deployable service. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you use Gemini or Google Cloud but want a framework that can also express model-agnostic agent teams. Reject it when your application is tightly aligned to another provider or needs LangGraph-style explicit checkpoint semantics. Compare the same representative workload with OpenAI Agents SDK, Semantic Kernel, LangGraph.
\item[5. Production proof] Start with the failure hypothesis: Local success can mask identity, quota, state-store, and region behavior at managed deployment time. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; local, Cloud Run, or Vertex AI\\
Choose when & You use Gemini or Google Cloud but want a framework that can also express model-agnostic agent teams.\\
Reject when & Your application is tightly aligned to another provider or needs LangGraph-style explicit checkpoint semantics.\\
Primary failure & Local success can mask identity, quota, state-store, and region behavior at managed deployment time.\\
Compare with & OpenAI Agents SDK; Semantic Kernel; LangGraph\\
\end{tabularx}
\paragraph{Minimal use in Python / Java / Go.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install google-adk
from google.adk.agents import Agent
\end{Verbatim}
\textbf{Primary sources.} \href{https://google.github.io/adk-docs/}{Google Agent Development Kit official documentation}.
\subsubsection{Mastra}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries M}}}\quad \badge{AI application framework} \quad \textbf{Languages:} TypeScript
\par\smallskip TypeScript-native agents, workflows, RAG, memory, evaluations, and observability for modern web stacks.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Mastra as a ai application framework between app event and typed application result; its defining mechanism is agent or workflow.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){App event};
\node[tech,right=of t0](t1){Agent or workflow};
\node[tech,right=of t1](t2){Typed application result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & App event -> Agent or workflow -> Typed application result\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] TypeScript-native agents, workflows, RAG, memory, evaluations, and observability for modern web stacks. Think of Mastra as a ai application framework between app event and typed application result; its defining mechanism is agent or workflow.
\item[2. Structure and execution] Follow the data path App event → Agent or workflow → Typed application result. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when the product team is TypeScript-first and wants agents and workflows close to application code. Reject it when python-only model research integrations dominate or a general durable workflow engine is already mandated. Compare the same representative workload with LangChain.js, Vercel AI SDK, Temporal.
\item[5. Production proof] Start with the failure hypothesis: In-process workflow state without deliberate persistence and idempotency can be lost or replay side effects. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library and Mastra Cloud\\
Choose when & The product team is TypeScript-first and wants agents and workflows close to application code.\\
Reject when & Python-only model research integrations dominate or a general durable workflow engine is already mandated.\\
Primary failure & In-process workflow state without deliberate persistence and idempotency can be lost or replay side effects.\\
Compare with & LangChain.js; Vercel AI SDK; Temporal\\
\end{tabularx}
\paragraph{Minimal use in TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
npm install @mastra/core
import { Mastra } from '@mastra/core'
\end{Verbatim}
\textbf{Primary sources.} \href{https://mastra.ai/docs}{Mastra official documentation}.
\subsubsection{Zep}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries Z}}}\quad \badge{Agent memory and context platform} \quad \textbf{Languages:} Python / TypeScript / REST
\par\smallskip Temporal knowledge and conversation memory designed to assemble relevant user and agent context over time.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Zep as a agent memory and context platform between conversation events and context for the next turn; its defining mechanism is temporal memory graph.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Conversation events};
\node[tech,right=of t0](t1){Temporal memory graph};
\node[tech,right=of t1](t2){Context for the next turn};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Conversation events -> Temporal memory graph -> Context for the next turn\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Temporal knowledge and conversation memory designed to assemble relevant user and agent context over time. Think of Zep as a agent memory and context platform between conversation events and context for the next turn; its defining mechanism is temporal memory graph.
\item[2. Structure and execution] Follow the data path Conversation events → Temporal memory graph → Context for the next turn. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when agents need durable, evolving user or entity context with retrieval and provenance beyond a short chat buffer. Reject it when simple session history, explicit relational facts, or application-owned event sourcing is sufficient. Compare the same representative workload with Mem0, Neo4j, PostgreSQL.
\item[5. Production proof] Start with the failure hypothesis: Automatically extracted memories can become stale, cross tenant boundaries, or be treated as authority without provenance and correction. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Cloud service or self-hosted components\\
Choose when & Agents need durable, evolving user or entity context with retrieval and provenance beyond a short chat buffer.\\
Reject when & Simple session history, explicit relational facts, or application-owned event sourcing is sufficient.\\
Primary failure & Automatically extracted memories can become stale, cross tenant boundaries, or be treated as authority without provenance and correction.\\
Compare with & Mem0; Neo4j; PostgreSQL\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install zep-cloud
from zep_cloud.client import Zep
\end{Verbatim}
\textbf{Primary sources.} \href{https://help.getzep.com/}{Zep official documentation}.
\subsubsection{Mem0}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries M}}}\quad \badge{Agent memory layer} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Extracts, stores, updates, and retrieves personalized memories for agents across sessions and data backends.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Mem0 as a agent memory layer between interaction + identity and selected context; its defining mechanism is extract and reconcile memory.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Interaction + identity};
\node[tech,right=of t0](t1){Extract and reconcile memory};
\node[tech,right=of t1](t2){Selected context};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Interaction + identity -> Extract and reconcile memory -> Selected context\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Extracts, stores, updates, and retrieves personalized memories for agents across sessions and data backends. Think of Mem0 as a agent memory layer between interaction + identity and selected context; its defining mechanism is extract and reconcile memory.
\item[2. Structure and execution] Follow the data path Interaction + identity → Extract and reconcile memory → Selected context. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when personalization across sessions matters and memory extraction can be evaluated against explicit product policy. Reject it when a bounded conversation window or application-owned profile table is clearer and easier to govern. Compare the same representative workload with Zep, LangGraph stores, custom memory service.
\item[5. Production proof] Start with the failure hypothesis: A plausible extracted preference can be wrong, overshared, or impossible for a user to inspect and delete. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library, self-hosted platform, or managed service\\
Choose when & Personalization across sessions matters and memory extraction can be evaluated against explicit product policy.\\
Reject when & A bounded conversation window or application-owned profile table is clearer and easier to govern.\\
Primary failure & A plausible extracted preference can be wrong, overshared, or impossible for a user to inspect and delete.\\
Compare with & Zep; LangGraph stores; custom memory service\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install mem0ai
from mem0 import Memory
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.mem0.ai/}{Mem0 official documentation}.
\subsubsection{Agno}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries A}}}\quad \badge{Multi-agent framework and runtime} \quad \textbf{Languages:} Python
\par\smallskip Framework for model-agnostic agents, teams, workflows, knowledge, memory, tools, and an operational runtime.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Agno as a multi-agent framework and runtime between request + session and tool result or response; its defining mechanism is agent or team runtime.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Request + session};
\node[tech,right=of t0](t1){Agent or team runtime};
\node[tech,right=of t1](t2){Tool result or response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Request + session -> Agent or team runtime -> Tool result or response\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Framework for model-agnostic agents, teams, workflows, knowledge, memory, tools, and an operational runtime. Think of Agno as a multi-agent framework and runtime between request + session and tool result or response; its defining mechanism is agent or team runtime.
\item[2. Structure and execution] Follow the data path Request + session → Agent or team runtime → Tool result or response. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you want a Python-first framework that joins agent construction with sessions, memory, knowledge, and serving. Reject it when you need an explicit durable state graph or a tiny direct model loop. Compare the same representative workload with LangGraph, CrewAI, PydanticAI.
\item[5. Production proof] Start with the failure hypothesis: Convenient shared memory and team abstractions can blur tenant boundaries and the owner of a side effect. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library, AgentOS, or cloud\\
Choose when & You want a Python-first framework that joins agent construction with sessions, memory, knowledge, and serving.\\
Reject when & You need an explicit durable state graph or a tiny direct model loop.\\
Primary failure & Convenient shared memory and team abstractions can blur tenant boundaries and the owner of a side effect.\\
Compare with & LangGraph; CrewAI; PydanticAI\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install agno
from agno.agent import Agent
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.agno.com/}{Agno official documentation}.
\subsubsection{Hugging Face smolagents}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries HFS}}}\quad \badge{Minimal agent framework} \quad \textbf{Languages:} Python
\par\smallskip Compact framework centered on CodeAgent, ToolCallingAgent, typed tools, model adapters, and optional sandbox execution.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Hugging Face smolagents as a minimal agent framework between task + tools and observed answer; its defining mechanism is code or tool-calling loop.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Task + tools};
\node[tech,right=of t0](t1){Code or tool-calling loop};
\node[tech,right=of t1](t2){Observed answer};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Task + tools -> Code or tool-calling loop -> Observed answer\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Compact framework centered on CodeAgent, ToolCallingAgent, typed tools, model adapters, and optional sandbox execution. Think of Hugging Face smolagents as a minimal agent framework between task + tools and observed answer; its defining mechanism is code or tool-calling loop.
\item[2. Structure and execution] Follow the data path Task + tools → Code or tool-calling loop → Observed answer. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem. Reject it when you require durable workflow state, a broad managed control plane, or complex multi-tenant orchestration. Compare the same representative workload with OpenAI Agents SDK, PydanticAI, LangGraph.
\item[5. Production proof] Start with the failure hypothesis: Generated code can exceed intended authority unless imports, network, filesystem, time, and credentials are constrained. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library with local, hosted, or sandboxed models\\
Choose when & You want a small inspectable agent loop or code-generating agent integrated with the Hugging Face ecosystem.\\
Reject when & You require durable workflow state, a broad managed control plane, or complex multi-tenant orchestration.\\
Primary failure & Generated code can exceed intended authority unless imports, network, filesystem, time, and credentials are constrained.\\
Compare with & OpenAI Agents SDK; PydanticAI; LangGraph\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install smolagents
from smolagents import CodeAgent
\end{Verbatim}
\textbf{Primary sources.} \href{https://huggingface.co/docs/smolagents/index}{Hugging Face smolagents official documentation}.
\subsubsection{Letta}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Stateful agent platform} \quad \textbf{Languages:} Python / TypeScript / REST
\par\smallskip Stateful agent runtime built around explicit memory blocks, archival memory, tools, messages, and long-running agent identity.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Letta as a stateful agent platform between messages + memory blocks and updated memory + response; its defining mechanism is stateful agent loop.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Messages + memory blocks};
\node[tech,right=of t0](t1){Stateful agent loop};
\node[tech,right=of t1](t2){Updated memory + response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Messages + memory blocks -> Stateful agent loop -> Updated memory + response\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Stateful agent runtime built around explicit memory blocks, archival memory, tools, messages, and long-running agent identity. Think of Letta as a stateful agent platform between messages + memory blocks and updated memory + response; its defining mechanism is stateful agent loop.
\item[2. Structure and execution] Follow the data path Messages + memory blocks → Stateful agent loop → Updated memory + response. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when persistent agent memory and inspectable state are central product requirements. Reject it when a stateless request pipeline or application-owned profile store is simpler and sufficient. Compare the same representative workload with Mem0, Zep, LangGraph.
\item[5. Production proof] Start with the failure hypothesis: Derived memories can become stale or over-authoritative without provenance, correction, expiration, and deletion controls. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted server or managed cloud\\
Choose when & Persistent agent memory and inspectable state are central product requirements.\\
Reject when & A stateless request pipeline or application-owned profile store is simpler and sufficient.\\
Primary failure & Derived memories can become stale or over-authoritative without provenance, correction, expiration, and deletion controls.\\
Compare with & Mem0; Zep; LangGraph\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install letta-client
from letta_client import Letta
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.letta.com/}{Letta official documentation}.
\subsubsection{Langroid}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Multi-agent programming framework} \quad \textbf{Languages:} Python
\par\smallskip Message-oriented framework for agents, tasks, tools, retrieval, and multi-agent delegation with explicit Python components.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Langroid as a multi-agent programming framework between task + agent messages and result or delegated message; its defining mechanism is task orchestration + tools.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Task + agent messages};
\node[tech,right=of t0](t1){Task orchestration + tools};
\node[tech,right=of t1](t2){Result or delegated message};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Task + agent messages -> Task orchestration + tools -> Result or delegated message\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Message-oriented framework for agents, tasks, tools, retrieval, and multi-agent delegation with explicit Python components. Think of Langroid as a multi-agent programming framework between task + agent messages and result or delegated message; its defining mechanism is task orchestration + tools.
\item[2. Structure and execution] Follow the data path Task + agent messages → Task orchestration + tools → Result or delegated message. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you want Python-first multi-agent collaboration with relatively transparent message and task control. Reject it when a managed platform, TypeScript runtime, or durable graph checkpointing is required. Compare the same representative workload with AutoGen, CrewAI, LangGraph.
\item[5. Production proof] Start with the failure hypothesis: Conversational delegation can loop or lose ownership unless budgets, termination, and tool authority are explicit. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Open-source library\\
Choose when & You want Python-first multi-agent collaboration with relatively transparent message and task control.\\
Reject when & A managed platform, TypeScript runtime, or durable graph checkpointing is required.\\
Primary failure & Conversational delegation can loop or lose ownership unless budgets, termination, and tool authority are explicit.\\
Compare with & AutoGen; CrewAI; LangGraph\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install langroid
import langroid as lr
\end{Verbatim}
\textbf{Primary sources.} \href{https://langroid.github.io/langroid/}{Langroid official documentation}.
\subsubsection{BeeAI Framework}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries BF}}}\quad \badge{Open-source agent framework} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Framework for composable agents, tools, workflows, memory, middleware, and observability across model providers.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of BeeAI Framework as a open-source agent framework between request + context and tool calls + response; its defining mechanism is agent runtime + middleware.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Request + context};
\node[tech,right=of t0](t1){Agent runtime + middleware};
\node[tech,right=of t1](t2){Tool calls + response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Request + context -> Agent runtime + middleware -> Tool calls + response\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Framework for composable agents, tools, workflows, memory, middleware, and observability across model providers. Think of BeeAI Framework as a open-source agent framework between request + context and tool calls + response; its defining mechanism is agent runtime + middleware.
\item[2. Structure and execution] Follow the data path Request + context → Agent runtime + middleware → Tool calls + response. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you want open multi-language agent components with middleware and production integration points. Reject it when a vendor-native SDK or explicit graph runtime already matches the product boundary. Compare the same representative workload with LangChain, Mastra, Semantic Kernel.
\item[5. Production proof] Start with the failure hypothesis: Middleware ordering and implicit context propagation can create surprising policy or tracing gaps. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library and platform integrations\\
Choose when & You want open multi-language agent components with middleware and production integration points.\\
Reject when & A vendor-native SDK or explicit graph runtime already matches the product boundary.\\
Primary failure & Middleware ordering and implicit context propagation can create surprising policy or tracing gaps.\\
Compare with & LangChain; Mastra; Semantic Kernel\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install beeai-framework
from beeai_framework.agents import Agent
\end{Verbatim}
\textbf{Primary sources.} \href{https://framework.beeai.dev/}{BeeAI Framework official documentation}.
\subsubsection{Strands Agents SDK}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries SAS}}}\quad \badge{Agent harness SDK} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip SDK for building an agent harness with lifecycle controls, tools, hooks, sessions, multi-agent patterns, MCP, guardrails, tracing, and evaluation.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Strands Agents SDK as a agent harness sdk between prompt + tools and response + trace; its defining mechanism is controlled agent loop + hooks.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Prompt + tools};
\node[tech,right=of t0](t1){Controlled agent loop + hooks};
\node[tech,right=of t1](t2){Response + trace};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Prompt + tools -> Controlled agent loop + hooks -> Response + trace\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] SDK for building an agent harness with lifecycle controls, tools, hooks, sessions, multi-agent patterns, MCP, guardrails, tracing, and evaluation. Think of Strands Agents SDK as a agent harness sdk between prompt + tools and response + trace; its defining mechanism is controlled agent loop + hooks.
\item[2. Structure and execution] Follow the data path Prompt + tools → Controlled agent loop + hooks → Response + trace. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you need an in-process model-agnostic agent loop with production lifecycle controls and strong extension points. Reject it when a managed control plane or explicit durable workflow graph is the dominant requirement. Compare the same representative workload with OpenAI Agents SDK, Google ADK, LangGraph.
\item[5. Production proof] Start with the failure hypothesis: A model-driven loop may repeatedly select costly or high-impact tools unless hooks enforce budgets and authorization. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Open-source library across models and clouds\\
Choose when & You need an in-process model-agnostic agent loop with production lifecycle controls and strong extension points.\\
Reject when & A managed control plane or explicit durable workflow graph is the dominant requirement.\\
Primary failure & A model-driven loop may repeatedly select costly or high-impact tools unless hooks enforce budgets and authorization.\\
Compare with & OpenAI Agents SDK; Google ADK; LangGraph\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install strands-agents strands-agents-tools
from strands import Agent
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/strands-agents/harness-sdk}{Strands Agents SDK official documentation}.
\subsubsection{Claude Agent SDK}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries CAS}}}\quad \badge{Coding and computer-use agent SDK} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip SDK for building agents with Claude Code's tool loop, filesystem and shell tools, MCP, hooks, permissions, and sessions.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Claude Agent SDK as a coding and computer-use agent sdk between task + workspace and changed files + result; its defining mechanism is claude agent loop + tools.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Task + workspace};
\node[tech,right=of t0](t1){Claude agent loop + tools};
\node[tech,right=of t1](t2){Changed files + result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Task + workspace -> Claude agent loop + tools -> Changed files + result\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] SDK for building agents with Claude Code's tool loop, filesystem and shell tools, MCP, hooks, permissions, and sessions. Think of Claude Agent SDK as a coding and computer-use agent sdk between task + workspace and changed files + result; its defining mechanism is claude agent loop + tools.
\item[2. Structure and execution] Follow the data path Task + workspace → Claude agent loop + tools → Changed files + result. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you need a coding or research agent with mature filesystem, terminal, and MCP behavior. Reject it when a provider-neutral application framework or narrowly scoped tool caller is required. Compare the same representative workload with Deep Agents, OpenAI Agents SDK, smolagents.
\item[5. Production proof] Start with the failure hypothesis: Broad filesystem and shell access can turn prompt injection into code execution or secret exposure. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library backed by Claude Code capabilities\\
Choose when & You need a coding or research agent with mature filesystem, terminal, and MCP behavior.\\
Reject when & A provider-neutral application framework or narrowly scoped tool caller is required.\\
Primary failure & Broad filesystem and shell access can turn prompt injection into code execution or secret exposure.\\
Compare with & Deep Agents; OpenAI Agents SDK; smolagents\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
npm install @anthropic-ai/claude-agent-sdk
import { query } from '@anthropic-ai/claude-agent-sdk'
\end{Verbatim}
\textbf{Primary sources.} \href{https://platform.claude.com/docs/en/agent-sdk/overview}{Claude Agent SDK official documentation}.
\subsubsection{Vercel AI SDK}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries VAS}}}\quad \badge{TypeScript AI application toolkit} \quad \textbf{Languages:} TypeScript
\par\smallskip Provider-neutral toolkit for text and object generation, streaming UI, tools, agents, embeddings, and frontend chat state.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Vercel AI SDK as a typescript ai application toolkit between ui message + model config and typed stream + ui state; its defining mechanism is streaming generate or tool loop.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){UI message + model config};
\node[tech,right=of t0](t1){Streaming generate or tool loop};
\node[tech,right=of t1](t2){Typed stream + UI state};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & UI message + model config -> Streaming generate or tool loop -> Typed stream + UI state\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Provider-neutral toolkit for text and object generation, streaming UI, tools, agents, embeddings, and frontend chat state. Think of Vercel AI SDK as a typescript ai application toolkit between ui message + model config and typed stream + ui state; its defining mechanism is streaming generate or tool loop.
\item[2. Structure and execution] Follow the data path UI message + model config → Streaming generate or tool loop → Typed stream + UI state. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when a TypeScript web product needs streaming model and tool interactions close to its UI framework. Reject it when durable multi-step orchestration or Python data and ML libraries dominate. Compare the same representative workload with Mastra, LangChain.js, provider SDKs.
\item[5. Production proof] Start with the failure hypothesis: UI and server streams can diverge after retries or reconnects unless message ids and tool effects are durable. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library for web, Node.js, and edge runtimes\\
Choose when & A TypeScript web product needs streaming model and tool interactions close to its UI framework.\\
Reject when & Durable multi-step orchestration or Python data and ML libraries dominate.\\
Primary failure & UI and server streams can diverge after retries or reconnects unless message ids and tool effects are durable.\\
Compare with & Mastra; LangChain.js; provider SDKs\\
\end{tabularx}
\paragraph{Minimal use in TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
npm install ai
import { generateText } from 'ai'
\end{Verbatim}
\textbf{Primary sources.} \href{https://ai-sdk.dev/docs/introduction}{Vercel AI SDK official documentation}.
\subsubsection{Microsoft Agent Framework}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries MAF}}}\quad \badge{Enterprise agent and workflow SDK} \quad \textbf{Languages:} Python / .NET
\par\smallskip Microsoft framework unifying agent abstractions, workflows, middleware, tools, MCP, state, and enterprise hosting patterns.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Microsoft Agent Framework as a enterprise agent and workflow sdk between enterprise event + state and governed result; its defining mechanism is agent or workflow runtime.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Enterprise event + state};
\node[tech,right=of t0](t1){Agent or workflow runtime};
\node[tech,right=of t1](t2){Governed result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Enterprise event + state -> Agent or workflow runtime -> Governed result\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Microsoft framework unifying agent abstractions, workflows, middleware, tools, MCP, state, and enterprise hosting patterns. Think of Microsoft Agent Framework as a enterprise agent and workflow sdk between enterprise event + state and governed result; its defining mechanism is agent or workflow runtime.
\item[2. Structure and execution] Follow the data path Enterprise event + state → Agent or workflow runtime → Governed result. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when a Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET. Reject it when another established framework already owns orchestration or the application only needs direct model calls. Compare the same representative workload with Semantic Kernel, AutoGen, LangGraph.
\item[5. Production proof] Start with the failure hypothesis: Migration from predecessor abstractions can hide changed state, serialization, or tool-call semantics. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library and Azure integrations\\
Choose when & A Microsoft or Azure estate needs typed agents and graph workflows across Python and .NET.\\
Reject when & Another established framework already owns orchestration or the application only needs direct model calls.\\
Primary failure & Migration from predecessor abstractions can hide changed state, serialization, or tool-call semantics.\\
Compare with & Semantic Kernel; AutoGen; LangGraph\\
\end{tabularx}
\paragraph{Minimal use in Python / .NET.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install agent-framework
from agent_framework import Agent
\end{Verbatim}
\textbf{Primary sources.} \href{https://learn.microsoft.com/en-us/agent-framework/overview/}{Microsoft Agent Framework official documentation}.
\subsubsection{CAMEL-AI}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries C}}}\quad \badge{Multi-agent research framework} \quad \textbf{Languages:} Python
\par\smallskip Framework for communicative agents, role playing, societies, workforce orchestration, tools, memory, and simulation.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of CAMEL-AI as a multi-agent research framework between task + roles and coordinated output; its defining mechanism is agent society + messages.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Task + roles};
\node[tech,right=of t0](t1){Agent society + messages};
\node[tech,right=of t1](t2){Coordinated output};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Task + roles -> Agent society + messages -> Coordinated output\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Framework for communicative agents, role playing, societies, workforce orchestration, tools, memory, and simulation. Think of CAMEL-AI as a multi-agent research framework between task + roles and coordinated output; its defining mechanism is agent society + messages.
\item[2. Structure and execution] Follow the data path Task + roles → Agent society + messages → Coordinated output. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you are researching multi-agent coordination, role specialization, or agent-society behavior. Reject it when a small production workflow needs strict control, simple operations, and narrow authority. Compare the same representative workload with AutoGen, CrewAI, Langroid.
\item[5. Production proof] Start with the failure hypothesis: More agents can amplify correlated errors, token cost, and unclear accountability rather than improve quality. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Open-source library\\
Choose when & You are researching multi-agent coordination, role specialization, or agent-society behavior.\\
Reject when & A small production workflow needs strict control, simple operations, and narrow authority.\\
Primary failure & More agents can amplify correlated errors, token cost, and unclear accountability rather than improve quality.\\
Compare with & AutoGen; CrewAI; Langroid\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install camel-ai
from camel.agents import ChatAgent
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.camel-ai.org/}{CAMEL-AI official documentation}.
\subsubsection{Rasa}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries R}}}\quad \badge{Conversational AI and dialogue framework} \quad \textbf{Languages:} Python / YAML
\par\smallskip Conversation stack for NLU, dialogue policy, flows, business logic, channels, and enterprise conversational assistants.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Rasa as a conversational ai and dialogue framework between message + conversation state and action + response; its defining mechanism is nlu and dialogue policy.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Message + conversation state};
\node[tech,right=of t0](t1){NLU and dialogue policy};
\node[tech,right=of t1](t2){Action + response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Message + conversation state -> NLU and dialogue policy -> Action + response\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Conversation stack for NLU, dialogue policy, flows, business logic, channels, and enterprise conversational assistants. Think of Rasa as a conversational ai and dialogue framework between message + conversation state and action + response; its defining mechanism is nlu and dialogue policy.
\item[2. Structure and execution] Follow the data path Message + conversation state → NLU and dialogue policy → Action + response. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when deterministic business conversations, channel integration, and controlled dialogue policy matter alongside LLM flexibility. Reject it when a free-form research agent or simple single-turn assistant is the product. Compare the same representative workload with Semantic Kernel, LangGraph, custom dialogue state machine.
\item[5. Production proof] Start with the failure hypothesis: Training stories and flows can drift from production language, while generative fallbacks bypass deterministic policy. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted services or managed platform\\
Choose when & Deterministic business conversations, channel integration, and controlled dialogue policy matter alongside LLM flexibility.\\
Reject when & A free-form research agent or simple single-turn assistant is the product.\\
Primary failure & Training stories and flows can drift from production language, while generative fallbacks bypass deterministic policy.\\
Compare with & Semantic Kernel; LangGraph; custom dialogue state machine\\
\end{tabularx}
\paragraph{Minimal use in Python / YAML.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install rasa
rasa init
\end{Verbatim}
\textbf{Primary sources.} \href{https://rasa.com/docs/}{Rasa official documentation}.
\subsubsection{Langflow}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Visual AI workflow builder} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Visual builder for LangChain-oriented components, agents, RAG flows, API exposure, and reusable components.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Langflow as a visual ai workflow builder between inputs + visual graph and api or playground result; its defining mechanism is components + agent nodes.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Inputs + visual graph};
\node[tech,right=of t0](t1){Components + agent nodes};
\node[tech,right=of t1](t2){API or playground result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Inputs + visual graph -> Components + agent nodes -> API or playground result\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Visual builder for LangChain-oriented components, agents, RAG flows, API exposure, and reusable components. Think of Langflow as a visual ai workflow builder between inputs + visual graph and api or playground result; its defining mechanism is components + agent nodes.
\item[2. Structure and execution] Follow the data path Inputs + visual graph → Components + agent nodes → API or playground result. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when cross-functional teams need to prototype and inspect component graphs visually before hardening them. Reject it when code review, typed contracts, or high-scale runtime control must be the primary authoring model. Compare the same representative workload with Flowise, Dify, LangGraph.
\item[5. Production proof] Start with the failure hypothesis: A readable canvas can conceal mutable prompts, credentials, package versions, and weak test coverage. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Desktop, self-hosted server, or cloud\\
Choose when & Cross-functional teams need to prototype and inspect component graphs visually before hardening them.\\
Reject when & Code review, typed contracts, or high-scale runtime control must be the primary authoring model.\\
Primary failure & A readable canvas can conceal mutable prompts, credentials, package versions, and weak test coverage.\\
Compare with & Flowise; Dify; LangGraph\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install langflow
langflow run
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.langflow.org/}{Langflow official documentation}.
\subsubsection{Dify}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries D}}}\quad \badge{LLM application platform} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Platform for visual workflows, agents, RAG knowledge bases, prompt operations, model providers, APIs, and application publishing.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Dify as a llm application platform between dataset + workflow and published ai application; its defining mechanism is visual orchestration + model tools.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Dataset + workflow};
\node[tech,right=of t0](t1){Visual orchestration + model tools};
\node[tech,right=of t1](t2){Published AI application};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Dataset + workflow -> Visual orchestration + model tools -> Published AI application\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Platform for visual workflows, agents, RAG knowledge bases, prompt operations, model providers, APIs, and application publishing. Think of Dify as a llm application platform between dataset + workflow and published ai application; its defining mechanism is visual orchestration + model tools.
\item[2. Structure and execution] Follow the data path Dataset + workflow → Visual orchestration + model tools → Published AI application. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when a product team wants an integrated low-code environment for RAG and agent applications. Reject it when fine-grained runtime ownership, minimal infrastructure, or code-native composition is mandatory. Compare the same representative workload with Langflow, Flowise, custom LangGraph app.
\item[5. Production proof] Start with the failure hypothesis: Platform convenience can concentrate secrets, tenant data, vector stores, and model routing in one large trust boundary. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted containers or managed cloud\\
Choose when & A product team wants an integrated low-code environment for RAG and agent applications.\\
Reject when & Fine-grained runtime ownership, minimal infrastructure, or code-native composition is mandatory.\\
Primary failure & Platform convenience can concentrate secrets, tenant data, vector stores, and model routing in one large trust boundary.\\
Compare with & Langflow; Flowise; custom LangGraph app\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
git clone https://github.com/langgenius/dify
docker compose up -d
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.dify.ai/}{Dify official documentation}.
\subsubsection{Flowise}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries F}}}\quad \badge{Visual LLM application builder} \quad \textbf{Languages:} TypeScript
\par\smallskip Drag-and-drop builder for chatflows, agentflows, tools, vector stores, evaluations, and embeddable APIs.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Flowise as a visual llm application builder between request + visual flow and chat or api response; its defining mechanism is nodes, agents, and tools.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Request + visual flow};
\node[tech,right=of t0](t1){Nodes, agents, and tools};
\node[tech,right=of t1](t2){Chat or API response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Request + visual flow -> Nodes, agents, and tools -> Chat or API response\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Drag-and-drop builder for chatflows, agentflows, tools, vector stores, evaluations, and embeddable APIs. Think of Flowise as a visual llm application builder between request + visual flow and chat or api response; its defining mechanism is nodes, agents, and tools.
\item[2. Structure and execution] Follow the data path Request + visual flow → Nodes, agents, and tools → Chat or API response. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when a JavaScript team wants fast visual assembly of LangChain-style RAG and agents. Reject it when strict typed application code or durable general workflow semantics outweigh visual speed. Compare the same representative workload with Langflow, Dify, n8n.
\item[5. Production proof] Start with the failure hypothesis: Exported flows can depend on hidden credentials, component versions, and server-local state. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Self-hosted Node.js service or cloud\\
Choose when & A JavaScript team wants fast visual assembly of LangChain-style RAG and agents.\\
Reject when & Strict typed application code or durable general workflow semantics outweigh visual speed.\\
Primary failure & Exported flows can depend on hidden credentials, component versions, and server-local state.\\
Compare with & Langflow; Dify; n8n\\
\end{tabularx}
\paragraph{Minimal use in TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
npm install -g flowise
npx flowise start
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.flowiseai.com/}{Flowise official documentation}.
\subsubsection{Cloudflare Agents SDK}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries CAS}}}\quad \badge{Durable edge agent SDK} \quad \textbf{Languages:} TypeScript
\par\smallskip SDK for stateful agents on Workers with durable identity, scheduling, WebSockets, SQL-backed state, MCP, and platform services.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Cloudflare Agents SDK as a durable edge agent sdk between client event + agent identity and state update + streamed response; its defining mechanism is durable object agent runtime.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Client event + agent identity};
\node[tech,right=of t0](t1){Durable Object agent runtime};
\node[tech,right=of t1](t2){State update + streamed response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Client event + agent identity -> Durable Object agent runtime -> State update + streamed response\\
Control plane & Agent loop, graph, workflow, or handoff policy decides the next bounded action.\\
State and trust boundary & Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] SDK for stateful agents on Workers with durable identity, scheduling, WebSockets, SQL-backed state, MCP, and platform services. Think of Cloudflare Agents SDK as a durable edge agent sdk between client event + agent identity and state update + streamed response; its defining mechanism is durable object agent runtime.
\item[2. Structure and execution] Follow the data path Client event + agent identity → Durable Object agent runtime → State update + streamed response. Agent loop, graph, workflow, or handoff policy decides the next bounded action.
\item[3. State, security, and operations] Keep messages, checkpoints, memory, tenant identity, budgets, and side-effect receipts explicit; model text is not authoritative state.
\item[4. Selection decision] Choose it when you need globally distributed stateful agents and already accept the Cloudflare runtime model. Reject it when portable self-hosting, GPU-local inference, or Python-first ML integration is required. Compare the same representative workload with Mastra, Vercel AI SDK, Durable Objects directly.
\item[5. Production proof] Start with the failure hypothesis: Per-agent durable state does not automatically solve cross-agent transactions, regional data rules, or tool authorization. Trace termination, recovery, delegation, tool authorization, context growth, quality, latency, and cost by step.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Cloudflare Workers and Durable Objects\\
Choose when & You need globally distributed stateful agents and already accept the Cloudflare runtime model.\\
Reject when & Portable self-hosting, GPU-local inference, or Python-first ML integration is required.\\
Primary failure & Per-agent durable state does not automatically solve cross-agent transactions, regional data rules, or tool authorization.\\
Compare with & Mastra; Vercel AI SDK; Durable Objects directly\\
\end{tabularx}
\paragraph{Minimal use in TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
npm install agents
import { Agent } from 'agents'
\end{Verbatim}
\textbf{Primary sources.} \href{https://developers.cloudflare.com/agents/}{Cloudflare Agents SDK official documentation}.
\clearpage\subsection{02. Model, embedding, and optimization libraries}
\textbf{Selection question.} Which representation and runtime boundary does the workload require?
\begin{center}\begin{tikzpicture}[tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]
\node[tech](c0){Data + model objective};
\node[tech,right=of c0](c1){Train, adapt, embed, compile};
\node[tech,right=of c1](c2){Versioned model artifact};
\draw[arr](c0)--(c1);\draw[arr](c1)--(c2);
\end{tikzpicture}\end{center}
\paragraph{Comparison matrix.}
\begin{longtable}{@{}p{20mm}p{22mm}p{26mm}p{46mm}p{46mm}@{}}
\toprule\textbf{Tool} & \textbf{Kind} & \textbf{Language / boundary} & \textbf{Choose when} & \textbf{Reject when}\\\midrule\endhead
PyTorch & Tensor and deep-learning framework & Python, C++, CUDA; Local, distributed training, mobile, or compiled runtimes & Custom model training, fine-tuning, research kernels, or ecosystem compatibility requires an expressive tensor framework. & Inference only can use a smaller runtime, or an existing JAX stack already owns accelerator compilation.\\
JAX & Array transformation and compiler framework & Python, XLA; Accelerator-compiled local or distributed programs & Functional numerical programs need compiler optimization, explicit sharding, or large accelerator training at scale. & Dynamic Python mutation and the PyTorch ecosystem matter more than compilation and functional transforms.\\
Hugging Face Transformers & Pretrained model library & Python, PyTorch, TensorFlow, JAX; Library; training and inference across local or hosted compute & You need reproducible access to pretrained architectures, tokenizers, checkpoints, and fine-tuning utilities. & A narrow production server or a provider SDK already defines the complete model boundary.\\
Sentence Transformers & Embedding and reranker library & Python, PyTorch; Library; local training and inference & You need a well-tested Python path to train or run text embedding and reranking models. & Only a remote embedding API is allowed or a high-throughput server already owns preprocessing and batching.\\
Hugging Face PEFT & Parameter-efficient fine-tuning library & Python, PyTorch; Library on local or distributed training stacks & Many task adapters must be trained or served while full-model fine-tuning is too expensive. & The task needs broad weight changes, the base model is unstable, or adapter serving complexity exceeds savings.\\
bitsandbytes & Quantization and low-bit optimizer library & Python, CUDA; Library on supported accelerator stacks & GPU memory is the limiting resource and supported hardware plus measured quality make 8-bit or 4-bit execution useful. & Unsupported accelerators, kernels, model layers, or accuracy requirements make the low-bit path fragile.\\
ONNX Runtime & Cross-platform inference runtime & C++, C\#, Python, Java, JavaScript; CPU, GPU, mobile, browser, and hardware execution providers & A portable, non-LLM-specific inference artifact must run across languages and hardware targets. & The model cannot export correctly or needs an LLM-specialized token scheduler and KV-cache control plane.\\
OpenVINO & Intel inference toolkit & C++, Python; Intel CPU, GPU, and NPU runtimes & Deployment targets Intel CPUs, integrated GPUs, or NPUs and hardware-aware optimization matters. & A CUDA-first fleet or unsupported model operators dominate the workload.\\
FlagEmbedding & Retrieval model library & Python, PyTorch; Library; local or distributed training and inference & Multilingual or advanced retrieval experiments need BGE models, rerankers, or trainable retrieval recipes. & A managed embedding API or Sentence Transformers abstraction already meets quality and operations needs.\\
Instructor & Structured-output library & Python, TypeScript; Library over model-provider clients & Typed extraction is central and the team wants a thin layer over existing provider SDKs. & Free-form dialogue, durable agent state, or deterministic business validation is the actual problem.\\
Cohere Rerank & Managed cross-encoder reranking API & REST, Python, TypeScript, Java; Managed API & A managed multilingual or domain-capable reranker gives measured relevance lift on a bounded candidate set. & Data cannot leave the trust boundary, latency is too tight, or an open local reranker performs adequately.\\
Hugging Face Accelerate & Distributed training and inference helper & Python; Library across local, cluster, and cloud accelerators & Existing PyTorch code must scale across devices with minimal structural change. & You need a full trainer, serving runtime, or highly specialized parallelism implementation.\\
DeepSpeed & Distributed model optimization library & Python, C++, CUDA; Library on GPU clusters & Large models exceed a single device and the team can tune distributed optimizer, communication, and checkpoint behavior. & Small models, heterogeneous portability, or minimal cluster complexity is more important.\\
Hugging Face TRL & Post-training library & Python; Library on Transformers, Accelerate, and PEFT & You need reproducible post-training recipes integrated with Hugging Face models and datasets. & The requirement is base-model pretraining, low-level distributed kernels, or online production serving.\\
Axolotl & LLM fine-tuning framework & Python, YAML; CLI on workstations or GPU clusters & Teams want repeatable fine-tuning recipes without writing a custom training loop. & Deep research requires low-level algorithm changes or the target architecture is unsupported.\\
Unsloth & Efficient fine-tuning library & Python, Triton; Library for supported GPU and model combinations & Single-node or modest GPU training needs strong speed and memory efficiency with common model families. & You require arbitrary architectures, maximum backend portability, or full control of kernel implementation.\\
\bottomrule\end{longtable}
\subsubsection{PyTorch}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries P}}}\quad \badge{Tensor and deep-learning framework} \quad \textbf{Languages:} Python / C++ / CUDA
\par\smallskip Eager tensor programming, automatic differentiation, neural-network modules, distributed training, compilation, and a broad research ecosystem.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of PyTorch as a tensor and deep-learning framework between tensors + modules and model parameters + graph; its defining mechanism is autograd, compile, distribute.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Tensors + modules};
\node[tech,right=of t0](t1){Autograd, compile, distribute};
\node[tech,right=of t1](t2){Model parameters + graph};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Tensors + modules -> Autograd, compile, distribute -> Model parameters + graph\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Eager tensor programming, automatic differentiation, neural-network modules, distributed training, compilation, and a broad research ecosystem. Think of PyTorch as a tensor and deep-learning framework between tensors + modules and model parameters + graph; its defining mechanism is autograd, compile, distribute.
\item[2. Structure and execution] Follow the data path Tensors + modules → Autograd, compile, distribute → Model parameters + graph. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when custom model training, fine-tuning, research kernels, or ecosystem compatibility requires an expressive tensor framework. Reject it when inference only can use a smaller runtime, or an existing JAX stack already owns accelerator compilation. Compare the same representative workload with JAX, TensorFlow, ONNX Runtime.
\item[5. Production proof] Start with the failure hypothesis: Python flexibility can hide graph breaks, device transfers, nondeterminism, and memory growth until scale. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local, distributed training, mobile, or compiled runtimes\\
Choose when & Custom model training, fine-tuning, research kernels, or ecosystem compatibility requires an expressive tensor framework.\\
Reject when & Inference only can use a smaller runtime, or an existing JAX stack already owns accelerator compilation.\\
Primary failure & Python flexibility can hide graph breaks, device transfers, nondeterminism, and memory growth until scale.\\
Compare with & JAX; TensorFlow; ONNX Runtime\\
\end{tabularx}
\paragraph{Minimal use in Python / C++ / CUDA.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install torch
import torch; y = torch.nn.Linear(8, 4)(torch.randn(2, 8))
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.pytorch.org/docs/stable/index.html}{PyTorch official documentation}.
\subsubsection{JAX}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries J}}}\quad \badge{Array transformation and compiler framework} \quad \textbf{Languages:} Python / XLA
\par\smallskip NumPy-like arrays with composable transformations such as automatic differentiation, vectorization, JIT compilation, and parallel sharding.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of JAX as a array transformation and compiler framework between pure array function and sharded accelerator result; its defining mechanism is transform + xla compile.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Pure array function};
\node[tech,right=of t0](t1){Transform + XLA compile};
\node[tech,right=of t1](t2){Sharded accelerator result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Pure array function -> Transform + XLA compile -> Sharded accelerator result\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] NumPy-like arrays with composable transformations such as automatic differentiation, vectorization, JIT compilation, and parallel sharding. Think of JAX as a array transformation and compiler framework between pure array function and sharded accelerator result; its defining mechanism is transform + xla compile.
\item[2. Structure and execution] Follow the data path Pure array function → Transform + XLA compile → Sharded accelerator result. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when functional numerical programs need compiler optimization, explicit sharding, or large accelerator training at scale. Reject it when dynamic Python mutation and the PyTorch ecosystem matter more than compilation and functional transforms. Compare the same representative workload with PyTorch, TensorFlow, NumPy.
\item[5. Production proof] Start with the failure hypothesis: Unexpected recompilation, shape polymorphism, host-device transfer, or PRNG misuse can erase performance and correctness. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Accelerator-compiled local or distributed programs\\
Choose when & Functional numerical programs need compiler optimization, explicit sharding, or large accelerator training at scale.\\
Reject when & Dynamic Python mutation and the PyTorch ecosystem matter more than compilation and functional transforms.\\
Primary failure & Unexpected recompilation, shape polymorphism, host-device transfer, or PRNG misuse can erase performance and correctness.\\
Compare with & PyTorch; TensorFlow; NumPy\\
\end{tabularx}
\paragraph{Minimal use in Python / XLA.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install jax
import jax.numpy as jnp; from jax import jit
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.jax.dev/en/latest/}{JAX official documentation}.
\subsubsection{Hugging Face Transformers}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries HFT}}}\quad \badge{Pretrained model library} \quad \textbf{Languages:} Python / PyTorch / TensorFlow / JAX
\par\smallskip Model, tokenizer, processor, generation, training, and pipeline APIs for a large pretrained-model ecosystem.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Hugging Face Transformers as a pretrained model library between hub artifact + tokenizer and task output; its defining mechanism is model forward or generate.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Hub artifact + tokenizer};
\node[tech,right=of t0](t1){Model forward or generate};
\node[tech,right=of t1](t2){Task output};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Hub artifact + tokenizer -> Model forward or generate -> Task output\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Model, tokenizer, processor, generation, training, and pipeline APIs for a large pretrained-model ecosystem. Think of Hugging Face Transformers as a pretrained model library between hub artifact + tokenizer and task output; its defining mechanism is model forward or generate.
\item[2. Structure and execution] Follow the data path Hub artifact + tokenizer → Model forward or generate → Task output. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when you need reproducible access to pretrained architectures, tokenizers, checkpoints, and fine-tuning utilities. Reject it when a narrow production server or a provider SDK already defines the complete model boundary. Compare the same representative workload with PyTorch, vLLM, sentence-transformers.
\item[5. Production proof] Start with the failure hypothesis: Trusting remote model code, mutable revisions, or default generation settings creates supply-chain and reproducibility risk. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; training and inference across local or hosted compute\\
Choose when & You need reproducible access to pretrained architectures, tokenizers, checkpoints, and fine-tuning utilities.\\
Reject when & A narrow production server or a provider SDK already defines the complete model boundary.\\
Primary failure & Trusting remote model code, mutable revisions, or default generation settings creates supply-chain and reproducibility risk.\\
Compare with & PyTorch; vLLM; sentence-transformers\\
\end{tabularx}
\paragraph{Minimal use in Python / PyTorch / TensorFlow / JAX.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install transformers
from transformers import AutoTokenizer, AutoModel
\end{Verbatim}
\textbf{Primary sources.} \href{https://huggingface.co/docs/transformers/index}{Hugging Face Transformers official documentation}.
\subsubsection{Sentence Transformers}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries ST}}}\quad \badge{Embedding and reranker library} \quad \textbf{Languages:} Python / PyTorch
\par\smallskip High-level bi-encoder, sparse encoder, and cross-encoder workflows for embeddings, semantic search, reranking, and fine-tuning.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Sentence Transformers as a embedding and reranker library between text pairs or corpus and vectors or relevance scores; its defining mechanism is encode or cross-score.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Text pairs or corpus};
\node[tech,right=of t0](t1){Encode or cross-score};
\node[tech,right=of t1](t2){Vectors or relevance scores};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Text pairs or corpus -> Encode or cross-score -> Vectors or relevance scores\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] High-level bi-encoder, sparse encoder, and cross-encoder workflows for embeddings, semantic search, reranking, and fine-tuning. Think of Sentence Transformers as a embedding and reranker library between text pairs or corpus and vectors or relevance scores; its defining mechanism is encode or cross-score.
\item[2. Structure and execution] Follow the data path Text pairs or corpus → Encode or cross-score → Vectors or relevance scores. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when you need a well-tested Python path to train or run text embedding and reranking models. Reject it when only a remote embedding API is allowed or a high-throughput server already owns preprocessing and batching. Compare the same representative workload with FlagEmbedding, Transformers, Cohere Rerank.
\item[5. Production proof] Start with the failure hypothesis: Using cosine similarity without matching prompts, normalization, pooling, language, and training objective silently degrades retrieval. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; local training and inference\\
Choose when & You need a well-tested Python path to train or run text embedding and reranking models.\\
Reject when & Only a remote embedding API is allowed or a high-throughput server already owns preprocessing and batching.\\
Primary failure & Using cosine similarity without matching prompts, normalization, pooling, language, and training objective silently degrades retrieval.\\
Compare with & FlagEmbedding; Transformers; Cohere Rerank\\
\end{tabularx}
\paragraph{Minimal use in Python / PyTorch.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install sentence-transformers
from sentence_transformers import SentenceTransformer
\end{Verbatim}
\textbf{Primary sources.} \href{https://sbert.net/}{Sentence Transformers official documentation}.
\subsubsection{Hugging Face PEFT}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries HFP}}}\quad \badge{Parameter-efficient fine-tuning library} \quad \textbf{Languages:} Python / PyTorch
\par\smallskip Adapters such as LoRA reduce trainable parameters and storage while integrating with Transformers and common training systems.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Hugging Face PEFT as a parameter-efficient fine-tuning library between frozen base + adapter config and mergeable adapter artifact; its defining mechanism is train low-rank parameters.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Frozen base + adapter config};
\node[tech,right=of t0](t1){Train low-rank parameters};
\node[tech,right=of t1](t2){Mergeable adapter artifact};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Frozen base + adapter config -> Train low-rank parameters -> Mergeable adapter artifact\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Adapters such as LoRA reduce trainable parameters and storage while integrating with Transformers and common training systems. Think of Hugging Face PEFT as a parameter-efficient fine-tuning library between frozen base + adapter config and mergeable adapter artifact; its defining mechanism is train low-rank parameters.
\item[2. Structure and execution] Follow the data path Frozen base + adapter config → Train low-rank parameters → Mergeable adapter artifact. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when many task adapters must be trained or served while full-model fine-tuning is too expensive. Reject it when the task needs broad weight changes, the base model is unstable, or adapter serving complexity exceeds savings. Compare the same representative workload with full fine-tuning, prompt tuning, vendor fine-tuning APIs.
\item[5. Production proof] Start with the failure hypothesis: A small adapter can overfit, target the wrong modules, or become incompatible with the exact base revision. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library on local or distributed training stacks\\
Choose when & Many task adapters must be trained or served while full-model fine-tuning is too expensive.\\
Reject when & The task needs broad weight changes, the base model is unstable, or adapter serving complexity exceeds savings.\\
Primary failure & A small adapter can overfit, target the wrong modules, or become incompatible with the exact base revision.\\
Compare with & full fine-tuning; prompt tuning; vendor fine-tuning APIs\\
\end{tabularx}
\paragraph{Minimal use in Python / PyTorch.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install peft
from peft import LoraConfig, get_peft_model
\end{Verbatim}
\textbf{Primary sources.} \href{https://huggingface.co/docs/peft/index}{Hugging Face PEFT official documentation}.
\subsubsection{bitsandbytes}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries B}}}\quad \badge{Quantization and low-bit optimizer library} \quad \textbf{Languages:} Python / CUDA
\par\smallskip Low-bit linear layers, quantization paths, and memory-saving optimizers commonly used for loading and adapting large models.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of bitsandbytes as a quantization and low-bit optimizer library between weights + quant config and reduced-memory model execution; its defining mechanism is low-bit kernels.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Weights + quant config};
\node[tech,right=of t0](t1){Low-bit kernels};
\node[tech,right=of t1](t2){Reduced-memory model execution};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Weights + quant config -> Low-bit kernels -> Reduced-memory model execution\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Low-bit linear layers, quantization paths, and memory-saving optimizers commonly used for loading and adapting large models. Think of bitsandbytes as a quantization and low-bit optimizer library between weights + quant config and reduced-memory model execution; its defining mechanism is low-bit kernels.
\item[2. Structure and execution] Follow the data path Weights + quant config → Low-bit kernels → Reduced-memory model execution. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when gPU memory is the limiting resource and supported hardware plus measured quality make 8-bit or 4-bit execution useful. Reject it when unsupported accelerators, kernels, model layers, or accuracy requirements make the low-bit path fragile. Compare the same representative workload with GPTQModel, AWQ, TensorRT-LLM quantization.
\item[5. Production proof] Start with the failure hypothesis: A model may fit memory but run slower, lose quality on critical slices, or fail on unsupported kernels. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library on supported accelerator stacks\\
Choose when & GPU memory is the limiting resource and supported hardware plus measured quality make 8-bit or 4-bit execution useful.\\
Reject when & Unsupported accelerators, kernels, model layers, or accuracy requirements make the low-bit path fragile.\\
Primary failure & A model may fit memory but run slower, lose quality on critical slices, or fail on unsupported kernels.\\
Compare with & GPTQModel; AWQ; TensorRT-LLM quantization\\
\end{tabularx}
\paragraph{Minimal use in Python / CUDA.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install bitsandbytes
from transformers import BitsAndBytesConfig
\end{Verbatim}
\textbf{Primary sources.} \href{https://huggingface.co/docs/bitsandbytes/main/en/index}{bitsandbytes official documentation}.
\subsubsection{ONNX Runtime}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries OR}}}\quad \badge{Cross-platform inference runtime} \quad \textbf{Languages:} C++ / C\# / Python / Java / JavaScript
\par\smallskip Executes ONNX graphs through pluggable hardware providers with graph optimization, quantization, and broad language bindings.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of ONNX Runtime as a cross-platform inference runtime between onnx graph + tensors and portable inference outputs; its defining mechanism is optimize + execution provider.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){ONNX graph + tensors};
\node[tech,right=of t0](t1){Optimize + execution provider};
\node[tech,right=of t1](t2){Portable inference outputs};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & ONNX graph + tensors -> Optimize + execution provider -> Portable inference outputs\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Executes ONNX graphs through pluggable hardware providers with graph optimization, quantization, and broad language bindings. Think of ONNX Runtime as a cross-platform inference runtime between onnx graph + tensors and portable inference outputs; its defining mechanism is optimize + execution provider.
\item[2. Structure and execution] Follow the data path ONNX graph + tensors → Optimize + execution provider → Portable inference outputs. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when a portable, non-LLM-specific inference artifact must run across languages and hardware targets. Reject it when the model cannot export correctly or needs an LLM-specialized token scheduler and KV-cache control plane. Compare the same representative workload with OpenVINO, TensorRT, PyTorch compile.
\item[5. Production proof] Start with the failure hypothesis: Exported operators, dynamic shapes, preprocessing, and provider fallback can change accuracy or performance. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & CPU, GPU, mobile, browser, and hardware execution providers\\
Choose when & A portable, non-LLM-specific inference artifact must run across languages and hardware targets.\\
Reject when & The model cannot export correctly or needs an LLM-specialized token scheduler and KV-cache control plane.\\
Primary failure & Exported operators, dynamic shapes, preprocessing, and provider fallback can change accuracy or performance.\\
Compare with & OpenVINO; TensorRT; PyTorch compile\\
\end{tabularx}
\paragraph{Minimal use in C++ / C\# / Python / Java / JavaScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install onnxruntime
import onnxruntime as ort; session = ort.InferenceSession('model.onnx')
\end{Verbatim}
\textbf{Primary sources.} \href{https://onnxruntime.ai/docs/}{ONNX Runtime official documentation}.
\subsubsection{OpenVINO}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries O}}}\quad \badge{Intel inference toolkit} \quad \textbf{Languages:} C++ / Python
\par\smallskip Model conversion, graph optimization, quantization, and inference across Intel hardware with runtime and model-server options.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of OpenVINO as a intel inference toolkit between framework model and intel-targeted compiled model; its defining mechanism is convert + optimize.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Framework model};
\node[tech,right=of t0](t1){Convert + optimize};
\node[tech,right=of t1](t2){Intel-targeted compiled model};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Framework model -> Convert + optimize -> Intel-targeted compiled model\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Model conversion, graph optimization, quantization, and inference across Intel hardware with runtime and model-server options. Think of OpenVINO as a intel inference toolkit between framework model and intel-targeted compiled model; its defining mechanism is convert + optimize.
\item[2. Structure and execution] Follow the data path Framework model → Convert + optimize → Intel-targeted compiled model. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when deployment targets Intel CPUs, integrated GPUs, or NPUs and hardware-aware optimization matters. Reject it when a CUDA-first fleet or unsupported model operators dominate the workload. Compare the same representative workload with ONNX Runtime, TensorRT, llama.cpp.
\item[5. Production proof] Start with the failure hypothesis: A converted model can change numerics or fall back to slower operations unless layer placement is inspected. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Intel CPU, GPU, and NPU runtimes\\
Choose when & Deployment targets Intel CPUs, integrated GPUs, or NPUs and hardware-aware optimization matters.\\
Reject when & A CUDA-first fleet or unsupported model operators dominate the workload.\\
Primary failure & A converted model can change numerics or fall back to slower operations unless layer placement is inspected.\\
Compare with & ONNX Runtime; TensorRT; llama.cpp\\
\end{tabularx}
\paragraph{Minimal use in C++ / Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install openvino
from openvino import Core; compiled = Core().compile_model('model.xml')
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.openvino.ai/}{OpenVINO official documentation}.
\subsubsection{FlagEmbedding}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries F}}}\quad \badge{Retrieval model library} \quad \textbf{Languages:} Python / PyTorch
\par\smallskip BGE-family dense, sparse, multi-vector embedding, reranking, and retrieval training utilities.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of FlagEmbedding as a retrieval model library between queries + corpus and ranked evidence; its defining mechanism is embed, retrieve, rerank.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Queries + corpus};
\node[tech,right=of t0](t1){Embed, retrieve, rerank};
\node[tech,right=of t1](t2){Ranked evidence};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Queries + corpus -> Embed, retrieve, rerank -> Ranked evidence\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] BGE-family dense, sparse, multi-vector embedding, reranking, and retrieval training utilities. Think of FlagEmbedding as a retrieval model library between queries + corpus and ranked evidence; its defining mechanism is embed, retrieve, rerank.
\item[2. Structure and execution] Follow the data path Queries + corpus → Embed, retrieve, rerank → Ranked evidence. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when multilingual or advanced retrieval experiments need BGE models, rerankers, or trainable retrieval recipes. Reject it when a managed embedding API or Sentence Transformers abstraction already meets quality and operations needs. Compare the same representative workload with Sentence Transformers, Tevatron, Cohere Rerank.
\item[5. Production proof] Start with the failure hypothesis: Comparing models with mismatched query instructions, pooling, normalization, or candidate sets produces invalid conclusions. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library; local or distributed training and inference\\
Choose when & Multilingual or advanced retrieval experiments need BGE models, rerankers, or trainable retrieval recipes.\\
Reject when & A managed embedding API or Sentence Transformers abstraction already meets quality and operations needs.\\
Primary failure & Comparing models with mismatched query instructions, pooling, normalization, or candidate sets produces invalid conclusions.\\
Compare with & Sentence Transformers; Tevatron; Cohere Rerank\\
\end{tabularx}
\paragraph{Minimal use in Python / PyTorch.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install FlagEmbedding
from FlagEmbedding import FlagModel
\end{Verbatim}
\textbf{Primary sources.} \href{https://bge-model.com/}{FlagEmbedding official documentation}.
\subsubsection{Instructor}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries I}}}\quad \badge{Structured-output library} \quad \textbf{Languages:} Python / TypeScript
\par\smallskip Adds schema-driven extraction, validation, retries, and streaming around model calls using typed application models.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Instructor as a structured-output library between prompt + schema and typed application object; its defining mechanism is generate, validate, retry.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Prompt + schema};
\node[tech,right=of t0](t1){Generate, validate, retry};
\node[tech,right=of t1](t2){Typed application object};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Prompt + schema -> Generate, validate, retry -> Typed application object\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Adds schema-driven extraction, validation, retries, and streaming around model calls using typed application models. Think of Instructor as a structured-output library between prompt + schema and typed application object; its defining mechanism is generate, validate, retry.
\item[2. Structure and execution] Follow the data path Prompt + schema → Generate, validate, retry → Typed application object. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when typed extraction is central and the team wants a thin layer over existing provider SDKs. Reject it when free-form dialogue, durable agent state, or deterministic business validation is the actual problem. Compare the same representative workload with PydanticAI, provider structured outputs, Guardrails AI.
\item[5. Production proof] Start with the failure hypothesis: Schema-valid data can remain semantically false, and blind retries can amplify cost or attack payloads. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library over model-provider clients\\
Choose when & Typed extraction is central and the team wants a thin layer over existing provider SDKs.\\
Reject when & Free-form dialogue, durable agent state, or deterministic business validation is the actual problem.\\
Primary failure & Schema-valid data can remain semantically false, and blind retries can amplify cost or attack payloads.\\
Compare with & PydanticAI; provider structured outputs; Guardrails AI\\
\end{tabularx}
\paragraph{Minimal use in Python / TypeScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install instructor
import instructor; client = instructor.from_provider('openai/gpt-5')
\end{Verbatim}
\textbf{Primary sources.} \href{https://python.useinstructor.com/}{Instructor official documentation}.
\subsubsection{Cohere Rerank}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries CR}}}\quad \badge{Managed cross-encoder reranking API} \quad \textbf{Languages:} REST / Python / TypeScript / Java
\par\smallskip Jointly scores a query with candidate documents to improve ordering after a cheaper first-stage retriever.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Cohere Rerank as a managed cross-encoder reranking api between query + candidates and reranked candidate list; its defining mechanism is joint relevance scoring.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Query + candidates};
\node[tech,right=of t0](t1){Joint relevance scoring};
\node[tech,right=of t1](t2){Reranked candidate list};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Query + candidates -> Joint relevance scoring -> Reranked candidate list\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Jointly scores a query with candidate documents to improve ordering after a cheaper first-stage retriever. Think of Cohere Rerank as a managed cross-encoder reranking api between query + candidates and reranked candidate list; its defining mechanism is joint relevance scoring.
\item[2. Structure and execution] Follow the data path Query + candidates → Joint relevance scoring → Reranked candidate list. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when a managed multilingual or domain-capable reranker gives measured relevance lift on a bounded candidate set. Reject it when data cannot leave the trust boundary, latency is too tight, or an open local reranker performs adequately. Compare the same representative workload with Sentence Transformers CrossEncoder, FlagEmbedding rerankers, mixedbread rerank.
\item[5. Production proof] Start with the failure hypothesis: Sending too many long candidates increases latency and cost, while truncation or weak first-stage recall caps any benefit. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed API\\
Choose when & A managed multilingual or domain-capable reranker gives measured relevance lift on a bounded candidate set.\\
Reject when & Data cannot leave the trust boundary, latency is too tight, or an open local reranker performs adequately.\\
Primary failure & Sending too many long candidates increases latency and cost, while truncation or weak first-stage recall caps any benefit.\\
Compare with & Sentence Transformers CrossEncoder; FlagEmbedding rerankers; mixedbread rerank\\
\end{tabularx}
\paragraph{Minimal use in REST / Python / TypeScript / Java.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install cohere
co.rerank(model='rerank-v3.5', query=q, documents=docs)
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.cohere.com/docs/rerank-overview}{Cohere Rerank official documentation}.
\subsubsection{Hugging Face Accelerate}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries HFA}}}\quad \badge{Distributed training and inference helper} \quad \textbf{Languages:} Python
\par\smallskip Lightweight abstraction for device placement, mixed precision, distributed launch, checkpointing, and big-model inference.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Hugging Face Accelerate as a distributed training and inference helper between pytorch program + config and distributed training result; its defining mechanism is accelerator placement + launch.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){PyTorch program + config};
\node[tech,right=of t0](t1){Accelerator placement + launch};
\node[tech,right=of t1](t2){Distributed training result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & PyTorch program + config -> Accelerator placement + launch -> Distributed training result\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Lightweight abstraction for device placement, mixed precision, distributed launch, checkpointing, and big-model inference. Think of Hugging Face Accelerate as a distributed training and inference helper between pytorch program + config and distributed training result; its defining mechanism is accelerator placement + launch.
\item[2. Structure and execution] Follow the data path PyTorch program + config → Accelerator placement + launch → Distributed training result. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when existing PyTorch code must scale across devices with minimal structural change. Reject it when you need a full trainer, serving runtime, or highly specialized parallelism implementation. Compare the same representative workload with DeepSpeed, PyTorch Distributed, Lightning Fabric.
\item[5. Production proof] Start with the failure hypothesis: A simple API can hide topology, synchronization, memory, and checkpoint bottlenecks that still require measurement. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library across local, cluster, and cloud accelerators\\
Choose when & Existing PyTorch code must scale across devices with minimal structural change.\\
Reject when & You need a full trainer, serving runtime, or highly specialized parallelism implementation.\\
Primary failure & A simple API can hide topology, synchronization, memory, and checkpoint bottlenecks that still require measurement.\\
Compare with & DeepSpeed; PyTorch Distributed; Lightning Fabric\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install accelerate
accelerate config
\end{Verbatim}
\textbf{Primary sources.} \href{https://huggingface.co/docs/accelerate/index}{Hugging Face Accelerate official documentation}.
\subsubsection{DeepSpeed}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries D}}}\quad \badge{Distributed model optimization library} \quad \textbf{Languages:} Python / C++ / CUDA
\par\smallskip Training and inference optimizations including ZeRO memory partitioning, mixed precision, parallelism, offload, and custom kernels.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of DeepSpeed as a distributed model optimization library between model + cluster topology and sharded training artifacts; its defining mechanism is zero and parallel runtime.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model + cluster topology};
\node[tech,right=of t0](t1){ZeRO and parallel runtime};
\node[tech,right=of t1](t2){Sharded training artifacts};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model + cluster topology -> ZeRO and parallel runtime -> Sharded training artifacts\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Training and inference optimizations including ZeRO memory partitioning, mixed precision, parallelism, offload, and custom kernels. Think of DeepSpeed as a distributed model optimization library between model + cluster topology and sharded training artifacts; its defining mechanism is zero and parallel runtime.
\item[2. Structure and execution] Follow the data path Model + cluster topology → ZeRO and parallel runtime → Sharded training artifacts. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when large models exceed a single device and the team can tune distributed optimizer, communication, and checkpoint behavior. Reject it when small models, heterogeneous portability, or minimal cluster complexity is more important. Compare the same representative workload with FSDP, Megatron Core, Accelerate.
\item[5. Production proof] Start with the failure hypothesis: Configuration can shift bottlenecks into network, CPU offload, or checkpoint recovery while appearing to solve GPU memory. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library on GPU clusters\\
Choose when & Large models exceed a single device and the team can tune distributed optimizer, communication, and checkpoint behavior.\\
Reject when & Small models, heterogeneous portability, or minimal cluster complexity is more important.\\
Primary failure & Configuration can shift bottlenecks into network, CPU offload, or checkpoint recovery while appearing to solve GPU memory.\\
Compare with & FSDP; Megatron Core; Accelerate\\
\end{tabularx}
\paragraph{Minimal use in Python / C++ / CUDA.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install deepspeed
deepseed train.py --deepspeed config.json
\end{Verbatim}
\textbf{Primary sources.} \href{https://www.deepspeed.ai/}{DeepSpeed official documentation}.
\subsubsection{Hugging Face TRL}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries HFT}}}\quad \badge{Post-training library} \quad \textbf{Languages:} Python
\par\smallskip Trainers and utilities for supervised fine-tuning, preference optimization, reward modeling, and reinforcement-learning-style post-training.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Hugging Face TRL as a post-training library between model + preference data and adapted checkpoint; its defining mechanism is post-training objective + trainer.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model + preference data};
\node[tech,right=of t0](t1){Post-training objective + trainer};
\node[tech,right=of t1](t2){Adapted checkpoint};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model + preference data -> Post-training objective + trainer -> Adapted checkpoint\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Trainers and utilities for supervised fine-tuning, preference optimization, reward modeling, and reinforcement-learning-style post-training. Think of Hugging Face TRL as a post-training library between model + preference data and adapted checkpoint; its defining mechanism is post-training objective + trainer.
\item[2. Structure and execution] Follow the data path Model + preference data → Post-training objective + trainer → Adapted checkpoint. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when you need reproducible post-training recipes integrated with Hugging Face models and datasets. Reject it when the requirement is base-model pretraining, low-level distributed kernels, or online production serving. Compare the same representative workload with Axolotl, LLaMA Factory, custom Transformers trainer.
\item[5. Production proof] Start with the failure hypothesis: Reward or preference data leakage and narrow evaluators can optimize style while degrading factual or safety behavior. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library on Transformers, Accelerate, and PEFT\\
Choose when & You need reproducible post-training recipes integrated with Hugging Face models and datasets.\\
Reject when & The requirement is base-model pretraining, low-level distributed kernels, or online production serving.\\
Primary failure & Reward or preference data leakage and narrow evaluators can optimize style while degrading factual or safety behavior.\\
Compare with & Axolotl; LLaMA Factory; custom Transformers trainer\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install trl
from trl import SFTTrainer
\end{Verbatim}
\textbf{Primary sources.} \href{https://huggingface.co/docs/trl/index}{Hugging Face TRL official documentation}.
\subsubsection{Axolotl}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries A}}}\quad \badge{LLM fine-tuning framework} \quad \textbf{Languages:} Python / YAML
\par\smallskip Configuration-driven fine-tuning across model families with LoRA, quantization, distributed backends, datasets, and evaluation hooks.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Axolotl as a llm fine-tuning framework between dataset + yaml config and checkpoint + metrics; its defining mechanism is training launcher + adapters.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Dataset + YAML config};
\node[tech,right=of t0](t1){Training launcher + adapters};
\node[tech,right=of t1](t2){Checkpoint + metrics};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Dataset + YAML config -> Training launcher + adapters -> Checkpoint + metrics\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Configuration-driven fine-tuning across model families with LoRA, quantization, distributed backends, datasets, and evaluation hooks. Think of Axolotl as a llm fine-tuning framework between dataset + yaml config and checkpoint + metrics; its defining mechanism is training launcher + adapters.
\item[2. Structure and execution] Follow the data path Dataset + YAML config → Training launcher + adapters → Checkpoint + metrics. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when teams want repeatable fine-tuning recipes without writing a custom training loop. Reject it when deep research requires low-level algorithm changes or the target architecture is unsupported. Compare the same representative workload with TRL, LLaMA Factory, Unsloth.
\item[5. Production proof] Start with the failure hypothesis: A copied YAML can train successfully with the wrong chat template, packing, masking, or evaluation split. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & CLI on workstations or GPU clusters\\
Choose when & Teams want repeatable fine-tuning recipes without writing a custom training loop.\\
Reject when & Deep research requires low-level algorithm changes or the target architecture is unsupported.\\
Primary failure & A copied YAML can train successfully with the wrong chat template, packing, masking, or evaluation split.\\
Compare with & TRL; LLaMA Factory; Unsloth\\
\end{tabularx}
\paragraph{Minimal use in Python / YAML.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install axolotl
axolotl train config.yml
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.axolotl.ai/}{Axolotl official documentation}.
\subsubsection{Unsloth}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries U}}}\quad \badge{Efficient fine-tuning library} \quad \textbf{Languages:} Python / Triton
\par\smallskip Optimized kernels and memory-efficient workflows for fine-tuning and reinforcement learning on supported open models.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Unsloth as a efficient fine-tuning library between model + dataset and adapter or merged model; its defining mechanism is optimized kernels + peft trainer.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model + dataset};
\node[tech,right=of t0](t1){Optimized kernels + PEFT trainer};
\node[tech,right=of t1](t2){Adapter or merged model};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model + dataset -> Optimized kernels + PEFT trainer -> Adapter or merged model\\
Control plane & The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.\\
State and trust boundary & Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Optimized kernels and memory-efficient workflows for fine-tuning and reinforcement learning on supported open models. Think of Unsloth as a efficient fine-tuning library between model + dataset and adapter or merged model; its defining mechanism is optimized kernels + peft trainer.
\item[2. Structure and execution] Follow the data path Model + dataset → Optimized kernels + PEFT trainer → Adapter or merged model. The training or compilation plan binds data, objective, precision, parallelism, optimizer, and target runtime.
\item[3. State, security, and operations] Datasets, tokenizers, code, configs, checkpoints, adapters, seeds, and evaluation sets form one versioned artifact graph.
\item[4. Selection decision] Choose it when single-node or modest GPU training needs strong speed and memory efficiency with common model families. Reject it when you require arbitrary architectures, maximum backend portability, or full control of kernel implementation. Compare the same representative workload with TRL, Axolotl, PEFT.
\item[5. Production proof] Start with the failure hypothesis: Speed claims may not transfer across sequence lengths, quantization, GPUs, or unsupported model revisions. Measure task quality by slice together with memory, throughput, convergence, reproducibility, and export compatibility.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library for supported GPU and model combinations\\
Choose when & Single-node or modest GPU training needs strong speed and memory efficiency with common model families.\\
Reject when & You require arbitrary architectures, maximum backend portability, or full control of kernel implementation.\\
Primary failure & Speed claims may not transfer across sequence lengths, quantization, GPUs, or unsupported model revisions.\\
Compare with & TRL; Axolotl; PEFT\\
\end{tabularx}
\paragraph{Minimal use in Python / Triton.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install unsloth
from unsloth import FastLanguageModel
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.unsloth.ai/}{Unsloth official documentation}.
\clearpage\subsection{03. Model serving, gateways, and inference}
\textbf{Selection question.} What is the latency-throughput-memory-reliability frontier?
\begin{center}\begin{tikzpicture}[tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue},node distance=8mm]
\node[tech](c0){Model + token workload};
\node[tech,right=of c0](c1){Route, batch, schedule, execute};
\node[tech,right=of c1](c2){Tokens under an SLO};
\draw[arr](c0)--(c1);\draw[arr](c1)--(c2);
\end{tikzpicture}\end{center}
\paragraph{Comparison matrix.}
\begin{longtable}{@{}p{20mm}p{22mm}p{26mm}p{46mm}p{46mm}@{}}
\toprule\textbf{Tool} & \textbf{Kind} & \textbf{Language / boundary} & \textbf{Choose when} & \textbf{Reject when}\\\midrule\endhead
vLLM & LLM inference engine & Python, C++, CUDA; Open-source server; Kubernetes or managed integrations & You serve many concurrent open-weight requests and need strong throughput with broad model support. & The target model or accelerator needs vendor-specific kernels, or the workload is tiny and local simplicity dominates.\\
SGLang & LLM inference engine & Python, C++, CUDA; Open-source server and runtime & Workloads share long prefixes, use structured outputs, or need high-performance agent/model programs. & The model/backend matrix is unsupported or your team prioritizes vLLM ecosystem familiarity.\\
Hugging Face Text Generation Inference & LLM inference server & Rust, Python, CUDA; Containerized open-source server & You want a mature container, Hugging Face integration, and a deliberately bounded supported-model surface. & You need the newest model architecture immediately or more flexible engine internals.\\
TensorRT-LLM & Optimized inference SDK & Python, C++, CUDA; Compiled engines; Triton or custom server & NVIDIA GPUs are fixed, maximum performance matters, and you can own engine-build and compatibility work. & Rapid model churn, heterogeneous hardware, or portability matters more than peak tuned throughput.\\
NVIDIA Triton Inference Server & Multi-framework inference server & C++, Python, client SDKs; Containerized server on CPU or GPU & One platform must serve heterogeneous model formats with standardized scheduling and observability. & You need an LLM-specialized scheduler with first-class KV-cache and token-aware routing out of the box.\\
llama.cpp & Portable inference runtime & C, C++; Local binary, library, or HTTP server & Offline, desktop, edge, or commodity-hardware deployment values portability and quantized models. & Large multi-tenant GPU fleets need sophisticated distributed scheduling and operational APIs.\\
Ollama & Local model manager and server & Go, C++; Desktop or local server & Developers need a low-friction local model experience and reproducible Modelfiles. & You need production multi-node scheduling, strict multi-tenancy, or fine control of GPU kernels.\\
KServe & Kubernetes serving platform & Go, Python, Kubernetes YAML; Kubernetes CRDs and controllers & Your organization already operates Kubernetes and needs a common lifecycle for many model types. & A small team lacks cluster operations, or subsecond always-warm LLM serving needs direct engine control.\\
Ray Serve & Distributed serving library & Python; Ray cluster or managed Anyscale & Inference is a multi-stage Python graph or must share a Ray data, training, or actor ecosystem. & A standard single-model server or Kubernetes CRD meets the requirement with less runtime complexity.\\
BentoML & Model packaging and service framework & Python; Containers, Kubernetes, BentoCloud & You need repeatable packaging for custom Python preprocessing and multiple model frameworks. & Only a bare optimized LLM server is needed, or the organization has a mandated serving control plane.\\
LocalAI & Local OpenAI-compatible platform & Go, C++, Python backends; Binary or containers; local and on-prem & A private or offline environment needs one familiar API across heterogeneous local models. & Peak LLM throughput, narrow minimal attack surface, or managed operations are the primary goals.\\
Modal & Serverless AI compute platform & Python; Managed serverless CPU and GPU & Bursty GPU workloads, batch jobs, and custom containers should scale without operating a cluster. & Hard on-prem requirements, fixed long-running utilization, or deep infrastructure control dominates.\\
Baseten & Managed inference platform & Python, REST; Managed GPU endpoints & A team wants managed production inference with custom code and performance engineering support. & Self-hosting economics, unusual infrastructure constraints, or full portability dominate.\\
Hugging Face Inference Endpoints & Managed model endpoint & REST, Python; Managed dedicated or autoscaling infrastructure & You want Hub-native deployment without operating servers and accept provider-region constraints. & The model needs unsupported custom infrastructure or self-hosted unit economics are decisive.\\
Hugging Face Text Embeddings Inference & Embedding inference server & Rust, Python client; Containerized CPU or GPU server & Self-hosted embedding or reranking models need optimized batching and an operational HTTP boundary. & Low traffic can call a library directly, or a managed API removes the operational burden.\\
LiteLLM & LLM gateway and provider adapter & Python; Library or self-hosted proxy & Many model providers must sit behind one application contract with centralized spend and routing policy. & One provider and a direct SDK are simpler, or lowest-level streaming semantics must remain provider specific.\\
Portkey AI Gateway & Managed or self-hosted AI gateway & REST, Python, JavaScript; Managed cloud, self-hosted, or hybrid gateway & Teams want a control plane in front of hosted models without building routing and governance from scratch. & Strictly local inference or an existing API gateway plus observability stack already meets requirements.\\
Kong AI Gateway & API and AI gateway & Lua, Go, declarative config; Kubernetes, VM, or managed gateway & An organization already uses Kong and wants AI traffic under the same identity, networking, and policy plane. & A lightweight library proxy is enough or inference scheduling inside the GPU fleet is the main problem.\\
Envoy AI Gateway & Kubernetes AI gateway & Go, Kubernetes YAML; Kubernetes Gateway API and Envoy & Kubernetes Gateway API is already strategic and teams need an open, infrastructure-native AI gateway. & A developer library or hosted control plane is preferable to operating gateway controllers and CRDs.\\
Amazon SageMaker AI Endpoints & Managed model endpoint platform & Python, containers, AWS APIs; AWS managed real-time, serverless, asynchronous, and batch inference & AWS identity, VPC integration, deployment variants, and managed operations are primary requirements. & A portable Kubernetes stack or specialized LLM runtime gives better control and utilization.\\
Vertex AI Endpoints & Managed model endpoint platform & Python, REST, containers; Google Cloud managed online prediction & Google Cloud identity, data, and managed ML lifecycle outweigh the need for a portable serving control plane. & The workload needs local or multi-cloud control, or a specialized runtime should be operated directly.\\
Azure Machine Learning Managed Online Endpoints & Managed model endpoint platform & Python, CLI, YAML, REST; Azure managed online endpoint & Azure identity, private networking, and ML workspace governance are organizational constraints. & Direct model servers or another cloud platform offer a simpler established operating path.\\
llm-d & Kubernetes distributed LLM serving stack & Go, Python, Kubernetes; Kubernetes with vLLM or compatible engines & A Kubernetes fleet needs production routing and scale above individual vLLM or SGLang servers. & A single node or managed endpoint meets the SLO with much less control-plane complexity.\\
NVIDIA Dynamo & Distributed inference serving framework & Python, Rust, CUDA; Containers or Kubernetes on accelerated clusters & Large NVIDIA fleets need a programmable distributed data plane around TensorRT-LLM, vLLM, or SGLang. & Portability across accelerators or a small stateless deployment is the priority.\\
NVIDIA NIM & Packaged inference microservices & Python, CUDA, containers; NVIDIA AI Enterprise containers and APIs & You need supported accelerated deployment with packaged model-specific optimizations and NVIDIA enterprise operations. & Open-source engine control, non-NVIDIA hardware, or rapid unsupported model experimentation dominates.\\
TorchServe & PyTorch model server & Python, Java; Container or server process & Legacy or established PyTorch serving estates need standardized handlers and model management. & A new LLM system needs an actively optimized token scheduler and modern model support.\\
DeepSpeed-MII & DeepSpeed inference serving library & Python, CUDA; Library and server on GPU clusters & A DeepSpeed-centered GPU stack needs a direct path from supported models to optimized inference. & Broad OpenAI-compatible ecosystem support or Kubernetes-native fleet control is more important.\\
OpenLLM & Open model service toolkit & Python; Local service or container & A Python team wants a convenient bridge from open models to a BentoML-style service. & You need the strongest specialized scheduler or a fully managed inference API.\\
LMDeploy & LLM inference and deployment toolkit & Python, C++, CUDA; Local server, containers, or cluster integration & Supported models need high-throughput deployment and quantization in its CUDA-oriented ecosystem. & Your hardware, model, or operations standard aligns better with vLLM, SGLang, or TensorRT-LLM.\\
MLC LLM & Machine-learning compilation deployment stack & Python, C++, TVM; Web, mobile, desktop, edge, and server targets & One model must run efficiently across edge, browser, mobile, and heterogeneous hardware. & A centralized CUDA fleet needs mature multi-tenant scheduling rather than portable compilation.\\
Replicate & Managed model execution platform & Python, JavaScript, REST; Managed cloud API & A team wants rapid access to diverse models or simple custom container deployment without operating accelerators. & Strict private networking, predictable dedicated capacity, or self-hosted control is required.\\
Runpod Serverless & GPU serverless platform & Python, containers, REST; Managed GPU workers and endpoints & Variable GPU workloads need quick deployment and per-second-style capacity without owning a cluster. & Tight tail-latency guarantees or a fully integrated managed ML control plane is mandatory.\\
Together AI Inference & Managed inference API & Python, TypeScript, REST; Managed serverless or dedicated endpoints & You need broad open-model access, fast experimentation, or dedicated hosted capacity through a familiar API. & Self-hosting, a different cloud boundary, or a model unavailable in the catalog is required.\\
Fireworks AI & Managed generative AI inference platform & Python, REST; Serverless, on-demand, or dedicated cloud deployment & High-performance managed open-model inference and deployment flexibility matter more than infrastructure ownership. & A self-hosted control plane or a provider with a required model and region is mandatory.\\
GroqCloud & Managed low-latency inference API & Python, TypeScript, REST; Managed Groq accelerator cloud & Very low token latency for supported models is a dominant interactive requirement. & You need arbitrary model deployment, self-hosting, or broader accelerator portability.\\
Cerebras Inference & Managed high-speed inference API & Python, REST; Managed Cerebras cloud & Token generation speed for supported models materially improves an interactive or agentic workload. & Custom weights, self-hosting, or a broader deployment matrix is required.\\
Amazon Bedrock & Managed foundation-model platform & Python, Java, REST; AWS managed regional service & AWS identity, networking, governance, and access to several model providers in one service are priorities. & Self-hosted open models, maximum kernel control, or another cloud boundary is required.\\
Cloudflare Workers AI & Edge inference platform & TypeScript, Python, REST; Cloudflare Workers and managed accelerators & Edge applications need low-operations inference close to Workers, Durable Objects, or Vectorize. & Custom large-model deployment, CUDA-level tuning, or cloud portability is mandatory.\\
Databricks Model Serving & Managed model and agent serving & Python, REST; Databricks managed endpoints & Models and features already live in Databricks and Unity Catalog governance is an organizational boundary. & A lightweight public API or independently operated Kubernetes engine is preferable.\\
\bottomrule\end{longtable}
\subsubsection{vLLM}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries V}}}\quad \badge{LLM inference engine} \quad \textbf{Languages:} Python / C++ / CUDA
\par\smallskip High-throughput OpenAI-compatible serving with PagedAttention, continuous batching, parallelism, prefix caching, and quantization.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of vLLM as a llm inference engine between requests + weights and streaming tokens; its defining mechanism is schedule + paged kv cache.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Requests + weights};
\node[tech,right=of t0](t1){Schedule + paged KV cache};
\node[tech,right=of t1](t2){Streaming tokens};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Requests + weights -> Schedule + paged KV cache -> Streaming tokens\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] High-throughput OpenAI-compatible serving with PagedAttention, continuous batching, parallelism, prefix caching, and quantization. Think of vLLM as a llm inference engine between requests + weights and streaming tokens; its defining mechanism is schedule + paged kv cache.
\item[2. Structure and execution] Follow the data path Requests + weights → Schedule + paged KV cache → Streaming tokens. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when you serve many concurrent open-weight requests and need strong throughput with broad model support. Reject it when the target model or accelerator needs vendor-specific kernels, or the workload is tiny and local simplicity dominates. Compare the same representative workload with SGLang, TGI, TensorRT-LLM.
\item[5. Production proof] Start with the failure hypothesis: A benchmark at one prompt length and concurrency hides queue collapse, KV pressure, and tail latency. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Open-source server; Kubernetes or managed integrations\\
Choose when & You serve many concurrent open-weight requests and need strong throughput with broad model support.\\
Reject when & The target model or accelerator needs vendor-specific kernels, or the workload is tiny and local simplicity dominates.\\
Primary failure & A benchmark at one prompt length and concurrency hides queue collapse, KV pressure, and tail latency.\\
Compare with & SGLang; TGI; TensorRT-LLM\\
\end{tabularx}
\paragraph{Minimal use in Python / C++ / CUDA.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install vllm
vllm serve MODEL --dtype auto
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.vllm.ai/}{vLLM official documentation} \quad \href{https://docs.vllm.ai/en/latest/design/arch_overview/}{vLLM architecture} \quad \href{https://docs.vllm.ai/en/latest/serving/openai_compatible_server/}{vLLM serving guide}.
\subsubsection{SGLang}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries S}}}\quad \badge{LLM inference engine} \quad \textbf{Languages:} Python / C++ / CUDA
\par\smallskip Serving and structured-generation runtime with RadixAttention, prefix reuse, continuous batching, parallelism, and OpenAI-compatible APIs.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of SGLang as a llm inference engine between requests + shared prefixes and structured token streams; its defining mechanism is radix cache + scheduler.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Requests + shared prefixes};
\node[tech,right=of t0](t1){Radix cache + scheduler};
\node[tech,right=of t1](t2){Structured token streams};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Requests + shared prefixes -> Radix cache + scheduler -> Structured token streams\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Serving and structured-generation runtime with RadixAttention, prefix reuse, continuous batching, parallelism, and OpenAI-compatible APIs. Think of SGLang as a llm inference engine between requests + shared prefixes and structured token streams; its defining mechanism is radix cache + scheduler.
\item[2. Structure and execution] Follow the data path Requests + shared prefixes → Radix cache + scheduler → Structured token streams. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when workloads share long prefixes, use structured outputs, or need high-performance agent/model programs. Reject it when the model/backend matrix is unsupported or your team prioritizes vLLM ecosystem familiarity. Compare the same representative workload with vLLM, TensorRT-LLM, TGI.
\item[5. Production proof] Start with the failure hypothesis: Prefix-cache hit claims can disappear under tenant isolation, prompt churn, or incompatible routing. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Open-source server and runtime\\
Choose when & Workloads share long prefixes, use structured outputs, or need high-performance agent/model programs.\\
Reject when & The model/backend matrix is unsupported or your team prioritizes vLLM ecosystem familiarity.\\
Primary failure & Prefix-cache hit claims can disappear under tenant isolation, prompt churn, or incompatible routing.\\
Compare with & vLLM; TensorRT-LLM; TGI\\
\end{tabularx}
\paragraph{Minimal use in Python / C++ / CUDA.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install sglang
python -m sglang.launch_server --model-path MODEL
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.sglang.ai/}{SGLang official documentation}.
\subsubsection{Hugging Face Text Generation Inference}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries HFT}}}\quad \badge{LLM inference server} \quad \textbf{Languages:} Rust / Python / CUDA
\par\smallskip Hugging Face server with continuous batching, tensor parallelism, quantization, streaming, and model-hub integration.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Hugging Face Text Generation Inference as a llm inference server between hub model + requests and token stream; its defining mechanism is router + model shards.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Hub model + requests};
\node[tech,right=of t0](t1){Router + model shards};
\node[tech,right=of t1](t2){Token stream};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Hub model + requests -> Router + model shards -> Token stream\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Hugging Face server with continuous batching, tensor parallelism, quantization, streaming, and model-hub integration. Think of Hugging Face Text Generation Inference as a llm inference server between hub model + requests and token stream; its defining mechanism is router + model shards.
\item[2. Structure and execution] Follow the data path Hub model + requests → Router + model shards → Token stream. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when you want a mature container, Hugging Face integration, and a deliberately bounded supported-model surface. Reject it when you need the newest model architecture immediately or more flexible engine internals. Compare the same representative workload with vLLM, SGLang, TensorRT-LLM.
\item[5. Production proof] Start with the failure hypothesis: Assuming every Hub model is supported can fail at startup or silently lose an optimization. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Containerized open-source server\\
Choose when & You want a mature container, Hugging Face integration, and a deliberately bounded supported-model surface.\\
Reject when & You need the newest model architecture immediately or more flexible engine internals.\\
Primary failure & Assuming every Hub model is supported can fail at startup or silently lose an optimization.\\
Compare with & vLLM; SGLang; TensorRT-LLM\\
\end{tabularx}
\paragraph{Minimal use in Rust / Python / CUDA.}
\begin{Verbatim}[fontsize=\scriptsize]
docker run ghcr.io/huggingface/text-generation-inference --model-id MODEL
\end{Verbatim}
\textbf{Primary sources.} \href{https://huggingface.co/docs/text-generation-inference/index}{Hugging Face Text Generation Inference official documentation}.
\subsubsection{TensorRT-LLM}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries T}}}\quad \badge{Optimized inference SDK} \quad \textbf{Languages:} Python / C++ / CUDA
\par\smallskip NVIDIA stack for compiling and executing transformer models with fused kernels, quantization, inflight batching, and multi-GPU support.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of TensorRT-LLM as a optimized inference sdk between model + hardware profile and gpu-specific inference; its defining mechanism is build optimized engine.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model + hardware profile};
\node[tech,right=of t0](t1){Build optimized engine};
\node[tech,right=of t1](t2){GPU-specific inference};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model + hardware profile -> Build optimized engine -> GPU-specific inference\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] NVIDIA stack for compiling and executing transformer models with fused kernels, quantization, inflight batching, and multi-GPU support. Think of TensorRT-LLM as a optimized inference sdk between model + hardware profile and gpu-specific inference; its defining mechanism is build optimized engine.
\item[2. Structure and execution] Follow the data path Model + hardware profile → Build optimized engine → GPU-specific inference. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when nVIDIA GPUs are fixed, maximum performance matters, and you can own engine-build and compatibility work. Reject it when rapid model churn, heterogeneous hardware, or portability matters more than peak tuned throughput. Compare the same representative workload with vLLM, SGLang, TGI.
\item[5. Production proof] Start with the failure hypothesis: A model, GPU, driver, CUDA, and engine mismatch can invalidate the artifact or its performance assumptions. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Compiled engines; Triton or custom server\\
Choose when & NVIDIA GPUs are fixed, maximum performance matters, and you can own engine-build and compatibility work.\\
Reject when & Rapid model churn, heterogeneous hardware, or portability matters more than peak tuned throughput.\\
Primary failure & A model, GPU, driver, CUDA, and engine mismatch can invalidate the artifact or its performance assumptions.\\
Compare with & vLLM; SGLang; TGI\\
\end{tabularx}
\paragraph{Minimal use in Python / C++ / CUDA.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install tensorrt_llm
trtllm-build --checkpoint_dir MODEL --output_dir ENGINE
\end{Verbatim}
\textbf{Primary sources.} \href{https://nvidia.github.io/TensorRT-LLM/}{TensorRT-LLM official documentation}.
\subsubsection{NVIDIA Triton Inference Server}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries NTI}}}\quad \badge{Multi-framework inference server} \quad \textbf{Languages:} C++ / Python / client SDKs
\par\smallskip Model repository, dynamic batching, ensembles, metrics, and backends for TensorRT, PyTorch, ONNX, Python, and more.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of NVIDIA Triton Inference Server as a multi-framework inference server between inference request and model response; its defining mechanism is backend + batch scheduler.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Inference request};
\node[tech,right=of t0](t1){Backend + batch scheduler};
\node[tech,right=of t1](t2){Model response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Inference request -> Backend + batch scheduler -> Model response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Model repository, dynamic batching, ensembles, metrics, and backends for TensorRT, PyTorch, ONNX, Python, and more. Think of NVIDIA Triton Inference Server as a multi-framework inference server between inference request and model response; its defining mechanism is backend + batch scheduler.
\item[2. Structure and execution] Follow the data path Inference request → Backend + batch scheduler → Model response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when one platform must serve heterogeneous model formats with standardized scheduling and observability. Reject it when you need an LLM-specialized scheduler with first-class KV-cache and token-aware routing out of the box. Compare the same representative workload with KServe, BentoML, Ray Serve.
\item[5. Production proof] Start with the failure hypothesis: Generic dynamic batching tuned by request count can violate latency when token or tensor shapes vary widely. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Containerized server on CPU or GPU\\
Choose when & One platform must serve heterogeneous model formats with standardized scheduling and observability.\\
Reject when & You need an LLM-specialized scheduler with first-class KV-cache and token-aware routing out of the box.\\
Primary failure & Generic dynamic batching tuned by request count can violate latency when token or tensor shapes vary widely.\\
Compare with & KServe; BentoML; Ray Serve\\
\end{tabularx}
\paragraph{Minimal use in C++ / Python / client SDKs.}
\begin{Verbatim}[fontsize=\scriptsize]
docker run nvcr.io/nvidia/tritonserver tritonserver --model-repository=/models
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.nvidia.com/deeplearning/triton-inference-server/user-guide/docs/}{NVIDIA Triton Inference Server official documentation}.
\subsubsection{llama.cpp}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries LC}}}\quad \badge{Portable inference runtime} \quad \textbf{Languages:} C / C++
\par\smallskip Portable CPU/GPU inference with GGUF quantization and broad hardware backends for local and edge use.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of llama.cpp as a portable inference runtime between gguf model + prompt and local tokens; its defining mechanism is portable quantized runtime.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){GGUF model + prompt};
\node[tech,right=of t0](t1){Portable quantized runtime};
\node[tech,right=of t1](t2){Local tokens};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & GGUF model + prompt -> Portable quantized runtime -> Local tokens\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Portable CPU/GPU inference with GGUF quantization and broad hardware backends for local and edge use. Think of llama.cpp as a portable inference runtime between gguf model + prompt and local tokens; its defining mechanism is portable quantized runtime.
\item[2. Structure and execution] Follow the data path GGUF model + prompt → Portable quantized runtime → Local tokens. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when offline, desktop, edge, or commodity-hardware deployment values portability and quantized models. Reject it when large multi-tenant GPU fleets need sophisticated distributed scheduling and operational APIs. Compare the same representative workload with Ollama, LocalAI, vLLM.
\item[5. Production proof] Start with the failure hypothesis: Choosing a quantization only by file size can cause unacceptable quality or context-memory behavior. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Local binary, library, or HTTP server\\
Choose when & Offline, desktop, edge, or commodity-hardware deployment values portability and quantized models.\\
Reject when & Large multi-tenant GPU fleets need sophisticated distributed scheduling and operational APIs.\\
Primary failure & Choosing a quantization only by file size can cause unacceptable quality or context-memory behavior.\\
Compare with & Ollama; LocalAI; vLLM\\
\end{tabularx}
\paragraph{Minimal use in C / C++.}
\begin{Verbatim}[fontsize=\scriptsize]
git clone https://github.com/ggml-org/llama.cpp
./llama-server -m model.gguf
\end{Verbatim}
\textbf{Primary sources.} \href{https://github.com/ggml-org/llama.cpp}{llama.cpp official documentation}.
\subsubsection{Ollama}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries O}}}\quad \badge{Local model manager and server} \quad \textbf{Languages:} Go / C++
\par\smallskip Simple model packaging, pulling, local lifecycle, and HTTP APIs built around portable inference backends.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Ollama as a local model manager and server between model manifest + prompt and chat or embeddings api; its defining mechanism is local model service.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model manifest + prompt};
\node[tech,right=of t0](t1){Local model service};
\node[tech,right=of t1](t2){Chat or embeddings API};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model manifest + prompt -> Local model service -> Chat or embeddings API\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Simple model packaging, pulling, local lifecycle, and HTTP APIs built around portable inference backends. Think of Ollama as a local model manager and server between model manifest + prompt and chat or embeddings api; its defining mechanism is local model service.
\item[2. Structure and execution] Follow the data path Model manifest + prompt → Local model service → Chat or embeddings API. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when developers need a low-friction local model experience and reproducible Modelfiles. Reject it when you need production multi-node scheduling, strict multi-tenancy, or fine control of GPU kernels. Compare the same representative workload with llama.cpp, LocalAI, LM Studio.
\item[5. Production proof] Start with the failure hypothesis: A convenient developer daemon is not automatically hardened, capacity-planned production infrastructure. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Desktop or local server\\
Choose when & Developers need a low-friction local model experience and reproducible Modelfiles.\\
Reject when & You need production multi-node scheduling, strict multi-tenancy, or fine control of GPU kernels.\\
Primary failure & A convenient developer daemon is not automatically hardened, capacity-planned production infrastructure.\\
Compare with & llama.cpp; LocalAI; LM Studio\\
\end{tabularx}
\paragraph{Minimal use in Go / C++.}
\begin{Verbatim}[fontsize=\scriptsize]
ollama run MODEL
curl http://localhost:11434/api/chat
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.ollama.com/}{Ollama official documentation}.
\subsubsection{KServe}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries K}}}\quad \badge{Kubernetes serving platform} \quad \textbf{Languages:} Go / Python / Kubernetes YAML
\par\smallskip InferenceService control plane with autoscaling, revisions, canaries, predictors, explainers, and model runtimes.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of KServe as a kubernetes serving platform between model artifact + policy and autoscaled endpoint; its defining mechanism is inferenceservice controller.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model artifact + policy};
\node[tech,right=of t0](t1){InferenceService controller};
\node[tech,right=of t1](t2){Autoscaled endpoint};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model artifact + policy -> InferenceService controller -> Autoscaled endpoint\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] InferenceService control plane with autoscaling, revisions, canaries, predictors, explainers, and model runtimes. Think of KServe as a kubernetes serving platform between model artifact + policy and autoscaled endpoint; its defining mechanism is inferenceservice controller.
\item[2. Structure and execution] Follow the data path Model artifact + policy → InferenceService controller → Autoscaled endpoint. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when your organization already operates Kubernetes and needs a common lifecycle for many model types. Reject it when a small team lacks cluster operations, or subsecond always-warm LLM serving needs direct engine control. Compare the same representative workload with Ray Serve, BentoML, managed endpoints.
\item[5. Production proof] Start with the failure hypothesis: Autoscaling on CPU or request count can misread token-heavy GPU workloads and cause cold-start thrash. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Kubernetes CRDs and controllers\\
Choose when & Your organization already operates Kubernetes and needs a common lifecycle for many model types.\\
Reject when & A small team lacks cluster operations, or subsecond always-warm LLM serving needs direct engine control.\\
Primary failure & Autoscaling on CPU or request count can misread token-heavy GPU workloads and cause cold-start thrash.\\
Compare with & Ray Serve; BentoML; managed endpoints\\
\end{tabularx}
\paragraph{Minimal use in Go / Python / Kubernetes YAML.}
\begin{Verbatim}[fontsize=\scriptsize]
kubectl apply -f inferenceservice.yaml
kubectl get inferenceservices
\end{Verbatim}
\textbf{Primary sources.} \href{https://kserve.github.io/website/}{KServe official documentation}.
\subsubsection{Ray Serve}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries RS}}}\quad \badge{Distributed serving library} \quad \textbf{Languages:} Python
\par\smallskip Python-native composition, autoscaling, batching, model multiplexing, and distributed deployments on Ray.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Ray Serve as a distributed serving library between request and composed response; its defining mechanism is deployment graph + actors.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Request};
\node[tech,right=of t0](t1){Deployment graph + actors};
\node[tech,right=of t1](t2){Composed response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Request -> Deployment graph + actors -> Composed response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Python-native composition, autoscaling, batching, model multiplexing, and distributed deployments on Ray. Think of Ray Serve as a distributed serving library between request and composed response; its defining mechanism is deployment graph + actors.
\item[2. Structure and execution] Follow the data path Request → Deployment graph + actors → Composed response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when inference is a multi-stage Python graph or must share a Ray data, training, or actor ecosystem. Reject it when a standard single-model server or Kubernetes CRD meets the requirement with less runtime complexity. Compare the same representative workload with KServe, BentoML, vLLM.
\item[5. Production proof] Start with the failure hypothesis: Actor topology and object-store movement can add hidden serialization, placement, and failure costs. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Ray cluster or managed Anyscale\\
Choose when & Inference is a multi-stage Python graph or must share a Ray data, training, or actor ecosystem.\\
Reject when & A standard single-model server or Kubernetes CRD meets the requirement with less runtime complexity.\\
Primary failure & Actor topology and object-store movement can add hidden serialization, placement, and failure costs.\\
Compare with & KServe; BentoML; vLLM\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install 'ray[serve]'
serve run app:deployment
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.ray.io/en/latest/serve/}{Ray Serve official documentation}.
\subsubsection{BentoML}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries B}}}\quad \badge{Model packaging and service framework} \quad \textbf{Languages:} Python
\par\smallskip Package models and Python inference code as services with runners, batching, observability, and deployment tooling.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of BentoML as a model packaging and service framework between model + python code and versioned api container; its defining mechanism is bento service.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model + Python code};
\node[tech,right=of t0](t1){Bento service};
\node[tech,right=of t1](t2){Versioned API container};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model + Python code -> Bento service -> Versioned API container\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Package models and Python inference code as services with runners, batching, observability, and deployment tooling. Think of BentoML as a model packaging and service framework between model + python code and versioned api container; its defining mechanism is bento service.
\item[2. Structure and execution] Follow the data path Model + Python code → Bento service → Versioned API container. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when you need repeatable packaging for custom Python preprocessing and multiple model frameworks. Reject it when only a bare optimized LLM server is needed, or the organization has a mandated serving control plane. Compare the same representative workload with Ray Serve, KServe, Modal.
\item[5. Production proof] Start with the failure hypothesis: Bundling arbitrary application dependencies can create large, slow, difficult-to-secure images. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Containers, Kubernetes, BentoCloud\\
Choose when & You need repeatable packaging for custom Python preprocessing and multiple model frameworks.\\
Reject when & Only a bare optimized LLM server is needed, or the organization has a mandated serving control plane.\\
Primary failure & Bundling arbitrary application dependencies can create large, slow, difficult-to-secure images.\\
Compare with & Ray Serve; KServe; Modal\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install bentoml
bentoml serve service:svc
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.bentoml.com/}{BentoML official documentation}.
\subsubsection{LocalAI}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Local OpenAI-compatible platform} \quad \textbf{Languages:} Go / C++ / Python backends
\par\smallskip OpenAI-compatible APIs over multiple local text, image, audio, and embedding backends.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of LocalAI as a local openai-compatible platform between api request and local multimodal result; its defining mechanism is backend router.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){API request};
\node[tech,right=of t0](t1){Backend router};
\node[tech,right=of t1](t2){Local multimodal result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & API request -> Backend router -> Local multimodal result\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] OpenAI-compatible APIs over multiple local text, image, audio, and embedding backends. Think of LocalAI as a local openai-compatible platform between api request and local multimodal result; its defining mechanism is backend router.
\item[2. Structure and execution] Follow the data path API request → Backend router → Local multimodal result. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when a private or offline environment needs one familiar API across heterogeneous local models. Reject it when peak LLM throughput, narrow minimal attack surface, or managed operations are the primary goals. Compare the same representative workload with Ollama, llama.cpp, vLLM.
\item[5. Production proof] Start with the failure hypothesis: Enabling many backends and galleries expands supply-chain and configuration risk. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Binary or containers; local and on-prem\\
Choose when & A private or offline environment needs one familiar API across heterogeneous local models.\\
Reject when & Peak LLM throughput, narrow minimal attack surface, or managed operations are the primary goals.\\
Primary failure & Enabling many backends and galleries expands supply-chain and configuration risk.\\
Compare with & Ollama; llama.cpp; vLLM\\
\end{tabularx}
\paragraph{Minimal use in Go / C++ / Python backends.}
\begin{Verbatim}[fontsize=\scriptsize]
docker run localai/localai:latest
curl http://localhost:8080/v1/models
\end{Verbatim}
\textbf{Primary sources.} \href{https://localai.io/}{LocalAI official documentation}.
\subsubsection{Modal}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries M}}}\quad \badge{Serverless AI compute platform} \quad \textbf{Languages:} Python
\par\smallskip Python-defined images, functions, volumes, queues, GPUs, autoscaling, web endpoints, jobs, and model serving.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Modal as a serverless ai compute platform between python function + image and endpoint or batch result; its defining mechanism is managed elastic compute.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Python function + image};
\node[tech,right=of t0](t1){Managed elastic compute};
\node[tech,right=of t1](t2){Endpoint or batch result};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Python function + image -> Managed elastic compute -> Endpoint or batch result\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Python-defined images, functions, volumes, queues, GPUs, autoscaling, web endpoints, jobs, and model serving. Think of Modal as a serverless ai compute platform between python function + image and endpoint or batch result; its defining mechanism is managed elastic compute.
\item[2. Structure and execution] Follow the data path Python function + image → Managed elastic compute → Endpoint or batch result. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when bursty GPU workloads, batch jobs, and custom containers should scale without operating a cluster. Reject it when hard on-prem requirements, fixed long-running utilization, or deep infrastructure control dominates. Compare the same representative workload with Baseten, Runpod, Kubernetes.
\item[5. Production proof] Start with the failure hypothesis: Cold start, image construction, data locality, and egress can dominate short requests if unmeasured. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed serverless CPU and GPU\\
Choose when & Bursty GPU workloads, batch jobs, and custom containers should scale without operating a cluster.\\
Reject when & Hard on-prem requirements, fixed long-running utilization, or deep infrastructure control dominates.\\
Primary failure & Cold start, image construction, data locality, and egress can dominate short requests if unmeasured.\\
Compare with & Baseten; Runpod; Kubernetes\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install modal
modal run app.py
\end{Verbatim}
\textbf{Primary sources.} \href{https://modal.com/docs/guide}{Modal official documentation}.
\subsubsection{Baseten}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries B}}}\quad \badge{Managed inference platform} \quad \textbf{Languages:} Python / REST
\par\smallskip Model packaging and high-performance serving with autoscaling, production chains, observability, and dedicated deployment options.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Baseten as a managed inference platform between model package and autoscaled api; its defining mechanism is managed deployment.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model package};
\node[tech,right=of t0](t1){Managed deployment};
\node[tech,right=of t1](t2){Autoscaled API};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model package -> Managed deployment -> Autoscaled API\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Model packaging and high-performance serving with autoscaling, production chains, observability, and dedicated deployment options. Think of Baseten as a managed inference platform between model package and autoscaled api; its defining mechanism is managed deployment.
\item[2. Structure and execution] Follow the data path Model package → Managed deployment → Autoscaled API. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when a team wants managed production inference with custom code and performance engineering support. Reject it when self-hosting economics, unusual infrastructure constraints, or full portability dominate. Compare the same representative workload with Modal, Hugging Face Endpoints, Replicate.
\item[5. Production proof] Start with the failure hypothesis: A managed benchmark can hide steady-state GPU cost, scale-to-zero latency, and portability limits. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed GPU endpoints\\
Choose when & A team wants managed production inference with custom code and performance engineering support.\\
Reject when & Self-hosting economics, unusual infrastructure constraints, or full portability dominate.\\
Primary failure & A managed benchmark can hide steady-state GPU cost, scale-to-zero latency, and portability limits.\\
Compare with & Modal; Hugging Face Endpoints; Replicate\\
\end{tabularx}
\paragraph{Minimal use in Python / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install truss
truss push
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.baseten.co/}{Baseten official documentation}.
\subsubsection{Hugging Face Inference Endpoints}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries HFI}}}\quad \badge{Managed model endpoint} \quad \textbf{Languages:} REST / Python
\par\smallskip Managed deployment of Hub or custom models with selectable engines, hardware, security, and autoscaling.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Hugging Face Inference Endpoints as a managed model endpoint between hub model + config and https inference; its defining mechanism is managed endpoint.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Hub model + config};
\node[tech,right=of t0](t1){Managed endpoint};
\node[tech,right=of t1](t2){HTTPS inference};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Hub model + config -> Managed endpoint -> HTTPS inference\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Managed deployment of Hub or custom models with selectable engines, hardware, security, and autoscaling. Think of Hugging Face Inference Endpoints as a managed model endpoint between hub model + config and https inference; its defining mechanism is managed endpoint.
\item[2. Structure and execution] Follow the data path Hub model + config → Managed endpoint → HTTPS inference. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when you want Hub-native deployment without operating servers and accept provider-region constraints. Reject it when the model needs unsupported custom infrastructure or self-hosted unit economics are decisive. Compare the same representative workload with Baseten, Modal, cloud-provider endpoints.
\item[5. Production proof] Start with the failure hypothesis: Default task settings and autoscaling behavior may not match streaming, long-context, or private-network requirements. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed dedicated or autoscaling infrastructure\\
Choose when & You want Hub-native deployment without operating servers and accept provider-region constraints.\\
Reject when & The model needs unsupported custom infrastructure or self-hosted unit economics are decisive.\\
Primary failure & Default task settings and autoscaling behavior may not match streaming, long-context, or private-network requirements.\\
Compare with & Baseten; Modal; cloud-provider endpoints\\
\end{tabularx}
\paragraph{Minimal use in REST / Python.}
\begin{Verbatim}[fontsize=\scriptsize]
from huggingface_hub import InferenceClient
client = InferenceClient(model=ENDPOINT_URL)
\end{Verbatim}
\textbf{Primary sources.} \href{https://huggingface.co/docs/inference-endpoints/index}{Hugging Face Inference Endpoints official documentation}.
\subsubsection{Hugging Face Text Embeddings Inference}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries HFT}}}\quad \badge{Embedding inference server} \quad \textbf{Languages:} Rust / Python client
\par\smallskip High-throughput server for text embeddings, rerankers, tokenization, dynamic batching, and OpenAI-compatible embedding APIs for supported models.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Hugging Face Text Embeddings Inference as a embedding inference server between text requests and vectors or rerank scores; its defining mechanism is tokenize + dynamic batch.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Text requests};
\node[tech,right=of t0](t1){Tokenize + dynamic batch};
\node[tech,right=of t1](t2){Vectors or rerank scores};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Text requests -> Tokenize + dynamic batch -> Vectors or rerank scores\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] High-throughput server for text embeddings, rerankers, tokenization, dynamic batching, and OpenAI-compatible embedding APIs for supported models. Think of Hugging Face Text Embeddings Inference as a embedding inference server between text requests and vectors or rerank scores; its defining mechanism is tokenize + dynamic batch.
\item[2. Structure and execution] Follow the data path Text requests → Tokenize + dynamic batch → Vectors or rerank scores. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when self-hosted embedding or reranking models need optimized batching and an operational HTTP boundary. Reject it when low traffic can call a library directly, or a managed API removes the operational burden. Compare the same representative workload with Sentence Transformers, Infinity, managed embedding APIs.
\item[5. Production proof] Start with the failure hypothesis: A tokenizer or pooling mismatch between offline indexing and online querying corrupts the entire retrieval space. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Containerized CPU or GPU server\\
Choose when & Self-hosted embedding or reranking models need optimized batching and an operational HTTP boundary.\\
Reject when & Low traffic can call a library directly, or a managed API removes the operational burden.\\
Primary failure & A tokenizer or pooling mismatch between offline indexing and online querying corrupts the entire retrieval space.\\
Compare with & Sentence Transformers; Infinity; managed embedding APIs\\
\end{tabularx}
\paragraph{Minimal use in Rust / Python client.}
\begin{Verbatim}[fontsize=\scriptsize]
docker run ghcr.io/huggingface/text-embeddings-inference --model-id MODEL
\end{Verbatim}
\textbf{Primary sources.} \href{https://huggingface.co/docs/text-embeddings-inference/index}{Hugging Face Text Embeddings Inference official documentation}.
\subsubsection{LiteLLM}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{LLM gateway and provider adapter} \quad \textbf{Languages:} Python
\par\smallskip Normalizes many model-provider APIs and adds routing, budgets, virtual keys, caching, logging, and gateway controls.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of LiteLLM as a llm gateway and provider adapter between openai-style request and normalized model response; its defining mechanism is policy + provider routing.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){OpenAI-style request};
\node[tech,right=of t0](t1){Policy + provider routing};
\node[tech,right=of t1](t2){Normalized model response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & OpenAI-style request -> Policy + provider routing -> Normalized model response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Normalizes many model-provider APIs and adds routing, budgets, virtual keys, caching, logging, and gateway controls. Think of LiteLLM as a llm gateway and provider adapter between openai-style request and normalized model response; its defining mechanism is policy + provider routing.
\item[2. Structure and execution] Follow the data path OpenAI-style request → Policy + provider routing → Normalized model response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when many model providers must sit behind one application contract with centralized spend and routing policy. Reject it when one provider and a direct SDK are simpler, or lowest-level streaming semantics must remain provider specific. Compare the same representative workload with Portkey, Kong AI Gateway, custom gateway.
\item[5. Production proof] Start with the failure hypothesis: A common API can conceal provider differences in token accounting, tool calls, safety, retries, and error semantics. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Library or self-hosted proxy\\
Choose when & Many model providers must sit behind one application contract with centralized spend and routing policy.\\
Reject when & One provider and a direct SDK are simpler, or lowest-level streaming semantics must remain provider specific.\\
Primary failure & A common API can conceal provider differences in token accounting, tool calls, safety, retries, and error semantics.\\
Compare with & Portkey; Kong AI Gateway; custom gateway\\
\end{tabularx}
\paragraph{Minimal use in Python.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install 'litellm[proxy]'
litellm --model openai/MODEL
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.litellm.ai/}{LiteLLM official documentation}.
\subsubsection{Portkey AI Gateway}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries PAG}}}\quad \badge{Managed or self-hosted AI gateway} \quad \textbf{Languages:} REST / Python / JavaScript
\par\smallskip Gateway for provider routing, fallbacks, budgets, guardrails, caching, observability, and organization controls across AI APIs.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Portkey AI Gateway as a managed or self-hosted ai gateway between application request and provider response + trace; its defining mechanism is gateway policy + routing.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Application request};
\node[tech,right=of t0](t1){Gateway policy + routing};
\node[tech,right=of t1](t2){Provider response + trace};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Application request -> Gateway policy + routing -> Provider response + trace\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Gateway for provider routing, fallbacks, budgets, guardrails, caching, observability, and organization controls across AI APIs. Think of Portkey AI Gateway as a managed or self-hosted ai gateway between application request and provider response + trace; its defining mechanism is gateway policy + routing.
\item[2. Structure and execution] Follow the data path Application request → Gateway policy + routing → Provider response + trace. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when teams want a control plane in front of hosted models without building routing and governance from scratch. Reject it when strictly local inference or an existing API gateway plus observability stack already meets requirements. Compare the same representative workload with LiteLLM, Kong AI Gateway, custom Envoy filters.
\item[5. Production proof] Start with the failure hypothesis: Gateway retries or fallbacks can duplicate side effects, violate data residency, or mix incompatible model behavior. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Managed cloud, self-hosted, or hybrid gateway\\
Choose when & Teams want a control plane in front of hosted models without building routing and governance from scratch.\\
Reject when & Strictly local inference or an existing API gateway plus observability stack already meets requirements.\\
Primary failure & Gateway retries or fallbacks can duplicate side effects, violate data residency, or mix incompatible model behavior.\\
Compare with & LiteLLM; Kong AI Gateway; custom Envoy filters\\
\end{tabularx}
\paragraph{Minimal use in REST / Python / JavaScript.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install portkey-ai
from portkey_ai import Portkey
\end{Verbatim}
\textbf{Primary sources.} \href{https://portkey.ai/docs/}{Portkey AI Gateway official documentation}.
\subsubsection{Kong AI Gateway}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries KAG}}}\quad \badge{API and AI gateway} \quad \textbf{Languages:} Lua / Go / declarative config
\par\smallskip Extends API gateway controls with model-provider routing, usage governance, semantic policies, plugins, and observability.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Kong AI Gateway as a api and ai gateway between client request and governed upstream response; its defining mechanism is gateway plugins + provider route.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Client request};
\node[tech,right=of t0](t1){Gateway plugins + provider route};
\node[tech,right=of t1](t2){Governed upstream response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Client request -> Gateway plugins + provider route -> Governed upstream response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Extends API gateway controls with model-provider routing, usage governance, semantic policies, plugins, and observability. Think of Kong AI Gateway as a api and ai gateway between client request and governed upstream response; its defining mechanism is gateway plugins + provider route.
\item[2. Structure and execution] Follow the data path Client request → Gateway plugins + provider route → Governed upstream response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when an organization already uses Kong and wants AI traffic under the same identity, networking, and policy plane. Reject it when a lightweight library proxy is enough or inference scheduling inside the GPU fleet is the main problem. Compare the same representative workload with Portkey, LiteLLM, Envoy AI Gateway.
\item[5. Production proof] Start with the failure hypothesis: Request normalization and plugins can become a central failure point or leak prompt content into logs. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Kubernetes, VM, or managed gateway\\
Choose when & An organization already uses Kong and wants AI traffic under the same identity, networking, and policy plane.\\
Reject when & A lightweight library proxy is enough or inference scheduling inside the GPU fleet is the main problem.\\
Primary failure & Request normalization and plugins can become a central failure point or leak prompt content into logs.\\
Compare with & Portkey; LiteLLM; Envoy AI Gateway\\
\end{tabularx}
\paragraph{Minimal use in Lua / Go / declarative config.}
\begin{Verbatim}[fontsize=\scriptsize]
deck gateway apply -o kong.yaml
kong migrations bootstrap
\end{Verbatim}
\textbf{Primary sources.} \href{https://developer.konghq.com/ai-gateway/}{Kong AI Gateway official documentation}.
\subsubsection{Envoy AI Gateway}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries EAG}}}\quad \badge{Kubernetes AI gateway} \quad \textbf{Languages:} Go / Kubernetes YAML
\par\smallskip Open source gateway that routes generative AI traffic through Gateway API resources and Envoy data planes with provider-aware policy.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Envoy AI Gateway as a kubernetes ai gateway between gateway api request and selected model endpoint; its defining mechanism is ai routing policy + envoy.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Gateway API request};
\node[tech,right=of t0](t1){AI routing policy + Envoy};
\node[tech,right=of t1](t2){Selected model endpoint};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Gateway API request -> AI routing policy + Envoy -> Selected model endpoint\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Open source gateway that routes generative AI traffic through Gateway API resources and Envoy data planes with provider-aware policy. Think of Envoy AI Gateway as a kubernetes ai gateway between gateway api request and selected model endpoint; its defining mechanism is ai routing policy + envoy.
\item[2. Structure and execution] Follow the data path Gateway API request → AI routing policy + Envoy → Selected model endpoint. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when kubernetes Gateway API is already strategic and teams need an open, infrastructure-native AI gateway. Reject it when a developer library or hosted control plane is preferable to operating gateway controllers and CRDs. Compare the same representative workload with Kong AI Gateway, LiteLLM, Portkey.
\item[5. Production proof] Start with the failure hypothesis: Control-plane and provider-extension version skew can break routing or expose raw provider differences. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Kubernetes Gateway API and Envoy\\
Choose when & Kubernetes Gateway API is already strategic and teams need an open, infrastructure-native AI gateway.\\
Reject when & A developer library or hosted control plane is preferable to operating gateway controllers and CRDs.\\
Primary failure & Control-plane and provider-extension version skew can break routing or expose raw provider differences.\\
Compare with & Kong AI Gateway; LiteLLM; Portkey\\
\end{tabularx}
\paragraph{Minimal use in Go / Kubernetes YAML.}
\begin{Verbatim}[fontsize=\scriptsize]
kubectl apply -f aigatewayroute.yaml
kubectl get aigatewayroutes
\end{Verbatim}
\textbf{Primary sources.} \href{https://aigateway.envoyproxy.io/docs/}{Envoy AI Gateway official documentation}.
\subsubsection{Amazon SageMaker AI Endpoints}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries ASA}}}\quad \badge{Managed model endpoint platform} \quad \textbf{Languages:} Python / containers / AWS APIs
\par\smallskip Managed endpoint lifecycle, autoscaling, variants, monitoring, networking, and custom containers across AWS compute choices.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Amazon SageMaker AI Endpoints as a managed model endpoint platform between model artifact + endpoint config and authenticated inference; its defining mechanism is managed variant fleet.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model artifact + endpoint config};
\node[tech,right=of t0](t1){Managed variant fleet};
\node[tech,right=of t1](t2){Authenticated inference};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model artifact + endpoint config -> Managed variant fleet -> Authenticated inference\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Managed endpoint lifecycle, autoscaling, variants, monitoring, networking, and custom containers across AWS compute choices. Think of Amazon SageMaker AI Endpoints as a managed model endpoint platform between model artifact + endpoint config and authenticated inference; its defining mechanism is managed variant fleet.
\item[2. Structure and execution] Follow the data path Model artifact + endpoint config → Managed variant fleet → Authenticated inference. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when aWS identity, VPC integration, deployment variants, and managed operations are primary requirements. Reject it when a portable Kubernetes stack or specialized LLM runtime gives better control and utilization. Compare the same representative workload with Vertex AI endpoints, Azure ML endpoints, KServe.
\item[5. Production proof] Start with the failure hypothesis: Instance and autoscaling defaults can produce high cold-start cost, weak accelerator utilization, or region lock-in. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & AWS managed real-time, serverless, asynchronous, and batch inference\\
Choose when & AWS identity, VPC integration, deployment variants, and managed operations are primary requirements.\\
Reject when & A portable Kubernetes stack or specialized LLM runtime gives better control and utilization.\\
Primary failure & Instance and autoscaling defaults can produce high cold-start cost, weak accelerator utilization, or region lock-in.\\
Compare with & Vertex AI endpoints; Azure ML endpoints; KServe\\
\end{tabularx}
\paragraph{Minimal use in Python / containers / AWS APIs.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install sagemaker
from sagemaker.model import Model
\end{Verbatim}
\textbf{Primary sources.} \href{https://docs.aws.amazon.com/sagemaker/latest/dg/deploy-model.html}{Amazon SageMaker AI Endpoints official documentation}.
\subsubsection{Vertex AI Endpoints}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries VAE}}}\quad \badge{Managed model endpoint platform} \quad \textbf{Languages:} Python / REST / containers
\par\smallskip Managed deployment, traffic splitting, autoscaling, private networking, accelerators, logging, and model registry integration.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Vertex AI Endpoints as a managed model endpoint platform between model resource + deployment and online prediction; its defining mechanism is managed endpoint and traffic split.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model resource + deployment};
\node[tech,right=of t0](t1){Managed endpoint and traffic split};
\node[tech,right=of t1](t2){Online prediction};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model resource + deployment -> Managed endpoint and traffic split -> Online prediction\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Managed deployment, traffic splitting, autoscaling, private networking, accelerators, logging, and model registry integration. Think of Vertex AI Endpoints as a managed model endpoint platform between model resource + deployment and online prediction; its defining mechanism is managed endpoint and traffic split.
\item[2. Structure and execution] Follow the data path Model resource + deployment → Managed endpoint and traffic split → Online prediction. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when google Cloud identity, data, and managed ML lifecycle outweigh the need for a portable serving control plane. Reject it when the workload needs local or multi-cloud control, or a specialized runtime should be operated directly. Compare the same representative workload with SageMaker AI endpoints, Azure ML endpoints, KServe.
\item[5. Production proof] Start with the failure hypothesis: Regional quotas, model upload identity, machine selection, and cold starts can invalidate a lab benchmark. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Google Cloud managed online prediction\\
Choose when & Google Cloud identity, data, and managed ML lifecycle outweigh the need for a portable serving control plane.\\
Reject when & The workload needs local or multi-cloud control, or a specialized runtime should be operated directly.\\
Primary failure & Regional quotas, model upload identity, machine selection, and cold starts can invalidate a lab benchmark.\\
Compare with & SageMaker AI endpoints; Azure ML endpoints; KServe\\
\end{tabularx}
\paragraph{Minimal use in Python / REST / containers.}
\begin{Verbatim}[fontsize=\scriptsize]
pip install google-cloud-aiplatform
aiplatform.Endpoint.create(display_name='model')
\end{Verbatim}
\textbf{Primary sources.} \href{https://cloud.google.com/vertex-ai/docs/predictions/overview}{Vertex AI Endpoints official documentation}.
\subsubsection{Azure Machine Learning Managed Online Endpoints}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries AML}}}\quad \badge{Managed model endpoint platform} \quad \textbf{Languages:} Python / CLI / YAML / REST
\par\smallskip Managed HTTPS endpoints with deployments, traffic splitting, autoscaling, identity, networking, and monitoring for custom model containers.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of Azure Machine Learning Managed Online Endpoints as a managed model endpoint platform between model + environment and authenticated scoring response; its defining mechanism is managed deployment + traffic.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Model + environment};
\node[tech,right=of t0](t1){Managed deployment + traffic};
\node[tech,right=of t1](t2){Authenticated scoring response};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Model + environment -> Managed deployment + traffic -> Authenticated scoring response\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Managed HTTPS endpoints with deployments, traffic splitting, autoscaling, identity, networking, and monitoring for custom model containers. Think of Azure Machine Learning Managed Online Endpoints as a managed model endpoint platform between model + environment and authenticated scoring response; its defining mechanism is managed deployment + traffic.
\item[2. Structure and execution] Follow the data path Model + environment → Managed deployment + traffic → Authenticated scoring response. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when azure identity, private networking, and ML workspace governance are organizational constraints. Reject it when direct model servers or another cloud platform offer a simpler established operating path. Compare the same representative workload with SageMaker AI endpoints, Vertex AI endpoints, KServe.
\item[5. Production proof] Start with the failure hypothesis: Quota, image, environment, identity, and probe misconfiguration can leave a deployment healthy-looking but unavailable. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Azure managed online endpoint\\
Choose when & Azure identity, private networking, and ML workspace governance are organizational constraints.\\
Reject when & Direct model servers or another cloud platform offer a simpler established operating path.\\
Primary failure & Quota, image, environment, identity, and probe misconfiguration can leave a deployment healthy-looking but unavailable.\\
Compare with & SageMaker AI endpoints; Vertex AI endpoints; KServe\\
\end{tabularx}
\paragraph{Minimal use in Python / CLI / YAML / REST.}
\begin{Verbatim}[fontsize=\scriptsize]
az ml online-endpoint create -f endpoint.yml
az ml online-deployment create -f deployment.yml
\end{Verbatim}
\textbf{Primary sources.} \href{https://learn.microsoft.com/en-us/azure/machine-learning/concept-endpoints-online}{Azure Machine Learning Managed Online Endpoints official documentation}.
\subsubsection{llm-d}
\noindent\colorbox{navy}{\parbox[c][10mm][c]{10mm}{\centering\textcolor{white}{\Large\bfseries L}}}\quad \badge{Kubernetes distributed LLM serving stack} \quad \textbf{Languages:} Go / Python / Kubernetes
\par\smallskip Open serving stack for intelligent routing, KV-cache-aware scheduling, disaggregated prefill and decode, and distributed inference.
\par\smallskip\colorbox{paper}{\parbox{0.96\linewidth}{\textbf{Mental model.} Think of llm-d as a kubernetes distributed llm serving stack between token request + cache hints and stream under fleet slo; its defining mechanism is router + distributed inference workers.}}
\begin{center}\begin{tikzpicture}[node distance=8mm,tech/.style={draw=cyan,fill=paper,rounded corners,align=center,text width=42mm,minimum height=12mm,font=\small},arr/.style={-{Stealth},thick,draw=blue}]
\node[tech](t0){Token request + cache hints};
\node[tech,right=of t0](t1){Router + distributed inference workers};
\node[tech,right=of t1](t2){Stream under fleet SLO};
\draw[arr](t0)--(t1);\draw[arr](t1)--(t2);
\end{tikzpicture}\end{center}
\paragraph{Framework structure and design.}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{34mm}X}
Data plane & Token request + cache hints -> Router + distributed inference workers -> Stream under fleet SLO\\
Control plane & Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.\\
State and trust boundary & Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.\\
\end{tabularx}
\paragraph{Five-part tutorial.}
\begin{description}[leftmargin=38mm,style=nextline]
\item[1. Problem and abstraction] Open serving stack for intelligent routing, KV-cache-aware scheduling, disaggregated prefill and decode, and distributed inference. Think of llm-d as a kubernetes distributed llm serving stack between token request + cache hints and stream under fleet slo; its defining mechanism is router + distributed inference workers.
\item[2. Structure and execution] Follow the data path Token request + cache hints → Router + distributed inference workers → Stream under fleet SLO. Admission, routing, batching, scheduling, cache placement, parallelism, and autoscaling turn requests into accelerator work.
\item[3. State, security, and operations] Queues, KV cache, model revisions, adapters, quotas, routing policy, and request identity must survive or fail predictably.
\item[4. Selection decision] Choose it when a Kubernetes fleet needs production routing and scale above individual vLLM or SGLang servers. Reject it when a single node or managed endpoint meets the SLO with much less control-plane complexity. Compare the same representative workload with KServe LLMInferenceService, NVIDIA Dynamo, Ray Serve.
\item[5. Production proof] Start with the failure hypothesis: Router, cache, scheduler, and engine version skew can erase expected locality or make failover unsafe. Replay real prompt and output lengths at realistic concurrency; report time to first token, inter-token latency, p95 and p99 latency, throughput, memory, errors, and cost.
\end{description}
\begin{tabularx}{\linewidth}{>{\bfseries\color{blue}}p{27mm}X}
Deployment & Kubernetes with vLLM or compatible engines\\
Choose when & A Kubernetes fleet needs production routing and scale above individual vLLM or SGLang servers.\\
Reject when & A single node or managed endpoint meets the SLO with much less control-plane complexity.\\
Primary failure & Router, cache, scheduler, and engine version skew can erase expected locality or make failover unsafe.\\
Compare with & KServe LLMInferenceService; NVIDIA Dynamo; Ray Serve\\
\end{tabularx}
\paragraph{Minimal use in Go / Python / Kubernetes.}
\begin{Verbatim}[fontsize=\scriptsize]
kubectl apply -k github.com/llm-d/llm-d
kubectl get pods
\end{Verbatim}
\textbf{Primary sources.} \href{https://llm-d.ai/docs/dev}{llm-d official documentation} \quad \href{https://llm-d.ai/docs/architecture}{llm-d architecture} \quad \href{https://kserve.github.io/website/docs/model-serving/generative-inference/llmisvc/llmisvc-overview}{KServe LLMInferenceService}.
